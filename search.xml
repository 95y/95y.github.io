<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>前端性能优化实战：围绕 Core Web Vitals 建立系统方法</title>
      <link href="/2026/07/22/frontend-performance-core-web-vitals/"/>
      <url>/2026/07/22/frontend-performance-core-web-vitals/</url>
      
        <content type="html"><![CDATA[<p>性能优化最容易陷入两个误区：一是把 Lighthouse 分数当作唯一目标，二是看到某条建议就立即修改代码。真正稳定的优化需要先找到用户遇到的瓶颈，再围绕指标建立“测量、定位、修改、验证”的闭环。</p><p>本文从当前稳定的三个 Core Web Vitals 指标出发，整理一套可以直接用于项目的前端性能优化方法。</p><h2 id="先定义什么叫“快”"><a href="#先定义什么叫“快”" class="headerlink" title="先定义什么叫“快”"></a>先定义什么叫“快”</h2><p>Core Web Vitals 分别关注加载速度、交互响应和视觉稳定性：</p><table><thead><tr><th>指标</th><th>关注点</th><th>良好标准（P75）</th></tr></thead><tbody><tr><td>LCP</td><td>主要内容何时可见</td><td>≤ 2.5 秒</td></tr><tr><td>INP</td><td>点击、输入等交互多久得到下一帧反馈</td><td>≤ 200 毫秒</td></tr><tr><td>CLS</td><td>页面是否发生意外位移</td><td>≤ 0.1</td></tr></tbody></table><p>这里的 P75 表示至少 75% 的真实访问需要达到目标。实验室中的一次高分不能代表所有用户，尤其不能代表低端设备和弱网环境。</p><p>一个更合理的工作流是：</p><ol><li>用 PageSpeed Insights 或真实用户监控发现问题。</li><li>用 Chrome DevTools 和 Lighthouse 在本地复现问题。</li><li>找到影响指标的具体资源、长任务或布局变化。</li><li>一次只修改一类因素，记录修改前后的结果。</li><li>发布后继续观察真实用户数据。</li></ol><h2 id="优化-LCP：让关键内容更早出现"><a href="#优化-LCP：让关键内容更早出现" class="headerlink" title="优化 LCP：让关键内容更早出现"></a>优化 LCP：让关键内容更早出现</h2><p>LCP 变慢通常可以拆成四个阶段：服务器响应慢、浏览器发现资源晚、资源下载慢、资源下载后仍然无法渲染。</p><h3 id="1-优先处理首屏关键资源"><a href="#1-优先处理首屏关键资源" class="headerlink" title="1. 优先处理首屏关键资源"></a>1. 优先处理首屏关键资源</h3><p>如果首屏大图是 LCP 元素，它应该直接出现在初始 HTML 中，并获得较高的加载优先级：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">img</span></span></span><br><span class="line"><span class="tag">  <span class="attr">src</span>=<span class="string">&quot;/images/hero.webp&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">srcset</span>=<span class="string">&quot;/images/hero-768.webp 768w, /images/hero-1440.webp 1440w&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">sizes</span>=<span class="string">&quot;100vw&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">width</span>=<span class="string">&quot;1440&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">height</span>=<span class="string">&quot;810&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">fetchpriority</span>=<span class="string">&quot;high&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">alt</span>=<span class="string">&quot;产品首页界面&quot;</span></span></span><br><span class="line"><span class="tag">/&gt;</span></span><br></pre></td></tr></table></figure><p>首屏 LCP 图片不要设置 <code>loading=&quot;lazy&quot;</code>。浏览器如果要等到 JavaScript 执行之后才能发现图片地址，也会白白损失加载时间。</p><p>对于 CSS 背景图等不容易被预加载扫描器发现的资源，可以谨慎使用 preload：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span></span></span><br><span class="line"><span class="tag">  <span class="attr">rel</span>=<span class="string">&quot;preload&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">href</span>=<span class="string">&quot;/images/hero.webp&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">as</span>=<span class="string">&quot;image&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">type</span>=<span class="string">&quot;image/webp&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">fetchpriority</span>=<span class="string">&quot;high&quot;</span></span></span><br><span class="line"><span class="tag">/&gt;</span></span><br></pre></td></tr></table></figure><p>Preload 只应用于真正关键的少量资源。预加载过多会让普通资源争抢带宽，结果可能适得其反。</p><h3 id="2-减少首屏阻塞"><a href="#2-减少首屏阻塞" class="headerlink" title="2. 减少首屏阻塞"></a>2. 减少首屏阻塞</h3><ul><li>删除没有使用的 CSS 和 JavaScript。</li><li>按路由或功能拆分代码，避免所有页面共享一个巨大入口包。</li><li>非关键脚本使用 <code>defer</code>，第三方脚本尽量延后加载。</li><li>静态资源开启 Brotli 或 gzip，并配置长期缓存。</li><li>使用 CDN 缩短资源传输距离，但不要用 CDN 掩盖后端响应慢的问题。</li><li>对文字型站点优先使用系统字体，避免字体下载阻塞文本绘制。</li></ul><h3 id="3-延迟首屏之外的内容"><a href="#3-延迟首屏之外的内容" class="headerlink" title="3. 延迟首屏之外的内容"></a>3. 延迟首屏之外的内容</h3><p>首屏之外的图片和 iframe 可以使用浏览器原生懒加载：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">img</span></span></span><br><span class="line"><span class="tag">  <span class="attr">src</span>=<span class="string">&quot;/images/article-cover.webp&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">width</span>=<span class="string">&quot;960&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">height</span>=<span class="string">&quot;540&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">loading</span>=<span class="string">&quot;lazy&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">decoding</span>=<span class="string">&quot;async&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">alt</span>=<span class="string">&quot;文章封面&quot;</span></span></span><br><span class="line"><span class="tag">/&gt;</span></span><br></pre></td></tr></table></figure><p>这能减少初始请求数量，但不要对所有图片一刀切。是否懒加载取决于资源是不是首屏关键内容。</p><h2 id="优化-INP：不要长时间占用主线程"><a href="#优化-INP：不要长时间占用主线程" class="headerlink" title="优化 INP：不要长时间占用主线程"></a>优化 INP：不要长时间占用主线程</h2><p>一次交互通常包含三段时间：等待主线程、执行事件处理器、浏览器完成布局与绘制。任何一段过长，用户都会觉得按钮“没反应”。</p><h3 id="1-找出长任务"><a href="#1-找出长任务" class="headerlink" title="1. 找出长任务"></a>1. 找出长任务</h3><p>在 Chrome DevTools Performance 面板中录制一次真实操作，重点查看超过 50 毫秒的 Long Task。也可以在开发环境中临时监听：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> observer = <span class="keyword">new</span> <span class="title class_">PerformanceObserver</span>(<span class="function">(<span class="params">list</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> entry <span class="keyword">of</span> list.<span class="title function_">getEntries</span>()) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">warn</span>(<span class="string">&#x27;Long task:&#x27;</span>, <span class="title class_">Math</span>.<span class="title function_">round</span>(entry.<span class="property">duration</span>), <span class="string">&#x27;ms&#x27;</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">observer.<span class="title function_">observe</span>(&#123; <span class="attr">type</span>: <span class="string">&#x27;longtask&#x27;</span>, <span class="attr">buffered</span>: <span class="literal">true</span> &#125;)</span><br></pre></td></tr></table></figure><h3 id="2-拆分大任务"><a href="#2-拆分大任务" class="headerlink" title="2. 拆分大任务"></a>2. 拆分大任务</h3><p>不要在一次点击中同步完成所有计算。可以把非关键工作拆成小块，在浏览器有机会绘制之后继续：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">processItems</span>(<span class="params">items</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> batchSize = <span class="number">100</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> index = <span class="number">0</span>; index &lt; items.<span class="property">length</span>; index += batchSize) &#123;</span><br><span class="line">    <span class="title function_">processBatch</span>(items.<span class="title function_">slice</span>(index, index + batchSize))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">await</span> <span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="function">(<span class="params">resolve</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">setTimeout</span>(resolve, <span class="number">0</span>)</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果计算本身非常重，应考虑 Web Worker。拆分代码只能给主线程喘息机会，并不会减少总计算量。</p><h3 id="3-控制渲染范围"><a href="#3-控制渲染范围" class="headerlink" title="3. 控制渲染范围"></a>3. 控制渲染范围</h3><ul><li>长列表使用分页或虚拟滚动。</li><li>输入联想请求使用防抖，并取消过期请求。</li><li>避免因为一个局部状态变化而重新渲染整棵组件树。</li><li>不要在循环中交替读取布局信息和修改样式。</li><li>对不影响当前交互结果的统计、日志和预加载任务延后执行。</li></ul><p>交互发生后，应尽快给用户可见反馈。即使后台任务还没有结束，也可以先更新按钮状态或显示轻量的处理中提示。</p><h2 id="优化-CLS：提前为内容保留空间"><a href="#优化-CLS：提前为内容保留空间" class="headerlink" title="优化 CLS：提前为内容保留空间"></a>优化 CLS：提前为内容保留空间</h2><p>CLS 的本质不是“页面发生了变化”，而是内容在用户没有预期时突然移动。</p><h3 id="1-明确媒体尺寸"><a href="#1-明确媒体尺寸" class="headerlink" title="1. 明确媒体尺寸"></a>1. 明确媒体尺寸</h3><p>为图片和视频提供尺寸或宽高比，浏览器才能在资源下载前预留空间：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-class">.article-cover</span> &#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">aspect-ratio</span>: <span class="number">16</span> / <span class="number">9</span>;</span><br><span class="line">  <span class="attribute">object-fit</span>: cover;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-为异步模块提供稳定容器"><a href="#2-为异步模块提供稳定容器" class="headerlink" title="2. 为异步模块提供稳定容器"></a>2. 为异步模块提供稳定容器</h3><p>广告、推荐列表、评论和远程卡片都应该提前拥有最小高度。加载中的骨架屏必须与最终内容尺寸接近，否则骨架屏消失时仍然会产生位移。</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-class">.recommendation-panel</span> &#123;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">320px</span>;</span><br><span class="line">  <span class="attribute">contain</span>: layout paint;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-避免在已有内容上方插入元素"><a href="#3-避免在已有内容上方插入元素" class="headerlink" title="3. 避免在已有内容上方插入元素"></a>3. 避免在已有内容上方插入元素</h3><p>提示条和活动横幅如果在页面加载后插入顶部，会把用户正在阅读的内容整体向下推。更好的方式是预留区域，或者把提示设计成不会改变文档流的覆盖层，同时确保它不会挡住主要操作。</p><h2 id="动画只使用合成友好的属性"><a href="#动画只使用合成友好的属性" class="headerlink" title="动画只使用合成友好的属性"></a>动画只使用合成友好的属性</h2><p>浏览器渲染大致经历 Style、Layout、Paint 和 Composite。动画 <code>width</code>、<code>height</code>、<code>top</code> 等属性往往会触发布局或绘制，而 <code>transform</code> 与 <code>opacity</code> 通常可以在合成阶段完成。</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-class">.card</span> &#123;</span><br><span class="line">  <span class="attribute">transition</span>: transform <span class="number">180ms</span> ease, opacity <span class="number">180ms</span> ease;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.card</span><span class="selector-pseudo">:hover</span> &#123;</span><br><span class="line">  <span class="attribute">transform</span>: <span class="built_in">translateY</span>(-<span class="number">4px</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@media</span> (<span class="attribute">prefers-reduced-motion</span>: reduce) &#123;</span><br><span class="line">  <span class="selector-class">.card</span> &#123;</span><br><span class="line">    <span class="attribute">transition</span>: none;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>动画应该表达状态变化，而不是单纯增加装饰。还要尊重系统的“减少动态效果”设置。</p><h2 id="建立性能预算"><a href="#建立性能预算" class="headerlink" title="建立性能预算"></a>建立性能预算</h2><p>性能优化不能只靠发布前临时检查。可以在项目中定义一组可执行的预算：</p><table><thead><tr><th>项目</th><th align="right">示例预算</th></tr></thead><tbody><tr><td>首屏 JavaScript（压缩后）</td><td align="right">≤ 170 KB</td></tr><tr><td>首屏 CSS（压缩后）</td><td align="right">≤ 50 KB</td></tr><tr><td>首屏图片总量</td><td align="right">≤ 500 KB</td></tr><tr><td>单个同步任务</td><td align="right">&lt; 50 ms</td></tr><tr><td>Lighthouse Performance</td><td align="right">≥ 90</td></tr></tbody></table><p>预算需要结合业务调整，但一定要能够在 CI 中自动检查。否则随着依赖和功能持续增加，性能通常只会缓慢退化。</p><h2 id="一份可执行的检查清单"><a href="#一份可执行的检查清单" class="headerlink" title="一份可执行的检查清单"></a>一份可执行的检查清单</h2><h3 id="加载阶段"><a href="#加载阶段" class="headerlink" title="加载阶段"></a>加载阶段</h3><ul><li><input disabled="" type="checkbox"> 首屏 LCP 资源能从初始 HTML 中被发现。</li><li><input disabled="" type="checkbox"> 首屏图片没有被错误地懒加载。</li><li><input disabled="" type="checkbox"> 非关键 JavaScript 不阻塞 HTML 解析。</li><li><input disabled="" type="checkbox"> 图片使用合适的格式、尺寸和响应式资源。</li><li><input disabled="" type="checkbox"> 字体数量和字重得到控制。</li></ul><h3 id="交互阶段"><a href="#交互阶段" class="headerlink" title="交互阶段"></a>交互阶段</h3><ul><li><input disabled="" type="checkbox"> 点击和输入过程中没有明显长任务。</li><li><input disabled="" type="checkbox"> 大量计算已拆分或移动到 Worker。</li><li><input disabled="" type="checkbox"> 长列表没有一次性渲染全部内容。</li><li><input disabled="" type="checkbox"> 用户操作能在 100 毫秒左右获得可见反馈。</li></ul><h3 id="稳定性"><a href="#稳定性" class="headerlink" title="稳定性"></a>稳定性</h3><ul><li><input disabled="" type="checkbox"> 图片、视频和 iframe 都声明了尺寸。</li><li><input disabled="" type="checkbox"> 异步内容拥有稳定的占位区域。</li><li><input disabled="" type="checkbox"> 页面不会在已有内容上方突然插入模块。</li><li><input disabled="" type="checkbox"> 动画主要使用 <code>transform</code> 和 <code>opacity</code>。</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>前端性能优化不是寻找一条万能配置，而是理解时间花在哪里。围绕 LCP、INP 和 CLS 建立测量闭环后，每一次优化都会变得更可解释：你知道问题影响了谁、修改解决了什么、发布后是否真的改善。</p><p>先建立数据和预算，再处理最影响真实用户的瓶颈，通常比盲目追求一次满分更有价值。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://web.dev/articles/vitals">Web Vitals</a></li><li><a href="https://web.dev/articles/optimize-lcp">Optimize Largest Contentful Paint</a></li><li><a href="https://web.dev/articles/optimize-inp">Optimize Interaction to Next Paint</a></li><li><a href="https://web.dev/articles/optimize-cls">Optimize Cumulative Layout Shift</a></li><li><a href="https://developer.mozilla.org/en-US/docs/Web/Performance/Guides/Lazy_loading">MDN：Lazy loading</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端开发 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 工程化 </tag>
            
            <tag> 前端性能 </tag>
            
            <tag> Core Web Vitals </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>工具链进入新阶段：TypeScript 6、Node 26 与 Vite 8.1</title>
      <link href="/2026/07/15/frontend-chronicle-2026-07-typescript-6-node-26-vite-8-1/"/>
      <url>/2026/07/15/frontend-chronicle-2026-07-typescript-6-node-26-vite-8-1/</url>
      
        <content type="html"><![CDATA[<p>2026 年上半年，我们连续做了三次看似相关、实际上不能绑在一起的升级：TypeScript 6 调整类型检查与旧选项，Node 26 带来新的运行时&#x2F;V8 并默认启用 Temporal，Vite 8.1 又给超大项目提供实验性的 bundled dev mode。</p><p>如果把它们写成“2026 工具链全面升级”，一个失败很难归因。真实操作是三张工单、三次兼容矩阵和三套验收。下面保留这份维护日志，而不是做功能列表。</p><h2 id="3-月：TypeScript-6，先处理弃用而不是追-TypeScript-7"><a href="#3-月：TypeScript-6，先处理弃用而不是追-TypeScript-7" class="headerlink" title="3 月：TypeScript 6，先处理弃用而不是追 TypeScript 7"></a>3 月：TypeScript 6，先处理弃用而不是追 TypeScript 7</h2><p>TypeScript 6.0 很特殊：官方计划它成为现有 JavaScript 代码库的最后一个主要版本，为后续原生 Go 实现铺路。它既有新能力，也承担清理旧配置与 6→7 迁移准备。</p><p>我们的 web app 旧 tsconfig：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;compilerOptions&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;target&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ES2020&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;module&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ESNext&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;moduleResolution&quot;</span><span class="punctuation">:</span> <span class="string">&quot;node&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;strict&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;types&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;vite/client&quot;</span><span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p><code>moduleResolution: node</code> 实际指向旧 node10 算法，在 TS 6 被弃用。对由 Vite 打包的应用，更合适的是 bundler；直接由 Node 执行的包则应评估 nodenext：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;compilerOptions&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;target&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ES2022&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;module&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Preserve&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;moduleResolution&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Bundler&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;strict&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;types&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;vite/client&quot;</span><span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这不是全项目通用答案。CLI&#x2F;Node 服务如果复制 bundler 解析，tsc 可能允许运行时 Node 找不到的导入。每个 package 按最终由谁加载选择策略。</p><h3 id="stableTypeOrdering-只用于迁移验证"><a href="#stableTypeOrdering-只用于迁移验证" class="headerlink" title="stableTypeOrdering 只用于迁移验证"></a><code>stableTypeOrdering</code> 只用于迁移验证</h3><p>TS 6 提供 <code>--stableTypeOrdering</code>，帮助检查未来原生编译器可能观察到的类型排序差异。我们在 CI 增加一条非阻断任务，针对生成声明与类型快照的库运行；普通应用没有因为“新 flag”就全开。</p><p>同时处理移除&#x2F;弃用的老模块目标、<code>no-default-lib</code> 等选项，并显式记录受新默认影响的配置。升级后跑的不只是 <code>tsc --noEmit</code>，还要生成 <code>.d.ts</code>、打包库并在 consumer 项目使用。</p><h2 id="5-月：Node-26-先进入-canary，不进入生产-LTS"><a href="#5-月：Node-26-先进入-canary，不进入生产-LTS" class="headerlink" title="5 月：Node 26 先进入 canary，不进入生产 LTS"></a>5 月：Node 26 先进入 canary，不进入生产 LTS</h2><p>Node 26.0.0 发布时属于 Current，官方计划到 10 月进入 LTS。我们不会因为 Current 发布就替换所有生产镜像，但会让一条 canary CI 提前运行测试，发现未来 LTS 阻塞项。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">strategy:</span></span><br><span class="line">  <span class="attr">matrix:</span></span><br><span class="line">    <span class="attr">node:</span> [<span class="string">&quot;22&quot;</span>, <span class="string">&quot;24&quot;</span>, <span class="string">&quot;26&quot;</span>]</span><br><span class="line"><span class="attr">continue-on-error:</span> <span class="string">$&#123;&#123;</span> <span class="string">matrix.node</span> <span class="string">==</span> <span class="string">&#x27;26&#x27;</span> <span class="string">&#125;&#125;</span></span><br></pre></td></tr></table></figure><p>真正的生产版本按项目支持策略决定；canary 失败要建 issue，但不会阻止主线。等目标版进入合适生命周期、依赖支持齐全，再转为 required。</p><h3 id="Temporal-默认启用，不代表马上重写所有-Date"><a href="#Temporal-默认启用，不代表马上重写所有-Date" class="headerlink" title="Temporal 默认启用，不代表马上重写所有 Date"></a>Temporal 默认启用，不代表马上重写所有 Date</h3><p>Node 26 默认启用 Temporal，解决许多时区、日历和不可变日期时间建模问题。我们先挑“将上海营业时间转换为用户时区”的纯函数做实验：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> opening = <span class="title class_">Temporal</span>.<span class="property">ZonedDateTime</span>.<span class="title function_">from</span>(</span><br><span class="line">  <span class="string">&#x27;2026-07-22T09:00:00+08:00[Asia/Shanghai]&#x27;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> inLondon = opening.<span class="title function_">withTimeZone</span>(<span class="string">&#x27;Europe/London&#x27;</span>)</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(inLondon.<span class="title function_">toString</span>())</span><br></pre></td></tr></table></figure><p>生产前仍要确认：浏览器是否支持或需要 polyfill，数据库&#x2F;JSON 如何序列化，API 是否继续传 ISO 字符串，团队对 Instant&#x2F;PlainDate&#x2F;ZonedDateTime 的选择是否一致。</p><p>前端与 Node 共享代码不能因为 Node 26 有全局 Temporal 就假设所有浏览器也有。边界层继续使用稳定字符串契约，内部逐步采用。</p><h3 id="主版本也有移除项"><a href="#主版本也有移除项" class="headerlink" title="主版本也有移除项"></a>主版本也有移除项</h3><p>Node 26 升级 V8 到 14.6、Undici 到 8，并移除&#x2F;弃用一批历史 API。例如依赖若仍调用已移除的 <code>http.Server.prototype.writeHeader()</code>，canary 会尽早失败。原生 addon 还要重新验证 ABI&#x2F;预编译二进制。</p><p>我们在 CI 记录 deprecation，并对 HTTP client、stream、原生图片包与信号退出跑专项测试。只验证语法新特性，会漏掉真正阻塞生产的旧 API。</p><h2 id="6-月：Vite-8-1-bundled-dev-只在大项目实验"><a href="#6-月：Vite-8-1-bundled-dev-只在大项目实验" class="headerlink" title="6 月：Vite 8.1 bundled dev 只在大项目实验"></a>6 月：Vite 8.1 bundled dev 只在大项目实验</h2><p>Vite 8.1 的 experimental bundled dev mode 面向模块数量极大的应用。Vite 原本按模块服务源码，项目到上万模块、浏览器要处理大量请求时，启动和 full reload 会被请求数量拖慢；bundled dev 用开发 bundle 降低这个开销，同时保留 HMR 目标。</p><p>配置明确带 experimental：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; defineConfig &#125; <span class="keyword">from</span> <span class="string">&#x27;vite&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">experimental</span>: &#123;</span><br><span class="line">    <span class="attr">bundledDev</span>: <span class="literal">true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>或者使用官方提供的实验 CLI flag。它当前重点覆盖浏览器侧与基础插件，第三方插件&#x2F;次要能力可能不工作，所以不能看到官方 benchmark 就设为所有项目默认。</p><h3 id="我们怎么判断项目是否“足够大”"><a href="#我们怎么判断项目是否“足够大”" class="headerlink" title="我们怎么判断项目是否“足够大”"></a>我们怎么判断项目是否“足够大”</h3><p>先在 Network&#x2F;性能日志记录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">冷启动到页面可用</span><br><span class="line">首次路由模块请求数</span><br><span class="line">full reload 时间</span><br><span class="line">单文件 HMR 时间</span><br><span class="line">代理环境额外延迟</span><br><span class="line">浏览器 CPU/内存</span><br></pre></td></tr></table></figure><p>小型营销站请求不到 300 个模块，开启实验模式收益有限；后台首页超过 7000 模块且走企业代理，full reload 明显受请求数量影响，才进入 A&#x2F;B。</p><p>结果：冷启动页面渲染约快 2.4 倍，full reload 快约 35%，HMR 接近原有体验；但一个虚拟模块插件失败。我们提交最小复现，日常默认仍保持关闭，只给自愿试用者脚本。</p><h2 id="三次升级之间唯一共享的是验证框架"><a href="#三次升级之间唯一共享的是验证框架" class="headerlink" title="三次升级之间唯一共享的是验证框架"></a>三次升级之间唯一共享的是验证框架</h2><p>我们用一张兼容矩阵串联，但不合并版本变更：</p><table><thead><tr><th>层</th><th>主要风险</th><th>验收</th></tr></thead><tbody><tr><td>TypeScript 6</td><td>类型&#x2F;声明&#x2F;解析改变</td><td>typecheck、d.ts consumer</td></tr><tr><td>Node 26</td><td>运行时移除、原生依赖</td><td>canary test、容器冒烟</td></tr><tr><td>Vite 8.1 实验模式</td><td>插件、dev 语义</td><td>双模式 E2E、HMR&#x2F;full reload</td></tr></tbody></table><p>每层失败都能单独关闭：回退 TS 版本、让 Node 26 canary 非阻断、关闭 <code>bundledDev</code>。没有一个“升级全部工具”的开关。</p><h2 id="删除历史包袱不等于删除兼容责任"><a href="#删除历史包袱不等于删除兼容责任" class="headerlink" title="删除历史包袱不等于删除兼容责任"></a>删除历史包袱不等于删除兼容责任</h2><p>TS 6 弃用旧解析、Node 26 移除旧 API、Vite 探索统一 bundling，看起来都在向现代基础设施收敛。但业务项目仍要服务自己的浏览器、部署平台和依赖生态。</p><p>我的维护顺序一直是：安全修复优先；受支持运行时其次；行为正确与可回退；最后才是速度和新语法。工具链越快，越不能用一次全量升级把风险混在一起。</p><p>到 2026 年 7 月，TypeScript 6 已进入主线；Node 26 仍在 canary 观察其生命周期与依赖支持；Vite 8.1 bundled dev 仍是大型后台的实验选项。这种“不整齐”的状态，反而比一张“已全面升级”海报更接近真实工程。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://devblogs.microsoft.com/typescript/announcing-typescript-6-0/">TypeScript 6.0 发布说明</a></li><li><a href="https://nodejs.org/en/blog/release/v26.0.0">Node.js 26.0.0 发布说明</a></li><li><a href="https://vite.dev/blog/announcing-vite8-1">Vite 8.1 发布说明</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 2026 </tag>
            
            <tag> 前端工程化 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>前端内存泄漏排查：监听器、闭包与未释放组件</title>
      <link href="/2026/04/17/frontend-debug-frontend-memory-leak/"/>
      <url>/2026/04/17/frontend-debug-frontend-memory-leak/</url>
      
        <content type="html"><![CDATA[<p>这次泄漏发生在一张带图片预览的工单详情页。客服连续打开、关闭工单两个小时后，页面从 300MB 涨到接近 1.5GB，输入开始卡顿；刷新立刻恢复。单独打开某一张工单看不出问题，只有重复路由切换才稳定复现。</p><p>最终 Retainer 链指向一个全局键盘快捷键监听器。监听器闭包持有预览组件实例，组件又持有解码后的图片 Blob。路由离开时 DOM 被移除，window 上的回调却从未解绑，所以整个对象图都无法被回收。</p><h2 id="先证明“持续增长”，不要看到内存上升就宣布泄漏"><a href="#先证明“持续增长”，不要看到内存上升就宣布泄漏" class="headerlink" title="先证明“持续增长”，不要看到内存上升就宣布泄漏"></a>先证明“持续增长”，不要看到内存上升就宣布泄漏</h2><p>JavaScript 引擎不会每次页面关闭立刻 GC，堆先升后降很正常。我们的固定复现脚本：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">1. 打开工单 A，打开图片预览，关闭预览，返回列表</span><br><span class="line">2. 打开工单 B，重复同样动作</span><br><span class="line">3. 连续循环 20 次</span><br><span class="line">4. 空闲并在 DevTools 中手动 GC</span><br><span class="line">5. 重复 3 轮</span><br></pre></td></tr></table></figure><p>Performance Monitor 里同时观察 JS heap、DOM Nodes、Event Listeners。每轮 GC 后基线仍上涨约 120MB，而且 detached DOM&#x2F;监听器数量同步增加，才有足够证据继续做 heap snapshot。</p><p>不要在包含 DevTools 自己选中对象、console 历史引用的状态下测；控制台打印大对象也可能让它被保留。使用干净 profile、固定数据与相同步骤。</p><h2 id="三张-Snapshot-比一张更有用"><a href="#三张-Snapshot-比一张更有用" class="headerlink" title="三张 Snapshot 比一张更有用"></a>三张 Snapshot 比一张更有用</h2><p>我们拍了：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">S0：刚登录并 GC</span><br><span class="line">S1：循环 20 次后 GC</span><br><span class="line">S2：再循环 20 次后 GC</span><br></pre></td></tr></table></figure><p>在 Comparison 视图比较 S2 vs S1，按 retained size 和数量增量排序。<code>PreviewController</code>、<code>Blob</code> 与 Detached HTMLDivElement 都线性增长。单看 shallow size 可能不大，retained size 才显示它通过引用链间接留住多少对象。</p><p>选中一个 <code>PreviewController</code> 查看 Retainers：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Window</span><br><span class="line">└── EventListener (keydown)</span><br><span class="line">    └── handleShortcut closure</span><br><span class="line">        └── previewController</span><br><span class="line">            └── currentImage</span><br><span class="line">                └── Blob</span><br></pre></td></tr></table></figure><p>GC 只能回收不可达对象；Window 是 GC root，只要监听器还在，下面整个链都可达。</p><h2 id="泄漏代码看起来没有任何“大对象”"><a href="#泄漏代码看起来没有任何“大对象”" class="headerlink" title="泄漏代码看起来没有任何“大对象”"></a>泄漏代码看起来没有任何“大对象”</h2><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">setupPreview</span>(<span class="params">previewController</span>) &#123;</span><br><span class="line">  <span class="keyword">function</span> <span class="title function_">handleShortcut</span>(<span class="params">event</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (event.<span class="property">key</span> === <span class="string">&#x27;Escape&#x27;</span>) previewController.<span class="title function_">close</span>()</span><br><span class="line">    <span class="keyword">if</span> (event.<span class="property">key</span> === <span class="string">&#x27;ArrowRight&#x27;</span>) previewController.<span class="title function_">next</span>()</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(<span class="string">&#x27;keydown&#x27;</span>, handleShortcut)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>函数里没有缓存数组，却通过闭包持有 controller。组件每次挂载都调用 setup，永不 remove。</p><p>修复让创建者返回对称销毁函数：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">setupPreview</span>(<span class="params">previewController</span>) &#123;</span><br><span class="line">  <span class="keyword">function</span> <span class="title function_">handleShortcut</span>(<span class="params">event</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (event.<span class="property">key</span> === <span class="string">&#x27;Escape&#x27;</span>) previewController.<span class="title function_">close</span>()</span><br><span class="line">    <span class="keyword">if</span> (event.<span class="property">key</span> === <span class="string">&#x27;ArrowRight&#x27;</span>) previewController.<span class="title function_">next</span>()</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(<span class="string">&#x27;keydown&#x27;</span>, handleShortcut)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">window</span>.<span class="title function_">removeEventListener</span>(<span class="string">&#x27;keydown&#x27;</span>, handleShortcut)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>React 中接入生命周期：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> controller = <span class="title function_">createPreviewController</span>(images)</span><br><span class="line">  <span class="keyword">const</span> removeShortcuts = <span class="title function_">setupPreview</span>(controller)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">removeShortcuts</span>()</span><br><span class="line">    controller.<span class="title function_">destroy</span>()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;, [images])</span><br></pre></td></tr></table></figure><p><code>removeEventListener</code> 必须拿到同一个函数引用。下面这样无法移除原 listener：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(<span class="string">&#x27;resize&#x27;</span>, <span class="function">() =&gt;</span> <span class="title function_">layout</span>())</span><br><span class="line"><span class="variable language_">window</span>.<span class="title function_">removeEventListener</span>(<span class="string">&#x27;resize&#x27;</span>, <span class="function">() =&gt;</span> <span class="title function_">layout</span>())</span><br></pre></td></tr></table></figure><p>两个箭头函数是不同对象。</p><h2 id="第二处泄漏：Object-URL-没有-revoke"><a href="#第二处泄漏：Object-URL-没有-revoke" class="headerlink" title="第二处泄漏：Object URL 没有 revoke"></a>第二处泄漏：Object URL 没有 revoke</h2><p>图片预览用 Blob 创建 URL：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> url = <span class="variable constant_">URL</span>.<span class="title function_">createObjectURL</span>(file)</span><br><span class="line">image.<span class="property">src</span> = url</span><br></pre></td></tr></table></figure><p>对象 URL 会让底层 Blob 保持可用，使用完应释放：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> url = <span class="variable constant_">URL</span>.<span class="title function_">createObjectURL</span>(file)</span><br><span class="line">image.<span class="property">src</span> = url</span><br><span class="line"></span><br><span class="line">image.<span class="title function_">addEventListener</span>(<span class="string">&#x27;load&#x27;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="variable constant_">URL</span>.<span class="title function_">revokeObjectURL</span>(url)</span><br><span class="line">&#125;, &#123; <span class="attr">once</span>: <span class="literal">true</span> &#125;)</span><br></pre></td></tr></table></figure><p>若图片还需下载&#x2F;再次展示，释放时机要跟组件生命周期一致，不能 load 后过早导致后续操作失效。我们在预览 controller destroy 中统一 revoke 当前与预加载 URL。</p><h2 id="第三处不是泄漏，是无上限缓存"><a href="#第三处不是泄漏，是无上限缓存" class="headerlink" title="第三处不是泄漏，是无上限缓存"></a>第三处不是泄漏，是无上限缓存</h2><p>修复 listener 后，内存仍会缓慢增长但最终不下降。另一个 Retainer 指向模块级 Map：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> ticketCache = <span class="keyword">new</span> <span class="title class_">Map</span>()</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">cacheTicket</span>(<span class="params">ticket</span>) &#123;</span><br><span class="line">  ticketCache.<span class="title function_">set</span>(ticket.<span class="property">id</span>, ticket)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Map 设计上永久持有所有访问过的工单，严格说是缓存策略问题，但用户体验与泄漏一样。改成有容量的 LRU&#x2F;TTL，只保存轻量数据；大 Blob 与 DOM 绝不进入通用缓存。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="variable constant_">MAX_ENTRIES</span> = <span class="number">50</span></span><br><span class="line"><span class="keyword">const</span> ticketCache = <span class="keyword">new</span> <span class="title class_">Map</span>()</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">setCache</span>(<span class="params">id, value</span>) &#123;</span><br><span class="line">  ticketCache.<span class="title function_">delete</span>(id)</span><br><span class="line">  ticketCache.<span class="title function_">set</span>(id, &#123; value, <span class="attr">expiresAt</span>: <span class="title class_">Date</span>.<span class="title function_">now</span>() + <span class="number">5</span> * <span class="number">60_000</span> &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (ticketCache.<span class="property">size</span> &gt; <span class="variable constant_">MAX_ENTRIES</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> oldest = ticketCache.<span class="title function_">keys</span>().<span class="title function_">next</span>().<span class="property">value</span></span><br><span class="line">    ticketCache.<span class="title function_">delete</span>(oldest)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>真实项目使用成熟缓存实现，示例只是说明容量与过期必须显式。</p><h2 id="请求没取消通常不是主要堆泄漏，但会制造失效更新"><a href="#请求没取消通常不是主要堆泄漏，但会制造失效更新" class="headerlink" title="请求没取消通常不是主要堆泄漏，但会制造失效更新"></a>请求没取消通常不是主要堆泄漏，但会制造失效更新</h2><p>组件离开后，长请求回调仍捕获 state&#x2F;大数据，至少在请求结束前保留它们，还可能写入已失效页面。统一用 AbortController：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> controller = <span class="keyword">new</span> <span class="title class_">AbortController</span>()</span><br><span class="line"></span><br><span class="line">  <span class="title function_">loadTicket</span>(ticketId, controller.<span class="property">signal</span>)</span><br><span class="line">    .<span class="title function_">then</span>(setTicket)</span><br><span class="line">    .<span class="title function_">catch</span>(<span class="function"><span class="params">error</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (error.<span class="property">name</span> !== <span class="string">&#x27;AbortError&#x27;</span>) <span class="title function_">reportError</span>(error)</span><br><span class="line">    &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="function">() =&gt;</span> controller.<span class="title function_">abort</span>()</span><br><span class="line">&#125;, [ticketId])</span><br></pre></td></tr></table></figure><p>取消客户端等待不保证服务器停止工作，但能释放客户端链路并防止过期结果覆盖。</p><h2 id="Observer、计时器和第三方实例逐个做对称检查"><a href="#Observer、计时器和第三方实例逐个做对称检查" class="headerlink" title="Observer、计时器和第三方实例逐个做对称检查"></a>Observer、计时器和第三方实例逐个做对称检查</h2><p>代码评审我会搜索：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rg <span class="string">&quot;addEventListener|setInterval|setTimeout|new (ResizeObserver|IntersectionObserver|MutationObserver)|subscribe\(&quot;</span> src</span><br></pre></td></tr></table></figure><p>并为每个资源找对应：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">addEventListener -&gt; removeEventListener</span><br><span class="line">setInterval -&gt; clearInterval</span><br><span class="line">Observer.observe -&gt; disconnect/unobserve</span><br><span class="line">store.subscribe -&gt; unsubscribe</span><br><span class="line">editor/chart/map constructor -&gt; destroy/dispose</span><br><span class="line">createObjectURL -&gt; revokeObjectURL</span><br><span class="line">fetch -&gt; abort/忽略过期结果</span><br></pre></td></tr></table></figure><p>不是每个 setTimeout 都会永久泄漏，执行后会释放；但长延迟、重复调度和捕获大对象仍值得检查。第三方编辑器常在 document 上注册事件，仅移除 DOM 容器不够，必须调用它的 destroy。</p><h2 id="WeakMap-不是万能解决方案"><a href="#WeakMap-不是万能解决方案" class="headerlink" title="WeakMap 不是万能解决方案"></a>WeakMap 不是万能解决方案</h2><p>有人建议把所有缓存改 WeakMap。WeakMap 只接受对象 key，且条目是否存在由 key 的可达性决定，不适合需要枚举、按 ID 查询或明确 TTL 的业务缓存。它适合给对象附加元数据，又不希望元数据反过来阻止对象回收。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> measurements = <span class="keyword">new</span> <span class="title class_">WeakMap</span>()</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">remember</span>(<span class="params">element, rect</span>) &#123;</span><br><span class="line">  measurements.<span class="title function_">set</span>(element, rect)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果 value 通过其他路径又持有 key 或全局引用仍在，WeakMap 也不会神奇清理整个系统。先画 Retainer 链，再选数据结构。</p><h2 id="修复后怎样证明不是“看起来降了”"><a href="#修复后怎样证明不是“看起来降了”" class="headerlink" title="修复后怎样证明不是“看起来降了”"></a>修复后怎样证明不是“看起来降了”</h2><p>用完全相同的 3×20 次循环重新拍 S0&#x2F;S1&#x2F;S2。修复后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">PreviewController 增量：趋近 0</span><br><span class="line">keydown listeners：回到基线</span><br><span class="line">Detached DOM：GC 后回到基线</span><br><span class="line">Blob retained size：不再线性增长</span><br><span class="line">总 heap：波动但平台化</span><br></pre></td></tr></table></figure><p>我们又把循环挂载做成 Playwright 压力用例，记录 <code>performance.memory</code> 只作趋势信号；CI 环境噪声大，不用一个绝对 MB 阈值直接判失败。更可靠的自动检查是 listener 数、组件 destroy mock、对象 URL revoke 和请求 abort 都被调用。</p><h2 id="最终复盘"><a href="#最终复盘" class="headerlink" title="最终复盘"></a>最终复盘</h2><p>这次问题不是“一张图太大”，而是 window listener → closure → controller → Blob 的完整保留链。修复 listener 后又发现对象 URL 与无上限 cache，说明内存问题经常有多个来源，必须修一处后重复实验。</p><p>我现在判断泄漏遵循：固定操作、多轮 GC 后看基线、比较 snapshot 增量、沿 Retainers 走到 GC root、修复后原步骤复测。看 Task Manager 猜，或把所有变量设成 null，都没有这条链可靠。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://developer.chrome.com/docs/devtools/memory-problems/">Chrome DevTools：Fix memory problems</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/URL/revokeObjectURL_static">MDN：URL.revokeObjectURL</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/AbortController">MDN：AbortController</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端排障 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端排障 </tag>
            
            <tag> 性能优化 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vite 8：Rolldown 统一内核正式落地</title>
      <link href="/2026/03/12/frontend-chronicle-2026-03-vite-8-rolldown/"/>
      <url>/2026/03/12/frontend-chronicle-2026-03-vite-8-rolldown/</url>
      
        <content type="html"><![CDATA[<p>Vite 8 稳定版发布那天，我们已经跑了三个月 Beta 影子构建，所以升级没有从“看迁移指南”开始，而是从那份失败清单开始。两个插件问题已在 Beta 阶段修复，剩下的是把正式版本放入主线、保留回退路径，并验证 Rolldown 带来的真实收益。</p><p>Vite 8 最大的底层变化，是以 Rolldown 作为统一的 Rust bundler，收拢过去 esbuild 与 Rollup 两条主要管线的差异。官方把它称为 Vite 2 以来最重要的架构升级。对应用团队来说，这句话最终要落到三件事：能否正确构建、插件是否兼容、自己的项目到底快多少。</p><h2 id="按官方建议把-bundler-变化单独隔离"><a href="#按官方建议把-bundler-变化单独隔离" class="headerlink" title="按官方建议把 bundler 变化单独隔离"></a>按官方建议把 bundler 变化单独隔离</h2><p>复杂项目可以先在 Vite 7 使用 <code>rolldown-vite</code> 验证，再升级 Vite 8。我们 Beta 阶段已经完成这步，所以正式升级拆成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">提交 1：Node 版本与 lockfile 统一</span><br><span class="line">提交 2：Vite 7 + rolldown-vite 产物验证</span><br><span class="line">提交 3：Vite 8 正式包与迁移项</span><br><span class="line">提交 4：可选插件/新功能</span><br></pre></td></tr></table></figure><p>如果提交 2 出错，知道是 bundler；提交 3 出错，更可能来自 Vite 8 其他变化。一次性升 Node、React 插件、Vite 和全部插件会失去这种归因。</p><h2 id="兼容层让配置能跑，不代表配置仍然合理"><a href="#兼容层让配置能跑，不代表配置仍然合理" class="headerlink" title="兼容层让配置能跑，不代表配置仍然合理"></a>兼容层让配置能跑，不代表配置仍然合理</h2><p>Vite 8 会为许多现有 esbuild&#x2F;rollupOptions 配置提供兼容转换。旧配置：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">esbuild</span>: &#123;</span><br><span class="line">    <span class="attr">drop</span>: [<span class="string">&#x27;debugger&#x27;</span>]</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">build</span>: &#123;</span><br><span class="line">    <span class="attr">rollupOptions</span>: &#123;</span><br><span class="line">      <span class="attr">output</span>: &#123;</span><br><span class="line">        <span class="attr">manualChunks</span>: &#123;</span><br><span class="line">          <span class="attr">charts</span>: [<span class="string">&#x27;echarts&#x27;</span>]</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>它可能继续工作，但我们逐项确认是否仍有必要。手工把图表库固定成 chunk 曾用于缓存，后来路由只在一个页面使用，强制分包反而让入口多请求。底层升级是删除历史微调的好机会，不是把它们永久交给兼容层。</p><h2 id="我们得到的性能数字"><a href="#我们得到的性能数字" class="headerlink" title="我们得到的性能数字"></a>我们得到的性能数字</h2><p>同一 CI 规格、清缓存后各跑五次取中位数：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">项目：后台 monorepo（约 6200 modules）</span><br><span class="line">Vite 7 production build：41.8s</span><br><span class="line">Vite 8 production build：9.7s</span><br><span class="line">产物 gzip 总量：差异 &lt; 1.5%</span><br><span class="line">E2E：全部通过</span><br></pre></td></tr></table></figure><p>这只是我们的记录，不是所有项目承诺。小型站点从 4 秒降到 2 秒，体感就没有同样夸张。瓶颈如果在类型检查、图片压缩或部署上传，bundler 加速也不会让总流水线按比例下降。</p><p>所以我们同时分阶段计时：依赖安装、类型检查、Vite build、E2E、上传。build 从 42 秒变 10 秒以后，typecheck 成为最长步骤，下一轮优化才有正确目标。</p><h2 id="生产前最容易漏的四条路径"><a href="#生产前最容易漏的四条路径" class="headerlink" title="生产前最容易漏的四条路径"></a>生产前最容易漏的四条路径</h2><h3 id="动态导入"><a href="#动态导入" class="headerlink" title="动态导入"></a>动态导入</h3><p>运行所有懒加载路由，不只访问首页。检查 chunk 404、base URL 和预加载关系。</p><h3 id="CSS-顺序"><a href="#CSS-顺序" class="headerlink" title="CSS 顺序"></a>CSS 顺序</h3><p>打开弹窗、深色模式和异步页面，做视觉 diff。chunk 改变可能暴露全局 CSS 偶然顺序。</p><h3 id="SSR-库模式"><a href="#SSR-库模式" class="headerlink" title="SSR&#x2F;库模式"></a>SSR&#x2F;库模式</h3><p>SSR 要直接启动构建产物，检查 external 与 Node&#x2F;edge API；库模式要在临时 consumer 中分别 import&#x2F;require（若承诺两种格式）。</p><h3 id="source-map"><a href="#source-map" class="headerlink" title="source map"></a>source map</h3><p>在 staging 主动抛一个错误，确认错误平台能还原到源码与 release。构建成功不证明上传&#x2F;映射路径正确。</p><h2 id="React-插件不必与-Vite-同一天升级"><a href="#React-插件不必与-Vite-同一天升级" class="headerlink" title="React 插件不必与 Vite 同一天升级"></a>React 插件不必与 Vite 同一天升级</h2><p>Vite 8 同期的 <code>@vitejs/plugin-react</code> v6 使用 Oxc 处理 React Refresh，默认不再依赖 Babel；需要 React Compiler 时提供显式集成路径。官方说明旧插件 v5 仍可配合 Vite 8。</p><p>我们利用这一点先保留 v5，只换 Vite&#x2F;Rolldown；主线稳定后再单独升级 React 插件。否则 Refresh 或 Compiler 出问题，很难判断来自哪层。</p><p>这种“允许旧插件继续工作”的兼容窗口，应该用来降低变更半径，而不是永远不升级。</p><h2 id="tsconfig-paths-支持要考虑开销和发布契约"><a href="#tsconfig-paths-支持要考虑开销和发布契约" class="headerlink" title="tsconfig paths 支持要考虑开销和发布契约"></a>tsconfig paths 支持要考虑开销和发布契约</h2><p>Vite 8 可以选择启用内置 tsconfig paths 解析：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">resolve</span>: &#123;</span><br><span class="line">    <span class="attr">tsconfigPaths</span>: <span class="literal">true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>它有小的性能成本，且 TypeScript path 并不会自动改变 npm 包的运行时 exports。应用内部可减少 alias 重复，发布库仍需明确公开入口并测试最终产物。我们只在一个 alias 很多的 app 启用，没有为了新功能全仓开启。</p><h2 id="浏览器-console-转发解决了一个真实协作问题"><a href="#浏览器-console-转发解决了一个真实协作问题" class="headerlink" title="浏览器 console 转发解决了一个真实协作问题"></a>浏览器 console 转发解决了一个真实协作问题</h2><p>开发代理&#x2F;远程环境中，错误只在浏览器 console，终端和自动化助手看不到。Vite 8 提供控制台转发能力：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">server</span>: &#123;</span><br><span class="line">    <span class="attr">forwardConsole</span>: <span class="literal">true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>开启前先考虑日志中的 token、个人信息和噪音，生产当然不应使用 dev server 配置。我们只在本地开发启用，并对请求 header 做脱敏，不让“可见性”变成泄密通道。</p><h2 id="回退不是降级整个发布"><a href="#回退不是降级整个发布" class="headerlink" title="回退不是降级整个发布"></a>回退不是降级整个发布</h2><p>升级当天保留上一份 lockfile&#x2F;构建镜像与独立 dist artifact。若 Vite 8 构建出现未知回归，可以用上一条稳定流水线重新构建，而不是 git reset 用户提交。</p><p>部署仍遵循静态资源先行、HTML 后切换，旧 chunk 保留回滚窗口。构建器升级不应该顺便改变成熟发布协议。</p><h2 id="从-Beta-到稳定版，证据可以复用"><a href="#从-Beta-到稳定版，证据可以复用" class="headerlink" title="从 Beta 到稳定版，证据可以复用"></a>从 Beta 到稳定版，证据可以复用</h2><p>Beta 影子构建留下的插件 fixture、产物对比和 E2E 在稳定版直接复用，正式升级只需确认版本回归和迁移项。这就是提前试用最实际的回报：不是抢先使用，而是把未知风险提前变成自动化测试。</p><p>Vite 8 的构建提升在大项目中非常明显，统一内核也让长期开发&#x2F;生产语义更有机会一致。但稳定版不是“无需测试”的同义词。我们最终在正确性、可观测性和回退都就绪后才切主线，速度数字反而是最后确认的一项。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://vite.dev/blog/announcing-vite8">Vite 8 发布说明</a></li><li><a href="https://vite.dev/guide/migration">Vite 8 Migration Guide</a></li><li><a href="https://registry.vite.dev/">Vite Plugin Registry</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Vite </tag>
            
            <tag> 2026 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>React Foundation：核心项目进入更独立的治理阶段</title>
      <link href="/2026/02/24/frontend-chronicle-2026-02-react-foundation/"/>
      <url>/2026/02/24/frontend-chronicle-2026-02-react-foundation/</url>
      
        <content type="html"><![CDATA[<p>2026 年 2 月，React Foundation 在 Linux Foundation 下正式启动。React、React Native 和 JSX 等项目不再由 Meta 持有，而由新的独立基金会承接。基金会董事会负责资源、基础设施与生态支持，技术方向则计划由独立的技术治理结构决定。</p><p>这条新闻不会让组件多一个 Hook，也不会让应用快一毫秒。但对一个依赖 React 十年的团队来说，治理、商标、CI、发布基础设施和维护者来源，本来就是技术风险的一部分。我们借这次变化更新了依赖治理文档，而不是写一段无关的示例组件。</p><h2 id="先区分基金会治理和技术治理"><a href="#先区分基金会治理和技术治理" class="headerlink" title="先区分基金会治理和技术治理"></a>先区分基金会治理和技术治理</h2><p>官方公告里有一个很重要的分离：基金会董事会与 React 的技术治理并不是同一件事。董事会由创始成员代表组成，负责基金会资源与生态；React 技术方向继续由贡献者&#x2F;维护者主导，并由临时领导委员会设计更独立的结构。</p><p>这能避免简单把“出资公司”理解为“直接决定 API”。最终章程、决策流程、维护者晋升和 RFC 透明度仍需后续观察，成立当天并不表示所有治理细节都已经完成。</p><p>官方列出的八个白金创始成员包括 Amazon、Callstack、Expo、Huawei、Meta、Microsoft、Software Mansion 和 Vercel。多家浏览器外&#x2F;云&#x2F;框架生态公司共同参与，至少让资源来源不再只挂在单一公司名下。</p><h2 id="对普通项目，明天的-package-json-不会变化"><a href="#对普通项目，明天的-package-json-不会变化" class="headerlink" title="对普通项目，明天的 package.json 不会变化"></a>对普通项目，明天的 package.json 不会变化</h2><p>React 的 npm 包名、许可证、升级路径不会因为基金会公告自动改变。我们没有发起“基金会迁移 PR”，也没有修改代码。真正做的是把依赖风险从版本号扩大到维护状态。</p><p>项目新增一份简短的依赖政策：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># dependency-policy.yml</span></span><br><span class="line"><span class="attr">critical:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">react</span></span><br><span class="line">    <span class="attr">owner:</span> <span class="string">frontend-platform</span></span><br><span class="line">    <span class="attr">review:</span> <span class="string">quarterly</span></span><br><span class="line">    <span class="attr">sources:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">https://react.dev/blog</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">https://github.com/facebook/react/releases</span></span><br><span class="line">    <span class="attr">checks:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">security-advisories</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">supported-versions</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">license</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">release-cadence</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">migration-window</span></span><br></pre></td></tr></table></figure><p>它不直接参与构建，但明确谁关注官方安全公告、谁安排升级窗口。核心依赖无人负责，比治理模式本身更现实的风险。</p><h2 id="2025-年的-RSC-安全事件说明“官方来源”有多重要"><a href="#2025-年的-RSC-安全事件说明“官方来源”有多重要" class="headerlink" title="2025 年的 RSC 安全事件说明“官方来源”有多重要"></a>2025 年的 RSC 安全事件说明“官方来源”有多重要</h2><p>基金会正式成立前后，React Server Components 生态曾出现需要快速升级的严重安全问题。类似事件中，团队最危险的做法是从群聊截图判断受影响版本。</p><p>我们的响应流程是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">官方安全公告 -&gt; 识别受影响框架/版本 -&gt; 锁定修复版</span><br><span class="line">-&gt; 测试与构建 -&gt; 灰度部署 -&gt; 验证线上实际版本 -&gt; 复盘</span><br></pre></td></tr></table></figure><p>框架经常封装 React 的服务器能力，不能只查 <code>react</code> 顶层版本；要看 Next&#x2F;其他框架公告、lockfile 实际解析与部署产物。基金会可以改善长期基础设施，不能替每个使用方完成供应链响应。</p><h2 id="SBOM-和锁文件比“我们用了-React-19”更准确"><a href="#SBOM-和锁文件比“我们用了-React-19”更准确" class="headerlink" title="SBOM 和锁文件比“我们用了 React 19”更准确"></a>SBOM 和锁文件比“我们用了 React 19”更准确</h2><p>monorepo 中可能同时存在多个 React&#x2F;renderer 版本，口头说主版本不够。我们在发布产物生成依赖清单并检查重复版本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">npm <span class="built_in">ls</span> react react-dom --all</span><br><span class="line">npm audit --omit=dev</span><br></pre></td></tr></table></figure><p><code>npm audit</code> 不是完整安全方案，但能作为一条输入；还需跟踪框架官方 advisory 和运行时暴露面。锁文件要纳入评审，生产镜像&#x2F;静态产物应能对应到具体 commit 与 release。</p><h2 id="基金会降低的是哪类风险"><a href="#基金会降低的是哪类风险" class="headerlink" title="基金会降低的是哪类风险"></a>基金会降低的是哪类风险</h2><p>可能的长期好处包括：</p><ul><li>仓库、网站、CI、商标和大会有独立法律&#x2F;资金载体；</li><li>多家公司能通过共同机制投入维护资源；</li><li>React 与 React Native&#x2F;JSX 等生态项目能获得持续支持；</li><li>技术决策机制有机会更公开地与单一公司组织解耦。</li></ul><p>但基金会结构并不自动保证 API 稳定、没有安全漏洞或所有 RFC 都符合某个团队偏好。工程选型仍要看 release 质量、维护者响应、文档、生态兼容和迁移成本。</p><h2 id="我们如何评估“关键开源依赖”"><a href="#我们如何评估“关键开源依赖”" class="headerlink" title="我们如何评估“关键开源依赖”"></a>我们如何评估“关键开源依赖”</h2><p>React 只是其中一个。对于构建器、框架、包管理器和监控 SDK，我们都会看：</p><ol><li>是否有多名活跃维护者，bus factor 怎样；</li><li>安全公告是否有明确渠道和修复节奏；</li><li>版本支持&#x2F;弃用政策是否可预测；</li><li>财务&#x2F;组织支持是否可持续；</li><li>是否存在开放的 issue&#x2F;RFC&#x2F;发布记录；</li><li>项目若停更，迁移出口和数据格式是否开放。</li></ol><p>这里没有一个分数能自动决定选型。基金会是积极信号，实际技术证据仍要持续观察。</p><h2 id="不因为治理变化做“框架逃生演习”，但保留边界"><a href="#不因为治理变化做“框架逃生演习”，但保留边界" class="headerlink" title="不因为治理变化做“框架逃生演习”，但保留边界"></a>不因为治理变化做“框架逃生演习”，但保留边界</h2><p>我们不会为假想的 React 停更重写 UI；会避免把领域逻辑全部绑在组件生命周期里。请求、权限、数据转换和业务规则尽量保持可独立测试，设计系统有清楚接口，部署不只支持单一云平台。</p><p>这不是为了随时换框架，而是正常的软件边界。即便永远使用 React，它也能让测试和升级更容易。</p><h2 id="这条新闻真正值得记录的原因"><a href="#这条新闻真正值得记录的原因" class="headerlink" title="这条新闻真正值得记录的原因"></a>这条新闻真正值得记录的原因</h2><p>React 从 Meta 内部项目成长为跨公司生态，组织形式终于跟上了影响范围。基金会正式落地，意味着开源项目的“谁拥有基础设施、谁提供资源、谁决定技术方向”开始被更明确地拆开。</p><p>对业务团队，最务实的动作不是庆祝或质疑口号，而是确保关键依赖有负责人、官方信息源、升级演练和可追踪产物。组织治理能降低一部分上游风险，下游使用责任仍然在我们自己手里。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://react.dev/blog/2026/02/24/the-react-foundation">React Foundation 正式成立公告</a></li><li><a href="https://react.dev/blog/2025/10/07/introducing-the-react-foundation">React Foundation 筹备公告</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> React </tag>
            
            <tag> 2026 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vite 8 Beta：Rolldown 开始统一开发与生产构建</title>
      <link href="/2025/12/03/frontend-chronicle-2025-12-vite-8-beta-rolldown/"/>
      <url>/2025/12/03/frontend-chronicle-2025-12-vite-8-beta-rolldown/</url>
      
        <content type="html"><![CDATA[<p>Vite 8 Beta 公布 Rolldown 集成后，我们做的第一件事不是更新主分支，而是在 CI 增加一条允许失败的“影子构建”。Vite 从早期开始在开发依赖优化&#x2F;转换与生产打包中组合 esbuild、Rollup；Rolldown 的目标是用 Rust 实现的统一 bundler 收拢主要路径，同时尽可能兼容 Rollup 插件。</p><p>这类底层替换最适合 Beta 阶段做真实反馈，也最不适合只看一次构建耗时就直接上线。我们需要知道：产物是否等价、插件钩子是否完整、动态导入与 CSS 顺序是否变化，以及构建快在哪一段。</p><h2 id="影子构建不覆盖正式-dist"><a href="#影子构建不覆盖正式-dist" class="headerlink" title="影子构建不覆盖正式 dist"></a>影子构建不覆盖正式 dist</h2><p>CI 同一次提交分别运行稳定版与 Beta，各输出独立目录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">npm run build:stable -- --outDir dist-stable</span><br><span class="line">npm run build:rolldown -- --outDir dist-rolldown</span><br></pre></td></tr></table></figure><p>Beta 分支不上传生产 CDN，不写正式 manifest。流水线保存两份 artifact 和统计：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;buildMs&quot;</span><span class="punctuation">:</span> <span class="number">8421</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;jsBytes&quot;</span><span class="punctuation">:</span> <span class="number">1832041</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;cssBytes&quot;</span><span class="punctuation">:</span> <span class="number">284190</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;chunks&quot;</span><span class="punctuation">:</span> <span class="number">76</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;warnings&quot;</span><span class="punctuation">:</span> <span class="number">0</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>连续跑多次取中位数，区分冷&#x2F;热缓存。CI 机器负载会抖动，单次“快 40%”不能作为结论。</p><h2 id="比较文件名之前，先比较业务语义"><a href="#比较文件名之前，先比较业务语义" class="headerlink" title="比较文件名之前，先比较业务语义"></a>比较文件名之前，先比较业务语义</h2><p>两个 bundler 的 chunk 划分或 hash 算法可能不同，逐文件名相等没有意义。我们的分层验证是：</p><ol><li>两份产物都能由静态服务器直接访问；</li><li>Playwright 跑登录、懒加载路由、上传与报表；</li><li>manifest 包含部署端需要的入口&#x2F;CSS；</li><li>source map 能在错误平台还原；</li><li>动态 import 失败恢复、CSP nonce 与 base 路径正确；</li><li>最后才比较体积、chunk 数与耗时。</li></ol><p>正确性过关以后，性能数字才有意义。</p><h2 id="第一个失败来自插件对-chunk-顺序的假设"><a href="#第一个失败来自插件对-chunk-顺序的假设" class="headerlink" title="第一个失败来自插件对 chunk 顺序的假设"></a>第一个失败来自插件对 chunk 顺序的假设</h2><p>自研 HTML 插件取 <code>bundle</code> 中“第一个 chunk”当入口：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">generateBundle</span>(<span class="params">_options, bundle</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> firstChunk = <span class="title class_">Object</span>.<span class="title function_">values</span>(bundle)</span><br><span class="line">    .<span class="title function_">find</span>(<span class="function"><span class="params">item</span> =&gt;</span> item.<span class="property">type</span> === <span class="string">&#x27;chunk&#x27;</span>)</span><br><span class="line">  <span class="title function_">writeEntry</span>(firstChunk.<span class="property">fileName</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>稳定构建里它碰巧成立，Rolldown 输出迭代顺序变化后选错。插件本来就写得不可靠，应该根据 <code>isEntry</code> 与明确 name 查找：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> entry = <span class="title class_">Object</span>.<span class="title function_">values</span>(bundle).<span class="title function_">find</span>(</span><br><span class="line">  <span class="function"><span class="params">item</span> =&gt;</span> item.<span class="property">type</span> === <span class="string">&#x27;chunk&#x27;</span> &amp;&amp; item.<span class="property">isEntry</span> &amp;&amp; item.<span class="property">name</span> === <span class="string">&#x27;index&#x27;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!entry) <span class="variable language_">this</span>.<span class="title function_">error</span>(<span class="string">&#x27;index entry not found&#x27;</span>)</span><br></pre></td></tr></table></figure><p>Beta 暴露的是既有 bug，而不是简单的“不兼容”。修复同时回到稳定分支，插件也加了 fixture 测试。</p><h2 id="第二个差异只发生在动态-import"><a href="#第二个差异只发生在动态-import" class="headerlink" title="第二个差异只发生在动态 import"></a>第二个差异只发生在动态 import</h2><p>一个页面用变量拼路径：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="variable language_">module</span> = <span class="keyword">await</span> <span class="keyword">import</span>(<span class="string">`./locales/<span class="subst">$&#123;language&#125;</span>.ts`</span>)</span><br></pre></td></tr></table></figure><p>不同分析&#x2F;插件路径对动态模式处理有差异。我们改成显式 glob：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> locales = <span class="keyword">import</span>.<span class="property">meta</span>.<span class="title function_">glob</span>(<span class="string">&#x27;./locales/*.ts&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">loadLocale</span>(<span class="params"><span class="attr">language</span>: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> loader = locales[<span class="string">`./locales/<span class="subst">$&#123;language&#125;</span>.ts`</span>]</span><br><span class="line">  <span class="keyword">if</span> (!loader) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`Unsupported locale: <span class="subst">$&#123;language&#125;</span>`</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="title function_">loader</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>支持集合从隐式目录变成可检查映射，也更容易处理错误语言。统一内核不能替我们让任意动态路径变得可静态分析。</p><h2 id="Rollup-兼容不等于内部-API-兼容"><a href="#Rollup-兼容不等于内部-API-兼容" class="headerlink" title="Rollup 兼容不等于内部 API 兼容"></a>Rollup 兼容不等于内部 API 兼容</h2><p>标准插件钩子大多能工作，导入 Vite&#x2F;Rollup 内部路径、修改私有 AST 字段或依赖输出顺序的插件风险最高。我们按类别盘点：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">官方框架插件：跟随 Beta 版本</span><br><span class="line">标准 Rollup 插件：跑 dev/build fixture</span><br><span class="line">自研标准钩子：重点检查输出与 sourcemap</span><br><span class="line">内部 API/monkey patch：先移除或隔离</span><br></pre></td></tr></table></figure><p>每个插件还要同时跑开发 HMR和生产 build。Beta 核心替换的目标之一是统一路径，但实际钩子在 dev&#x2F;build 的触发时机仍需验证。</p><h2 id="CSS-顺序不能只靠截图一页"><a href="#CSS-顺序不能只靠截图一页" class="headerlink" title="CSS 顺序不能只靠截图一页"></a>CSS 顺序不能只靠截图一页</h2><p>chunk 划分变化可能改变 CSS 提取与加载顺序，尤其多个异步页面都覆盖组件库变量时。我们既做视觉回归，也检查构建 warning，并减少依赖导入顺序的全局覆盖。</p><p>更稳的样式架构是设计 token、明确 layer 和组件作用域，而不是期待某个页面 chunk 恰好最后加载。构建器替换常把这种偶然顺序暴露出来。</p><h2 id="Beta-期间怎样处理上游问题"><a href="#Beta-期间怎样处理上游问题" class="headerlink" title="Beta 期间怎样处理上游问题"></a>Beta 期间怎样处理上游问题</h2><p>最小复现比上传整个公司仓库更有用。我们把失败插件缩成十几行 fixture，记录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Vite/Rolldown 版本</span><br><span class="line">Node 与操作系统</span><br><span class="line">最小配置</span><br><span class="line">预期/实际输出</span><br><span class="line">稳定版对照</span><br><span class="line">是否只在 dev/build/SSR 出现</span><br></pre></td></tr></table></figure><p>能证明是 Beta 回归的就提交上游 issue；业务私有假设则自己修。试用 prerelease 的价值包括给生态反馈，不只是提前享受速度。</p><h2 id="什么时候才允许进入主线"><a href="#什么时候才允许进入主线" class="headerlink" title="什么时候才允许进入主线"></a>什么时候才允许进入主线</h2><p>我们设了门槛：连续两周影子构建全绿；核心 E2E、SSR&#x2F;库模式（适用时）通过；关键插件有 fixture；产物体积无无法解释回退；错误平台 source map 验证；有一键回到稳定构建的脚本。</p><p>Beta 最终仍没有直接成为生产默认。等稳定版发布后，我们用同一套证据重新验证。这样试用工作不会浪费，主线也不承担不必要风险。</p><h2 id="Beta-阶段最有价值的收获"><a href="#Beta-阶段最有价值的收获" class="headerlink" title="Beta 阶段最有价值的收获"></a>Beta 阶段最有价值的收获</h2><p>构建时间改善很吸引人，但这次影子构建更早发现了入口顺序假设、动态导入和 CSS 覆盖问题。它们在现有工具中只是碰巧稳定，换内核后才显形。</p><p>底层统一能减少长期的开发&#x2F;生产语义差异，前提是插件生态和真实项目一起验证。对 Beta 最负责的使用方式不是发一张 benchmark 截图，而是让相同提交产生两份可运行产物，用测试说明差异，再把最小问题反馈给上游。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://vite.dev/blog/announcing-vite8-beta">Vite 8 Beta 发布说明</a></li><li><a href="https://rolldown.rs/">Rolldown 官方文档</a></li><li><a href="https://vite.dev/guide/api-plugin">Vite：Plugin API</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Vite </tag>
            
            <tag> 2025 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>React Compiler 1.0 与 Next.js 16：自动优化进入框架主线</title>
      <link href="/2025/10/21/frontend-chronicle-2025-10-react-compiler-next-16/"/>
      <url>/2025/10/21/frontend-chronicle-2025-10-react-compiler-next-16/</url>
      
        <content type="html"><![CDATA[<p>React Compiler 1.0 稳定以后，我们没有在整个 monorepo 打开开关。第一个试点是一张经常重渲染的排班表：父组件每次选中单元格，几十个无关行也会重新执行。代码里已有不少 <code>memo</code>、<code>useMemo</code> 和 <code>useCallback</code>，其中一部分有依据，另一部分只是前人为了“性能”加上。</p><p>Compiler 的价值是通过构建时分析自动加入记忆化，让开发者少手工维护引用稳定性；它的前提是组件遵守 React Rules——渲染纯净、状态不可变、Hook 调用合法。试点真正花时间的部分不是配置，而是 lint 把几处不纯代码翻了出来。</p><h2 id="先记录现状，不用“感觉更快”验收"><a href="#先记录现状，不用“感觉更快”验收" class="headerlink" title="先记录现状，不用“感觉更快”验收"></a>先记录现状，不用“感觉更快”验收</h2><p>我们保存三组基线：</p><ul><li>React Profiler 中选中一个单元格时的 commit 与组件 render 数；</li><li>低端设备上拖动排班范围的 Interaction to Next Paint；</li><li>生产 bundle、构建时间和测试结果。</li></ul><p>一段已有优化：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> visibleRows = <span class="title function_">useMemo</span>(</span><br><span class="line">  <span class="function">() =&gt;</span> <span class="title function_">filterRows</span>(rows, keyword, department),</span><br><span class="line">  [rows, keyword, department]</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> handleSelect = <span class="title function_">useCallback</span>(<span class="function"><span class="params">id</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="title function_">setSelectedId</span>(id)</span><br><span class="line">&#125;, [])</span><br></pre></td></tr></table></figure><p>不能看到 Compiler 就把它们全删。先启用、对比生成行为与性能，再逐步清理冗余 memo；有明确语义&#x2F;昂贵计算保护的手工优化也可能继续保留。</p><h2 id="lint-找到了一次-render-中的写操作"><a href="#lint-找到了一次-render-中的写操作" class="headerlink" title="lint 找到了一次 render 中的写操作"></a>lint 找到了一次 render 中的写操作</h2><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> recentRows = []</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">ScheduleRow</span>(<span class="params">&#123; row &#125;</span>) &#123;</span><br><span class="line">  recentRows.<span class="title function_">push</span>(row.<span class="property">id</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span>&#123;row.name&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>组件 render 修改模块级数组，重复渲染、并发或中断都会留下不同结果。编译器不能安全假设它是纯函数。正确做法取决于意图：如果用于调试就删除；如果用于统计，在事件或 Effect 中上报；如果只是计算列表，作为 render 的局部值并返回。</p><p>另一个问题是直接修改 props：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">SortableList</span>(<span class="params">&#123; items &#125;</span>) &#123;</span><br><span class="line">  items.<span class="title function_">sort</span>(<span class="function">(<span class="params">a, b</span>) =&gt;</span> a.<span class="property">rank</span> - b.<span class="property">rank</span>)</span><br><span class="line">  <span class="keyword">return</span> items.<span class="title function_">map</span>(<span class="function"><span class="params">item</span> =&gt;</span> <span class="language-xml"><span class="tag">&lt;<span class="name">Row</span> <span class="attr">key</span>=<span class="string">&#123;item.id&#125;</span> <span class="attr">item</span>=<span class="string">&#123;item&#125;</span> /&gt;</span></span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>改成创建新数组：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> sortedItems = [...items].<span class="title function_">sort</span>(<span class="function">(<span class="params">a, b</span>) =&gt;</span> a.<span class="property">rank</span> - b.<span class="property">rank</span>)</span><br></pre></td></tr></table></figure><p>这些修复即使不开 Compiler 也有价值。编译器诊断更像代码健康检查，不是为了通过而加一堆 disable。</p><h2 id="在-Next-js-16-中做小范围集成"><a href="#在-Next-js-16-中做小范围集成" class="headerlink" title="在 Next.js 16 中做小范围集成"></a>在 Next.js 16 中做小范围集成</h2><p>Next.js 16 提供 React Compiler 稳定集成。具体配置随版本文档演进，我们先只对试点 app 开启，并固定编译器版本&#x2F;lockfile：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="keyword">type</span> &#123; <span class="title class_">NextConfig</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;next&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="attr">nextConfig</span>: <span class="title class_">NextConfig</span> = &#123;</span><br><span class="line">  <span class="attr">reactCompiler</span>: <span class="literal">true</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> nextConfig</span><br></pre></td></tr></table></figure><p>编译器当前以构建插件形式工作，会增加一部分构建成本。我们在 CI 分别记录类型检查、编译、静态生成各阶段，不能只看总耗时后猜。</p><p>不满足规则的代码，编译器可以跳过部分优化而不是让应用全部坏掉，但诊断要进入可见的技术债清单。团队不能误以为打开 true 就“所有组件自动最快”。</p><h2 id="先让-Compiler-与手工-memo-共存"><a href="#先让-Compiler-与手工-memo-共存" class="headerlink" title="先让 Compiler 与手工 memo 共存"></a>先让 Compiler 与手工 memo 共存</h2><p>第一周不删除任何 <code>memo</code>，用 Profiler 确认排班表无关行 render 数下降、交互改善、结果正确。第二周才挑没有收益&#x2F;依赖难维护的 memo：</p><figure class="highlight diff"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="deletion">- export default memo(ScheduleRow)</span></span><br><span class="line"><span class="addition">+ export default ScheduleRow</span></span><br></pre></td></tr></table></figure><p>每批删除后跑相同交互脚本。如果某个子组件接收第三方可变对象或计算边界编译器无法证明，就保留手工优化并写明测量依据。</p><p>Compiler 不是禁止 useMemo&#x2F;useCallback，而是让它们从默认仪式回到针对性工具。</p><h2 id="Turbopack-成为默认是另一条独立迁移线"><a href="#Turbopack-成为默认是另一条独立迁移线" class="headerlink" title="Turbopack 成为默认是另一条独立迁移线"></a>Turbopack 成为默认是另一条独立迁移线</h2><p>Next.js 16 同时让 Turbopack 成为默认 bundler（开发和生产路径）。这和 React Compiler 都触及构建，若同时切换，出现 source map、插件或 bundle 差异时难归因。</p><p>我们的顺序：</p><ol><li>先在 webpack 路径启用 Compiler，验证 React 行为；</li><li>固定结果后使用默认 Turbopack 影子构建；</li><li>比较 routes、chunk、CSS、source map、Sentry、server external；</li><li>自定义 webpack 配置无法等价时暂时使用 <code>--webpack</code> 回退；</li><li>Turbopack 验证完成后才改主命令。</li></ol><p>性能优化不能以失去可观测性和构建正确性为代价。</p><h2 id="Cache-Components-不要与组件-memo-混为一谈"><a href="#Cache-Components-不要与组件-memo-混为一谈" class="headerlink" title="Cache Components 不要与组件 memo 混为一谈"></a>Cache Components 不要与组件 memo 混为一谈</h2><p>Next 16 的 Cache Components&#x2F;<code>use cache</code> 处理服务器页面、组件或函数的数据&#x2F;渲染缓存；React Compiler 的 memoization 处理组件&#x2F;Hook 的客户端或 React 计算优化。两者都出现“cache”，生命周期和风险完全不同。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">ProductSummary</span>(<span class="params">&#123; id &#125;</span>) &#123;</span><br><span class="line">  <span class="string">&#x27;use cache&#x27;</span></span><br><span class="line">  <span class="keyword">const</span> product = <span class="keyword">await</span> <span class="title function_">getProduct</span>(id)</span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">Summary</span> <span class="attr">product</span>=<span class="string">&#123;product&#125;</span> /&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>是否使用服务器缓存要基于数据新鲜度与用户隔离；是否让 Compiler 优化组件要基于纯函数规则和 render 数据流。不能因为 Compiler 安全就推导服务器数据也能跨请求缓存。</p><h2 id="试点发现的收益与限制"><a href="#试点发现的收益与限制" class="headerlink" title="试点发现的收益与限制"></a>试点发现的收益与限制</h2><p>排班表减少了大量无关行执行，INP 场景改善；构建时间略有增加，在可接受范围。几个使用动态模式的组件未能完全优化，保留手工 memo。更重要的是移除了 render 写全局、props 原地排序和缺失 Hook 规则的代码。</p><p>我们没有把所有性能问题归给 Compiler。一次拖动仍卡顿，Profiler 显示不是 React render，而是主线程上的布局测量与 600ms 同步计算，最后用虚拟化&#x2F;Worker 解决。自动 memo 不能优化网络、DOM layout 和算法复杂度。</p><h2 id="推广规则"><a href="#推广规则" class="headerlink" title="推广规则"></a>推广规则</h2><ul><li>先升级 <code>eslint-plugin-react-hooks</code> 并处理规则；</li><li>每个 app 独立启用，不在 monorepo 根一刀切；</li><li>保存 Profiler 与真实用户指标基线；</li><li>手工 memo 分批删除，有证据再改；</li><li>编译器诊断不做整库 silent suppress；</li><li>与 bundler&#x2F;框架缓存迁移分开提交。</li></ul><p>React Compiler 让性能优化更接近编译器可以证明的数据流，而不是开发者猜哪个引用要稳定。它不会替代架构和测量，但能把大量样板 memo 从日常代码中拿走。对我们来说，最大的收益甚至不是少写 <code>useMemo</code>，而是组件纯净规则第一次有了更强的自动反馈。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://react.dev/blog/2025/10/07/react-compiler-1">React Compiler 1.0 发布说明</a></li><li><a href="https://react.dev/learn/react-compiler/incremental-adoption">React Compiler：Incremental Adoption</a></li><li><a href="https://nextjs.org/blog/next-16">Next.js 16 发布说明</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> React </tag>
            
            <tag> 2025 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>TypeScript never 排查：类型为什么被收窄到不可能</title>
      <link href="/2025/08/15/frontend-debug-typescript-never-inference/"/>
      <url>/2025/08/15/frontend-debug-typescript-never-inference/</url>
      
        <content type="html"><![CDATA[<p><code>never</code> 第一次出现在业务代码里时很像 TypeScript 发脾气：变量明明存在，为什么提示 <code>Property &#39;id&#39; does not exist on type &#39;never&#39;</code>？后来我把它当成一个诊断信号——类型系统认为“走到这里已经没有任何可能值”。</p><p>有时这是推导信息不足，例如空数组；有时是控制流已经穷尽；还有时是上游模型写错，两个条件把所有可能性排除了。直接 <code>as any</code> 会把线索抹掉。下面按几个真实常见病例分别查。</p><h2 id="病例一：空数组没有元素信息"><a href="#病例一：空数组没有元素信息" class="headerlink" title="病例一：空数组没有元素信息"></a>病例一：空数组没有元素信息</h2><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> tasks = []</span><br><span class="line">tasks.<span class="title function_">push</span>(&#123; <span class="attr">id</span>: <span class="string">&#x27;build&#x27;</span>, <span class="attr">done</span>: <span class="literal">false</span> &#125;)</span><br></pre></td></tr></table></figure><p>在不同上下文和编译选项下，空数组可能被推导得过窄，React <code>useState([])</code> 也常得到 <code>never[]</code>：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> [tasks, setTasks] = <span class="title function_">useState</span>([])</span><br><span class="line"></span><br><span class="line"><span class="title function_">setTasks</span>(<span class="function"><span class="params">current</span> =&gt;</span> [</span><br><span class="line">  ...current,</span><br><span class="line">  &#123; <span class="attr">id</span>: <span class="string">&#x27;build&#x27;</span>, <span class="attr">done</span>: <span class="literal">false</span> &#125;</span><br><span class="line">])</span><br></pre></td></tr></table></figure><p>初始值没有告诉泛型元素类型。直接补业务类型：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> <span class="title class_">Task</span> = &#123;</span><br><span class="line">  <span class="attr">id</span>: <span class="built_in">string</span></span><br><span class="line">  <span class="attr">done</span>: <span class="built_in">boolean</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> [tasks, setTasks] = useState&lt;<span class="title class_">Task</span>[]&gt;([])</span><br></pre></td></tr></table></figure><p>这不是“骗过 TypeScript”，而是初始化数据确实不足以推导未来元素。容器 API 设计也应该允许调用方提供泛型。</p><h2 id="病例二：一个分支已经不可能进入"><a href="#病例二：一个分支已经不可能进入" class="headerlink" title="病例二：一个分支已经不可能进入"></a>病例二：一个分支已经不可能进入</h2><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> <span class="title class_">Status</span> = <span class="string">&#x27;idle&#x27;</span> | <span class="string">&#x27;loading&#x27;</span> | <span class="string">&#x27;success&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">label</span>(<span class="params"><span class="attr">status</span>: <span class="title class_">Status</span></span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (status === <span class="string">&#x27;idle&#x27;</span>) <span class="keyword">return</span> <span class="string">&#x27;等待&#x27;</span></span><br><span class="line">  <span class="keyword">if</span> (status === <span class="string">&#x27;loading&#x27;</span>) <span class="keyword">return</span> <span class="string">&#x27;加载中&#x27;</span></span><br><span class="line">  <span class="keyword">if</span> (status === <span class="string">&#x27;success&#x27;</span>) <span class="keyword">return</span> <span class="string">&#x27;完成&#x27;</span></span><br><span class="line"></span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(status) <span class="comment">// never</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里 <code>never</code> 是正确的：三种联合成员已经处理完。可以利用它做穷尽检查：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">assertNever</span>(<span class="params"><span class="attr">value</span>: <span class="built_in">never</span></span>): <span class="built_in">never</span> &#123;</span><br><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`Unexpected value: <span class="subst">$&#123;<span class="built_in">String</span>(value)&#125;</span>`</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">label</span>(<span class="params"><span class="attr">status</span>: <span class="title class_">Status</span></span>) &#123;</span><br><span class="line">  <span class="keyword">switch</span> (status) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">&#x27;idle&#x27;</span>: <span class="keyword">return</span> <span class="string">&#x27;等待&#x27;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="string">&#x27;loading&#x27;</span>: <span class="keyword">return</span> <span class="string">&#x27;加载中&#x27;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="string">&#x27;success&#x27;</span>: <span class="keyword">return</span> <span class="string">&#x27;完成&#x27;</span></span><br><span class="line">    <span class="attr">default</span>: <span class="keyword">return</span> <span class="title function_">assertNever</span>(status)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>以后 Status 新增 <code>&#39;error&#39;</code>，default 中的 status 不再是 never，编译器会提醒补分支。这里不应该“修掉 never”，它正在保护完整性。</p><h2 id="病例三：业务状态用多个布尔值，条件互相打架"><a href="#病例三：业务状态用多个布尔值，条件互相打架" class="headerlink" title="病例三：业务状态用多个布尔值，条件互相打架"></a>病例三：业务状态用多个布尔值，条件互相打架</h2><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> <span class="title class_">RequestState</span> = &#123;</span><br><span class="line">  <span class="attr">loading</span>: <span class="built_in">boolean</span></span><br><span class="line">  <span class="attr">success</span>: <span class="built_in">boolean</span></span><br><span class="line">  <span class="attr">error</span>?: <span class="title class_">Error</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它允许 <code>loading=true</code>、<code>success=true</code>、<code>error</code> 同时存在。开发者再写复杂判断，控制流很快进入难以理解的交集。</p><p>用可辨识联合表达合法状态：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> <span class="title class_">RequestState</span>&lt;T&gt; =</span><br><span class="line">  | &#123; <span class="attr">status</span>: <span class="string">&#x27;idle&#x27;</span> &#125;</span><br><span class="line">  | &#123; <span class="attr">status</span>: <span class="string">&#x27;loading&#x27;</span> &#125;</span><br><span class="line">  | &#123; <span class="attr">status</span>: <span class="string">&#x27;success&#x27;</span>; <span class="attr">data</span>: T &#125;</span><br><span class="line">  | &#123; <span class="attr">status</span>: <span class="string">&#x27;error&#x27;</span>; <span class="attr">error</span>: <span class="title class_">Error</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> render&lt;T&gt;(<span class="attr">state</span>: <span class="title class_">RequestState</span>&lt;T&gt;) &#123;</span><br><span class="line">  <span class="keyword">switch</span> (state.<span class="property">status</span>) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">&#x27;idle&#x27;</span>: <span class="keyword">return</span> <span class="string">&#x27;尚未加载&#x27;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="string">&#x27;loading&#x27;</span>: <span class="keyword">return</span> <span class="string">&#x27;加载中&#x27;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="string">&#x27;success&#x27;</span>: <span class="keyword">return</span> state.<span class="property">data</span></span><br><span class="line">    <span class="keyword">case</span> <span class="string">&#x27;error&#x27;</span>: <span class="keyword">return</span> state.<span class="property">error</span>.<span class="property">message</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>never</code> 经常是在提醒上游模型不够准确，而不是某个属性需要强制断言。</p><h2 id="病例四：类型守卫说了一个不真实的承诺"><a href="#病例四：类型守卫说了一个不真实的承诺" class="headerlink" title="病例四：类型守卫说了一个不真实的承诺"></a>病例四：类型守卫说了一个不真实的承诺</h2><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> <span class="title class_">Cat</span> = &#123; <span class="attr">kind</span>: <span class="string">&#x27;cat&#x27;</span>; <span class="title function_">meow</span>(): <span class="built_in">void</span> &#125;</span><br><span class="line"><span class="keyword">type</span> <span class="title class_">Dog</span> = &#123; <span class="attr">kind</span>: <span class="string">&#x27;dog&#x27;</span>; <span class="title function_">bark</span>(): <span class="built_in">void</span> &#125;</span><br><span class="line"><span class="keyword">type</span> <span class="title class_">Pet</span> = <span class="title class_">Cat</span> | <span class="title class_">Dog</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">isCat</span>(<span class="params"><span class="attr">pet</span>: <span class="title class_">Pet</span></span>): pet is <span class="title class_">Cat</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> pet.<span class="property">kind</span> === <span class="string">&#x27;dog&#x27;</span> <span class="comment">// 实现与谓词相反</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>TypeScript 会信任用户定义的 type predicate，错误守卫让后续分支收窄到荒谬状态。应该让谓词和实际检查一致，并给守卫写运行时测试：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">isCat</span>(<span class="params"><span class="attr">pet</span>: <span class="title class_">Pet</span></span>): pet is <span class="title class_">Cat</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> pet.<span class="property">kind</span> === <span class="string">&#x27;cat&#x27;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对不可信 JSON，不要仅用 <code>as Pet</code> 后再守卫一两个字段；使用完整 schema 校验建立可信类型。</p><h2 id="病例五：泛型条件类型分发后没有成员"><a href="#病例五：泛型条件类型分发后没有成员" class="headerlink" title="病例五：泛型条件类型分发后没有成员"></a>病例五：泛型条件类型分发后没有成员</h2><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> <span class="title class_">ExtractByKind</span>&lt;T, K&gt; = T <span class="keyword">extends</span> &#123; <span class="attr">kind</span>: K &#125; ? T : <span class="built_in">never</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> <span class="title class_">Event</span> =</span><br><span class="line">  | &#123; <span class="attr">kind</span>: <span class="string">&#x27;click&#x27;</span>; <span class="attr">x</span>: <span class="built_in">number</span>; <span class="attr">y</span>: <span class="built_in">number</span> &#125;</span><br><span class="line">  | &#123; <span class="attr">kind</span>: <span class="string">&#x27;submit&#x27;</span>; <span class="attr">formId</span>: <span class="built_in">string</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> <span class="title class_">ScrollEvent</span> = <span class="title class_">ExtractByKind</span>&lt;<span class="title class_">Event</span>, <span class="string">&#x27;scroll&#x27;</span>&gt; <span class="comment">// never</span></span><br></pre></td></tr></table></figure><p>这里没有 kind&#x3D;scroll 的成员，结果理应 never。若业务以为存在 scroll，应该修联合类型或调用参数，而不是在使用处断言。</p><p>调试复杂类型时，把每一步拆成命名别名：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> <span class="title class_">Kinds</span> = <span class="title class_">Event</span>[<span class="string">&#x27;kind&#x27;</span>]</span><br><span class="line"><span class="keyword">type</span> <span class="title class_">ClickOnly</span> = <span class="title class_">ExtractByKind</span>&lt;<span class="title class_">Event</span>, <span class="string">&#x27;click&#x27;</span>&gt;</span><br><span class="line"><span class="keyword">type</span> <span class="title class_">Missing</span> = <span class="title class_">ExtractByKind</span>&lt;<span class="title class_">Event</span>, <span class="string">&#x27;scroll&#x27;</span>&gt;</span><br></pre></td></tr></table></figure><p>IDE hover 每个中间类型，比盯着一条 200 字符条件类型有效。</p><h2 id="病例六：filter-以后推断比预期窄或不够窄"><a href="#病例六：filter-以后推断比预期窄或不够窄" class="headerlink" title="病例六：filter 以后推断比预期窄或不够窄"></a>病例六：<code>filter</code> 以后推断比预期窄或不够窄</h2><p>过滤空值建议提供正确守卫：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="attr">values</span>: <span class="title class_">Array</span>&lt;<span class="built_in">string</span> | <span class="literal">null</span>&gt; = [<span class="string">&#x27;a&#x27;</span>, <span class="literal">null</span>, <span class="string">&#x27;b&#x27;</span>]</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> isDefined&lt;T&gt;(<span class="attr">value</span>: T | <span class="literal">null</span> | <span class="literal">undefined</span>): value is T &#123;</span><br><span class="line">  <span class="keyword">return</span> value != <span class="literal">null</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> strings = values.<span class="title function_">filter</span>(isDefined) <span class="comment">// string[]</span></span><br></pre></td></tr></table></figure><p>不要写一个无论输入什么都声称 <code>value is never</code> 或不准确的通用 helper。类型工具越通用，错误影响范围越大。</p><h2 id="一个实际的-React-ref-误判"><a href="#一个实际的-React-ref-误判" class="headerlink" title="一个实际的 React ref 误判"></a>一个实际的 React ref 误判</h2><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> inputRef = <span class="title function_">useRef</span>(<span class="literal">null</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">focus</span>(<span class="params"></span>) &#123;</span><br><span class="line">  inputRef.<span class="property">current</span>?.<span class="title function_">focus</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>没有泛型时 current 只知道初始 null，某些上下文会导致属性访问错误。声明元素类型：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> inputRef = useRef&lt;<span class="title class_">HTMLInputElement</span>&gt;(<span class="literal">null</span>)</span><br></pre></td></tr></table></figure><p>注意 ref 在挂载前&#x2F;卸载后确实为 null，可选链仍有意义。非空断言 <code>inputRef.current!.focus()</code> 只有在调用时机被严格保证时才用，否则只是把错误推到运行时。</p><h2 id="我实际如何定位一条-never"><a href="#我实际如何定位一条-never" class="headerlink" title="我实际如何定位一条 never"></a>我实际如何定位一条 never</h2><ol><li>把报错表达式拆成中间变量，逐个看 hover 类型；</li><li>找到最早出现 never 的地方，不在最后属性访问处修；</li><li>检查空数组&#x2F;null 是否缺泛型上下文；</li><li>检查 if&#x2F;switch 前面的联合成员是否已全部排除；</li><li>检查自定义守卫、assertion function 是否说真话；</li><li>展开条件类型，看输入联合是否真的有匹配成员；</li><li>用 <code>satisfies</code> 验证对象契约，少用宽泛 <code>as</code>。</li></ol><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> transitions = &#123;</span><br><span class="line">  <span class="attr">idle</span>: [<span class="string">&#x27;loading&#x27;</span>],</span><br><span class="line">  <span class="attr">loading</span>: [<span class="string">&#x27;success&#x27;</span>, <span class="string">&#x27;error&#x27;</span>],</span><br><span class="line">  <span class="attr">success</span>: [<span class="string">&#x27;loading&#x27;</span>],</span><br><span class="line">  <span class="attr">error</span>: [<span class="string">&#x27;loading&#x27;</span>]</span><br><span class="line">&#125; <span class="keyword">satisfies</span> <span class="title class_">Record</span>&lt;<span class="built_in">string</span>, <span class="keyword">readonly</span> <span class="built_in">string</span>[]&gt;</span><br></pre></td></tr></table></figure><h2 id="any、双重断言和关闭-strict-为什么不算修复"><a href="#any、双重断言和关闭-strict-为什么不算修复" class="headerlink" title="any、双重断言和关闭 strict 为什么不算修复"></a><code>any</code>、双重断言和关闭 strict 为什么不算修复</h2><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> task = value <span class="keyword">as</span> <span class="built_in">unknown</span> <span class="keyword">as</span> <span class="title class_">Task</span></span><br></pre></td></tr></table></figure><p>双重断言可以让几乎任何类型通过，但没有产生运行时校验，也没有修正模型。它只适合极少数无法表达、又有外部证明的边界，并应封装和注释。大多数业务 never 都能通过补泛型、修联合&#x2F;守卫或删除不可能条件解决。</p><p>关闭 strict 会让更多问题晚到运行时，不会让代码逻辑更正确。</p><h2 id="把-never-当作“无可能值”来读"><a href="#把-never-当作“无可能值”来读" class="headerlink" title="把 never 当作“无可能值”来读"></a>把 never 当作“无可能值”来读</h2><p>看到错误时，把句子改写成：“在 TypeScript 已知条件下，这里没有任何可能值。”然后问为什么：确实穷尽了，还是信息丢了，还是上游撒了谎？</p><p>空数组要补信息；穷尽分支应该利用 never；错误守卫和状态模型要从源头修。理解这三类以后，<code>Property does not exist on never</code> 就不再像随机报错，而是一条很具体的推理结果。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://www.typescriptlang.org/docs/handbook/2/narrowing.html#the-never-type">TypeScript Handbook：Narrowing - The never type</a></li><li><a href="https://www.typescriptlang.org/docs/handbook/2/conditional-types.html">TypeScript Handbook：Conditional Types</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端排障 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> TypeScript </tag>
            
            <tag> 前端排障 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vite 7：现代浏览器 Baseline 与 Node 20 基线</title>
      <link href="/2025/06/24/frontend-chronicle-2025-06-vite-7-baseline/"/>
      <url>/2025/06/24/frontend-chronicle-2025-06-vite-7-baseline/</url>
      
        <content type="html"><![CDATA[<p>Vite 7 把默认浏览器目标对齐到 Web Platform Baseline 的 Widely Available 能力，并提高 Node 运行基线。对新项目，这是更现代、更可解释的默认；对我们那个仍运行在仓库 PDA WebView 上的后台，则不能只因为构建成功就接受。</p><p>兼容性不是构建工具替产品做的决定。我们先从真实访问数据和设备清单出发，再决定沿用默认目标、显式降低 target，还是给旧终端保留独立版本。</p><h2 id="先把“支持哪些浏览器”从口头要求变成数据"><a href="#先把“支持哪些浏览器”从口头要求变成数据" class="headerlink" title="先把“支持哪些浏览器”从口头要求变成数据"></a>先把“支持哪些浏览器”从口头要求变成数据</h2><p>团队原来的文档只有一句“支持主流浏览器和 Android”。这无法指导任何构建配置。我们补了三类证据：</p><ul><li>RUM 中浏览器&#x2F;系统&#x2F;关键 API 失败率；</li><li>企业客户设备清单与合同要求；</li><li>QA 可获得的最低版本真实设备。</li></ul><p>结果发现公众用户几乎都在现代浏览器，但 3% 的仓储用户使用固定 WebView，升级周期由设备部门控制。这 3% 不能用公共市场份额查询抹掉，因为他们贡献核心业务。</p><h2 id="Baseline-解决的是共同能力，不是全部产品环境"><a href="#Baseline-解决的是共同能力，不是全部产品环境" class="headerlink" title="Baseline 解决的是共同能力，不是全部产品环境"></a>Baseline 解决的是共同能力，不是全部产品环境</h2><p>Baseline 用跨浏览器可用性描述 Web 平台能力，比随手写“last 2 versions”更稳定。Vite 7 默认目标对应一个明确的 Widely Available 时间基线，能减少对过旧语法的无差别转换。</p><p>但以下环境仍要单独验证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">企业 WebView</span><br><span class="line">内嵌小程序/超级 App 浏览器</span><br><span class="line">智能电视/车机</span><br><span class="line">长期不更新的专用终端</span><br><span class="line">受合规约束的桌面镜像</span><br></pre></td></tr></table></figure><p>Baseline 不是浏览器测试替代品，也不涵盖所有非语法 API、CSS 和业务依赖行为。</p><h2 id="显式-target-是一份产品契约"><a href="#显式-target-是一份产品契约" class="headerlink" title="显式 target 是一份产品契约"></a>显式 target 是一份产品契约</h2><p>确认 PDA 无法及时升级后，我们没有隐含依赖旧默认，而是在配置中写清目标并加注释&#x2F;文档：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; defineConfig &#125; <span class="keyword">from</span> <span class="string">&#x27;vite&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">build</span>: &#123;</span><br><span class="line">    <span class="comment">// 仓储 PDA WebView 合同基线；移除前查 compatibility.md</span></span><br><span class="line">    <span class="attr">target</span>: [<span class="string">&#x27;chrome89&#x27;</span>]</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>这只是语法转换目标，不会自动 polyfill 所有 Web API。如果源码使用新 API，还要能力检测或选择性 polyfill：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!(<span class="string">&#x27;ResizeObserver&#x27;</span> <span class="keyword">in</span> <span class="variable language_">window</span>)) &#123;</span><br><span class="line">  <span class="keyword">await</span> <span class="keyword">import</span>(<span class="string">&#x27;./polyfills/resize-observer.js&#x27;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>polyfill 也要在真实设备上测性能与正确性，不能看包名认为“已兼容”。</p><h2 id="旧环境应该拖住所有用户吗"><a href="#旧环境应该拖住所有用户吗" class="headerlink" title="旧环境应该拖住所有用户吗"></a>旧环境应该拖住所有用户吗</h2><p>我们评估过三种方案：</p><ol><li>所有人下载同一份低目标 bundle：运维简单，现代用户承担额外转换&#x2F;体积；</li><li>modern&#x2F;legacy 双构建：体验更精细，发布和测试矩阵增大；</li><li>PDA 保留旧应用版本，新主站提高基线：产品分叉，需要明确支持期限。</li></ol><p>最后选择第 3 种，因为 PDA 页面本来就是独立入口、功能稳定；给它设置一年升级窗口，新主站使用 Vite 7 默认现代基线。技术债没有消失，但有了负责人和截止时间。</p><h2 id="Node-基线先于-Vite-升级"><a href="#Node-基线先于-Vite-升级" class="headerlink" title="Node 基线先于 Vite 升级"></a>Node 基线先于 Vite 升级</h2><p>Vite 7 要求新的 Node 版本范围。我们先更新：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">.node-version</span><br><span class="line">package.json engines</span><br><span class="line">CI setup-node</span><br><span class="line">开发容器</span><br><span class="line">部署/SSR 镜像</span><br></pre></td></tr></table></figure><p>然后仍在旧 Vite 上跑一遍 build&#x2F;test，确认 Node 升级没有让原生依赖和脚本失败。再升级 Vite 7，问题归因会清楚很多。</p><p>有同事只更新本地 Node，CI 使用版本号范围恰好选到不兼容小版本。最终我们在 CI 固定经过验证的版本，并让依赖更新工具单独提 Node 升级 PR，不依赖“&gt;&#x3D;20”自动漂移。</p><h2 id="用-capabilities-测试比-UA-分支可靠"><a href="#用-capabilities-测试比-UA-分支可靠" class="headerlink" title="用 capabilities 测试比 UA 分支可靠"></a>用 capabilities 测试比 UA 分支可靠</h2><p>业务代码以前这样判断：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="regexp">/Chrome\/(8\d|9[0-1])/</span>.<span class="title function_">test</span>(navigator.<span class="property">userAgent</span>)) &#123;</span><br><span class="line">  <span class="title function_">loadLegacyEditor</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>UA 容易伪装，也无法准确代表 WebView 功能。能做能力检测时直接检测：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> supportsModernEditor =</span><br><span class="line">  <span class="string">&#x27;ResizeObserver&#x27;</span> <span class="keyword">in</span> <span class="variable language_">window</span> &amp;&amp;</span><br><span class="line">  <span class="string">&#x27;structuredClone&#x27;</span> <span class="keyword">in</span> <span class="variable language_">window</span> &amp;&amp;</span><br><span class="line">  <span class="variable constant_">CSS</span>.<span class="title function_">supports</span>(<span class="string">&#x27;selector(:has(*))&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!supportsModernEditor) &#123;</span><br><span class="line">  <span class="title function_">showUnsupportedEditorNotice</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>复杂能力要测试实际行为而非只看属性存在。服务端统计仍可能需要 UA，但客户端功能分支尽量基于 capability。</p><h2 id="构建通过后，真实浏览器矩阵才开始"><a href="#构建通过后，真实浏览器矩阵才开始" class="headerlink" title="构建通过后，真实浏览器矩阵才开始"></a>构建通过后，真实浏览器矩阵才开始</h2><p>Vite&#x2F;esbuild 能转语法，不会检测 CSS、第三方 SDK 和运行时 API 全部兼容。我们的最低设备用例包含：登录、扫码、离线重试、打印、文件上传和长列表。错误平台还按浏览器版本聚合 SyntaxError&#x2F;ReferenceError。</p><p>生产 sourcemap 必须能还原，不然旧环境只报一个 <code>Unexpected token</code> 很难追到是否 target 漏转译某个依赖。</p><h2 id="默认变现代是一件好事，前提是我们知道例外"><a href="#默认变现代是一件好事，前提是我们知道例外" class="headerlink" title="默认变现代是一件好事，前提是我们知道例外"></a>默认变现代是一件好事，前提是我们知道例外</h2><p>过去为了“保险”永远转到很老，会增加 bundle 与编译负担；为了“性能”盲目跟随最新默认，又可能丢掉真实用户。Baseline 提供了比模糊市场版本更好的共同起点，业务例外仍要用数据覆盖。</p><p>Vite 7 升级最终让主站减少历史转换，PDA 则进入明确的退役计划。最大的成果不是快了多少毫秒，而是兼容范围从一句模糊口号变成了设备、配置、测试和结束日期都可追踪的契约。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://vite.dev/blog/announcing-vite7">Vite 7 发布说明</a></li><li><a href="https://web.dev/baseline">web.dev：Baseline</a></li><li><a href="https://vite.dev/config/build-options.html#build-target">Vite：Build Options - target</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Vite </tag>
            
            <tag> 2025 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Tailwind CSS 4 与 CRA 退场：工具链继续现代化</title>
      <link href="/2025/02/14/frontend-chronicle-2025-02-tailwind-4-and-cra-sunset/"/>
      <url>/2025/02/14/frontend-chronicle-2025-02-tailwind-4-and-cra-sunset/</url>
      
        <content type="html"><![CDATA[<p>2025 年初有两个很有时代感的变化：Tailwind CSS 4 把更多配置带回 CSS，并用新的引擎重做构建路径；React 团队正式停止推荐 Create React App 创建新项目。一个是样式工具主动拥抱现代 CSS，另一个是曾经的“零配置脚手架”不再能代表 React 应用的默认答案。</p><p>它们不是同一个技术事件，却都提醒我：默认工具会过期。迁移不能靠重新运行最新初始化命令，而要先找出旧项目到底依赖了哪些隐式行为。</p><h2 id="Tailwind-4-的主题从-JavaScript-回到-CSS"><a href="#Tailwind-4-的主题从-JavaScript-回到-CSS" class="headerlink" title="Tailwind 4 的主题从 JavaScript 回到 CSS"></a>Tailwind 4 的主题从 JavaScript 回到 CSS</h2><p>旧项目常在 <code>tailwind.config.js</code> 扩展主题：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = &#123;</span><br><span class="line">  <span class="attr">content</span>: [<span class="string">&#x27;./src/**/*.&#123;js,ts,jsx,tsx&#125;&#x27;</span>],</span><br><span class="line">  <span class="attr">theme</span>: &#123;</span><br><span class="line">    <span class="attr">extend</span>: &#123;</span><br><span class="line">      <span class="attr">colors</span>: &#123;</span><br><span class="line">        <span class="attr">brand</span>: &#123;</span><br><span class="line">          <span class="number">500</span>: <span class="string">&#x27;#2563eb&#x27;</span>,</span><br><span class="line">          <span class="number">600</span>: <span class="string">&#x27;#1d4ed8&#x27;</span></span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Tailwind 4 的 CSS-first 配置可以直接声明主题变量：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">@import</span> <span class="string">&quot;tailwindcss&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@theme</span> &#123;</span><br><span class="line">  <span class="attr">--color-brand-500</span>: <span class="built_in">oklch</span>(<span class="number">0.623</span> <span class="number">0.214</span> <span class="number">259.8</span>);</span><br><span class="line">  <span class="attr">--color-brand-600</span>: <span class="built_in">oklch</span>(<span class="number">0.546</span> <span class="number">0.245</span> <span class="number">262.9</span>);</span><br><span class="line">  <span class="attr">--font-display</span>: <span class="string">&quot;Noto Sans SC&quot;</span>, sans-serif;</span><br><span class="line">  <span class="attr">--breakpoint-3xl</span>: <span class="number">120rem</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这些变量不仅生成工具类，也可以在普通 CSS 中使用：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-class">.article-link</span> &#123;</span><br><span class="line">  <span class="attribute">color</span>: <span class="built_in">var</span>(--color-brand-<span class="number">600</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>设计 token 与 CSS 变量更接近，运行时主题和组件库共享也更自然。但不是把 JS 配置复制成 <code>@theme</code> 就完成，插件、动态 class、旧浏览器和 PostCSS 管道都要检查。</p><h2 id="动态拼类名仍然是内容扫描的敌人"><a href="#动态拼类名仍然是内容扫描的敌人" class="headerlink" title="动态拼类名仍然是内容扫描的敌人"></a>动态拼类名仍然是内容扫描的敌人</h2><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 构建器很难从任意拼接推导全部结果</span></span><br><span class="line">&lt;button className=&#123;<span class="string">`bg-<span class="subst">$&#123;color&#125;</span>-500`</span>&#125; /&gt;</span><br></pre></td></tr></table></figure><p>改成完整字符串映射：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> buttonColor = &#123;</span><br><span class="line">  <span class="attr">primary</span>: <span class="string">&#x27;bg-brand-500 hover:bg-brand-600 text-white&#x27;</span>,</span><br><span class="line">  <span class="attr">danger</span>: <span class="string">&#x27;bg-red-500 hover:bg-red-600 text-white&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&lt;button className=&#123;buttonColor[variant]&#125; /&gt;</span><br></pre></td></tr></table></figure><p>这不仅帮助 Tailwind 发现类名，也让组件允许哪些 variant 成为显式 API。升级引擎不会让运行时任意字符串自动变成 CSS。</p><h2 id="Tailwind-升级先做“截图”，后做“清理”"><a href="#Tailwind-升级先做“截图”，后做“清理”" class="headerlink" title="Tailwind 升级先做“截图”，后做“清理”"></a>Tailwind 升级先做“截图”，后做“清理”</h2><p>样式迁移最怕构建成功、页面却悄悄变了。我们选登录、表单、表格、弹窗、文章排版和深色模式做基线截图，再升级依赖。重点检查：</p><ul><li>Preflight 基础样式是否改变表单和标题；</li><li>border、ring、shadow 等默认语义；</li><li>自定义 plugin&#x2F;utilities 是否还被支持；</li><li>任意值与旧 opacity 写法；</li><li>CSS import 顺序和组件库覆盖；</li><li>目标浏览器是否支持生成的现代 CSS。</li></ul><p>先让视觉等价，再利用 container query、现代颜色等新能力。一次 PR 同时升级并重设计，很难审核每个像素差异来自哪里。</p><h2 id="CRA-的问题不是项目突然不能运行"><a href="#CRA-的问题不是项目突然不能运行" class="headerlink" title="CRA 的问题不是项目突然不能运行"></a>CRA 的问题不是项目突然不能运行</h2><p>React 团队停止推荐 CRA，并不意味着所有 <code>react-scripts</code> 应用当天报废。已有项目可以继续运行，但脚手架的依赖和默认构建链不再是新项目推荐路径，长期安全与兼容更新需要规划。</p><p>我们先扫描 CRA 项目的隐式依赖：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">REACT_APP_* / process.env</span><br><span class="line">PUBLIC_URL</span><br><span class="line">src/setupProxy.js</span><br><span class="line">Jest 配置与 jsdom</span><br><span class="line">SVGR 的 ReactComponent 导入</span><br><span class="line">CSS Modules 命名</span><br><span class="line">serviceWorker / manifest</span><br><span class="line">开发服务器 history fallback</span><br></pre></td></tr></table></figure><p>这些才是迁移成本。把 <code>react-scripts start</code> 改成 <code>vite</code>，环境变量和 SVG 导入就可能先坏。</p><h2 id="先决定项目需要“框架”还是“构建工具”"><a href="#先决定项目需要“框架”还是“构建工具”" class="headerlink" title="先决定项目需要“框架”还是“构建工具”"></a>先决定项目需要“框架”还是“构建工具”</h2><p>CRA 退场后没有唯一替代。React 官方建议新项目优先考虑能提供路由、数据获取、代码分割和部署集成的框架；如果应用不需要这些，可以选择 Vite、Parcel、Rsbuild 等构建工具。</p><p>我们用问题选，而不是按热度：</p><ol><li>是否需要 SEO&#x2F;SSR&#x2F;静态生成？</li><li>路由和数据缓存是否希望由框架统一？</li><li>是否只是嵌入已有后端的 SPA&#x2F;微前端？</li><li>部署平台是否支持服务器运行时？</li><li>团队是否有大量 webpack 定制？</li></ol><p>纯内部后台选择 Vite SPA；内容站选择全栈 React 框架；嵌入式小组件继续使用库模式构建。三个 React 项目不必有同一个答案。</p><h2 id="一个-CRA-到-Vite-的实际改动"><a href="#一个-CRA-到-Vite-的实际改动" class="headerlink" title="一个 CRA 到 Vite 的实际改动"></a>一个 CRA 到 Vite 的实际改动</h2><p>环境变量：</p><figure class="highlight diff"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="deletion">- const apiBase = process.env.REACT_APP_API_BASE</span></span><br><span class="line"><span class="addition">+ const apiBase = import.meta.env.VITE_API_BASE</span></span><br></pre></td></tr></table></figure><p>HTML 入口从 <code>public/index.html</code> 模板进入项目根并显式加载模块：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;root&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">&quot;module&quot;</span> <span class="attr">src</span>=<span class="string">&quot;/src/main.tsx&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure><p>测试不一定必须同时迁到 Vitest。我们第一阶段保留现有 Jest，用单独配置运行；构建稳定后再评估测试器，避免一次变更跨过开发、构建、测试和样式四条链。</p><p><code>setupProxy.js</code> 则迁到 Vite server proxy，但要明确它只服务本地开发，生产反向代理仍由部署层配置。</p><h2 id="“弹出配置”不再是最后退路"><a href="#“弹出配置”不再是最后退路" class="headerlink" title="“弹出配置”不再是最后退路"></a>“弹出配置”不再是最后退路</h2><p>CRA 时代，遇到深度配置常考虑 eject，一旦 eject 就接管整套 webpack&#x2F;Babel 配置。迁移到更直接的构建工具后，配置可见、插件可替换，但也意味着团队自己负责升级。</p><p>透明并不自动等于简单。我们给自定义项写原因：为什么需要、谁消费、如何测试、何时能删除。不然几年后又会形成另一份没人敢动的配置。</p><h2 id="两次迁移的共同原则"><a href="#两次迁移的共同原则" class="headerlink" title="两次迁移的共同原则"></a>两次迁移的共同原则</h2><p>Tailwind 4 与 CRA sunset 表面一个关于 CSS，一个关于脚手架，实际迁移方法很相似：</p><ul><li>先列出旧默认替你做了什么；</li><li>保存构建、视觉和用户路径基线；</li><li>先实现行为等价，再使用新能力；</li><li>将隐式配置改成明确契约；</li><li>不为统一技术栈强迫不同产品选择同一架构。</li></ul><p>现代 CSS 让 Tailwind 可以把更多能力交还浏览器，现代 React 项目则需要在框架与构建工具之间做架构选择。默认值减少并不是倒退，而是生态成熟后问题被分得更清楚。我们要做的，是让每个项目的选择有理由、有测试、也有下一次迁移的出口。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://tailwindcss.com/blog/tailwindcss-v4">Tailwind CSS v4.0 发布说明</a></li><li><a href="https://react.dev/blog/2025/02/14/sunsetting-create-react-app">React：Sunsetting Create React App</a></li><li><a href="https://react.dev/learn/creating-a-react-app">React：Creating a React App</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 前端工具链 </tag>
            
            <tag> 2025 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>React 19：Actions、use 与表单异步状态</title>
      <link href="/2024/12/05/frontend-chronicle-2024-12-react-19-actions/"/>
      <url>/2024/12/05/frontend-chronicle-2024-12-react-19-actions/</url>
      
        <content type="html"><![CDATA[<p>React 19 最适合用一个表单来理解。过去提交资料时，组件往往手工维护 pending、error、成功重置、乐观状态，再把这些状态传给设计系统按钮。Actions 把异步变更放进 React 的渲染&#x2F;表单模型，让 pending 与结果更自然地组合。</p><p>这不意味着现有 React Query&#x2F;Form 库都该删除，也不意味着服务器函数自动安全。我们拿“修改昵称”和“评论点赞”两个小功能试用，一个展示 <code>useActionState</code> 的提交状态，一个展示乐观更新失败时如何回退。</p><h2 id="修改昵称：状态来自-Action-的结果"><a href="#修改昵称：状态来自-Action-的结果" class="headerlink" title="修改昵称：状态来自 Action 的结果"></a>修改昵称：状态来自 Action 的结果</h2><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; useActionState &#125; <span class="keyword">from</span> <span class="string">&#x27;react&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> <span class="title class_">FormState</span> = &#123;</span><br><span class="line">  <span class="attr">error</span>?: <span class="built_in">string</span></span><br><span class="line">  <span class="attr">success</span>?: <span class="built_in">boolean</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">updateName</span>(<span class="params"></span></span><br><span class="line"><span class="params">  <span class="attr">_previousState</span>: <span class="title class_">FormState</span>,</span></span><br><span class="line"><span class="params">  <span class="attr">formData</span>: <span class="title class_">FormData</span></span></span><br><span class="line"><span class="params"></span>): <span class="title class_">Promise</span>&lt;<span class="title class_">FormState</span>&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> name = <span class="title class_">String</span>(formData.<span class="title function_">get</span>(<span class="string">&#x27;name&#x27;</span>) ?? <span class="string">&#x27;&#x27;</span>).<span class="title function_">trim</span>()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (name.<span class="property">length</span> &lt; <span class="number">2</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123; <span class="attr">error</span>: <span class="string">&#x27;昵称至少需要 2 个字符&#x27;</span> &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">&#x27;/api/profile/name&#x27;</span>, &#123;</span><br><span class="line">    <span class="attr">method</span>: <span class="string">&#x27;PUT&#x27;</span>,</span><br><span class="line">    <span class="attr">headers</span>: &#123; <span class="string">&#x27;Content-Type&#x27;</span>: <span class="string">&#x27;application/json&#x27;</span> &#125;,</span><br><span class="line">    <span class="attr">body</span>: <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(&#123; name &#125;)</span><br><span class="line">  &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!response.<span class="property">ok</span>) <span class="keyword">return</span> &#123; <span class="attr">error</span>: <span class="string">&#x27;保存失败，请稍后重试&#x27;</span> &#125;</span><br><span class="line">  <span class="keyword">return</span> &#123; <span class="attr">success</span>: <span class="literal">true</span> &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">NameForm</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [state, formAction, pending] = <span class="title function_">useActionState</span>(updateName, &#123;&#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">form</span> <span class="attr">action</span>=<span class="string">&#123;formAction&#125;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">label</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        昵称</span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">input</span> <span class="attr">name</span>=<span class="string">&quot;name&quot;</span> <span class="attr">disabled</span>=<span class="string">&#123;pending&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;/<span class="name">label</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">button</span> <span class="attr">disabled</span>=<span class="string">&#123;pending&#125;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        &#123;pending ? &#x27;保存中…&#x27; : &#x27;保存&#x27;&#125;</span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      &#123;state.error &amp;&amp; <span class="tag">&lt;<span class="name">p</span> <span class="attr">role</span>=<span class="string">&quot;alert&quot;</span>&gt;</span>&#123;state.error&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span>&#125;</span></span><br><span class="line"><span class="language-xml">      &#123;state.success &amp;&amp; <span class="tag">&lt;<span class="name">p</span>&gt;</span>已保存<span class="tag">&lt;/<span class="name">p</span>&gt;</span>&#125;</span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">form</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Action 执行期间 pending 自动反映提交状态，最终返回值成为新的 state。表单依然要有输入校验、错误文案和重复提交策略；API 仍要负责真正权限与数据校验。</p><h2 id="useFormStatus-让按钮不需要层层传-pending"><a href="#useFormStatus-让按钮不需要层层传-pending" class="headerlink" title="useFormStatus 让按钮不需要层层传 pending"></a><code>useFormStatus</code> 让按钮不需要层层传 pending</h2><p>设计系统按钮可以读取所在父表单状态：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; useFormStatus &#125; <span class="keyword">from</span> <span class="string">&#x27;react-dom&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">SubmitButton</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> &#123; pending &#125; = <span class="title function_">useFormStatus</span>()</span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">button</span> <span class="attr">type</span>=<span class="string">&quot;submit&quot;</span> <span class="attr">disabled</span>=<span class="string">&#123;pending&#125;</span> <span class="attr">aria-busy</span>=<span class="string">&#123;pending&#125;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      &#123;pending ? &#x27;提交中…&#x27; : &#x27;提交&#x27;&#125;</span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它必须渲染在对应 <code>&lt;form&gt;</code> 内部才能读到状态。按钮自身触发的那次提交信息要按 API 返回字段使用，不能把它当任意全局 loading。</p><h2 id="点赞适合乐观，支付不适合随便乐观"><a href="#点赞适合乐观，支付不适合随便乐观" class="headerlink" title="点赞适合乐观，支付不适合随便乐观"></a>点赞适合乐观，支付不适合随便乐观</h2><p>评论点赞失败后可以回到原数量，用户损失小，适合先更新 UI：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; startTransition, useOptimistic &#125; <span class="keyword">from</span> <span class="string">&#x27;react&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">LikeButton</span>(<span class="params">&#123; comment, onCommit &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [optimistic, addOptimistic] = <span class="title function_">useOptimistic</span>(</span><br><span class="line">    comment,</span><br><span class="line">    <span class="function">(<span class="params">current, <span class="attr">liked</span>: <span class="built_in">boolean</span></span>) =&gt;</span> (&#123;</span><br><span class="line">      ...current,</span><br><span class="line">      liked,</span><br><span class="line">      <span class="attr">likeCount</span>: current.<span class="property">likeCount</span> + (liked ? <span class="number">1</span> : -<span class="number">1</span>)</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">  <span class="keyword">function</span> <span class="title function_">toggle</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> next = !optimistic.<span class="property">liked</span></span><br><span class="line"></span><br><span class="line">    <span class="title function_">startTransition</span>(<span class="title function_">async</span> () =&gt; &#123;</span><br><span class="line">      <span class="title function_">addOptimistic</span>(next)</span><br><span class="line">      <span class="keyword">const</span> updated = <span class="keyword">await</span> <span class="title function_">updateLike</span>(comment.<span class="property">id</span>, next)</span><br><span class="line">      <span class="title function_">onCommit</span>(updated)</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;toggle&#125;</span> <span class="attr">aria-pressed</span>=<span class="string">&#123;optimistic.liked&#125;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      &#123;optimistic.liked ? &#x27;取消赞&#x27; : &#x27;点赞&#x27;&#125; &#123;optimistic.likeCount&#125;</span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>错误时 optimistic 状态会回到真实 value，但产品仍应给用户提示。连续快速点击还要考虑请求顺序与服务端幂等。扣款、库存最终确认等高风险动作不应该只凭“体验更快”直接显示成功。</p><h2 id="Server-Action-是网络入口，不是私有函数调用"><a href="#Server-Action-是网络入口，不是私有函数调用" class="headerlink" title="Server Action 是网络入口，不是私有函数调用"></a>Server Action 是网络入口，不是私有函数调用</h2><p>在支持 React Server Components&#x2F;Actions 的框架中，可以用 <code>use server</code> 定义服务器函数。它看起来像普通 import，框架实际会建立客户端可调用的网络引用。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;use server&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">changeRole</span>(<span class="params"><span class="attr">formData</span>: <span class="title class_">FormData</span></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> actor = <span class="keyword">await</span> <span class="title function_">requireAdmin</span>()</span><br><span class="line">  <span class="keyword">const</span> input = <span class="title function_">parseRoleChange</span>(formData)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">await</span> <span class="title function_">rateLimit</span>(actor.<span class="property">id</span>, <span class="string">&#x27;change-role&#x27;</span>)</span><br><span class="line">  <span class="keyword">await</span> userService.<span class="title function_">changeRole</span>(actor, input)</span><br><span class="line">  <span class="keyword">await</span> auditLog.<span class="title function_">write</span>(&#123; <span class="attr">actor</span>: actor.<span class="property">id</span>, <span class="attr">action</span>: <span class="string">&#x27;change-role&#x27;</span>, input &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>每次调用都重新鉴权，不能信任隐藏 input、用户 ID、价格或 role。框架生成不可猜测引用只能减少扫描，不是授权。服务端函数还要防 CSRF&#x2F;滥用、限制 payload 并处理审计，具体由所用框架安全指南决定。</p><h2 id="use-可以读取-Promise，但-Promise-从哪里来很重要"><a href="#use-可以读取-Promise，但-Promise-从哪里来很重要" class="headerlink" title="use 可以读取 Promise，但 Promise 从哪里来很重要"></a><code>use</code> 可以读取 Promise，但 Promise 从哪里来很重要</h2><p>React 19 的 <code>use</code> 可以在渲染中读取支持的 Promise，并与 Suspense 配合：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Suspense</span>, use &#125; <span class="keyword">from</span> <span class="string">&#x27;react&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">Comments</span>(<span class="params">&#123; commentsPromise &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> comments = <span class="title function_">use</span>(commentsPromise)</span><br><span class="line">  <span class="keyword">return</span> comments.<span class="title function_">map</span>(<span class="function"><span class="params">comment</span> =&gt;</span> <span class="language-xml"><span class="tag">&lt;<span class="name">p</span> <span class="attr">key</span>=<span class="string">&#123;comment.id&#125;</span>&gt;</span>&#123;comment.body&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">CommentPanel</span>(<span class="params">&#123; commentsPromise &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">Suspense</span> <span class="attr">fallback</span>=<span class="string">&#123;</span>&lt;<span class="attr">p</span>&gt;</span>评论加载中…<span class="tag">&lt;/<span class="name">p</span>&gt;</span>&#125;&gt;</span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">Comments</span> <span class="attr">commentsPromise</span>=<span class="string">&#123;commentsPromise&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">Suspense</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>不要在 Client Component 每次 render 临时创建一个未缓存 Promise 再交给 <code>use</code>，React 会警告。Promise 通常来自支持 Suspense 缓存的框架&#x2F;数据层，或由服务器组件传入。</p><p><code>use</code> 与普通 Hook 的规则也不完全相同，例如可以在某些条件分支调用，但仍只能在渲染上下文使用。升级时以官方文档为准，不凭名字猜。</p><h2 id="ref-作为-prop-简化组件，但隐式返回会踩类型"><a href="#ref-作为-prop-简化组件，但隐式返回会踩类型" class="headerlink" title="ref 作为 prop 简化组件，但隐式返回会踩类型"></a>ref 作为 prop 简化组件，但隐式返回会踩类型</h2><p>函数组件可以直接接收 ref prop：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">SearchInput</span>(<span class="params">&#123; ref, ...props &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">input</span> <span class="attr">ref</span>=<span class="string">&#123;ref&#125;</span> <span class="attr">type</span>=<span class="string">&quot;search&quot;</span> &#123;<span class="attr">...props</span>&#125; /&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>callback ref 还支持返回清理函数。旧代码中这种隐式返回：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;div ref=&#123;<span class="function"><span class="params">node</span> =&gt;</span> (instance = node)&#125; /&gt;</span><br></pre></td></tr></table></figure><p>会返回赋值结果，TypeScript 可能把它误认为不合法 cleanup。改成花括号明确不返回：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;div ref=&#123;<span class="function"><span class="params">node</span> =&gt;</span> &#123; instance = node &#125;&#125; /&gt;</span><br></pre></td></tr></table></figure><p>这类细节需要按 React 19 Upgrade Guide 和 codemod 处理，而不是只装新版本。</p><h2 id="没有重写成熟表单"><a href="#没有重写成熟表单" class="headerlink" title="没有重写成熟表单"></a>没有重写成熟表单</h2><p>已有复杂表单使用成熟 schema、字段数组和草稿恢复库，迁到 Action 并不会自动变简单。我们只在新表单和服务端框架路径试用；稳定系统继续原方案，等确实需要改动时再评估。</p><p>React 19 给出的不是“唯一数据层”，而是一套更懂异步 mutation 的基础能力。库可以与 Actions 组合，而不必被全部替代。</p><h2 id="试用后的判断"><a href="#试用后的判断" class="headerlink" title="试用后的判断"></a>试用后的判断</h2><p>Actions 最明显地减少了 pending&#x2F;error&#x2F;表单提交之间的手工胶水，useOptimistic 让乐观状态的生命周期更明确，<code>use</code> 把资源读取与 Suspense 连接起来。与此同时，竞态、幂等、鉴权、校验和错误提示仍是业务责任。</p><p>我们最终保留两个试点，并给它们补了慢网、失败、双击、权限不足和乐观回退测试。新 API 能让正确模式更好写，但不会替开发者决定哪些操作可以乐观、哪些输入可信。把这条边界守住，React 19 的表单能力才不是又一轮语法迁移。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://react.dev/blog/2024/12/05/react-19">React 19 发布说明</a></li><li><a href="https://react.dev/reference/react/useActionState">React：useActionState</a></li><li><a href="https://react.dev/reference/react/useOptimistic">React：useOptimistic</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> React </tag>
            
            <tag> 2024 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vite 6：Environment API 面向多运行时框架</title>
      <link href="/2024/11/26/frontend-chronicle-2024-11-vite-6-environment-api/"/>
      <url>/2024/11/26/frontend-chronicle-2024-11-vite-6-environment-api/</url>
      
        <content type="html"><![CDATA[<p>Vite 6 的 Environment API 不是普通 SPA 开发者每天都会调用的新功能。它主要服务框架与插件作者：同一个应用可能同时存在浏览器、Node SSR、边缘 runtime、测试沙箱，过去很多插件只靠 <code>ssr: true/false</code> 或全局分支猜自己正在处理哪一套模块。</p><p>我用一个极小的 SSR 插件做了升级实验。目标不是教人手写全栈框架，而是检查插件里那些默认“只有 client 与 Node 两个世界”的假设。</p><h2 id="一个插件中的三个错误假设"><a href="#一个插件中的三个错误假设" class="headerlink" title="一个插件中的三个错误假设"></a>一个插件中的三个错误假设</h2><p>旧插件负责注入构建信息：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">runtimeInfoPlugin</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;runtime-info&#x27;</span>,</span><br><span class="line">    <span class="title function_">transform</span>(<span class="params">code, id, options</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (!id.<span class="title function_">endsWith</span>(<span class="string">&#x27;runtime-info.ts&#x27;</span>)) <span class="keyword">return</span></span><br><span class="line"></span><br><span class="line">      <span class="keyword">const</span> runtime = options?.<span class="property">ssr</span> ? <span class="string">&#x27;server&#x27;</span> : <span class="string">&#x27;client&#x27;</span></span><br><span class="line">      <span class="keyword">return</span> code.<span class="title function_">replace</span>(<span class="string">&#x27;__RUNTIME__&#x27;</span>, <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(runtime))</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它假设：</p><ol><li>非 client 就是 Node server；</li><li>server 可以使用 <code>process</code>、文件系统；</li><li>同一个模块在所有服务端环境中都以相同方式执行。</li></ol><p>加入 edge runtime 后，这些假设全可能错误。Edge 可能有 Web API，却没有完整 Node API；测试环境又可能模拟 DOM。</p><h2 id="环境应该成为显式上下文"><a href="#环境应该成为显式上下文" class="headerlink" title="环境应该成为显式上下文"></a>环境应该成为显式上下文</h2><p>Environment API 的方向是让开发服务器&#x2F;模块 runner 能表达多个命名环境，插件钩子在环境上下文中工作。Vite 6 发布时这套 API 仍是实验性，具体配置与接口会继续调整，所以业务应用不应该把内部形态写死。</p><p>插件设计可以先从环境无关开始：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">createRuntimeModule</span>(<span class="params"><span class="attr">runtime</span>: <span class="string">&#x27;client&#x27;</span> | <span class="string">&#x27;ssr&#x27;</span> | <span class="string">&#x27;edge&#x27;</span></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`export const runtime = <span class="subst">$&#123;<span class="built_in">JSON</span>.stringify(runtime)&#125;</span>`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>只有真正需要时再通过插件上下文选择实现；核心转换函数保持纯净并分别测试。这样即使实验 API 变化，业务逻辑也不必重写。</p><h2 id="条件导出是另一个运行时边界"><a href="#条件导出是另一个运行时边界" class="headerlink" title="条件导出是另一个运行时边界"></a>条件导出是另一个运行时边界</h2><p>一个包可能针对浏览器与服务端提供不同入口：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;exports&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;.&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;browser&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/browser.js&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;node&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/node.js&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;default&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/index.js&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>框架开发服务器需要在每个环境按正确 conditions 解析模块。若 client 模块解析到 node 入口，可能把 <code>node:fs</code> 带进浏览器；edge 解析到 node 入口则上线才失败。</p><p>我们的 fixture 分别运行 client build、Node SSR 和 edge-like sandbox，断言解析到的 runtime 字符串与禁止 API。只跑一次 <code>vite build</code> 无法覆盖多环境模块图。</p><h2 id="HMR-在-SSR-环境不只是刷新浏览器"><a href="#HMR-在-SSR-环境不只是刷新浏览器" class="headerlink" title="HMR 在 SSR 环境不只是刷新浏览器"></a>HMR 在 SSR 环境不只是刷新浏览器</h2><p>客户端 HMR 可以替换样式或组件，SSR 开发还要让服务器模块 runner 失效并重新执行。模块顶层如果创建数据库连接、定时器或全局注册，每次热更新都可能泄漏。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="attr">dispose</span>: (<span class="function">() =&gt;</span> <span class="built_in">void</span>) | <span class="literal">undefined</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">startRuntime</span>(<span class="params"></span>) &#123;</span><br><span class="line">  dispose?.()</span><br><span class="line">  <span class="keyword">const</span> timer = <span class="built_in">setInterval</span>(refreshCache, <span class="number">30_000</span>)</span><br><span class="line">  dispose = <span class="function">() =&gt;</span> <span class="built_in">clearInterval</span>(timer)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这只是简化示意。更好的框架&#x2F;插件集成应使用明确的生命周期钩子，不让模块重新执行悄悄创建不可控资源。Environment API 让多环境 HMR 更可表达，也要求插件别把 transform 当一次性过程。</p><h2 id="import-meta-env-SSR-够用时，不要为了新-API-重写"><a href="#import-meta-env-SSR-够用时，不要为了新-API-重写" class="headerlink" title="import.meta.env.SSR 够用时，不要为了新 API 重写"></a><code>import.meta.env.SSR</code> 够用时，不要为了新 API 重写</h2><p>普通 Vite SSR 应用只是偶尔分支：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">env</span>.<span class="property">SSR</span>) &#123;</span><br><span class="line">  <span class="comment">// server-only path</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这不代表一定要直接使用实验 Environment API。官方发布说明明确它面向框架作者和高级场景。普通 SPA&#x2F;简单 SSR 首先升级 Vite、跑测试、处理 Sass 等迁移项即可。</p><p>选择实验 API 的标准应该是：确实要编排多个运行环境，并愿意跟随版本反馈&#x2F;调整。为了“用上 Vite 6 新特性”引入并没有业务需求的环境配置，只会增加维护。</p><h2 id="Sass-默认-API-变化反而更贴近日常升级"><a href="#Sass-默认-API-变化反而更贴近日常升级" class="headerlink" title="Sass 默认 API 变化反而更贴近日常升级"></a>Sass 默认 API 变化反而更贴近日常升级</h2><p>Vite 6 的普通项目更可能遇到 Sass modern API 与弃用项。我们在升级日志中搜索 warning，检查自定义 importer 与全局样式注入，不让 deprecation 淹没。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">css</span>: &#123;</span><br><span class="line">    <span class="attr">preprocessorOptions</span>: &#123;</span><br><span class="line">      <span class="attr">scss</span>: &#123;</span><br><span class="line">        <span class="attr">additionalData</span>: <span class="string">&#x27;@use &quot;@/styles/tokens.scss&quot; as *;&#x27;</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p><code>@import</code>&#x2F;旧 API 迁移要结合 Sass 当前文档，不能因为 Vite build 成功就忽略未来移除警告。</p><h2 id="多环境插件测试矩阵"><a href="#多环境插件测试矩阵" class="headerlink" title="多环境插件测试矩阵"></a>多环境插件测试矩阵</h2><p>我们给插件仓库加了下面的 fixture：</p><table><thead><tr><th>场景</th><th>重点</th></tr></thead><tbody><tr><td>client dev</td><td>HMR、浏览器无 Node 内建</td></tr><tr><td>client build</td><td>条件导出、资源与 source map</td></tr><tr><td>Node SSR dev</td><td>模块重新执行与资源清理</td></tr><tr><td>Node SSR build</td><td>externalization 与产物启动</td></tr><tr><td>edge-like runtime</td><td>禁止 fs&#x2F;process 假设、Web API</td></tr></tbody></table><p>每个 fixture 都最小化，只验证插件承诺的能力。矩阵看起来多，却比在一个全功能 demo 中难以定位失败更省时间。</p><h2 id="Vite-6-给出的方向"><a href="#Vite-6-给出的方向" class="headerlink" title="Vite 6 给出的方向"></a>Vite 6 给出的方向</h2><p>Vite 从快速 SPA 构建工具逐渐成为许多全栈框架的共享基础设施。Environment API 试图把框架过去各自实现的模块运行环境下沉成公共能力，让插件更准确地知道“当前模块将在哪里运行”。</p><p>对应用开发者，正确动作可能只是升级并保持标准配置；对框架&#x2F;插件作者，则应尽早清理 client&#x2F;Node 二分假设，建立多环境测试。实验 API 最有价值的用法，是帮助生态反馈真实需求，而不是把不稳定接口包装成业务项目必须追的新潮写法。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://vite.dev/blog/announcing-vite6">Vite 6 发布说明</a></li><li><a href="https://vite.dev/guide/api-environment">Vite：Environment API</a></li><li><a href="https://vite.dev/guide/ssr">Vite：SSR</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Vite </tag>
            
            <tag> 2024 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Next.js 15：缓存默认值调整与 Turbopack Dev 稳定</title>
      <link href="/2024/10/21/frontend-chronicle-2024-10-nextjs-15-caching/"/>
      <url>/2024/10/21/frontend-chronicle-2024-10-nextjs-15-caching/</url>
      
        <content type="html"><![CDATA[<p>Next.js 15 升级最危险的部分不是编译报错，而是页面仍然能打开、数据新鲜度却悄悄改变。缓存默认值调整后，一些依赖旧行为的 GET Route Handler 和客户端导航会更频繁请求；另一些团队则反而庆幸，不再因为“默认缓存”看到旧列表。</p><p>我们把这次升级当成缓存需求审计，而不是把所有路由统一改回旧默认。每条数据先回答允许多旧、谁触发更新、能否跨用户共享，再写配置。</p><h2 id="先做一张数据新鲜度表"><a href="#先做一张数据新鲜度表" class="headerlink" title="先做一张数据新鲜度表"></a>先做一张数据新鲜度表</h2><table><thead><tr><th>数据</th><th align="right">可接受陈旧</th><th>共享范围</th><th>更新方式</th></tr></thead><tbody><tr><td>博客文章</td><td align="right">5 分钟</td><td>所有人</td><td>CMS webhook&#x2F;tag</td></tr><tr><td>商品库存</td><td align="right">10 秒内</td><td>按地区</td><td>下单后主动刷新</td></tr><tr><td>用户资料</td><td align="right">请求级</td><td>单用户</td><td>保存后立即可见</td></tr><tr><td>导航分类</td><td align="right">1 小时</td><td>所有人</td><td>后台发布失效</td></tr></tbody></table><p>这张表比“都用 force-dynamic”或“都缓存 60 秒”更有用。性能和正确性不是二选一，缓存策略必须对应业务容忍度。</p><h2 id="GET-Route-Handler-不再默认替我们决定缓存"><a href="#GET-Route-Handler-不再默认替我们决定缓存" class="headerlink" title="GET Route Handler 不再默认替我们决定缓存"></a>GET Route Handler 不再默认替我们决定缓存</h2><p>一个公开分类接口：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// app/api/categories/route.ts</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">GET</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> categories = <span class="keyword">await</span> categoryService.<span class="title function_">list</span>()</span><br><span class="line">  <span class="keyword">return</span> <span class="title class_">Response</span>.<span class="title function_">json</span>(categories)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Next.js 15 中 GET Route Handler 默认不缓存。若分类数据明确可以静态缓存，就显式选择：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> dynamic = <span class="string">&#x27;force-static&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">GET</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="title class_">Response</span>.<span class="title function_">json</span>(<span class="keyword">await</span> categoryService.<span class="title function_">list</span>())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>具体缓存 API 会继续演进，代码应跟随项目版本官方文档。这里真正的改变是“是否缓存”不再靠团队成员猜默认；公开数据显式 opt-in，用户数据保持动态。</p><h2 id="客户端返回页面后，为什么请求又发生了"><a href="#客户端返回页面后，为什么请求又发生了" class="headerlink" title="客户端返回页面后，为什么请求又发生了"></a>客户端返回页面后，为什么请求又发生了</h2><p>Next 15 调整了 Client Router Cache 的 page segment 默认 stale time。正常导航重新进入页面时更倾向拿到最新服务器结果；共享 layout、前进后退恢复和 loading 缓存仍有各自行为。</p><p>升级后产品同事反馈“返回列表时滚动位置与数据变了”。这不是单纯性能回退：此前用户从详情返回能立刻看到旧列表，现在可能重新请求并应用新排序。我们根据产品需求决定：浏览历史应恢复位置，库存应刷新，筛选参数保持在 URL。</p><p>端到端测试覆盖：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">列表 -&gt; 滚动 -&gt; 详情 -&gt; 浏览器返回</span><br><span class="line">列表 -&gt; 新增记录 -&gt; 回到列表</span><br><span class="line">列表 -&gt; 后台修改库存 -&gt; 客户端导航回来</span><br></pre></td></tr></table></figure><p>缓存行为必须放在用户导航序列中验证，单独请求某个 URL 看不出客户端 router 层。</p><h2 id="异步-Request-API-不能只靠-codemod-后不读代码"><a href="#异步-Request-API-不能只靠-codemod-后不读代码" class="headerlink" title="异步 Request API 不能只靠 codemod 后不读代码"></a>异步 Request API 不能只靠 codemod 后不读代码</h2><p><code>cookies()</code>、<code>headers()</code>、<code>draftMode()</code> 以及 page 的 params&#x2F;searchParams 向异步 API 迁移。示意：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; cookies &#125; <span class="keyword">from</span> <span class="string">&#x27;next/headers&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">AccountPage</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> cookieStore = <span class="keyword">await</span> <span class="title function_">cookies</span>()</span><br><span class="line">  <span class="keyword">const</span> session = cookieStore.<span class="title function_">get</span>(<span class="string">&#x27;session&#x27;</span>)</span><br><span class="line">  <span class="keyword">const</span> account = <span class="keyword">await</span> <span class="title function_">getAccount</span>(session?.<span class="property">value</span>)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">AccountView</span> <span class="attr">account</span>=<span class="string">&#123;account&#125;</span> /&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>官方 codemod 可以完成大量机械修改，但我们逐个检查调用链：被改成 async 后是否影响缓存&#x2F;静态判定，函数消费者是否正确 await，测试 mock 是否仍返回同步对象。自动转换是起点，不是行为证明。</p><h2 id="写入和失效必须在同一个业务流程闭合"><a href="#写入和失效必须在同一个业务流程闭合" class="headerlink" title="写入和失效必须在同一个业务流程闭合"></a>写入和失效必须在同一个业务流程闭合</h2><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;use server&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> &#123; revalidateTag &#125; <span class="keyword">from</span> <span class="string">&#x27;next/cache&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">updateProfile</span>(<span class="params"><span class="attr">formData</span>: <span class="title class_">FormData</span></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> user = <span class="keyword">await</span> <span class="title function_">requireUser</span>()</span><br><span class="line">  <span class="keyword">const</span> input = <span class="title function_">parseProfile</span>(formData)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">await</span> profileService.<span class="title function_">update</span>(user.<span class="property">id</span>, input)</span><br><span class="line">  <span class="title function_">revalidateTag</span>(<span class="string">`profile:<span class="subst">$&#123;user.id&#125;</span>`</span>)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> &#123; <span class="attr">ok</span>: <span class="literal">true</span> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里最重要的两行不是 revalidate，而是鉴权与输入解析。Server Action 是可以由客户端触发的服务器入口，仍要像 API 一样对待。缓存 tag 还要包含正确作用域，不能失效或复用到其他用户。</p><p>我们测试“保存返回成功后当前页面与重新导航都看到新值”，避免写入成功、缓存仍旧造成假失败。</p><h2 id="Turbopack-Dev-stable-用影子命令验证"><a href="#Turbopack-Dev-stable-用影子命令验证" class="headerlink" title="Turbopack Dev stable 用影子命令验证"></a>Turbopack Dev stable 用影子命令验证</h2><p>Next.js 15 将 Turbopack 开发模式标记稳定。项目有自定义 webpack 配置，所以没有直接替换日常命令，而是让 CI&#x2F;开发者并行运行一段时间：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">npm run dev:webpack</span><br><span class="line">npm run dev:turbo</span><br></pre></td></tr></table></figure><p>比较路由首次编译、Fast Refresh、CSS、SVG、Sentry 和自定义 loader。Turbopack 内置能力不需要照搬 webpack loader，但项目若使用特别转换，就要找等价支持或保留 webpack。</p><p>开发模式 stable 不等于当时生产 build 的所有路径也同样 stable，不能把两个阶段混为一谈。</p><h2 id="instrumentation-进入稳定路径后补了请求观测"><a href="#instrumentation-进入稳定路径后补了请求观测" class="headerlink" title="instrumentation 进入稳定路径后补了请求观测"></a>instrumentation 进入稳定路径后补了请求观测</h2><p>框架缓存与服务器组件让一部分“前端慢”发生在服务器。我们利用 instrumentation 接入 trace&#x2F;错误平台，记录 route、fetch 下游耗时和缓存相关标签（避免高基数&#x2F;敏感值）。</p><p>没有服务器观测时，用户只看到 loading，前端 Network 里只有一个框架请求，很难知道是 CMS、数据库还是渲染慢。</p><h2 id="升级后的性能变化怎样解释"><a href="#升级后的性能变化怎样解释" class="headerlink" title="升级后的性能变化怎样解释"></a>升级后的性能变化怎样解释</h2><p>不缓存默认能提高新鲜度，也可能增加后端流量和 TTFB。我们上线时同时看：</p><ul><li>GET Route Handler 请求量与缓存命中；</li><li>下游 CMS&#x2F;数据库 QPS；</li><li>服务器 p95 与错误率；</li><li>客户端导航等待；</li><li>用户保存后看到新数据的成功率。</li></ul><p>某个公开字典接口流量上涨后，我们依据表中“一小时可陈旧”显式缓存，而不是整站恢复旧默认。另一个账户接口保持动态，接受少量性能成本换取正确隔离。</p><h2 id="缓存默认值变化教会我的事"><a href="#缓存默认值变化教会我的事" class="headerlink" title="缓存默认值变化教会我的事"></a>缓存默认值变化教会我的事</h2><p>框架默认值会根据生态反馈调整，业务的新鲜度契约却不应该跟版本漂移。Next 15 把不少缓存行为推向更显式的选择，迁移时最重要的是建立数据清单和端到端更新测试。</p><p>以后再升级缓存模型，我们不需要从“这个版本默认是什么”开始争论，而是拿出每条数据的陈旧上限、共享范围与失效来源，再映射到新 API。这才是能跨版本保留的工程资产。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://nextjs.org/blog/next-15">Next.js 15 发布说明</a></li><li><a href="https://nextjs.org/docs/app/guides/caching">Next.js：Caching</a></li><li><a href="https://nextjs.org/docs/app/guides/upgrading/version-15">Next.js 15 Upgrade Guide</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Next.js </tag>
            
            <tag> 2024 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vue 3.5：响应式性能、解构与 SSR 细节继续成熟</title>
      <link href="/2024/09/03/frontend-chronicle-2024-09-vue-3-5-reactivity/"/>
      <url>/2024/09/03/frontend-chronicle-2024-09-vue-3-5-reactivity/</url>
      
        <content type="html"><![CDATA[<p>Vue 3.5 是一类“升级后业务代码可能一行不改，但大型应用会慢慢受益”的版本。响应式核心经过重构，官方强调内存与深层响应数组等场景的性能改善；同时，响应式 props 解构、watcher 清理、模板 ref 和 SSR 水合策略这些长期摩擦点被逐个补上。</p><p>我们用一个实时搜索组件验证升级。它需要解构 props、监听关键词、取消旧请求，并在 SSR 页面中延迟水合推荐区域。这个例子刚好串起 3.5 几项最实用的变化。</p><h2 id="解构-props-终于不再轻易丢响应"><a href="#解构-props-终于不再轻易丢响应" class="headerlink" title="解构 props 终于不再轻易丢响应"></a>解构 props 终于不再轻易丢响应</h2><p>过去在 <code>&lt;script setup&gt;</code> 里直接解构 <code>defineProps</code>，变量可能只是当时的值，后续 prop 更新不会触发依赖。常见写法是始终保留 <code>props</code> 对象或 <code>toRefs</code>：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> props = defineProps&lt;&#123; <span class="attr">keyword</span>: <span class="built_in">string</span>; <span class="attr">limit</span>?: <span class="built_in">number</span> &#125;&gt;()</span><br><span class="line"><span class="title function_">watch</span>(<span class="function">() =&gt;</span> props.<span class="property">keyword</span>, search)</span><br></pre></td></tr></table></figure><p>Vue 3.5 将 Reactive Props Destructure 稳定化，编译器会为同一 <code>&lt;script setup&gt;</code> 中的访问做转换：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">&lt;script setup lang=&quot;ts&quot;&gt;</span><br><span class="line">const &#123; keyword, limit = 20 &#125; = defineProps&lt;&#123;</span><br><span class="line">  keyword: string</span><br><span class="line">  limit?: number</span><br><span class="line">&#125;&gt;()</span><br><span class="line"></span><br><span class="line">watchEffect(() =&gt; &#123;</span><br><span class="line">  console.log(&#x27;search keyword:&#x27;, keyword, &#x27;limit:&#x27;, limit)</span><br><span class="line">&#125;)</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure><p>默认值也更自然，不需要额外 <code>withDefaults</code>。但这是编译器识别的 props 解构语义，不应该外推成“所有 reactive 对象都能安全普通解构”。</p><p>把变量直接传给需要 watch source 的函数时，还要考虑传值是否失去 getter。显式包装最清楚：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">watch</span>(<span class="function">() =&gt;</span> keyword, <span class="function"><span class="params">value</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;keyword changed:&#x27;</span>, value)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><h2 id="onWatcherCleanup-让取消逻辑靠近副作用"><a href="#onWatcherCleanup-让取消逻辑靠近副作用" class="headerlink" title="onWatcherCleanup 让取消逻辑靠近副作用"></a><code>onWatcherCleanup</code> 让取消逻辑靠近副作用</h2><p>搜索组件最容易出现旧请求覆盖新结果。Vue 3.5 提供 <code>onWatcherCleanup</code>，可以在 watcher 失效重跑前清理当前副作用：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; onWatcherCleanup, ref, watch &#125; <span class="keyword">from</span> <span class="string">&#x27;vue&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> result = ref&lt;<span class="title class_">SearchResult</span>[]&gt;([])</span><br><span class="line"><span class="keyword">const</span> pending = <span class="title function_">ref</span>(<span class="literal">false</span>)</span><br><span class="line"></span><br><span class="line"><span class="title function_">watch</span>(<span class="function">() =&gt;</span> keyword, <span class="keyword">async</span> value =&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> controller = <span class="keyword">new</span> <span class="title class_">AbortController</span>()</span><br><span class="line">  <span class="title function_">onWatcherCleanup</span>(<span class="function">() =&gt;</span> controller.<span class="title function_">abort</span>())</span><br><span class="line"></span><br><span class="line">  pending.<span class="property">value</span> = <span class="literal">true</span></span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">`/api/search?q=<span class="subst">$&#123;<span class="built_in">encodeURIComponent</span>(value)&#125;</span>`</span>, &#123;</span><br><span class="line">      <span class="attr">signal</span>: controller.<span class="property">signal</span></span><br><span class="line">    &#125;)</span><br><span class="line">    result.<span class="property">value</span> = <span class="keyword">await</span> response.<span class="title function_">json</span>()</span><br><span class="line">  &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">    <span class="keyword">if</span> ((error <span class="keyword">as</span> <span class="title class_">Error</span>).<span class="property">name</span> !== <span class="string">&#x27;AbortError&#x27;</span>) <span class="keyword">throw</span> error</span><br><span class="line">  &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (!controller.<span class="property">signal</span>.<span class="property">aborted</span>) pending.<span class="property">value</span> = <span class="literal">false</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>清理注册必须发生在 watcher 同步执行阶段，不能等第一个 await 之后才注册。这个限制很合理：Vue 需要在副作用开始时知道如何让它失效。</p><p>旧的 <code>onCleanup</code> 回调参数仍然有对应使用方式。迁移不是为了把全项目 API 搜索替换，而是在新代码里让资源创建与释放放在一起。</p><h2 id="useTemplateRef-让模板与脚本的连接更明确"><a href="#useTemplateRef-让模板与脚本的连接更明确" class="headerlink" title="useTemplateRef 让模板与脚本的连接更明确"></a><code>useTemplateRef</code> 让模板与脚本的连接更明确</h2><p>以前模板 ref 通常要声明一个同名 <code>ref(null)</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">&lt;script setup&gt;</span><br><span class="line">const input = ref(null)</span><br><span class="line">onMounted(() =&gt; input.value.focus())</span><br><span class="line">&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;template&gt;&lt;input ref=&quot;input&quot; /&gt;&lt;/template&gt;</span><br></pre></td></tr></table></figure><p>3.5 的 <code>useTemplateRef</code> 让字符串 key 关系显式，也改善类型推导：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">&lt;script setup lang=&quot;ts&quot;&gt;</span><br><span class="line">import &#123; onMounted, useTemplateRef &#125; from &#x27;vue&#x27;</span><br><span class="line"></span><br><span class="line">const searchInput = useTemplateRef&lt;HTMLInputElement&gt;(&#x27;search-input&#x27;)</span><br><span class="line"></span><br><span class="line">onMounted(() =&gt; &#123;</span><br><span class="line">  searchInput.value?.focus()</span><br><span class="line">&#125;)</span><br><span class="line">&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;input ref=&quot;search-input&quot; type=&quot;search&quot; /&gt;</span><br><span class="line">&lt;/template&gt;</span><br></pre></td></tr></table></figure><p>仍要处理 null：挂载前、条件渲染消失后，元素都可能不存在。template ref 是逃生舱，不应替代声明式 props&#x2F;state。</p><h2 id="延迟水合适合低优先级区域"><a href="#延迟水合适合低优先级区域" class="headerlink" title="延迟水合适合低优先级区域"></a>延迟水合适合低优先级区域</h2><p>SSR 页面中，首屏下方的推荐区 HTML 已经存在，但不需要立刻下载&#x2F;执行交互。3.5 的异步组件 hydration strategy 可以按空闲、可见、媒体查询或交互延迟水合。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; defineAsyncComponent, hydrateOnVisible &#125; <span class="keyword">from</span> <span class="string">&#x27;vue&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="title class_">RecommendationPanel</span> = <span class="title function_">defineAsyncComponent</span>(&#123;</span><br><span class="line">  <span class="attr">loader</span>: <span class="function">() =&gt;</span> <span class="keyword">import</span>(<span class="string">&#x27;./RecommendationPanel.vue&#x27;</span>),</span><br><span class="line">  <span class="attr">hydrate</span>: <span class="title function_">hydrateOnVisible</span>(&#123; <span class="attr">rootMargin</span>: <span class="string">&#x27;200px&#x27;</span> &#125;)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>延迟水合并非免费：用户可能在组件水合前快速交互，策略要保证事件被合理处理或该区域本来就不急。登录按钮、购买按钮这类关键交互不适合为了分数盲目推迟。</p><p>我们用真实慢网和快速滚动测试，而不是只看 Lighthouse 一次结果。</p><h2 id="data-allow-mismatch-只用于已知差异"><a href="#data-allow-mismatch-只用于已知差异" class="headerlink" title="data-allow-mismatch 只用于已知差异"></a><code>data-allow-mismatch</code> 只用于已知差异</h2><p>SSR 中某些文本确实不可避免地因服务器&#x2F;客户端不同，例如本地化时间。3.5 允许更细粒度抑制已知 hydration mismatch：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">time</span> <span class="attr">data-allow-mismatch</span>=<span class="string">&quot;text&quot;</span>&gt;</span>&#123;&#123; localTime &#125;&#125;<span class="tag">&lt;/<span class="name">time</span>&gt;</span></span><br></pre></td></tr></table></figure><p>这不应成为压掉所有水合警告的开关。随机数、非法 HTML、不同数据快照造成的结构差异仍要修。允许 mismatch 前必须能解释差异为何安全、为何不能提供服务器快照。</p><h2 id="响应式核心重构怎么验证"><a href="#响应式核心重构怎么验证" class="headerlink" title="响应式核心重构怎么验证"></a>响应式核心重构怎么验证</h2><p>官方称重构在保持行为一致的同时降低内存占用，并改善大型&#x2F;深层响应场景。业务项目不需要读取内部 dependency link 来证明它。我们做的是：</p><ol><li>跑 computed、watch、effectScope 与组件库完整测试；</li><li>在重复打开&#x2F;关闭复杂表单后比较 heap 趋势；</li><li>对大数组筛选场景记录交互和更新耗时；</li><li>检查依赖 Vue 内部 API 的调试插件；</li><li>SSR 页面扫描 hydration warning。</li></ol><p>如果项目规模小，数字可能没有肉眼差异，这不影响内部维护质量的价值。不要为了制造“升级收益”写不可信 benchmark。</p><h2 id="这次小版本最值得的地方"><a href="#这次小版本最值得的地方" class="headerlink" title="这次小版本最值得的地方"></a>这次小版本最值得的地方</h2><p>Vue 3.5 没有要求团队学习一种全新组件范式，而是在已有模型上打磨：解构更符合直觉，watcher 有清晰清理入口，模板 ref 与 SSR 水合更精细，底层响应式减少长期成本。</p><p>这种版本适合正常升级，但依然要完整回归，特别是 computed&#x2F;watch、SSR 和依赖内部实现的插件。框架越成熟，重要变化越可能藏在内存、编译和边界 API，而不是发布会上的新语法数量。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://blog.vuejs.org/posts/vue-3-5">Vue 3.5 发布说明</a></li><li><a href="https://vuejs.org/guide/essentials/watchers.html#side-effect-cleanup">Vue：Watchers - Side Effect Cleanup</a></li><li><a href="https://vuejs.org/guide/components/async.html#lazy-hydration">Vue：Async Components - Lazy Hydration</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Vue </tag>
            
            <tag> 2024 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hydration mismatch 排查：服务端 HTML 为什么和客户端不同</title>
      <link href="/2024/08/16/frontend-debug-ssr-hydration-mismatch/"/>
      <url>/2024/08/16/frontend-debug-ssr-hydration-mismatch/</url>
      
        <content type="html"><![CDATA[<p>这次水合错误只在生产首屏出现，客户端路由跳转完全正常。页面上的“活动剩余时间”先显示 8 小时，闪一下变成 0 小时，控制台提示服务器 HTML 与客户端不一致。开发机在同一时区，所以一直没复现。</p><p>最后发现服务器按 UTC 格式化，用户浏览器按本地时区格式化；两边还分别调用了一次 <code>Date.now()</code>。水合不是“把客户端重新渲染一次”这么简单，它要求客户端第一次 render 与服务器已经输出的内容一致，然后 React 才能把事件和状态接到现有 DOM 上。</p><h2 id="页面在-React-接手前已经经历了三份形态"><a href="#页面在-React-接手前已经经历了三份形态" class="headerlink" title="页面在 React 接手前已经经历了三份形态"></a>页面在 React 接手前已经经历了三份形态</h2><p>排查时我会把它们分开：</p><ol><li>服务器响应里的原始 HTML 字符串；</li><li>浏览器解析&#x2F;纠正后的 DOM；</li><li>React 首次客户端 render 期望的树。</li></ol><p>即使 1 与 React 字符串看起来一样，非法标签嵌套也可能让浏览器在第 2 步修改结构。例如：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">BadMarkup</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">p</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      说明文字</span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">div</span>&gt;</span>块级详情<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>浏览器解析器会重排不合法的 <code>&lt;p&gt;&lt;div&gt;</code> 结构，React 水合时看到的 DOM 已经不是服务器组件以为的树。先用 HTML validator&#x2F;Elements 面板检查真实结构，不要只对比 View Source。</p><h2 id="事故代码：两边使用了不同的“现在”"><a href="#事故代码：两边使用了不同的“现在”" class="headerlink" title="事故代码：两边使用了不同的“现在”"></a>事故代码：两边使用了不同的“现在”</h2><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Countdown</span>(<span class="params">&#123; endsAt &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> hours = <span class="title class_">Math</span>.<span class="title function_">max</span>(</span><br><span class="line">    <span class="number">0</span>,</span><br><span class="line">    <span class="title class_">Math</span>.<span class="title function_">ceil</span>((<span class="keyword">new</span> <span class="title class_">Date</span>(endsAt).<span class="title function_">getTime</span>() - <span class="title class_">Date</span>.<span class="title function_">now</span>()) / <span class="number">3_600_000</span>)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">strong</span>&gt;</span>剩余 &#123;hours&#125; 小时<span class="tag">&lt;/<span class="name">strong</span>&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>服务器 render 和浏览器 hydration 有时间差，临界点可能跨小时；如果再用本地化格式化，时区&#x2F;语言也会改变文本。正确方案不是给根元素加 suppress，而是把首屏快照固定。</p><p>服务端生成一致的参考时间：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Page</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> renderedAt = <span class="title class_">Date</span>.<span class="title function_">now</span>()</span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">Countdown</span> <span class="attr">endsAt</span>=<span class="string">&#123;campaign.endsAt&#125;</span> <span class="attr">renderedAt</span>=<span class="string">&#123;renderedAt&#125;</span> /&gt;</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">Countdown</span>(<span class="params">&#123; endsAt, renderedAt &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [now, setNow] = <span class="title function_">useState</span>(renderedAt)</span><br><span class="line"></span><br><span class="line">  <span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> timer = <span class="built_in">setInterval</span>(<span class="function">() =&gt;</span> <span class="title function_">setNow</span>(<span class="title class_">Date</span>.<span class="title function_">now</span>()), <span class="number">60_000</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="function">() =&gt;</span> <span class="built_in">clearInterval</span>(timer)</span><br><span class="line">  &#125;, [])</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> hours = <span class="title class_">Math</span>.<span class="title function_">max</span>(<span class="number">0</span>, <span class="title class_">Math</span>.<span class="title function_">ceil</span>((<span class="title class_">Date</span>.<span class="title function_">parse</span>(endsAt) - now) / <span class="number">3_600_000</span>))</span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">strong</span>&gt;</span>剩余 &#123;hours&#125; 小时<span class="tag">&lt;/<span class="name">strong</span>&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>renderedAt</code> 被序列化到页面，客户端首次使用同一值，水合后才开始本地计时。</p><h2 id="浏览器能力不要在-render-分支里直接决定首屏结构"><a href="#浏览器能力不要在-render-分支里直接决定首屏结构" class="headerlink" title="浏览器能力不要在 render 分支里直接决定首屏结构"></a>浏览器能力不要在 render 分支里直接决定首屏结构</h2><p>这段代码在服务器没有 window，客户端有 window，第一次输出必然不同：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">ThemeLabel</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> dark = <span class="keyword">typeof</span> <span class="variable language_">window</span> !== <span class="string">&#x27;undefined&#x27;</span></span><br><span class="line">    &amp;&amp; <span class="variable language_">window</span>.<span class="title function_">matchMedia</span>(<span class="string">&#x27;(prefers-color-scheme: dark)&#x27;</span>).<span class="property">matches</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">span</span>&gt;</span>&#123;dark ? &#x27;深色&#x27; : &#x27;浅色&#x27;&#125;<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果主题可以由 Cookie 提供，服务器和客户端都读同一初始值最好；否则先渲染确定占位，水合后更新：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">ThemeLabel</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [theme, setTheme] = <span class="title function_">useState</span>(<span class="string">&#x27;unknown&#x27;</span>)</span><br><span class="line"></span><br><span class="line">  <span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> query = <span class="variable language_">window</span>.<span class="title function_">matchMedia</span>(<span class="string">&#x27;(prefers-color-scheme: dark)&#x27;</span>)</span><br><span class="line">    <span class="title function_">setTheme</span>(query.<span class="property">matches</span> ? <span class="string">&#x27;dark&#x27;</span> : <span class="string">&#x27;light&#x27;</span>)</span><br><span class="line">  &#125;, [])</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">span</span>&gt;</span>&#123;theme === &#x27;unknown&#x27; ? &#x27;跟随系统&#x27; : theme === &#x27;dark&#x27; ? &#x27;深色&#x27; : &#x27;浅色&#x27;&#125;<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这会在水合后多一次 render，但结构可预测。避免布局跳动可以预留尺寸或用 CSS media query 直接控制纯视觉主题。</p><h2 id="数据快照不一致比随机数更难发现"><a href="#数据快照不一致比随机数更难发现" class="headerlink" title="数据快照不一致比随机数更难发现"></a>数据快照不一致比随机数更难发现</h2><p>服务器查询库存是 5，HTML 发出后库存变成 4；客户端 hydration 前又请求一次得到 4，就产生 mismatch。解决方案不是要求数据永远不变，而是让客户端首屏使用服务器同一快照，水合后再 revalidate。</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">id</span>=<span class="string">&quot;__PAGE_DATA__&quot;</span> <span class="attr">type</span>=<span class="string">&quot;application/json&quot;</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">  &#123;<span class="string">&quot;productId&quot;</span>:<span class="number">42</span>,<span class="string">&quot;stock&quot;</span>:<span class="number">5</span>,<span class="string">&quot;version&quot;</span>:<span class="string">&quot;inventory-901&quot;</span>&#125;</span></span><br><span class="line"><span class="language-javascript"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure><p>框架通常提供自己的序列化&#x2F;数据传递机制，优先使用它，避免手拼 JSON 带来 XSS。缓存库也应 hydrate 服务器 query cache，而不是客户端第一次 render 就显示另一份 loading&#x2F;data。</p><h2 id="随机-key-会让问题升级为节点重建"><a href="#随机-key-会让问题升级为节点重建" class="headerlink" title="随机 key 会让问题升级为节点重建"></a>随机 key 会让问题升级为节点重建</h2><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">items.<span class="title function_">map</span>(<span class="function"><span class="params">item</span> =&gt;</span> <span class="language-xml"><span class="tag">&lt;<span class="name">Row</span> <span class="attr">key</span>=<span class="string">&#123;Math.random()&#125;</span> <span class="attr">item</span>=<span class="string">&#123;item&#125;</span> /&gt;</span></span>)</span><br></pre></td></tr></table></figure><p>服务器与客户端 key 不同，会破坏身份；即使没立即报 hydration 文本错误，后续更新也会反复卸载组件。使用数据稳定 ID：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">items.<span class="title function_">map</span>(<span class="function"><span class="params">item</span> =&gt;</span> <span class="language-xml"><span class="tag">&lt;<span class="name">Row</span> <span class="attr">key</span>=<span class="string">&#123;item.id&#125;</span> <span class="attr">item</span>=<span class="string">&#123;item&#125;</span> /&gt;</span></span>)</span><br></pre></td></tr></table></figure><p>没有 ID 时，先修数据模型；数组 index 只适合顺序和成员永不变化的静态列表。</p><h2 id="我怎么抓到“水合前”的-DOM"><a href="#我怎么抓到“水合前”的-DOM" class="headerlink" title="我怎么抓到“水合前”的 DOM"></a>我怎么抓到“水合前”的 DOM</h2><p>React 很快就可能替换不匹配子树，Elements 面板看到的是事后结果。几种方法：</p><ol><li>Network 中保存 Document response，拿到原始 HTML；</li><li>禁用 JavaScript 重新加载，看浏览器解析后的 DOM；</li><li>在入口 <code>hydrateRoot</code> 前打断点，复制 <code>document.documentElement.outerHTML</code>；</li><li>使用 React 19 更详细的 hydration diff 找第一个分歧节点；</li><li>固定时区、语言、Date&#x2F;random 和接口 fixture 在 CI 复现。</li></ol><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(&#123;</span><br><span class="line">  <span class="attr">timezone</span>: <span class="title class_">Intl</span>.<span class="title class_">DateTimeFormat</span>().<span class="title function_">resolvedOptions</span>().<span class="property">timeZone</span>,</span><br><span class="line">  <span class="attr">language</span>: navigator.<span class="property">language</span>,</span><br><span class="line">  <span class="attr">html</span>: <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;root&#x27;</span>)?.<span class="property">innerHTML</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>线上日志不能上传完整页面个人数据，只记录必要环境和组件边界。</p><h2 id="suppressHydrationWarning-的使用范围要非常小"><a href="#suppressHydrationWarning-的使用范围要非常小" class="headerlink" title="suppressHydrationWarning 的使用范围要非常小"></a><code>suppressHydrationWarning</code> 的使用范围要非常小</h2><p>如果时间戳文本确实无法在两侧一致，可以在单个元素上抑制警告：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;time suppressHydrationWarning&gt;&#123;clientFormattedTime&#125;&lt;/time&gt;</span><br></pre></td></tr></table></figure><p>它通常只对一层生效，也不会修复事件绑定、结构和业务状态。给整个 <code>&lt;main&gt;</code> 加上以后“控制台干净”不等于水合正确。每个 suppress 都应有注释说明差异来源和为什么安全。</p><p>第三方编辑器&#x2F;地图完全依赖浏览器时，可以使用框架提供的 client-only 动态加载边界，而不是让服务器伪造一棵完全不同的 DOM。</p><h2 id="浏览器扩展也可能改-DOM，但不要先甩锅"><a href="#浏览器扩展也可能改-DOM，但不要先甩锅" class="headerlink" title="浏览器扩展也可能改 DOM，但不要先甩锅"></a>浏览器扩展也可能改 DOM，但不要先甩锅</h2><p>密码管理器、翻译和广告扩展可能在 React 加载前插入属性或节点。若只发生在少量用户且 diff 指向扩展属性，可以在干净 profile 复现对比。但绝大多数 hydration 错误仍来自自己的不确定渲染。</p><p>先固定时间&#x2F;数据、检查 HTML、缩小组件边界；证据指向扩展后再分类上报，不要把所有“本地复现不了”都归因于用户环境。</p><h2 id="CI-增加了一个控制台零容忍用例"><a href="#CI-增加了一个控制台零容忍用例" class="headerlink" title="CI 增加了一个控制台零容忍用例"></a>CI 增加了一个控制台零容忍用例</h2><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">test</span>(<span class="string">&#x27;product page hydrates without warnings&#x27;</span>, <span class="title function_">async</span> (&#123; page &#125;) =&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="attr">errors</span>: <span class="built_in">string</span>[] = []</span><br><span class="line">  page.<span class="title function_">on</span>(<span class="string">&#x27;console&#x27;</span>, <span class="function"><span class="params">message</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (message.<span class="title function_">type</span>() === <span class="string">&#x27;error&#x27;</span>) errors.<span class="title function_">push</span>(message.<span class="title function_">text</span>())</span><br><span class="line">  &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">await</span> page.<span class="title function_">goto</span>(<span class="string">&#x27;/products/42&#x27;</span>)</span><br><span class="line">  <span class="keyword">await</span> page.<span class="title function_">getByRole</span>(<span class="string">&#x27;button&#x27;</span>, &#123; <span class="attr">name</span>: <span class="string">&#x27;加入购物车&#x27;</span> &#125;).<span class="title function_">click</span>()</span><br><span class="line"></span><br><span class="line">  <span class="title function_">expect</span>(errors.<span class="title function_">filter</span>(<span class="function"><span class="params">text</span> =&gt;</span> <span class="regexp">/hydration/i</span>.<span class="title function_">test</span>(text))).<span class="title function_">toEqual</span>([])</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>测试服务器固定时区与数据，再加一组不同时区浏览器。既验证没有 warning，也点击关键按钮确认事件真正接上。</p><h2 id="最后的分流顺序"><a href="#最后的分流顺序" class="headerlink" title="最后的分流顺序"></a>最后的分流顺序</h2><ul><li>文本不同：时间、随机数、本地化、数据快照；</li><li>结构不同：非法 HTML、条件分支、第三方 DOM 修改；</li><li>只在首屏：SSR 与首次 client render 输入不一致；</li><li>只在生产：缓存、时区、压缩&#x2F;插件或环境变量差异；</li><li>suppress 后仍异常：它从来没有修复结构和状态。</li></ul><p>这次活动倒计时最终通过服务器时间快照修复，并补了跨时区水合测试。Hydration mismatch 不是无关紧要的开发 warning，它意味着 React 不能确信现有 DOM 与组件树一致，可能重建子树、丢状态或绑定错误。找到两边第一次 render 的第一个不同输入，问题通常就能落到具体代码上。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://react.dev/reference/react-dom/client/hydrateRoot">React：hydrateRoot</a></li><li><a href="https://react.dev/blog/2024/12/05/react-19#diffs-for-hydration-errors">React 19：Hydration error diffs</a></li><li><a href="https://html.spec.whatwg.org/multipage/parsing.html">WHATWG：Parsing HTML documents</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端排障 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端排障 </tag>
            
            <tag> SSR </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vite 5：Rollup 4 与现代 Node 基线</title>
      <link href="/2023/11/16/frontend-chronicle-2023-11-vite-5-rollup-4/"/>
      <url>/2023/11/16/frontend-chronicle-2023-11-vite-5-rollup-4/</url>
      
        <content type="html"><![CDATA[<p>Vite 5 的升级分支一开始就失败在 CI，连业务源码都没来得及编译。原因是 CI 还在 Node 16，而 Vite 5 要求受支持的现代 Node 版本。这个报错反而帮我们确定了正确顺序：先统一运行时，再迁 ESM 配置和废弃 API，最后才评估 Rollup 4 产物与性能。</p><p>把所有变化塞进一个依赖更新 PR，会让任何失败都难定位。我们把升级拆成三次可回滚提交，下面是当时的迁移笔记。</p><h2 id="第-1-步：让四个地方使用同一个-Node"><a href="#第-1-步：让四个地方使用同一个-Node" class="headerlink" title="第 1 步：让四个地方使用同一个 Node"></a>第 1 步：让四个地方使用同一个 Node</h2><p>项目中的 Node 版本散落在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">.nvmrc</span><br><span class="line">package.json engines</span><br><span class="line">.github/workflows/ci.yml</span><br><span class="line">Dockerfile</span><br></pre></td></tr></table></figure><p>先统一到 Vite 5 支持范围内的版本，重新生成依赖安装结果，跑旧 Vite 的测试和 build。这样如果原生依赖或 npm lockfile 出问题，可以确定是 Node 升级，而不是 Vite 5。</p><p>同时检查部署平台，不只看构建容器。SSR&#x2F;预渲染脚本如果运行在旧 Node，即使静态 build 在 CI 成功，运行时仍会失败。</p><h2 id="第-2-步：清掉-CJS-Node-API-警告"><a href="#第-2-步：清掉-CJS-Node-API-警告" class="headerlink" title="第 2 步：清掉 CJS Node API 警告"></a>第 2 步：清掉 CJS Node API 警告</h2><p>旧工具脚本这样加载 Vite：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// scripts/build.cjs</span></span><br><span class="line"><span class="keyword">const</span> &#123; build &#125; = <span class="built_in">require</span>(<span class="string">&#x27;vite&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="title function_">build</span>(&#123; <span class="attr">mode</span>: <span class="string">&#x27;production&#x27;</span> &#125;)</span><br></pre></td></tr></table></figure><p>Vite 5 弃用 CJS Node API 后，我们把脚本改成 ESM：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// scripts/build.mjs</span></span><br><span class="line"><span class="keyword">import</span> &#123; build &#125; <span class="keyword">from</span> <span class="string">&#x27;vite&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">await</span> <span class="title function_">build</span>(&#123; <span class="attr">mode</span>: <span class="string">&#x27;production&#x27;</span> &#125;)</span><br></pre></td></tr></table></figure><p>配置也统一为 <code>vite.config.mjs/ts</code> 或处在 <code>type: module</code> 包范围内。这里不能只全局改扩展名：<code>__dirname</code>、JSON 导入、CommonJS 插件与测试 config 都要逐个处理。</p><p>使用 Vite API 的内部工具比普通应用更容易踩到变化，所以我们用 <code>rg &quot;require\([&#39;\&quot;]vite&quot;</code> 搜出所有直接调用点，并直接执行每个脚本。</p><h2 id="第-3-步：升级以后比较两份-dist"><a href="#第-3-步：升级以后比较两份-dist" class="headerlink" title="第 3 步：升级以后比较两份 dist"></a>第 3 步：升级以后比较两份 dist</h2><p>Vite 5 使用 Rollup 4。官方说明包含性能与行为更新，但业务最需要的是产物等价。我们保存旧版和新版构建：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">dist-v4/</span><br><span class="line">dist-v5/</span><br></pre></td></tr></table></figure><p>比较：入口和 chunk 数量、gzip&#x2F;brotli 体积、manifest、CSS 顺序、动态导入、图片路径与 source map。再让同一套 Playwright 用例分别访问两份静态产物。</p><p>某个 snapshot 测试依赖 chunk 文件排列顺序，升级后失败。排查发现业务并不应该依赖顺序，于是修了测试；另一个 SSR externalization 差异则确实影响运行，必须调整包出口。对比的目标不是让每个字节一致，而是识别哪些差异有业务含义。</p><h2 id="server-warmup-不是越多越快"><a href="#server-warmup-不是越多越快" class="headerlink" title="server.warmup 不是越多越快"></a>server.warmup 不是越多越快</h2><p>Vite 5 提供 <code>server.warmup</code>，可以在启动时预转换常用模块，减少打开页面时的转换瀑布：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">server</span>: &#123;</span><br><span class="line">    <span class="attr">warmup</span>: &#123;</span><br><span class="line">      <span class="attr">clientFiles</span>: [</span><br><span class="line">        <span class="string">&#x27;./src/main.ts&#x27;</span>,</span><br><span class="line">        <span class="string">&#x27;./src/router.ts&#x27;</span>,</span><br><span class="line">        <span class="string">&#x27;./src/layouts/AppLayout.vue&#x27;</span></span><br><span class="line">      ]</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>如果把整个 src glob 进去，就重新把按需开发变成启动前全量工作。我们从真实首页请求瀑布和转换日志挑三个稳定入口，并比较“server ready”与“页面可交互”两个时间。启动日志更快不代表开发者更早看到页面。</p><h2 id="慢启动最后定位到-barrel-file"><a href="#慢启动最后定位到-barrel-file" class="headerlink" title="慢启动最后定位到 barrel file"></a>慢启动最后定位到 barrel file</h2><p>升级后某个项目仍慢，打开 <code>DEBUG=vite:*</code>&#x2F;性能分析后发现，入口通过一个 <code>components/index.ts</code> 重导出两百多个组件：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> * <span class="keyword">from</span> <span class="string">&#x27;./charts&#x27;</span></span><br><span class="line"><span class="keyword">export</span> * <span class="keyword">from</span> <span class="string">&#x27;./editors&#x27;</span></span><br><span class="line"><span class="keyword">export</span> * <span class="keyword">from</span> <span class="string">&#x27;./tables&#x27;</span></span><br></pre></td></tr></table></figure><p>首页只用一个 Button，却把大量模块带进转换图。我们改为直接入口和包级显式 exports。这个问题不是 Vite 5 制造的，升级只是提供了重新测量的机会。</p><h2 id="插件兼容要覆盖-dev、build-与-SSR"><a href="#插件兼容要覆盖-dev、build-与-SSR" class="headerlink" title="插件兼容要覆盖 dev、build 与 SSR"></a>插件兼容要覆盖 dev、build 与 SSR</h2><p>自研插件分别用了 <code>transform</code>、虚拟模块和 <code>generateBundle</code>。测试矩阵如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">dev: 模块能加载、HMR 能更新</span><br><span class="line">build: 产物与 manifest 正确</span><br><span class="line">preview: base 路径与动态 chunk 正确</span><br><span class="line">SSR: external/resolve 行为正确</span><br></pre></td></tr></table></figure><p>只有 <code>vite dev</code> 能启动不能证明 generateBundle 钩子兼容；只有 build 成功也不能证明 HMR。我们给每个关键插件建了最小 fixture，类似官方 ecosystem CI 的下游验证思路。</p><h2 id="废弃-API-不要拖到下一个大版本"><a href="#废弃-API-不要拖到下一个大版本" class="headerlink" title="废弃 API 不要拖到下一个大版本"></a>废弃 API 不要拖到下一个大版本</h2><p>Vite 大版本节奏较快，长期忽略 warning 会让多批变化同时到来。升级完成后 CI 会扫描构建日志中的 Vite deprecation（对可控警告维护 allowlist），新警告作为需要处理的技术债，而不是等构建失败才看。</p><p>同样，内部插件不导入 Vite 未公开路径。少写几十行绕过 API 的“捷径”，能省掉以后每次主版本追内部实现的成本。</p><h2 id="升级结果"><a href="#升级结果" class="headerlink" title="升级结果"></a>升级结果</h2><p>Rollup 4 构建有实际改善，Node&#x2F;ESM 统一让配置更干净，warmup 只在一个大型后台保留。更重要的是升级过程留下了产物对比与插件 fixture，后续主版本不再靠人工点两页。</p><p>Vite 5 本身是一轮相对直接的大版本，但它体现了现代前端工具的现实：Node 生命周期、ESM 包边界、生产构建器与插件生态绑定在一起。升级顺序从运行时开始，验收以最终产物结束，中间每层都能单独回滚，才不会把“依赖升成功”误认为项目已经迁完。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://vite.dev/blog/announcing-vite5">Vite 5.0 发布说明</a></li><li><a href="https://vite.dev/guide/performance">Vite：Performance</a></li><li><a href="https://vite.dev/guide/migration">Vite：Migration from v4</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Vite </tag>
            
            <tag> 2023 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Bun 1.0：运行时、包管理器与构建工具走向一体化</title>
      <link href="/2023/09/08/frontend-chronicle-2023-09-bun-1-runtime-toolkit/"/>
      <url>/2023/09/08/frontend-chronicle-2023-09-bun-1-runtime-toolkit/</url>
      
        <content type="html"><![CDATA[<p>Bun 1.0 发布后，我给自己定了一个很具体的试用范围：不改线上 Node 服务，先让它安装一个前端仓库、跑单元测试，再执行两个内部脚本。这样既能测到 package manager、runtime 和 test runner，又不会因为“启动快”就跳过生产兼容性。</p><p>结论不是简单的“能替代”或“不能替代”。安装与脚本启动确实快，很多 Web&#x2F;Node API 可以直接运行；但测试 mock、原生 addon、lockfile 流程和公司代理都需要逐项验证。一体化工具减少了组合数量，也把更多职责集中到同一次升级里。</p><h2 id="先用现有-package-json，不重写项目"><a href="#先用现有-package-json，不重写项目" class="headerlink" title="先用现有 package.json，不重写项目"></a>先用现有 package.json，不重写项目</h2><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;scripts&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;lint&quot;</span><span class="punctuation">:</span> <span class="string">&quot;eslint src&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;test&quot;</span><span class="punctuation">:</span> <span class="string">&quot;vitest run&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="string">&quot;vite build&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;check-links&quot;</span><span class="punctuation">:</span> <span class="string">&quot;node scripts/check-links.mjs&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>第一步只是运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">bun install</span><br><span class="line">bun run lint</span><br><span class="line">bun run build</span><br><span class="line">bun scripts/check-links.mjs</span><br></pre></td></tr></table></figure><p>不要为了让试验漂亮而先改掉所有失败依赖。记录哪些命令不改即可运行、哪些需要适配、哪些有行为差异，这才是兼容性报告。</p><p>安装速度之外，我们检查 lockfile 是否稳定、peer dependency 警告、私有 registry、postinstall 和补丁工具。CI 需要可复现安装，不是开发机第二次有缓存时最快。</p><h2 id="内置测试器从一个纯函数开始"><a href="#内置测试器从一个纯函数开始" class="headerlink" title="内置测试器从一个纯函数开始"></a>内置测试器从一个纯函数开始</h2><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// price.ts</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">applyDiscount</span>(<span class="params"><span class="attr">price</span>: <span class="built_in">number</span>, <span class="attr">rate</span>: <span class="built_in">number</span></span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (price &lt; <span class="number">0</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;price must be positive&#x27;</span>)</span><br><span class="line">  <span class="keyword">if</span> (rate &lt; <span class="number">0</span> || rate &gt; <span class="number">1</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;invalid rate&#x27;</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="title class_">Math</span>.<span class="title function_">round</span>(price * (<span class="number">1</span> - rate) * <span class="number">100</span>) / <span class="number">100</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; describe, expect, test &#125; <span class="keyword">from</span> <span class="string">&#x27;bun:test&#x27;</span></span><br><span class="line"><span class="keyword">import</span> &#123; applyDiscount &#125; <span class="keyword">from</span> <span class="string">&#x27;./price&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="title function_">describe</span>(<span class="string">&#x27;applyDiscount&#x27;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="title function_">test</span>(<span class="string">&#x27;keeps currency precision&#x27;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">expect</span>(<span class="title function_">applyDiscount</span>(<span class="number">19.9</span>, <span class="number">0.1</span>)).<span class="title function_">toBe</span>(<span class="number">17.91</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>这类测试迁移很顺。真正困难的是项目里大量依赖 Jest&#x2F;Vitest 特定 mock、fake timer、jsdom 和 transform 插件的用例。API 名称相似不代表边缘行为完全一致。</p><p>我们抽取 50 条代表性测试：纯函数、React&#x2F;Vue 组件、网络 mock、定时器、snapshot、动态 import、覆盖率。逐条对比结果与耗时，而不是让 2000 条测试第一次全量失败后靠搜索替换。</p><h2 id="Node-兼容要按项目实际-API-测"><a href="#Node-兼容要按项目实际-API-测" class="headerlink" title="Node 兼容要按项目实际 API 测"></a>Node 兼容要按项目实际 API 测</h2><p>Bun 以 Node.js 兼容为重要目标，但“兼容 Node”范围很大。我们的脚本用到：</p><ul><li><code>node:fs/promises</code> 与 glob；</li><li>子进程和退出码；</li><li>TLS&#x2F;企业代理；</li><li>一个含原生二进制的图片处理包；</li><li>ESM&#x2F;CJS 混合内部包；</li><li><code>process</code> 信号和优雅退出。</li></ul><p>文件和普通 HTTP 测试通过，某个原生模块安装脚本与预编译二进制选择出现差异。于是生产服务被明确列为“暂不迁移”，图片脚本保留 Node。这比全局切换 runtime 后线上才发现好得多。</p><h2 id="一个小-HTTP-服务能说明体验，不能证明生产可用"><a href="#一个小-HTTP-服务能说明体验，不能证明生产可用" class="headerlink" title="一个小 HTTP 服务能说明体验，不能证明生产可用"></a>一个小 HTTP 服务能说明体验，不能证明生产可用</h2><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> server = <span class="title class_">Bun</span>.<span class="title function_">serve</span>(&#123;</span><br><span class="line">  <span class="attr">port</span>: <span class="number">3000</span>,</span><br><span class="line">  <span class="title function_">fetch</span>(<span class="params">request</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> url = <span class="keyword">new</span> <span class="title function_">URL</span>(request.<span class="property">url</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (url.<span class="property">pathname</span> === <span class="string">&#x27;/health&#x27;</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="title class_">Response</span>.<span class="title function_">json</span>(&#123; <span class="attr">ok</span>: <span class="literal">true</span> &#125;)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&#x27;Not found&#x27;</span>, &#123; <span class="attr">status</span>: <span class="number">404</span> &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Listening on <span class="subst">$&#123;server.url&#125;</span>`</span>)</span><br></pre></td></tr></table></figure><p>几行代码就能启动服务，很适合原型。但生产评估还要覆盖：请求上限、超时、body 大小、TLS、日志、metrics&#x2F;trace、优雅退出、容器信号、内存曲线和故障恢复。Hello World 的 QPS 不能代替业务负载。</p><p>我们用真实 JSON payload 和下游 mock 做短时压测，关注 p95、错误率与内存，而不是只截取最高吞吐。运行时差异往往在压力、取消和异常路径下出现。</p><h2 id="一体化最舒服，也意味着升级半径更大"><a href="#一体化最舒服，也意味着升级半径更大" class="headerlink" title="一体化最舒服，也意味着升级半径更大"></a>一体化最舒服，也意味着升级半径更大</h2><p>传统栈可能分别升级 Node、npm、Vitest 和 bundler。Bun 把 runtime、package manager、test 和 bundler放进一个版本，默认配合更顺，版本变更也可能同时触及多条链。</p><p>因此我们没有让 <code>bun upgrade</code> 随开发者个人执行，而是在分支中固定版本，跑：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">全新安装 -&gt; lint -&gt; 单测 -&gt; 生产构建 -&gt; E2E -&gt; 脚本冒烟</span><br></pre></td></tr></table></figure><p>失败时记录属于安装、运行时、测试还是打包。工具是一体的，诊断仍要分层。</p><h2 id="lockfile-与双包管理器不能长期并存"><a href="#lockfile-与双包管理器不能长期并存" class="headerlink" title="lockfile 与双包管理器不能长期并存"></a>lockfile 与双包管理器不能长期并存</h2><p>试验阶段保留 npm lockfile 作为基线可以理解，正式采用后不能让团队有人 npm install、有人 bun install，两份 lockfile 轮流修改。依赖解析结果可能不同，CI 也失去唯一来源。</p><p>我们的仓库在试验分支使用 Bun lockfile，主分支仍保持 npm。只有评审决定采用后才会一次性切换，并删除旧 lockfile、更新贡献文档和 CI。没有进入“两个都能用”的灰色状态。</p><h2 id="最终采用范围"><a href="#最终采用范围" class="headerlink" title="最终采用范围"></a>最终采用范围</h2><p>Bun 被用于几个无原生依赖的内部脚本和独立工具；主前端仓库暂时继续 npm + Vite&#x2F;Vitest；生产 Node 服务没有迁移。选择不是永远不变，后续版本可以按同一兼容矩阵重测。</p><p>这次试用最值得的不是省下多少秒，而是把“运行时兼容”从模糊印象变成了清单。Bun 1.0 证明 JavaScript 工具链可以被重新组合，性能竞争也推动整个生态进步。团队是否采用，则要看自己的依赖、测试和运维，而不是只看一条 benchmark。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://bun.sh/blog/bun-v1.0">Bun 1.0 发布说明</a></li><li><a href="https://bun.sh/docs/runtime/nodejs-apis">Bun：Node.js compatibility</a></li><li><a href="https://bun.sh/docs/test">Bun：Test runner</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 2023 </tag>
            
            <tag> Bun </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ESM/CJS 混用排查：ERR_REQUIRE_ESM 与默认导入错误</title>
      <link href="/2023/08/18/frontend-debug-esm-cjs-module-errors/"/>
      <url>/2023/08/18/frontend-debug-esm-cjs-module-errors/</url>
      
        <content type="html"><![CDATA[<p>这条错误出现在一个已经运行两年的发布脚本里：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Error [ERR_REQUIRE_ESM]: require() of ES Module ... not supported</span><br></pre></td></tr></table></figure><p>业务代码没有改，依赖升级后，某个工具包从同时发布 CommonJS&#x2F;ESM 改成纯 ESM。脚本仍由 CommonJS 执行，用 <code>require()</code> 同步加载它，于是直接失败。</p><p>网上常见答案是“把 require 改成 import”或“package.json 加 type: module”。这两句可能有效，也可能让整个仓库的配置文件一起坏掉。排查模块错误必须先确认：报错文件被 Node 当成什么格式、它实际加载了哪个出口、编译后的代码又是什么。</p><h2 id="第一步只用-Node-复现，不让框架包住错误"><a href="#第一步只用-Node-复现，不让框架包住错误" class="headerlink" title="第一步只用 Node 复现，不让框架包住错误"></a>第一步只用 Node 复现，不让框架包住错误</h2><p>原脚本经过任务 runner，堆栈很长。我们先建最小文件：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// reproduce.cjs</span></span><br><span class="line"><span class="keyword">const</span> formatter = <span class="built_in">require</span>(<span class="string">&#x27;esm-only-formatter&#x27;</span>)</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(formatter)</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">node reproduce.cjs</span><br></pre></td></tr></table></figure><p>仍然抛 ERR_REQUIRE_ESM，说明与 webpack&#x2F;测试器无关。再查看依赖自身 <code>package.json</code>：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;module&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;exports&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./index.js&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>它公开的是 ESM <code>.js</code>。CommonJS 的 <code>require</code> 是同步 API，不能同步执行一个可能包含 top-level await 的 ESM 模块。</p><h2 id="最小修复：在-CJS-边界使用动态-import"><a href="#最小修复：在-CJS-边界使用动态-import" class="headerlink" title="最小修复：在 CJS 边界使用动态 import"></a>最小修复：在 CJS 边界使用动态 import</h2><p>发布脚本暂时不能整体转 ESM，因为几个旧插件只提供 CJS。我们把纯 ESM 依赖收敛到一个异步加载函数：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// formatter-loader.cjs</span></span><br><span class="line"><span class="keyword">let</span> formatterPromise</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">loadFormatter</span>(<span class="params"></span>) &#123;</span><br><span class="line">  formatterPromise ??= <span class="keyword">import</span>(<span class="string">&#x27;esm-only-formatter&#x27;</span>)</span><br><span class="line">  <span class="keyword">return</span> formatterPromise</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = &#123; loadFormatter &#125;</span><br></pre></td></tr></table></figure><p>调用链改为 async：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> &#123; loadFormatter &#125; = <span class="built_in">require</span>(<span class="string">&#x27;./formatter-loader.cjs&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">buildRelease</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> &#123; <span class="attr">default</span>: format &#125; = <span class="keyword">await</span> <span class="title function_">loadFormatter</span>()</span><br><span class="line">  <span class="keyword">const</span> result = <span class="title function_">format</span>(<span class="keyword">await</span> <span class="title function_">readManifest</span>())</span><br><span class="line">  <span class="keyword">await</span> <span class="title function_">writeRelease</span>(result)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title function_">buildRelease</span>().<span class="title function_">catch</span>(<span class="function"><span class="params">error</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">error</span>(error)</span><br><span class="line">  process.<span class="property">exitCode</span> = <span class="number">1</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>动态 import 在 CommonJS 中可用，但返回 Promise。若原 API 必须同步，就不能假装一行替换没有架构影响；要么向上传播异步，要么固定兼容的旧依赖版本并规划迁移。</p><h2 id="require-is-not-defined-是相反方向"><a href="#require-is-not-defined-是相反方向" class="headerlink" title="require is not defined 是相反方向"></a><code>require is not defined</code> 是相反方向</h2><p>另一个项目在 <code>package.json</code> 加了 <code>&quot;type&quot;: &quot;module&quot;</code> 后，<code>vite.config.js</code> 报：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ReferenceError: require is not defined in ES module scope</span><br></pre></td></tr></table></figure><p>该 package 范围内 <code>.js</code> 已按 ESM 解释，配置仍写：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">&#x27;node:path&#x27;</span>)</span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = &#123; <span class="comment">/* ... */</span> &#125;</span><br></pre></td></tr></table></figure><p>有两种明确修法：把配置转换为 ESM，或在仍需 CJS 时改名 <code>.cjs</code>。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// vite.config.mjs</span></span><br><span class="line"><span class="keyword">import</span> path <span class="keyword">from</span> <span class="string">&#x27;node:path&#x27;</span></span><br><span class="line"><span class="keyword">import</span> &#123; fileURLToPath &#125; <span class="keyword">from</span> <span class="string">&#x27;node:url&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> currentDir = path.<span class="title function_">dirname</span>(<span class="title function_">fileURLToPath</span>(<span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">url</span>))</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="attr">resolve</span>: &#123;</span><br><span class="line">    <span class="attr">alias</span>: &#123; <span class="string">&#x27;@&#x27;</span>: path.<span class="title function_">join</span>(currentDir, <span class="string">&#x27;src&#x27;</span>) &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>不要在 ESM 顶部伪造一个全局 require 然后继续混写，除非确实需要 <code>createRequire</code> 加载特定 CJS&#x2F;JSON，且边界很清楚。</p><h2 id="js-到底是什么，由最近的-package-边界决定"><a href="#js-到底是什么，由最近的-package-边界决定" class="headerlink" title=".js 到底是什么，由最近的 package 边界决定"></a><code>.js</code> 到底是什么，由最近的 package 边界决定</h2><p>快速判断表：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">.mjs                         -&gt; ESM</span><br><span class="line">.cjs                         -&gt; CommonJS</span><br><span class="line">.js + 最近 package type=module -&gt; ESM</span><br><span class="line">.js + type=commonjs/未声明      -&gt; CommonJS</span><br></pre></td></tr></table></figure><p>monorepo 中每个 package 可能有自己的 <code>package.json</code>，同名 <code>.js</code> 在不同目录下解释不同。Node 错误堆栈给出的绝对路径很重要，要沿路径向上找最近 package.json，而不是只看仓库根。</p><h2 id="exports-会让“文件明明存在”也无法导入"><a href="#exports-会让“文件明明存在”也无法导入" class="headerlink" title="exports 会让“文件明明存在”也无法导入"></a><code>exports</code> 会让“文件明明存在”也无法导入</h2><p>依赖升级后还有一种错误：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ERR_PACKAGE_PATH_NOT_EXPORTED</span><br></pre></td></tr></table></figure><p>旧代码绕过公开入口：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> parser <span class="keyword">from</span> <span class="string">&#x27;some-package/lib/internal/parser.js&#x27;</span></span><br></pre></td></tr></table></figure><p>包新增 <code>exports</code> 后，只允许声明的子路径：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;exports&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;.&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/index.js&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;./parser&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/parser.js&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>应改用 <code>some-package/parser</code> 或公开 API，而不是继续拼 <code>node_modules</code> 路径。深层文件不是稳定契约，构建器也可能与 Node 对 exports 的处理不同。</p><h2 id="默认导入多一层-default-从哪里来"><a href="#默认导入多一层-default-从哪里来" class="headerlink" title="默认导入多一层 .default 从哪里来"></a>默认导入多一层 <code>.default</code> 从哪里来</h2><p>CommonJS 只有 <code>module.exports</code>，ESM 有 default&#x2F;named exports。转译器会为了互操作生成包装，配置不同就可能出现：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="variable language_">module</span> = <span class="built_in">require</span>(<span class="string">&#x27;./compiled-module&#x27;</span>)</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="variable language_">module</span>.<span class="property">default</span>)</span><br></pre></td></tr></table></figure><p>排查不要根据 TypeScript 源码猜，直接打印&#x2F;查看最终导出：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">node -e <span class="string">&quot;console.log(require(&#x27;./dist/index.cjs&#x27;))&quot;</span></span><br><span class="line">node -e <span class="string">&quot;import(&#x27;./dist/index.js&#x27;).then(console.log)&quot;</span></span><br></pre></td></tr></table></figure><p>库同时发布两种格式时，两条命令都应该是 CI 消费测试。只跑源码单测无法保证消费者看到的形状。</p><h2 id="TypeScript-配置必须与真实运行时一致"><a href="#TypeScript-配置必须与真实运行时一致" class="headerlink" title="TypeScript 配置必须与真实运行时一致"></a>TypeScript 配置必须与真实运行时一致</h2><p>最隐蔽的情况是 IDE 和 <code>tsc</code> 都通过，Node 执行 dist 失败。比如 TypeScript 按 bundler 方式允许省略扩展名，产物却直接交给 Node ESM：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; parse &#125; <span class="keyword">from</span> <span class="string">&#x27;./parse&#x27;</span></span><br></pre></td></tr></table></figure><p>编译后仍是无扩展导入，Node ESM 找不到。对于直接由 Node 执行的项目，使用与 Node ESM 匹配的 <code>module</code>&#x2F;<code>moduleResolution</code>（如项目版本支持的 NodeNext 组合），并在源码中写输出时有效的路径：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; parse &#125; <span class="keyword">from</span> <span class="string">&#x27;./parse.js&#x27;</span></span><br></pre></td></tr></table></figure><p>TypeScript 能把它解析到 <code>parse.ts</code>，输出给 Node 时保留 <code>.js</code>。具体选项应与所用 TS&#x2F;Node 版本核对，不能从前端 bundler 项目复制 tsconfig。</p><h2 id="测试环境正常、生产失败怎么办"><a href="#测试环境正常、生产失败怎么办" class="headerlink" title="测试环境正常、生产失败怎么办"></a>测试环境正常、生产失败怎么办</h2><p>Jest&#x2F;Vitest&#x2F;tsx 等工具可能转换模块或提供自定义解析，使源码中的混用暂时可行；生产 Node 直接运行 dist 时就失败。因此我们的最小验收始终包含：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">npm pack</span><br><span class="line"><span class="built_in">mkdir</span> /tmp/package-consumer</span><br><span class="line"><span class="comment"># 在临时 consumer 中安装 tarball</span></span><br><span class="line"><span class="comment"># 分别用 import 与 require（如果声称支持）加载公开入口</span></span><br><span class="line">node dist/cli.js --<span class="built_in">help</span></span><br></pre></td></tr></table></figure><p>测试发布 tarball 能发现 package files 漏发、exports 错路径和 types 指向源码等问题。</p><h2 id="一张实用故障分流表"><a href="#一张实用故障分流表" class="headerlink" title="一张实用故障分流表"></a>一张实用故障分流表</h2><table><thead><tr><th>错误</th><th>先检查</th></tr></thead><tbody><tr><td>ERR_REQUIRE_ESM</td><td>CJS 是否 require 纯 ESM；能否动态 import</td></tr><tr><td>require is not defined</td><td>文件是否进入 ESM package scope</td></tr><tr><td>Cannot use import statement outside a module</td><td>文件是否被当成 CJS；转换是否未执行</td></tr><tr><td>ERR_MODULE_NOT_FOUND</td><td>ESM 相对扩展名、大小写、exports</td></tr><tr><td>ERR_PACKAGE_PATH_NOT_EXPORTED</td><td>是否深层导入未公开文件</td></tr><tr><td>default 为 undefined&#x2F;多一层</td><td>最终 CJS&#x2F;ESM 导出形状与 interop 配置</td></tr></tbody></table><h2 id="结论不是“全都改-ESM”"><a href="#结论不是“全都改-ESM”" class="headerlink" title="结论不是“全都改 ESM”"></a>结论不是“全都改 ESM”</h2><p>新项目统一 ESM 通常更清楚，遗留仓库也可以分包迁移；但强行在一次 PR 改所有脚本、测试与构建配置，会放大风险。关键是边界明确：<code>.cjs/.mjs/type</code> 一致、exports 是唯一公开入口、tsconfig 符合真实运行方式、最终产物有消费测试。</p><p>那次 ERR_REQUIRE_ESM 最终只改了一个 loader 和上层 async 调用，却顺带补上了 package 测试。之后再看到模块错误，我们不再试十组 compiler flags，而是从报错文件的实际格式开始，一层层核对解析契约。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://nodejs.org/api/esm.html">Node.js：ECMAScript modules</a></li><li><a href="https://nodejs.org/api/packages.html">Node.js：Packages</a></li><li><a href="https://www.typescriptlang.org/docs/handbook/modules/reference.html">TypeScript：Modules Reference</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端排障 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Node.js </tag>
            
            <tag> 前端排障 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Next.js 13.4：App Router 稳定后的缓存与边界课题</title>
      <link href="/2023/05/04/frontend-chronicle-2023-05-nextjs-app-router-stable/"/>
      <url>/2023/05/04/frontend-chronicle-2023-05-nextjs-app-router-stable/</url>
      
        <content type="html"><![CDATA[<p>Next.js 13.4 把 App Router 标记为 stable 后，我们才把它放进正式生产路线。stable 的含义不是“所有生态问题已经消失”，而是核心路由能力可以开始承担生产流量。Server Actions 当时仍是 alpha，所以我们没有把试验性写入接口作为迁移前提。</p><p>第一个生产路由是商品分类页。它有 SEO、分页、筛选和 CMS 推荐位，读多写少，适合发挥 Server Component 与 Streaming；购物车和结算仍留在 Pages Router。渐进迁移让团队可以观察真实缓存和运行成本，而不是一次性把所有风险叠起来。</p><h2 id="先定义验收指标，再搬目录"><a href="#先定义验收指标，再搬目录" class="headerlink" title="先定义验收指标，再搬目录"></a>先定义验收指标，再搬目录</h2><p>迁移前保存基线：</p><ul><li>首屏 HTML 是否包含核心商品内容；</li><li>页面客户端 JavaScript 体积；</li><li>TTFB、LCP 与交互响应；</li><li>CMS 更新到线上可见的最长时间；</li><li>登录价格、地区库存是否可能被公共缓存；</li><li>404、慢接口、部分接口失败时的页面表现。</li></ul><p>如果只以“URL 能打开”为完成标准，最容易漏掉的是数据新鲜度和跨用户缓存。</p><h2 id="默认服务器组件，交互从叶子开始"><a href="#默认服务器组件，交互从叶子开始" class="headerlink" title="默认服务器组件，交互从叶子开始"></a>默认服务器组件，交互从叶子开始</h2><p>分类页在服务器读取数据：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// app/category/[slug]/page.tsx</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">CategoryPage</span>(<span class="params">&#123; params, searchParams &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> filters = <span class="title function_">parseFilters</span>(searchParams)</span><br><span class="line">  <span class="keyword">const</span> category = <span class="keyword">await</span> <span class="title function_">getCategory</span>(params.<span class="property">slug</span>)</span><br><span class="line">  <span class="keyword">const</span> products = <span class="keyword">await</span> <span class="title function_">getProducts</span>(&#123; <span class="attr">category</span>: params.<span class="property">slug</span>, ...filters &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">CategoryLayout</span> <span class="attr">category</span>=<span class="string">&#123;category&#125;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">FilterBar</span> <span class="attr">initialFilters</span>=<span class="string">&#123;filters&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">ProductGrid</span> <span class="attr">products</span>=<span class="string">&#123;products&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">CategoryLayout</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>筛选控件需要读取 URL、处理点击，成为 Client Component；商品卡片如果只是链接与图片，可以继续留在服务器输出。我们用构建分析检查客户端边界，避免一个顶层 <code>use client</code> 把整个商品树带进 bundle。</p><h2 id="缓存不是一个开关，而是几段生命周期"><a href="#缓存不是一个开关，而是几段生命周期" class="headerlink" title="缓存不是一个开关，而是几段生命周期"></a>缓存不是一个开关，而是几段生命周期</h2><p>初学 App Router 时，很容易把“fetch 缓存”“请求内去重”“路由导航缓存”混成同一件事。排查旧数据要先问它存在哪一层、由谁失效。</p><p>同一次服务器渲染中，多处读取同一数据可能被记忆化&#x2F;复用；跨请求的数据缓存可以保留结果；客户端导航还可能保留已访问的路由数据。不同 Next 版本的默认行为会变化，因此我们不背一句固定结论，而是把每个数据源的需求写进封装。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">getCategory</span>(<span class="params"><span class="attr">slug</span>: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">`<span class="subst">$&#123;CMS_URL&#125;</span>/categories/<span class="subst">$&#123;slug&#125;</span>`</span>, &#123;</span><br><span class="line">    <span class="attr">next</span>: &#123;</span><br><span class="line">      <span class="attr">revalidate</span>: <span class="number">300</span>,</span><br><span class="line">      <span class="attr">tags</span>: [<span class="string">`category:<span class="subst">$&#123;slug&#125;</span>`</span>]</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!response.<span class="property">ok</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`CMS <span class="subst">$&#123;response.status&#125;</span>`</span>)</span><br><span class="line">  <span class="keyword">return</span> response.<span class="title function_">json</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>CMS webhook 在发布后触发对应 tag 失效。带登录用户价格的请求则不进入共享公共缓存，用户身份也不作为隐式全局变量在缓存函数中读取。</p><h2 id="缓存故障不会报错，所以要写行为测试"><a href="#缓存故障不会报错，所以要写行为测试" class="headerlink" title="缓存故障不会报错，所以要写行为测试"></a>缓存故障不会报错，所以要写行为测试</h2><p>一次测试环境事故中，运营修改分类标题，接口返回新值，页面仍显示旧标题。没有 500，也没有控制台错误。最后发现 webhook 失效了文章 tag，没有失效分类 tag。</p><p>我们补了一条端到端流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">发布分类标题 V1 -&gt; 首次访问看到 V1</span><br><span class="line">CMS 修改为 V2 -&gt; 触发 webhook</span><br><span class="line">新会话访问 -&gt; 在 SLA 时间内看到 V2</span><br><span class="line">已有客户端导航返回 -&gt; 不得永久停留 V1</span><br></pre></td></tr></table></figure><p>缓存正确性必须从写入&#x2F;发布一路测试到用户可见结果。单测 <code>revalidateTag()</code> 被调用，只证明函数执行了，不能证明所有缓存层都更新。</p><h2 id="error-loading-边界按业务可恢复范围划分"><a href="#error-loading-边界按业务可恢复范围划分" class="headerlink" title="error&#x2F;loading 边界按业务可恢复范围划分"></a>error&#x2F;loading 边界按业务可恢复范围划分</h2><p>商品列表失败可以显示重试，分类不存在应该走 notFound，推荐位失败可以直接隐藏。我们没有让根 <code>error.tsx</code> 接住所有故障。</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;use client&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">function</span> <span class="title function_">CategoryError</span>(<span class="params">&#123; reset &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">section</span> <span class="attr">role</span>=<span class="string">&quot;alert&quot;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">h2</span>&gt;</span>商品列表暂时加载失败<span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;()</span> =&gt;</span> reset()&#125;&gt;重新加载<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">section</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>错误边界提供恢复 UI，服务器日志仍记录 trace ID。给用户显示一个可联系的错误编号，比直接输出数据库堆栈安全。</p><h2 id="Server-Actions-alpha-没有被用来赶进度"><a href="#Server-Actions-alpha-没有被用来赶进度" class="headerlink" title="Server Actions alpha 没有被用来赶进度"></a>Server Actions alpha 没有被用来赶进度</h2><p>13.4 同时提供 Server Actions alpha。它能把 mutation 与缓存失效放在同一服务端函数中，方向很吸引人：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;use server&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">renameCategory</span>(<span class="params"><span class="attr">formData</span>: <span class="title class_">FormData</span></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> user = <span class="keyword">await</span> <span class="title function_">requireAdmin</span>()</span><br><span class="line">  <span class="keyword">const</span> input = <span class="title function_">parseRenameInput</span>(formData)</span><br><span class="line">  <span class="keyword">await</span> categoryService.<span class="title function_">rename</span>(user, input)</span><br><span class="line">  <span class="title function_">revalidateTag</span>(<span class="string">`category:<span class="subst">$&#123;input.slug&#125;</span>`</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但当时 alpha 意味着 API 和部署要求仍可能变化。核心商品页继续使用成熟 API 路由，内部工具才做受控实验。技术选型里“晚一点用”不等于拒绝创新，而是把试验风险放在合适业务范围。</p><p>无论 Server Action 还是 API 路由，服务端入口都要鉴权、校验、限制频率并记录审计。函数写在组件旁边不会让浏览器输入变可信。</p><h2 id="第三方组件经常决定-use-client-的位置"><a href="#第三方组件经常决定-use-client-的位置" class="headerlink" title="第三方组件经常决定 use client 的位置"></a>第三方组件经常决定 use client 的位置</h2><p>一些旧组件在模块顶层读取 <code>window</code>，导入服务器组件就报错。短期可以包客户端适配层：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;use client&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="title class_">LegacyCarousel</span> <span class="keyword">from</span> <span class="string">&#x27;legacy-carousel&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">CarouselClient</span>(<span class="params">props</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">LegacyCarousel</span> &#123;<span class="attr">...props</span>&#125; /&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但如果一个轮播库让半页变客户端，应该评估替换。迁移清单不能只有代码目录，还要包含组件库对 RSC 的兼容程度。</p><h2 id="生产后我们看到了什么"><a href="#生产后我们看到了什么" class="headerlink" title="生产后我们看到了什么"></a>生产后我们看到了什么</h2><p>核心内容进入首屏 HTML，分类页客户端 JS 下降；慢 CMS 时 Streaming 让布局和骨架更早出现。与此同时，团队花了更多时间理解缓存与服务端日志，部署平台的 CPU&#x2F;响应时间也进入前端指标。</p><p>Pages Router 并没有立即删除。支付、复杂编辑器和依赖旧 context 的页面继续稳定运行，后续只在需求触及它们时迁移。官方也明确支持渐进采用，这让迁移不必成为一场停摆式重写。</p><h2 id="stable-是开始承担责任，不是停止学习"><a href="#stable-是开始承担责任，不是停止学习" class="headerlink" title="stable 是开始承担责任，不是停止学习"></a>stable 是开始承担责任，不是停止学习</h2><p>Next 13.4 的 App Router stable 给了生产采用信号，但新架构把更多事情交给框架后，团队必须理解服务器&#x2F;客户端模块、缓存新鲜度、序列化和运行时。配置少了，不等于决策少了。</p><p>我们最终把每条路由当成一个可独立验收的迁移单元：测 HTML、JS、数据更新、错误恢复和平台成本。这样即便后续版本调整默认缓存行为，业务需求仍然有明确测试保护。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://nextjs.org/blog/next-13-4">Next.js 13.4 发布说明</a></li><li><a href="https://nextjs.org/docs/app/guides/caching">Next.js：Caching</a></li><li><a href="https://nextjs.org/docs/app/guides/migrating/app-router-migration">Next.js：Incremental Adoption</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Next.js </tag>
            
            <tag> 2023 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vite 4：共享工具链生态进入稳定扩张期</title>
      <link href="/2022/12/09/frontend-chronicle-2022-12-vite-4-ecosystem/"/>
      <url>/2022/12/09/frontend-chronicle-2022-12-vite-4-ecosystem/</url>
      
        <content type="html"><![CDATA[<p>Vite 4 发布时，项目本身的变更不算惊天动地：生产构建升级到 Rollup 3，继续清理与演进。但在我们的 monorepo 里，Vite 已经不只是启动前端的命令。应用用 Vite，单元测试用 Vitest，组件文档和一个静态文档站也在复用相近的模块解析。</p><p>工具链开始共享以后，最大的收益不是少写几份配置，而是 alias、环境和插件行为终于能保持一致；最大的风险也同样明显：一个 Vite 插件升级可能同时影响开发、测试和文档。</p><h2 id="以前三套工具有三份-alias"><a href="#以前三套工具有三份-alias" class="headerlink" title="以前三套工具有三份 alias"></a>以前三套工具有三份 alias</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">vite.config.ts       @ -&gt; src</span><br><span class="line">vitest.config.ts     @ -&gt; src（偶尔漏改）</span><br><span class="line">tsconfig.json        @/* -&gt; src/*</span><br></pre></td></tr></table></figure><p>某次目录从 <code>src/services</code> 改为 <code>src/api</code>，应用能运行，测试在 CI 才找不到模块。我们把共享解析提取成普通配置模块：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// tooling/aliases.ts</span></span><br><span class="line"><span class="keyword">import</span> &#123; fileURLToPath, <span class="variable constant_">URL</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;node:url&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> aliases = &#123;</span><br><span class="line">  <span class="string">&#x27;@&#x27;</span>: <span class="title function_">fileURLToPath</span>(<span class="keyword">new</span> <span class="title function_">URL</span>(<span class="string">&#x27;../apps/admin/src&#x27;</span>, <span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">url</span>)),</span><br><span class="line">  <span class="string">&#x27;@shared&#x27;</span>: <span class="title function_">fileURLToPath</span>(<span class="keyword">new</span> <span class="title function_">URL</span>(<span class="string">&#x27;../packages/shared/src&#x27;</span>, <span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">url</span>))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// vite.config.ts</span></span><br><span class="line"><span class="keyword">import</span> &#123; defineConfig &#125; <span class="keyword">from</span> <span class="string">&#x27;vite&#x27;</span></span><br><span class="line"><span class="keyword">import</span> vue <span class="keyword">from</span> <span class="string">&#x27;@vitejs/plugin-vue&#x27;</span></span><br><span class="line"><span class="keyword">import</span> &#123; aliases &#125; <span class="keyword">from</span> <span class="string">&#x27;./tooling/aliases&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">plugins</span>: [<span class="title function_">vue</span>()],</span><br><span class="line">  <span class="attr">resolve</span>: &#123; <span class="attr">alias</span>: aliases &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>Vitest 可以在同一配置体系上合并测试选项。TypeScript paths 仍要同步，必要时用工具生成或做一致性测试，避免“双重真相”。</p><h2 id="测试复用转换能力，但环境并不等于浏览器"><a href="#测试复用转换能力，但环境并不等于浏览器" class="headerlink" title="测试复用转换能力，但环境并不等于浏览器"></a>测试复用转换能力，但环境并不等于浏览器</h2><p>Vitest 复用 Vite 的模块图和插件，Vue&#x2F;React、TS、alias 的体验更一致。可这不代表测试自动运行在真实浏览器。jsdom&#x2F;happy-dom 对布局、导航、Observer 等 API 的实现与浏览器有差异。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; describe, expect, it &#125; <span class="keyword">from</span> <span class="string">&#x27;vitest&#x27;</span></span><br><span class="line"><span class="keyword">import</span> &#123; normalizePrice &#125; <span class="keyword">from</span> <span class="string">&#x27;@/domain/price&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="title function_">describe</span>(<span class="string">&#x27;normalizePrice&#x27;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="title function_">it</span>(<span class="string">&#x27;keeps zero as a valid value&#x27;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">expect</span>(<span class="title function_">normalizePrice</span>(<span class="number">0</span>)).<span class="title function_">toBe</span>(<span class="number">0</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>纯逻辑适合快速单元测试；拖拽、焦点、真实渲染和跨页路由仍由 Playwright 等浏览器 E2E 覆盖。统一工具链不意味着只保留一种测试层级。</p><h2 id="升级-Rollup-3，最先检查的是插件而不是业务代码"><a href="#升级-Rollup-3，最先检查的是插件而不是业务代码" class="headerlink" title="升级 Rollup 3，最先检查的是插件而不是业务代码"></a>升级 Rollup 3，最先检查的是插件而不是业务代码</h2><p>Vite 4 生产构建基于 Rollup 3。大多数标准项目升级平稳，自定义插件更需要关注：是否使用废弃钩子、假设输出对象结构、读取内部路径或依赖特定 chunk 顺序。</p><p>我们有个生成 asset manifest 的插件：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">assetManifestPlugin</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;asset-manifest&#x27;</span>,</span><br><span class="line">    <span class="title function_">generateBundle</span>(<span class="params">_options, bundle</span>) &#123;</span><br><span class="line">      <span class="keyword">const</span> files = <span class="title class_">Object</span>.<span class="title function_">values</span>(bundle).<span class="title function_">map</span>(<span class="function"><span class="params">item</span> =&gt;</span> item.<span class="property">fileName</span>)</span><br><span class="line">      <span class="variable language_">this</span>.<span class="title function_">emitFile</span>(&#123;</span><br><span class="line">        <span class="attr">type</span>: <span class="string">&#x27;asset&#x27;</span>,</span><br><span class="line">        <span class="attr">fileName</span>: <span class="string">&#x27;asset-list.json&#x27;</span>,</span><br><span class="line">        <span class="attr">source</span>: <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(files, <span class="literal">null</span>, <span class="number">2</span>)</span><br><span class="line">      &#125;)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>测试不能只断言 build 成功，还要验证清单包含入口、动态 chunk 和 CSS，文件名&#x2F;顺序不被消费者错误依赖。插件接口看似很小，往往连接着部署系统。</p><h2 id="monorepo-源码包要明确边界"><a href="#monorepo-源码包要明确边界" class="headerlink" title="monorepo 源码包要明确边界"></a>monorepo 源码包要明确边界</h2><p>开发时直接 alias 到 <code>packages/shared/src</code> 很方便，HMR 也快；发布时却可能绕过包的 exports，业务用到消费者本不该访问的内部文件。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;@acme/shared&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;exports&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;.&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/index.js&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;./date&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/date.js&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>我们让应用日常通过公开包入口导入，并在 CI 对打包后的 package 做消费测试。开发体验不能牺牲发布契约，否则换仓库或外部复用时会集中爆炸。</p><h2 id="Node-版本开始决定整个前端工作区能否启动"><a href="#Node-版本开始决定整个前端工作区能否启动" class="headerlink" title="Node 版本开始决定整个前端工作区能否启动"></a>Node 版本开始决定整个前端工作区能否启动</h2><p>Vite 大版本会跟随 Node 生命周期提高要求。monorepo 中只要一个脚本仍固定旧 Node，就可能出现应用能启动、文档或测试失败。我们把 Node 版本提升作为独立 PR：</p><ol><li>更新开发版本文件、CI 与 Docker；</li><li>全新安装依赖并检查 lockfile；</li><li>跑应用 build、Vitest、文档构建和组件测试；</li><li>再升级 Vite&#x2F;Rollup 相关依赖。</li></ol><p>把运行时与构建器分开，回归范围更容易判断。</p><h2 id="环境变量也需要区分测试与构建"><a href="#环境变量也需要区分测试与构建" class="headerlink" title="环境变量也需要区分测试与构建"></a>环境变量也需要区分测试与构建</h2><p>应用中的 <code>import.meta.env</code> 在测试里可能走不同 mode。我们不让测试隐式读取开发机 <code>.env.local</code>，而是在测试 setup 中提供最小明确值，并禁止生产 secret 进入客户端前缀。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// vitest.config.ts</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">test</span>: &#123;</span><br><span class="line">    <span class="attr">environment</span>: <span class="string">&#x27;jsdom&#x27;</span>,</span><br><span class="line">    <span class="attr">env</span>: &#123;</span><br><span class="line">      <span class="attr">VITE_API_BASE</span>: <span class="string">&#x27;https://api.test.invalid&#x27;</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>更好的业务设计是把 API client 的 base URL 作为依赖注入，减少全局环境变量散落。</p><h2 id="共享不等于把所有配置塞进一个文件"><a href="#共享不等于把所有配置塞进一个文件" class="headerlink" title="共享不等于把所有配置塞进一个文件"></a>共享不等于把所有配置塞进一个文件</h2><p>我们尝试过一个 500 行 <code>vite.shared.ts</code>，包含 Vue、React、SSR、library build 和测试选项，最终条件分支比原来更多。后来只共享稳定原语：alias、通用 define、少量自研插件；框架插件和 build 目标留在各项目。</p><p>共享配置的判断标准是“这些项目真的必须同步变化吗”，而不是“代码看起来重复”。两行重复比一个充满 mode 判断的通用工厂更容易维护。</p><h2 id="Vite-4-这一年的实际意义"><a href="#Vite-4-这一年的实际意义" class="headerlink" title="Vite 4 这一年的实际意义"></a>Vite 4 这一年的实际意义</h2><p>Vite 4 单独看像一次平稳大版本，放在生态里却能看到工具链平台已经形成：元框架、测试、文档、组件工具围绕相近插件能力协作。官方还用 vite-ecosystem-ci 在核心变更前验证下游项目，这种生态测试比承诺“完全兼容”更实际。</p><p>我们的收获也是双向的：开发&#x2F;测试解析一致，配置更少漂移；同时把关键插件加入构建产物测试，不再把 dev server 能启动当作升级完成。共享基础设施越多，越需要真实下游验证和明确责任边界。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://vite.dev/blog/announcing-vite4.html">Vite 4.0 发布说明</a></li><li><a href="https://vitest.dev/">Vitest 官方文档</a></li><li><a href="https://vite.dev/guide/performance">Vite：Performance</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Vite </tag>
            
            <tag> 2022 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Next.js 13：App Router 与 Server Components 落地</title>
      <link href="/2022/10/25/frontend-chronicle-2022-10-nextjs-13-app-router/"/>
      <url>/2022/10/25/frontend-chronicle-2022-10-nextjs-13-app-router/</url>
      
        <content type="html"><![CDATA[<p>Next.js 13 发布 <code>app</code> 目录时，它还是 beta。我们没有把现有 Pages Router 主站整体搬过去，而是选了一个只读的“帮助中心”做实验：有分类布局、文章详情、搜索框和登录用户的收藏按钮。它刚好能覆盖布局、服务器数据、流式 loading 和一小块客户端交互。</p><p>这次试迁最大的认知变化，是组件不再默认等于客户端 JavaScript。页面与 layout 默认可以是 Server Component，只有需要 state、事件或浏览器 API 的叶子才声明 <code>use client</code>。</p><h2 id="文件结构先表达页面边界"><a href="#文件结构先表达页面边界" class="headerlink" title="文件结构先表达页面边界"></a>文件结构先表达页面边界</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">app/</span><br><span class="line">  help/</span><br><span class="line">    layout.tsx</span><br><span class="line">    loading.tsx</span><br><span class="line">    error.tsx</span><br><span class="line">    page.tsx</span><br><span class="line">    [slug]/</span><br><span class="line">      page.tsx</span><br><span class="line">      FavoriteButton.tsx</span><br></pre></td></tr></table></figure><p>共享布局可以读取分类并包裹子页面：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// app/help/layout.tsx</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">HelpLayout</span>(<span class="params">&#123; children &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> categories = <span class="keyword">await</span> <span class="title function_">getHelpCategories</span>()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">div</span> <span class="attr">className</span>=<span class="string">&quot;help-layout&quot;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">HelpSidebar</span> <span class="attr">categories</span>=<span class="string">&#123;categories&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">main</span>&gt;</span>&#123;children&#125;<span class="tag">&lt;/<span class="name">main</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>布局在路由间保持，避免 Pages Router 中用 <code>_app</code> 和条件判断拼不同壳。嵌套目录直接对应 UI 层级，代价是团队必须理解每个 segment 的 loading&#x2F;error&#x2F;not-found 范围。</p><h2 id="数据读取直接发生在服务器页面"><a href="#数据读取直接发生在服务器页面" class="headerlink" title="数据读取直接发生在服务器页面"></a>数据读取直接发生在服务器页面</h2><p>文章页可以是 async 组件：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// app/help/[slug]/page.tsx</span></span><br><span class="line"><span class="keyword">import</span> &#123; notFound &#125; <span class="keyword">from</span> <span class="string">&#x27;next/navigation&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">HelpArticle</span>(<span class="params">&#123; params &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> article = <span class="keyword">await</span> <span class="title function_">getArticle</span>(params.<span class="property">slug</span>)</span><br><span class="line">  <span class="keyword">if</span> (!article) <span class="title function_">notFound</span>()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">article</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">h1</span>&gt;</span>&#123;article.title&#125;<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">ArticleBody</span> <span class="attr">html</span>=<span class="string">&#123;article.html&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">FavoriteButton</span> <span class="attr">articleId</span>=<span class="string">&#123;article.id&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">article</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>数据库客户端和 Markdown 解析器可以留在服务器模块里，不进入浏览器包。传给客户端组件的 props 则要可序列化，并且只包含它真正需要的数据。不要把完整数据库对象连内部字段一起传下去。</p><h2 id="use-client-放高一层，客户端边界就跟着扩大"><a href="#use-client-放高一层，客户端边界就跟着扩大" class="headerlink" title="use client 放高一层，客户端边界就跟着扩大"></a><code>use client</code> 放高一层，客户端边界就跟着扩大</h2><p>收藏按钮需要 state 和 click：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;use client&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> &#123; useState &#125; <span class="keyword">from</span> <span class="string">&#x27;react&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">FavoriteButton</span>(<span class="params">&#123; articleId &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [saved, setSaved] = <span class="title function_">useState</span>(<span class="literal">false</span>)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">toggle</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> next = !saved</span><br><span class="line">    <span class="title function_">setSaved</span>(next)</span><br><span class="line">    <span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">`/api/favorites/<span class="subst">$&#123;articleId&#125;</span>`</span>, &#123;</span><br><span class="line">      <span class="attr">method</span>: next ? <span class="string">&#x27;PUT&#x27;</span> : <span class="string">&#x27;DELETE&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;toggle&#125;</span>&gt;</span>&#123;saved ? &#x27;已收藏&#x27; : &#x27;收藏&#x27;&#125;<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果为了这个按钮在 <code>layout.tsx</code> 顶部写 <code>use client</code>，布局导入的导航、分类和大部分子树都会进入客户端边界。我们把交互组件尽量下沉，把服务器渲染的内容作为 children 组合，而不是把整页变回 SPA。</p><p>客户端组件仍可以接收 Server Component 输出作为 children；边界不是“客户端组件下面全都必须客户端”，关键看模块导入关系与框架组合方式。</p><h2 id="loading-tsx-不是全局-Spinner"><a href="#loading-tsx-不是全局-Spinner" class="headerlink" title="loading.tsx 不是全局 Spinner"></a>loading.tsx 不是全局 Spinner</h2><p>在 help segment 放 <code>loading.tsx</code> 后，框架会用 Suspense&#x2F;Streaming 在内容准备期间提供 fallback：</p><figure class="highlight tsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">function</span> <span class="title function_">LoadingHelp</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">div</span> <span class="attr">aria-busy</span>=<span class="string">&quot;true&quot;</span> <span class="attr">className</span>=<span class="string">&quot;article-skeleton&quot;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">div</span> <span class="attr">className</span>=<span class="string">&quot;line title&quot;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">div</span> <span class="attr">className</span>=<span class="string">&quot;line&quot;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">div</span> <span class="attr">className</span>=<span class="string">&quot;line&quot;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>骨架应该尽量保持最终布局，避免大幅位移。嵌套边界还能让侧栏先显示、文章内容稍后流式到达。我们用慢接口测试，不只在本地秒开环境看一眼。</p><p><code>error.tsx</code> 必须是 Client Component，因为它需要错误交互与 reset。它负责当前路由段的恢复，但日志上报仍要脱敏，不能把服务器错误细节直接展示给用户。</p><h2 id="缓存比目录结构更容易让人误判"><a href="#缓存比目录结构更容易让人误判" class="headerlink" title="缓存比目录结构更容易让人误判"></a>缓存比目录结构更容易让人误判</h2><p>Next 13 初期的数据缓存语义仍在演进。我们没有把“默认行为”当永恒规则，而是为每类数据写需求：帮助文章可缓存多久，登录收藏必须多新，预览草稿绝不能被公共缓存。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">getArticle</span>(<span class="params"><span class="attr">slug</span>: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">`<span class="subst">$&#123;CMS_URL&#125;</span>/articles/<span class="subst">$&#123;slug&#125;</span>`</span>, &#123;</span><br><span class="line">    <span class="attr">next</span>: &#123; <span class="attr">revalidate</span>: <span class="number">300</span> &#125;</span><br><span class="line">  &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (response.<span class="property">status</span> === <span class="number">404</span>) <span class="keyword">return</span> <span class="literal">null</span></span><br><span class="line">  <span class="keyword">if</span> (!response.<span class="property">ok</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`CMS <span class="subst">$&#123;response.status&#125;</span>`</span>)</span><br><span class="line">  <span class="keyword">return</span> response.<span class="title function_">json</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>代码示例只代表当时可用的思路，框架缓存默认值在后续大版本继续调整。真正稳妥的是把新鲜度当业务需求测试：CMS 修改文章后，多少时间&#x2F;哪个失效动作必须让用户看到新内容。</p><h2 id="搜索页暴露了-URL-与客户端状态的选择"><a href="#搜索页暴露了-URL-与客户端状态的选择" class="headerlink" title="搜索页暴露了 URL 与客户端状态的选择"></a>搜索页暴露了 URL 与客户端状态的选择</h2><p>搜索关键字应该出现在 URL，便于分享和前进后退。页面服务器读取 search params，交互输入在客户端更新路由。我们避免把搜索结果同时保存到全局 store，减少两份来源。</p><p>如果输入每次按键都导航，请求会很多；客户端做轻量 debounce，并让旧请求&#x2F;导航失效。App Router 不会自动替产品决定防抖、空状态和错误恢复。</p><h2 id="Turbopack-alpha-只用来试，不作为生产承诺"><a href="#Turbopack-alpha-只用来试，不作为生产承诺" class="headerlink" title="Turbopack alpha 只用来试，不作为生产承诺"></a>Turbopack alpha 只用来试，不作为生产承诺</h2><p>Next 13 同时带来 Turbopack alpha。我们在实验分支测冷启动与 HMR，但项目有自定义 webpack 插件，功能并不完全对等，因此没有把 alpha 工具写进正式开发流程。</p><p>性能数字必须在自己的模块图、插件和机器上测；alpha 更重要的任务是发现不兼容并反馈，而不是为了发布会数字替换稳定链路。</p><h2 id="Pages-与-App-可以共存，这让迁移变得现实"><a href="#Pages-与-App-可以共存，这让迁移变得现实" class="headerlink" title="Pages 与 App 可以共存，这让迁移变得现实"></a>Pages 与 App 可以共存，这让迁移变得现实</h2><p>我们只迁 <code>/help</code>，登录、支付与后台仍在 <code>pages</code>。需要注意两个 router 之间不能共享所有布局状态，跨边界导航也要真实测试。但逐路由迁移让回滚简单：实验路由不稳时，不影响核心交易。</p><p>验收包含：</p><ul><li>直接访问和客户端跳转输出是否一致；</li><li>页面 JS 体积是否因 <code>use client</code> 放置不当增加；</li><li>慢 CMS 下 layout&#x2F;loading 顺序是否合理；</li><li>404&#x2F;error 边界是否工作；</li><li>登录收藏是否泄露跨用户缓存；</li><li>CMS 更新后的可见时间是否符合要求；</li><li>生产 Node&#x2F;边缘运行时是否支持依赖 API。</li></ul><h2 id="这次试迁的结论"><a href="#这次试迁的结论" class="headerlink" title="这次试迁的结论"></a>这次试迁的结论</h2><p>App Router 的价值不只是一套新目录。它把 Server Components、布局、Streaming 和数据生命周期放进路由模型，让“代码在哪执行、多少 JS 发给浏览器”成为组件级决策。</p><p>2022 年它仍处在 beta，我们选择小而真实的路由学习，而不是把实验性当成落后&#x2F;先进的标签。最终帮助中心保留了下来，也让团队为后续稳定版积累了缓存和边界经验。面对这种架构升级，最好的第一步往往不是迁首页，而是选一个能独立验收、失败可回滚、又包含真实交互的路由。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://nextjs.org/blog/next-13">Next.js 13 发布说明</a></li><li><a href="https://nextjs.org/docs/app/getting-started/server-and-client-components">Next.js：Server and Client Components</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Next.js </tag>
            
            <tag> 2022 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>跨域 Cookie 排查：CORS、SameSite 与 credentials</title>
      <link href="/2022/09/16/frontend-debug-cors-cookie-samesite/"/>
      <url>/2022/09/16/frontend-debug-cors-cookie-samesite/</url>
      
        <content type="html"><![CDATA[<p>这个问题的现场很典型：登录接口返回 200，Network 里能看到 <code>Set-Cookie</code>，下一次请求却仍然是 401。本地开发从未出现，测试环境一换成 <code>app.example.test</code> 调 <code>api.vendor.test</code> 就失败。</p><p>排查跨域 Cookie 最容易把 CORS、SameSite、Domain、Secure 和 fetch credentials 混成一团。我后来固定先问两个问题：Cookie 有没有被浏览器保存？已经保存的 Cookie 为什么没有随请求发送？这是两道不同的门。</p><h2 id="先画清-origin-与-site，不要只说“跨域”"><a href="#先画清-origin-与-site，不要只说“跨域”" class="headerlink" title="先画清 origin 与 site，不要只说“跨域”"></a>先画清 origin 与 site，不要只说“跨域”</h2><p>假设：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">页面：https://app.example.com</span><br><span class="line">接口：https://api.example.com</span><br></pre></td></tr></table></figure><p>它们 origin 不同（host 不同），所以 fetch 受 CORS 约束；但通常属于 same-site（scheme + registrable domain 相同），SameSite 判断与完全第三方站点又不同。</p><p>另一种：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">页面：https://app.example.com</span><br><span class="line">接口：https://login.vendor.com</span><br></pre></td></tr></table></figure><p>这是 cross-origin 且 cross-site，Cookie 的 SameSite 限制更严格，也会受到浏览器第三方 Cookie 策略影响。排障记录必须写完整 URL、scheme 和站点关系，“前后端跨域”信息太少。</p><h2 id="第一道门：响应里的-Cookie-有没有保存"><a href="#第一道门：响应里的-Cookie-有没有保存" class="headerlink" title="第一道门：响应里的 Cookie 有没有保存"></a>第一道门：响应里的 Cookie 有没有保存</h2><p>登录响应：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">Set-Cookie</span><span class="punctuation">: </span>session=abc...; Path=/; HttpOnly; Secure; SameSite=None</span><br></pre></td></tr></table></figure><p>在 DevTools Network 的 Cookies&#x2F;Issues 面板查看浏览器是否标注 blocked reason，再到 Application → Cookies 确认实际存储。常见失败原因：</p><ul><li><code>SameSite=None</code> 没有同时使用 <code>Secure</code>；</li><li>HTTPS 页面调用 HTTP 接口，或 Secure Cookie 在非 HTTPS 环境；</li><li><code>Domain</code> 与响应主机不匹配；</li><li>过期时间已过、值格式非法或浏览器策略拦截第三方 Cookie；</li><li>CORS 凭据配置不成立，响应不能被客户端接受。</li></ul><p>HttpOnly Cookie 不会出现在 <code>document.cookie</code>，这是安全属性的预期行为，不能用 JS 读不到来判断未保存。</p><h2 id="第二道门：保存以后为什么没有发送"><a href="#第二道门：保存以后为什么没有发送" class="headerlink" title="第二道门：保存以后为什么没有发送"></a>第二道门：保存以后为什么没有发送</h2><p>浏览器会根据请求 URL 的 Domain、Path、Secure、SameSite 和 credentials 再做一次判断。例如 Cookie 的 Path 是 <code>/auth</code>，请求 <code>/profile</code> 就不会带。</p><p>客户端跨 origin fetch 要明确包含凭据：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">&#x27;https://api.example.com/profile&#x27;</span>, &#123;</span><br><span class="line">  <span class="attr">credentials</span>: <span class="string">&#x27;include&#x27;</span>,</span><br><span class="line">  <span class="attr">headers</span>: &#123;</span><br><span class="line">    <span class="title class_">Accept</span>: <span class="string">&#x27;application/json&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>axios 等库有自己的对应选项，不能把 fetch 配置名原样套用。然后在 Network 的 Request Headers&#x2F;Cookies 查看实际请求，不要只打印代码中的 options。</p><h2 id="服务端-CORS-必须与凭据配套"><a href="#服务端-CORS-必须与凭据配套" class="headerlink" title="服务端 CORS 必须与凭据配套"></a>服务端 CORS 必须与凭据配套</h2><p>服务端响应需要明确允许页面 origin，并允许凭据：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> allowedOrigins = <span class="keyword">new</span> <span class="title class_">Set</span>([</span><br><span class="line">  <span class="string">&#x27;https://app.example.com&#x27;</span>,</span><br><span class="line">  <span class="string">&#x27;https://admin.example.com&#x27;</span></span><br><span class="line">])</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">cors</span>(<span class="params">req, res, next</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> origin = req.<span class="property">headers</span>.<span class="property">origin</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (allowedOrigins.<span class="title function_">has</span>(origin)) &#123;</span><br><span class="line">    res.<span class="title function_">setHeader</span>(<span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span>, origin)</span><br><span class="line">    res.<span class="title function_">setHeader</span>(<span class="string">&#x27;Access-Control-Allow-Credentials&#x27;</span>, <span class="string">&#x27;true&#x27;</span>)</span><br><span class="line">    res.<span class="title function_">setHeader</span>(<span class="string">&#x27;Vary&#x27;</span>, <span class="string">&#x27;Origin&#x27;</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">next</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>带凭据请求不能使用 <code>Access-Control-Allow-Origin: *</code>。动态回显 origin 也不能无条件照抄请求头，否则任意恶意站点都可能被允许；必须先匹配严格 allowlist。</p><p><code>Vary: Origin</code> 很重要：如果 CDN 缓存响应，而缓存 key 没区分 Origin，可能把给 A 站的允许头返回给 B 站，产生随机 CORS 故障或安全问题。</p><h2 id="预检通过，实际响应仍可能失败"><a href="#预检通过，实际响应仍可能失败" class="headerlink" title="预检通过，实际响应仍可能失败"></a>预检通过，实际响应仍可能失败</h2><p>自定义 header、非简单方法或内容类型会触发 OPTIONS 预检：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">OPTIONS</span> <span class="string">/profile</span> <span class="meta">HTTP/1.1</span></span><br><span class="line"><span class="attribute">Origin</span><span class="punctuation">: </span>https://app.example.com</span><br><span class="line"><span class="attribute">Access-Control-Request-Method</span><span class="punctuation">: </span>PUT</span><br><span class="line"><span class="attribute">Access-Control-Request-Headers</span><span class="punctuation">: </span>content-type,x-csrf-token</span><br></pre></td></tr></table></figure><p>服务端要返回允许的方法和 header：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">Access-Control-Allow-Origin</span><span class="punctuation">: </span>https://app.example.com</span><br><span class="line"><span class="attribute">Access-Control-Allow-Credentials</span><span class="punctuation">: </span>true</span><br><span class="line"><span class="attribute">Access-Control-Allow-Methods</span><span class="punctuation">: </span>GET,POST,PUT</span><br><span class="line"><span class="attribute">Access-Control-Allow-Headers</span><span class="punctuation">: </span>Content-Type,X-CSRF-Token</span><br></pre></td></tr></table></figure><p>只给 OPTIONS 配好 CORS 还不够，实际 PUT 响应也要包含对应 allow-origin&#x2F;credentials。我们遇到过网关接管 OPTIONS，业务服务的 500 响应却没有 CORS 头，浏览器最终只显示 CORS error，掩盖真正的服务端异常。</p><p>排查时同时看预检和实际请求；必要时在服务器日志用 request ID 找原始状态。</p><h2 id="Domain-的常见误区"><a href="#Domain-的常见误区" class="headerlink" title="Domain 的常见误区"></a>Domain 的常见误区</h2><p>不写 Domain 时会形成 host-only Cookie，只发送给设置它的精确主机，通常更安全。如果要在子域共享，可以设置合适的 Domain，但不能设置成无关域，也不应为了“省配置”放大范围。</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># 仅 api.example.com</span><br><span class="line"><span class="attribute">Set-Cookie</span><span class="punctuation">: </span>session=...; Path=/; Secure; HttpOnly; SameSite=Lax</span><br><span class="line"></span><br><span class="line"># 可能覆盖 example.com 的多个子域</span><br><span class="line"><span class="attribute">Set-Cookie</span><span class="punctuation">: </span>session=...; Domain=example.com; Path=/; Secure; HttpOnly; SameSite=Lax</span><br></pre></td></tr></table></figure><p>Cookie 前导点的历史写法不应作为判断重点，浏览器按规范处理域匹配。真正要确认的是谁设置、哪些主机需要发送、是否有同名 Cookie 在不同 Domain&#x2F;Path 下互相干扰。</p><p>同名 Cookie 存在两份时，服务端解析库可能取到意外的一份。Application 面板按 Domain 和 Path 全部展开，比只看键名可靠。</p><h2 id="SameSite-不是-CORS-的替代品"><a href="#SameSite-不是-CORS-的替代品" class="headerlink" title="SameSite 不是 CORS 的替代品"></a>SameSite 不是 CORS 的替代品</h2><p>SameSite 控制 Cookie 在跨站上下文中的发送，CORS 控制页面脚本能否读取跨 origin 响应。它们解决不同问题。</p><ul><li><code>Strict</code>：跨站导航也严格限制，安全强但可能影响外部链接后的登录态；</li><li><code>Lax</code>：适合很多第一方会话，是常见默认策略；</li><li><code>None</code>：允许跨站发送，必须配 Secure，并要面对第三方 Cookie 政策。</li></ul><p>如果应用完全依赖 third-party Cookie，不能只确认 <code>SameSite=None</code> 就认为长期可用。浏览器隐私策略会限制它，架构上优先考虑同站反向代理、顶级导航授权或标准身份协议，而不是要求用户关闭保护。</p><h2 id="Cookie-登录还需要-CSRF-防护"><a href="#Cookie-登录还需要-CSRF-防护" class="headerlink" title="Cookie 登录还需要 CSRF 防护"></a>Cookie 登录还需要 CSRF 防护</h2><p><code>credentials: include</code> 与宽松跨站 Cookie 让浏览器自动带会话，也意味着跨站请求伪造风险。CORS 不是完整 CSRF 防护，因为表单等某些请求并不依赖脚本读取响应。</p><p>常见组合是 SameSite、CSRF token、Origin&#x2F;Referer 校验与敏感操作二次确认。下面只是 token 形态示意：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">&#x27;https://api.example.com/profile&#x27;</span>, &#123;</span><br><span class="line">  <span class="attr">method</span>: <span class="string">&#x27;PUT&#x27;</span>,</span><br><span class="line">  <span class="attr">credentials</span>: <span class="string">&#x27;include&#x27;</span>,</span><br><span class="line">  <span class="attr">headers</span>: &#123;</span><br><span class="line">    <span class="string">&#x27;Content-Type&#x27;</span>: <span class="string">&#x27;application/json&#x27;</span>,</span><br><span class="line">    <span class="string">&#x27;X-CSRF-Token&#x27;</span>: csrfToken</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">body</span>: <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(profile)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>token 的生成、绑定和验证要由服务端安全设计，不是多加一个 header 就完成。</p><h2 id="为什么本地代理会把问题全部藏起来"><a href="#为什么本地代理会把问题全部藏起来" class="headerlink" title="为什么本地代理会把问题全部藏起来"></a>为什么本地代理会把问题全部藏起来</h2><p>Vite&#x2F;webpack dev proxy 让浏览器请求 <code>/api</code>，看起来与页面同源：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">server</span>: &#123;</span><br><span class="line">  <span class="attr">proxy</span>: &#123;</span><br><span class="line">    <span class="string">&#x27;/api&#x27;</span>: <span class="string">&#x27;https://api.example.com&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>浏览器看不到代理后面的跨 origin，自然不会触发生产 CORS 场景。开发体验可以保留代理，但测试环境至少要有一条使用真实 HTTPS 域名和接近生产站点关系的端到端用例。</p><p>不要用 <code>--disable-web-security</code> 作为验证，它只把问题从测试机隐藏，上线用户仍会失败。</p><h2 id="当时的根因与修复"><a href="#当时的根因与修复" class="headerlink" title="当时的根因与修复"></a>当时的根因与修复</h2><p>事故中有两个根因叠加：前端 fetch 没有 <code>credentials: &#39;include&#39;</code>；网关使用 <code>Access-Control-Allow-Origin: *</code>。Cookie 的 SameSite 配置本身没错，但请求根本没有通过凭据型 CORS 的完整链路。</p><p>修复后我们做了自动化场景：首次登录保存 Cookie、刷新仍登录、会话过期、从不同允许 origin 访问、恶意 origin 被拒绝、预检和实际响应都带正确 Vary&#x2F;CORS 头。这样以后改网关不会只测一个 200。</p><h2 id="我的排查顺序"><a href="#我的排查顺序" class="headerlink" title="我的排查顺序"></a>我的排查顺序</h2><ol><li>写完整页面&#x2F;接口 URL，判断 cross-origin 与 cross-site；</li><li>看响应 Cookie blocked reason，确认是否真正保存；</li><li>看 Application 中 Domain&#x2F;Path&#x2F;SameSite&#x2F;Secure 与同名 Cookie；</li><li>看下一次请求是否携带，检查前端 credentials；</li><li>分别检查 OPTIONS 和实际响应的 CORS 头；</li><li>检查 CDN 的 Vary&#x2F;缓存以及网关是否吞掉错误响应头；</li><li>最后再评估第三方 Cookie 政策、CSRF 和身份架构。</li></ol><p>把“保存”与“发送”分开后，这类问题通常不再玄学。浏览器每次阻止 Cookie 都有具体规则，找到它在哪一道门被拒绝，比反复改 SameSite 字符串快得多。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Guides/CORS">MDN：CORS</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Reference/Headers/Set-Cookie">MDN：Set-Cookie</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Fetch_API/Using_Fetch#including_credentials">MDN：Using Fetch - Including credentials</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端排障 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端排障 </tag>
            
            <tag> Web 安全 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Node.js 18：原生 Fetch 拉近浏览器与服务器 API</title>
      <link href="/2022/04/19/frontend-chronicle-2022-04-node-18-fetch/"/>
      <url>/2022/04/19/frontend-chronicle-2022-04-node-18-fetch/</url>
      
        <content type="html"><![CDATA[<p>Node.js 18 把全局 <code>fetch</code> 带进运行时（最初是实验状态）以后，服务端小工具终于可以直接使用和浏览器相似的 Request、Response、Headers API。我们删掉了一个只为发两条 HTTP 请求而安装的客户端库，却很快发现：API 统一了，服务端网络工程的复杂度一点也没消失。</p><p><code>fetch</code> 只在网络层拒绝时抛异常，HTTP 404&#x2F;500 仍会正常返回 Response；默认没有“业务想要的五秒超时”；重试、代理、连接池、可观测性也不会凭空出现。真正可用的封装需要把这些策略写清楚。</p><h2 id="第一版封装犯了两个错误"><a href="#第一版封装犯了两个错误" class="headerlink" title="第一版封装犯了两个错误"></a>第一版封装犯了两个错误</h2><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">getJson</span>(<span class="params">url</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(url)</span><br><span class="line">  <span class="keyword">return</span> response.<span class="title function_">json</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>错误一：500 响应也会继续解析，调用方可能把错误 JSON 当成功数据。错误二：上游一直不返回时，请求可以长时间挂着，占用服务资源。</p><p>我们先定义一个带状态和响应片段的错误：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">HttpError</span> <span class="keyword">extends</span> <span class="title class_ inherited__">Error</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params">message, &#123; status, url, body, cause &#125; = &#123;&#125;</span>) &#123;</span><br><span class="line">    <span class="variable language_">super</span>(message, &#123; cause &#125;)</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">name</span> = <span class="string">&#x27;HttpError&#x27;</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">status</span> = status</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">url</span> = url</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">body</span> = body</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>再做最小的状态&#x2F;超时处理：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">requestJson</span>(<span class="params">url, options = &#123;&#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> timeout = options.<span class="property">timeout</span> ?? <span class="number">5000</span></span><br><span class="line">  <span class="keyword">const</span> signal = <span class="title class_">AbortSignal</span>.<span class="title function_">timeout</span>(timeout)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">let</span> response</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    response = <span class="keyword">await</span> <span class="title function_">fetch</span>(url, &#123; ...options, signal &#125;)</span><br><span class="line">  &#125; <span class="keyword">catch</span> (cause) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">HttpError</span>(<span class="string">`Request failed: <span class="subst">$&#123;url&#125;</span>`</span>, &#123; url, cause &#125;)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!response.<span class="property">ok</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> body = <span class="keyword">await</span> response.<span class="title function_">text</span>()</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">HttpError</span>(<span class="string">`HTTP <span class="subst">$&#123;response.status&#125;</span>: <span class="subst">$&#123;url&#125;</span>`</span>, &#123;</span><br><span class="line">      <span class="attr">status</span>: response.<span class="property">status</span>,</span><br><span class="line">      url,</span><br><span class="line">      <span class="attr">body</span>: body.<span class="title function_">slice</span>(<span class="number">0</span>, <span class="number">1000</span>)</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> response.<span class="title function_">json</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里的 <code>AbortSignal.timeout</code> 支持范围要与实际 Node 18 小版本核对；较早环境可以自行创建 controller + timer。关键是超时成为显式策略，并且错误保留 URL&#x2F;status&#x2F;cause。</p><h2 id="外部-signal-与超时要能组合"><a href="#外部-signal-与超时要能组合" class="headerlink" title="外部 signal 与超时要能组合"></a>外部 signal 与超时要能组合</h2><p>调用方可能在任务取消时已经有 signal，封装不能直接覆盖。可以组合信号（具体 API 同样要按目标 Node 版本确认），或者手动转发 abort：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">createTimeoutSignal</span>(<span class="params">ms, externalSignal</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> controller = <span class="keyword">new</span> <span class="title class_">AbortController</span>()</span><br><span class="line">  <span class="keyword">const</span> timer = <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> controller.<span class="title function_">abort</span>(<span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;timeout&#x27;</span>)), ms)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> <span class="title function_">abortFromOutside</span> = (<span class="params"></span>) =&gt; controller.<span class="title function_">abort</span>(externalSignal.<span class="property">reason</span>)</span><br><span class="line">  externalSignal?.<span class="title function_">addEventListener</span>(<span class="string">&#x27;abort&#x27;</span>, abortFromOutside, &#123; <span class="attr">once</span>: <span class="literal">true</span> &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">signal</span>: controller.<span class="property">signal</span>,</span><br><span class="line">    <span class="title function_">dispose</span>(<span class="params"></span>) &#123;</span><br><span class="line">      <span class="built_in">clearTimeout</span>(timer)</span><br><span class="line">      externalSignal?.<span class="title function_">removeEventListener</span>(<span class="string">&#x27;abort&#x27;</span>, abortFromOutside)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用后在 finally 中 dispose，避免监听器和 timer 留存。取消不是“无关紧要的异常”，要区分用户取消、任务超时与真正网络失败，监控告警级别也不同。</p><h2 id="不是所有失败都可以重试"><a href="#不是所有失败都可以重试" class="headerlink" title="不是所有失败都可以重试"></a>不是所有失败都可以重试</h2><p>GET 查询在网络错误、429 或部分 5xx 下可以有限重试，并带退避与随机抖动；POST 支付、创建订单则可能产生重复写入，除非服务端支持幂等键。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">retryGet</span>(<span class="params">url, &#123; attempts = <span class="number">3</span>, signal &#125; = &#123;&#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> lastError</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> attempt = <span class="number">1</span>; attempt &lt;= attempts; attempt += <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">await</span> <span class="title function_">requestJson</span>(url, &#123; signal &#125;)</span><br><span class="line">    &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">      lastError = error</span><br><span class="line">      <span class="keyword">const</span> retryable = !error.<span class="property">status</span> || error.<span class="property">status</span> === <span class="number">429</span> || error.<span class="property">status</span> &gt;= <span class="number">500</span></span><br><span class="line">      <span class="keyword">if</span> (!retryable || attempt === attempts) <span class="keyword">throw</span> error</span><br><span class="line"></span><br><span class="line">      <span class="keyword">const</span> delay = <span class="number">200</span> * <span class="number">2</span> ** (attempt - <span class="number">1</span>) + <span class="title class_">Math</span>.<span class="title function_">random</span>() * <span class="number">100</span></span><br><span class="line">      <span class="keyword">await</span> <span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> <span class="built_in">setTimeout</span>(resolve, delay))</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">throw</span> lastError</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>生产代码还应读取 <code>Retry-After</code>、让等待可取消并设置总截止时间。每层都重试会放大流量，所以网关、SDK 和业务服务要明确由哪一层负责。</p><h2 id="Response-body-是流，只能消费一次"><a href="#Response-body-是流，只能消费一次" class="headerlink" title="Response body 是流，只能消费一次"></a>Response body 是流，只能消费一次</h2><p>Fetch 的响应体不是随时可重复读取的普通对象：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(url)</span><br><span class="line"><span class="keyword">const</span> text = <span class="keyword">await</span> response.<span class="title function_">text</span>()</span><br><span class="line"><span class="keyword">const</span> json = <span class="keyword">await</span> response.<span class="title function_">json</span>() <span class="comment">// body 已经被消费，会失败</span></span><br></pre></td></tr></table></figure><p>调试中想既记录又解析，可以先读取文本再 JSON.parse，或在合适场景 clone；但 clone 大响应也会有内存代价。下载大文件时应流式处理，而不是 <code>arrayBuffer()</code> 后全部放进内存。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; pipeline &#125; <span class="keyword">from</span> <span class="string">&#x27;node:stream/promises&#x27;</span></span><br><span class="line"><span class="keyword">import</span> &#123; createWriteStream &#125; <span class="keyword">from</span> <span class="string">&#x27;node:fs&#x27;</span></span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Readable</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;node:stream&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(fileUrl)</span><br><span class="line"><span class="keyword">if</span> (!response.<span class="property">ok</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`HTTP <span class="subst">$&#123;response.status&#125;</span>`</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">await</span> <span class="title function_">pipeline</span>(</span><br><span class="line">  <span class="title class_">Readable</span>.<span class="title function_">fromWeb</span>(response.<span class="property">body</span>),</span><br><span class="line">  <span class="title function_">createWriteStream</span>(<span class="string">&#x27;./archive.zip&#x27;</span>)</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>Web Stream 与 Node stream 的桥接让标准 API 可以进入现有文件管道，但要处理取消、磁盘错误和临时文件清理。</p><h2 id="浏览器封装不能原样搬到服务器"><a href="#浏览器封装不能原样搬到服务器" class="headerlink" title="浏览器封装不能原样搬到服务器"></a>浏览器封装不能原样搬到服务器</h2><p>浏览器关心 CORS、Cookie credentials 和页面生命周期；服务器没有浏览器 CORS 限制，却要处理企业代理、证书、DNS、连接复用与 SSRF。</p><p>尤其是服务端根据用户输入请求 URL 时，必须限制协议、主机和重定向，防止访问内网元数据地址：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">assertAllowedUrl</span>(<span class="params">input</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> url = <span class="keyword">new</span> <span class="title function_">URL</span>(input)</span><br><span class="line">  <span class="keyword">if</span> (url.<span class="property">protocol</span> !== <span class="string">&#x27;https:&#x27;</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;HTTPS required&#x27;</span>)</span><br><span class="line">  <span class="keyword">if</span> (![<span class="string">&#x27;api.example.com&#x27;</span>, <span class="string">&#x27;images.example.com&#x27;</span>].<span class="title function_">includes</span>(url.<span class="property">hostname</span>)) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;Host not allowed&#x27;</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> url</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>allowlist 只是简化示例，生产还要考虑 DNS rebinding、重定向目标和 IP 范围。统一 API 不等于统一威胁模型。</p><h2 id="日志要能关联，而不是打印整个响应"><a href="#日志要能关联，而不是打印整个响应" class="headerlink" title="日志要能关联，而不是打印整个响应"></a>日志要能关联，而不是打印整个响应</h2><p>我们给每个外部请求记录目标服务名、路径模板、状态、耗时、重试次数和 trace ID，不记录完整 token&#x2F;个人数据。错误正文最多截断并脱敏。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> startedAt = performance.<span class="title function_">now</span>()</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">await</span> <span class="title function_">requestJson</span>(url, options)</span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">  metrics.<span class="title function_">observe</span>(<span class="string">&#x27;outbound_http_duration_ms&#x27;</span>, performance.<span class="title function_">now</span>() - startedAt, &#123;</span><br><span class="line">    <span class="attr">service</span>: <span class="string">&#x27;profile-api&#x27;</span></span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>仅记录 URL 容易把用户 ID 变成高基数指标；使用路径模板更适合聚合。</p><h2 id="原生-Fetch-给了统一基础，不是完整-SDK"><a href="#原生-Fetch-给了统一基础，不是完整-SDK" class="headerlink" title="原生 Fetch 给了统一基础，不是完整 SDK"></a>原生 Fetch 给了统一基础，不是完整 SDK</h2><p>Node 18 的 fetch 让跨运行时库和小脚本少了一项依赖，也让 Request&#x2F;Response&#x2F;Web Streams 更常见。真正业务服务仍可能需要成熟客户端提供代理、连接调优、插件与更丰富的错误模型。</p><p>是否替换第三方库不应该只看“Node 已经内置”。对简单 JSON 调用，原生 fetch 足够；对复杂认证、重试、上传进度和企业网络，保留专用客户端可能更可靠。无论选哪一个，状态检查、截止时间、取消、幂等与可观测性都必须由团队明确承担。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://nodejs.org/en/blog/release/v18.0.0">Node.js 18.0.0 发布说明</a></li><li><a href="https://nodejs.org/api/globals.html#fetch">Node.js：Global fetch</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Fetch_API">MDN：Fetch API</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Node.js </tag>
            
            <tag> 2022 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>React 18：并发渲染、自动批处理与流式 SSR</title>
      <link href="/2022/03/29/frontend-chronicle-2022-03-react-18-concurrent-rendering/"/>
      <url>/2022/03/29/frontend-chronicle-2022-03-react-18-concurrent-rendering/</url>
      
        <content type="html"><![CDATA[<p>React 18 升级分支第一次跑起来时，测试群里马上出现两条消息：为什么开发环境接口请求两次？为什么一个依赖“setState 后马上读 DOM”的用例失败了？相比发布说明里的并发渲染，这两个具体问题更能代表迁移现场。</p><p>我们没有一开始就使用所有新 API。先切换 <code>createRoot</code>，让现有应用跑在新根节点上，修正副作用与批处理时序，再挑一个卡顿搜索页验证 transition。这样能把“版本行为变化”和“新增优化”分开。</p><h2 id="第一处改动只是根节点-API"><a href="#第一处改动只是根节点-API" class="headerlink" title="第一处改动只是根节点 API"></a>第一处改动只是根节点 API</h2><p>React 17 写法：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="title class_">ReactDOM</span> <span class="keyword">from</span> <span class="string">&#x27;react-dom&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="title class_">ReactDOM</span>.<span class="title function_">render</span>(<span class="language-xml"><span class="tag">&lt;<span class="name">App</span> /&gt;</span></span>, <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;root&#x27;</span>))</span><br></pre></td></tr></table></figure><p>React 18：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; createRoot &#125; <span class="keyword">from</span> <span class="string">&#x27;react-dom/client&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> container = <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;root&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> root = <span class="title function_">createRoot</span>(container)</span><br><span class="line">root.<span class="title function_">render</span>(<span class="language-xml"><span class="tag">&lt;<span class="name">App</span> /&gt;</span></span>)</span><br></pre></td></tr></table></figure><p>新的并发基础由新 root 启用。API 改完页面能显示，不代表迁移完成；状态批处理、严格模式开发检查与 SSR 水合都需要回归。</p><h2 id="自动批处理让一次异步回调只提交一次"><a href="#自动批处理让一次异步回调只提交一次" class="headerlink" title="自动批处理让一次异步回调只提交一次"></a>自动批处理让一次异步回调只提交一次</h2><p>旧版本中，React 事件之外的 Promise&#x2F;timeout 回调里的多个 setState 可能分别渲染。React 18 新 root 下覆盖范围更广：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">handleSave</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="title function_">saveForm</span>().<span class="title function_">then</span>(<span class="function"><span class="params">result</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">setData</span>(result)</span><br><span class="line">    <span class="title function_">setSaving</span>(<span class="literal">false</span>)</span><br><span class="line">    <span class="title function_">setMessage</span>(<span class="string">&#x27;保存成功&#x27;</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这些更新通常会被批处理，减少提交次数。大多数代码只会更快，但依赖“第一次 setState 后 DOM 立刻改变”的逻辑会出问题。</p><p>我们找到过一段打印代码：更新状态后立即读取元素高度。正确方向是把打印触发放到状态提交后的 Effect，或把要打印的数据直接传给打印模块，而不是依赖中间 DOM。确实必须同步刷新的极少场景可以使用 <code>flushSync</code>，但它会打断优化，不应该成为通用兼容补丁。</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; flushSync &#125; <span class="keyword">from</span> <span class="string">&#x27;react-dom&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="title function_">flushSync</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="title function_">setExpanded</span>(<span class="literal">true</span>)</span><br><span class="line">&#125;)</span><br><span class="line">panelRef.<span class="property">current</span>?.<span class="title function_">focus</span>()</span><br></pre></td></tr></table></figure><p>每个 <code>flushSync</code> 都应说明为什么浏览器 API 必须立刻看到更新。</p><h2 id="开发环境重复请求，真正问题是请求没有生命周期"><a href="#开发环境重复请求，真正问题是请求没有生命周期" class="headerlink" title="开发环境重复请求，真正问题是请求没有生命周期"></a>开发环境重复请求，真正问题是请求没有生命周期</h2><p>StrictMode 下，React 会在开发环境额外执行 setup&#x2F;cleanup 流程，帮助发现不纯渲染和缺少清理的 Effect。下面代码会暴露问题：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="title function_">fetch</span>(<span class="string">`/api/orders/<span class="subst">$&#123;orderId&#125;</span>`</span>)</span><br><span class="line">    .<span class="title function_">then</span>(<span class="function"><span class="params">response</span> =&gt;</span> response.<span class="title function_">json</span>())</span><br><span class="line">    .<span class="title function_">then</span>(setOrder)</span><br><span class="line">&#125;, [orderId])</span><br></pre></td></tr></table></figure><p>组件卸载&#x2F;依赖变化后，请求仍可能返回并写入。修复可以使用 AbortController，或交给能缓存去重的数据层：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> controller = <span class="keyword">new</span> <span class="title class_">AbortController</span>()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">load</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">`/api/orders/<span class="subst">$&#123;orderId&#125;</span>`</span>, &#123;</span><br><span class="line">      <span class="attr">signal</span>: controller.<span class="property">signal</span></span><br><span class="line">    &#125;)</span><br><span class="line">    <span class="keyword">const</span> data = <span class="keyword">await</span> response.<span class="title function_">json</span>()</span><br><span class="line">    <span class="title function_">setOrder</span>(data)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">load</span>().<span class="title function_">catch</span>(<span class="function"><span class="params">error</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (error.<span class="property">name</span> !== <span class="string">&#x27;AbortError&#x27;</span>) <span class="title function_">reportError</span>(error)</span><br><span class="line">  &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="function">() =&gt;</span> controller.<span class="title function_">abort</span>()</span><br><span class="line">&#125;, [orderId])</span><br></pre></td></tr></table></figure><p>关闭 StrictMode 能隐藏第二次执行，却不会补上真实切路由时缺失的取消。我们把重复执行当成压力测试，而不是框架 Bug。</p><h2 id="transition-只解决“哪次渲染更紧急”"><a href="#transition-只解决“哪次渲染更紧急”" class="headerlink" title="transition 只解决“哪次渲染更紧急”"></a>transition 只解决“哪次渲染更紧急”</h2><p>搜索页有 5000 行本地数据。用户输入一个字符，输入框 state 与过滤列表同时更新，昂贵渲染让键盘反馈迟滞。这里很适合把输入保持为紧急更新，列表查询视为可中断的非紧急更新：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">SearchPage</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [input, setInput] = <span class="title function_">useState</span>(<span class="string">&#x27;&#x27;</span>)</span><br><span class="line">  <span class="keyword">const</span> [keyword, setKeyword] = <span class="title function_">useState</span>(<span class="string">&#x27;&#x27;</span>)</span><br><span class="line">  <span class="keyword">const</span> [isPending, startTransition] = <span class="title function_">useTransition</span>()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">function</span> <span class="title function_">handleChange</span>(<span class="params">event</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> value = event.<span class="property">target</span>.<span class="property">value</span></span><br><span class="line">    <span class="title function_">setInput</span>(value)</span><br><span class="line">    <span class="title function_">startTransition</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="title function_">setKeyword</span>(value)</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> result = <span class="title function_">filterLargeList</span>(keyword)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">input</span> <span class="attr">value</span>=<span class="string">&#123;input&#125;</span> <span class="attr">onChange</span>=<span class="string">&#123;handleChange&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">      &#123;isPending &amp;&amp; <span class="tag">&lt;<span class="name">span</span>&gt;</span>更新结果中…<span class="tag">&lt;/<span class="name">span</span>&gt;</span>&#125;</span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">ResultList</span> <span class="attr">items</span>=<span class="string">&#123;result&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>transition 不会让 <code>filterLargeList</code> 计算变便宜，也不会自动防抖网络请求。它让 React 可以优先提交输入反馈，并中断过时的非紧急渲染。若计算本身阻塞主线程太久，仍需优化算法、虚拟列表或 Worker。</p><h2 id="useDeferredValue-适合你控制不了更新来源时"><a href="#useDeferredValue-适合你控制不了更新来源时" class="headerlink" title="useDeferredValue 适合你控制不了更新来源时"></a><code>useDeferredValue</code> 适合你控制不了更新来源时</h2><p>如果 keyword 通过 props 传入，调用者不方便包 <code>startTransition</code>，可以延迟消费：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">ResultPanel</span>(<span class="params">&#123; keyword &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> deferredKeyword = <span class="title function_">useDeferredValue</span>(keyword)</span><br><span class="line">  <span class="keyword">const</span> isStale = deferredKeyword !== keyword</span><br><span class="line">  <span class="keyword">const</span> result = <span class="title function_">useMemo</span>(</span><br><span class="line">    <span class="function">() =&gt;</span> <span class="title function_">filterLargeList</span>(deferredKeyword),</span><br><span class="line">    [deferredKeyword]</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">div</span> <span class="attr">aria-busy</span>=<span class="string">&#123;isStale&#125;</span>&gt;</span><span class="tag">&lt;<span class="name">ResultList</span> <span class="attr">items</span>=<span class="string">&#123;result&#125;</span> /&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它同样不是固定毫秒延迟的 debounce，而是与渲染调度配合。需要减少接口请求次数时仍应做防抖、缓存与取消。</p><h2 id="服务端渲染的变化不能只在-CSR-项目里验证"><a href="#服务端渲染的变化不能只在-CSR-项目里验证" class="headerlink" title="服务端渲染的变化不能只在 CSR 项目里验证"></a>服务端渲染的变化不能只在 CSR 项目里验证</h2><p>React 18 改进了 Suspense 与流式 SSR，服务器可以先发送页面骨架，再逐步发送准备好的边界。我们没有手写底层流，而是等待框架集成，并在真实慢接口下测试：首字节、主要内容出现、脚本加载和交互恢复。</p><p>SSR 升级还要将 <code>hydrate</code> 改为 <code>hydrateRoot</code>，并检查服务端&#x2F;客户端首屏输出确定性。时间、随机数、浏览器环境分支会造成 hydration mismatch。</p><h2 id="升级后的回归清单"><a href="#升级后的回归清单" class="headerlink" title="升级后的回归清单"></a>升级后的回归清单</h2><ul><li>root 与 hydrate API 是否全部迁移；</li><li>Effect 是否有完整 cleanup，请求能否取消；</li><li>测试是否错误依赖 setState 后同步 DOM；</li><li>第三方组件是否在 render 中修改外部状态；</li><li>transition 只用于非紧急渲染，而非包装受控输入本身；</li><li>SSR 页面是否有水合警告和流式边界闪烁；</li><li>性能改动是否用 Profiler&#x2F;真实交互数据验证。</li></ul><h2 id="并发不是“同时在多个线程渲染”"><a href="#并发不是“同时在多个线程渲染”" class="headerlink" title="并发不是“同时在多个线程渲染”"></a>并发不是“同时在多个线程渲染”</h2><p>React 18 的 concurrent rendering 指渲染工作可以被调度、中断和放弃，不是把组件函数自动搬到多线程。组件必须保持纯净，因为一次未提交的渲染可能不产生 DOM，也可能稍后重试。</p><p>这次升级最有价值的结果，不只是列表输入变顺滑。StrictMode 帮我们修掉了几处缺失清理，自动批处理逼着旧代码摆脱中间 DOM 时序，transition 则让更新优先级变成可表达的产品意图。并发基础带来的要求其实很朴素：渲染做纯计算，副作用可重复和可清理，性能优化围绕用户感知而不是 API 数量。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://react.dev/blog/2022/03/29/react-v18">React 18 发布说明</a></li><li><a href="https://react.dev/reference/react/startTransition">React：startTransition</a></li><li><a href="https://react.dev/reference/react/StrictMode">React：StrictMode</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> React </tag>
            
            <tag> 2022 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vue 3 成为默认版本：生态迁移进入主线</title>
      <link href="/2022/02/07/frontend-chronicle-2022-02-vue-3-default/"/>
      <url>/2022/02/07/frontend-chronicle-2022-02-vue-3-default/</url>
      
        <content type="html"><![CDATA[<p>2022 年 Vue 3 成为 npm 与官方文档的默认版本时，变化的不只是 <code>npm install vue</code> 会装到哪一版。create-vue、Vite、Pinia 和新的 TypeScript&#x2F;编辑器工具逐渐组成一条完整默认路径，团队再也不能用“生态还没准备好”笼统推迟所有评估。</p><p>我们当时有三个 Vue 2 项目：一个还在维护的营销站、一个大型后台、一个准备启动的新系统。最后做了三个不同决定：新系统直接 Vue 3，大后台分阶段迁移，营销站只更新安全与关键依赖，不为追版本重写。框架成为默认，不代表所有旧项目都应该同一天升级。</p><h2 id="新项目先接受新默认，再删掉不需要的选项"><a href="#新项目先接受新默认，再删掉不需要的选项" class="headerlink" title="新项目先接受新默认，再删掉不需要的选项"></a>新项目先接受新默认，再删掉不需要的选项</h2><p>create-vue 生成项目时会询问 TypeScript、Router、Pinia、测试和 lint。我们没有全选，而是从产品需要出发：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">TypeScript: Yes</span><br><span class="line">Vue Router: Yes</span><br><span class="line">Pinia: Yes（存在跨路由会话状态）</span><br><span class="line">Vitest: Yes</span><br><span class="line">E2E: Yes（覆盖登录与支付路径）</span><br><span class="line">JSX: No</span><br></pre></td></tr></table></figure><p>最小入口非常直接：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; createApp &#125; <span class="keyword">from</span> <span class="string">&#x27;vue&#x27;</span></span><br><span class="line"><span class="keyword">import</span> &#123; createPinia &#125; <span class="keyword">from</span> <span class="string">&#x27;pinia&#x27;</span></span><br><span class="line"><span class="keyword">import</span> <span class="title class_">App</span> <span class="keyword">from</span> <span class="string">&#x27;./App.vue&#x27;</span></span><br><span class="line"><span class="keyword">import</span> router <span class="keyword">from</span> <span class="string">&#x27;./router&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="title function_">createApp</span>(<span class="title class_">App</span>)</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">createPinia</span>())</span><br><span class="line">  .<span class="title function_">use</span>(router)</span><br><span class="line">  .<span class="title function_">mount</span>(<span class="string">&#x27;#app&#x27;</span>)</span><br></pre></td></tr></table></figure><p>开箱即用的价值是工具之间已经走过兼容路径，不必从 webpack loader、Babel 和类型插件开始拼。但脚手架不是架构答案，目录边界、请求策略和状态归属仍要自己设计。</p><h2 id="Pinia-不是把-Vuex-mutation-改个名字"><a href="#Pinia-不是把-Vuex-mutation-改个名字" class="headerlink" title="Pinia 不是把 Vuex mutation 改个名字"></a>Pinia 不是把 Vuex mutation 改个名字</h2><p>旧 Vuex store 往往把所有东西放进一个根仓库，mutation、action 和 getter 按技术类型分散。迁移时我们按领域拆 store，而不是逐行翻译。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; defineStore &#125; <span class="keyword">from</span> <span class="string">&#x27;pinia&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> useSessionStore = <span class="title function_">defineStore</span>(<span class="string">&#x27;session&#x27;</span>, &#123;</span><br><span class="line">  <span class="attr">state</span>: <span class="function">() =&gt;</span> (&#123;</span><br><span class="line">    <span class="attr">user</span>: <span class="literal">null</span> <span class="keyword">as</span> <span class="title class_">User</span> | <span class="literal">null</span>,</span><br><span class="line">    <span class="attr">permissions</span>: [] <span class="keyword">as</span> <span class="built_in">string</span>[]</span><br><span class="line">  &#125;),</span><br><span class="line">  <span class="attr">getters</span>: &#123;</span><br><span class="line">    <span class="attr">signedIn</span>: <span class="function"><span class="params">state</span> =&gt;</span> state.<span class="property">user</span> !== <span class="literal">null</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">actions</span>: &#123;</span><br><span class="line">    <span class="keyword">async</span> <span class="title function_">restore</span>(<span class="params"></span>) &#123;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">user</span> = <span class="keyword">await</span> sessionApi.<span class="title function_">currentUser</span>()</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">permissions</span> = <span class="keyword">await</span> sessionApi.<span class="title function_">permissions</span>()</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="title function_">clear</span>(<span class="params"></span>) &#123;</span><br><span class="line">      <span class="variable language_">this</span>.$reset()</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>页面局部筛选、弹窗开关不因为用了 Pinia 就要进全局 store。只有需要跨组件&#x2F;路由共享、具有明确生命周期的状态才进入。服务器返回的数据还要考虑缓存和重新请求，不能一概永久放在 store 中。</p><h2 id="大后台的阻塞项不是-Vue-核心"><a href="#大后台的阻塞项不是-Vue-核心" class="headerlink" title="大后台的阻塞项不是 Vue 核心"></a>大后台的阻塞项不是 Vue 核心</h2><p>我们列出的迁移表里，Vue 核心 API 只占一小部分：</p><table><thead><tr><th>类别</th><th>需要确认的内容</th></tr></thead><tbody><tr><td>组件库</td><td>Vue 3 版本、主题变量、弹窗&#x2F;表单行为</td></tr><tr><td>路由</td><td>动态路由、导航守卫、keep-alive</td></tr><tr><td>状态</td><td>Vuex 版本、模块注册、持久化插件</td></tr><tr><td>构建</td><td>webpack loader、环境变量、SVG 与 worker</td></tr><tr><td>业务插件</td><td>依赖 Vue 2 实例 API、过滤器、全局 mixin</td></tr><tr><td>浏览器</td><td>Proxy 支持范围与企业终端基线</td></tr><tr><td>测试</td><td>shallowMount 行为、组件选择器、异步刷新</td></tr></tbody></table><p>一个停更的树形表格组件成了真正阻塞项。与其为了升级重写整套后台，我们先把它隔离到少量适配组件，其他路由用兼容构建逐步迁移。阻塞项有了名字、负责人和替代方案以后，讨论才从“能不能升”变成具体工程计划。</p><h2 id="script-setup-让组件短了，但边界不能省"><a href="#script-setup-让组件短了，但边界不能省" class="headerlink" title="script setup 让组件短了，但边界不能省"></a>script setup 让组件短了，但边界不能省</h2><p>新项目使用 <code>&lt;script setup&gt;</code> 后，props、emit 和模板变量更紧凑：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">&lt;script setup lang=&quot;ts&quot;&gt;</span><br><span class="line">import &#123; computed &#125; from &#x27;vue&#x27;</span><br><span class="line"></span><br><span class="line">const props = defineProps&lt;&#123;</span><br><span class="line">  price: number</span><br><span class="line">  count?: number</span><br><span class="line">&#125;&gt;()</span><br><span class="line"></span><br><span class="line">const emit = defineEmits&lt;&#123;</span><br><span class="line">  change: [count: number]</span><br><span class="line">&#125;&gt;()</span><br><span class="line"></span><br><span class="line">const total = computed(() =&gt; props.price * (props.count ?? 1))</span><br><span class="line">&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;button @click=&quot;emit(&#x27;change&#x27;, (count ?? 1) + 1)&quot;&gt;</span><br><span class="line">    合计 &#123;&#123; total &#125;&#125;</span><br><span class="line">  &lt;/button&gt;</span><br><span class="line">&lt;/template&gt;</span><br></pre></td></tr></table></figure><p>宏减少样板代码，不代表 props 可以随意修改或事件可以没有契约。我们仍要求组件说明受控值、默认值和事件时机，并用类型&#x2F;测试验证。</p><h2 id="Vite-很快，类型检查被拆到另一条链路"><a href="#Vite-很快，类型检查被拆到另一条链路" class="headerlink" title="Vite 很快，类型检查被拆到另一条链路"></a>Vite 很快，类型检查被拆到另一条链路</h2><p>Vite 转换 TypeScript 主要负责去除类型，完整类型检查通常由 <code>vue-tsc</code> 独立完成。开发时编辑器反馈，CI 中显式运行：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;scripts&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;dev&quot;</span><span class="punctuation">:</span> <span class="string">&quot;vite&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;typecheck&quot;</span><span class="punctuation">:</span> <span class="string">&quot;vue-tsc --noEmit&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;test&quot;</span><span class="punctuation">:</span> <span class="string">&quot;vitest run&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="string">&quot;npm run typecheck &amp;&amp; vite build&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这条分工让 dev server 不被全项目类型检查阻塞，但不能因此漏掉 CI。工具更快往往来自拆分职责，团队要确保每个职责仍有执行入口。</p><h2 id="迁移完成的标准不是“没有-Vue-2-语法”"><a href="#迁移完成的标准不是“没有-Vue-2-语法”" class="headerlink" title="迁移完成的标准不是“没有 Vue 2 语法”"></a>迁移完成的标准不是“没有 Vue 2 语法”</h2><p>每个路由迁移后，我们看的是业务行为：</p><ul><li>直接刷新、前进后退与权限重定向；</li><li>表单校验触发时机和错误定位；</li><li>keep-alive 页面恢复时是否重复请求；</li><li>全局弹窗&#x2F;消息是否受新 Teleport 层级影响；</li><li>埋点、错误上报和 source map 是否完整；</li><li>大列表与常用交互性能是否退化。</li></ul><p>语法扫描只能找到一部分兼容问题，真实用户路径才是迁移验收。</p><h2 id="三个项目，三种结果"><a href="#三个项目，三种结果" class="headerlink" title="三个项目，三种结果"></a>三个项目，三种结果</h2><p>新系统使用 Vue 3 + Vite + Pinia 后保持了较干净的默认链路；后台用了数月按路由迁移；营销站没有迁，因为收益抵不过测试和重写成本。这个结果比制定“一季度全部 Vue 3 化”更健康。</p><p>Vue 3 成为默认版本，代表生态主航道已经改变。它给新项目一个更明确起点，也让旧项目必须正视未来维护窗口。但默认值是决策输入，不是决策本身。盘点依赖、明确业务收益、保留回滚和逐路由验证，才是大型项目真正能走完升级的原因。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://blog.vuejs.org/posts/vue-3-as-the-new-default">Vue：Vue 3 as the New Default</a></li><li><a href="https://vuejs.org/guide/scaling-up/tooling.html">Vue：Tooling</a></li><li><a href="https://pinia.vuejs.org/">Pinia 官方文档</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Vue </tag>
            
            <tag> 2022 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Next.js 12：SWC、Middleware 与边缘运行时方向</title>
      <link href="/2021/10/26/frontend-chronicle-2021-10-nextjs-12-swc/"/>
      <url>/2021/10/26/frontend-chronicle-2021-10-nextjs-12-swc/</url>
      
        <content type="html"><![CDATA[<p>Next.js 12 宣布用 Rust 编写的 SWC 接手默认转译与压缩工作时，我们最关心的是两个数字：<code>next dev</code> 首次可用时间，以及 CI 中 <code>next build</code> 的耗时。实测确实有改善，但升级没有停在跑分，因为项目里藏着一个自定义 Babel 插件，会在编译时给埋点函数补页面标识。</p><p>只要 <code>.babelrc</code> 存在，框架可能回退到 Babel 路径，团队就会出现“别人都说 SWC 快，我们为什么没变化”的困惑。那次迁移最后变成了一个很好的工具链审计：哪些转换仍然必要，哪些已经被框架内置，哪些不该继续依赖 AST 魔法。</p><h2 id="先确认项目到底有没有走-SWC"><a href="#先确认项目到底有没有走-SWC" class="headerlink" title="先确认项目到底有没有走 SWC"></a>先确认项目到底有没有走 SWC</h2><p>旧 <code>.babelrc</code>：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;presets&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;next/babel&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;plugins&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">[</span><span class="string">&quot;./build/babel-plugin-page-track&quot;</span><span class="punctuation">,</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;project&quot;</span><span class="punctuation">:</span> <span class="string">&quot;console&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这个插件会把：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">track</span>(<span class="string">&#x27;open&#x27;</span>)</span><br></pre></td></tr></table></figure><p>变成带页面信息的调用。它方便，但也让每个源码文件的转换依赖内部插件。我们先统计插件真正覆盖的调用点，发现只有二十多个，最终改成显式封装：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">trackPageEvent</span>(<span class="params"></span></span><br><span class="line"><span class="params">  <span class="attr">page</span>: <span class="built_in">string</span>,</span></span><br><span class="line"><span class="params">  <span class="attr">event</span>: <span class="built_in">string</span>,</span></span><br><span class="line"><span class="params">  <span class="attr">payload</span>: <span class="title class_">Record</span>&lt;<span class="built_in">string</span>, <span class="built_in">unknown</span>&gt; = &#123;&#125;</span></span><br><span class="line"><span class="params"></span>) &#123;</span><br><span class="line">  analytics.<span class="title function_">track</span>(event, &#123; page, ...payload &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>移除 <code>.babelrc</code> 后，构建才完整走默认 SWC 路径。显式代码多写了一个参数，却更容易搜索、测试和迁移。</p><h2 id="跑分要固定条件，否则数字没有意义"><a href="#跑分要固定条件，否则数字没有意义" class="headerlink" title="跑分要固定条件，否则数字没有意义"></a>跑分要固定条件，否则数字没有意义</h2><p>我们分别记录：</p><ul><li>删除 <code>.next</code> 后的冷启动；</li><li>已有缓存时修改叶子组件的 Fast Refresh；</li><li>相同 Node 与依赖下的生产构建；</li><li>CI 全新安装后的端到端总耗时。</li></ul><p>只挑一次最快结果没有价值。开发机后台进程、文件系统缓存和 CI CPU 都会影响数字。更重要的是行为回归：JSX 转换、styled-jsx、source map、测试覆盖率和生产错误堆栈都要验证。</p><p>SWC 的优势不只来自“Rust 比 JavaScript 快”，还来自框架能为自己的约定做端到端整合。越多自定义 Babel 插件，越难享受这条优化路径。</p><h2 id="Middleware-最初吸引人，也最容易被塞满逻辑"><a href="#Middleware-最初吸引人，也最容易被塞满逻辑" class="headerlink" title="Middleware 最初吸引人，也最容易被塞满逻辑"></a>Middleware 最初吸引人，也最容易被塞满逻辑</h2><p>Next.js 12 的 Middleware 能在请求完成路由渲染前运行。多语言重写、A&#x2F;B 分流和轻量鉴权判断都很适合：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">NextResponse</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;next/server&#x27;</span></span><br><span class="line"><span class="keyword">import</span> <span class="keyword">type</span> &#123; <span class="title class_">NextRequest</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;next/server&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">middleware</span>(<span class="params"><span class="attr">request</span>: <span class="title class_">NextRequest</span></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> session = request.<span class="property">cookies</span>.<span class="title function_">get</span>(<span class="string">&#x27;session&#x27;</span>)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!session &amp;&amp; request.<span class="property">nextUrl</span>.<span class="property">pathname</span>.<span class="title function_">startsWith</span>(<span class="string">&#x27;/dashboard&#x27;</span>)) &#123;</span><br><span class="line">    <span class="keyword">const</span> login = <span class="keyword">new</span> <span class="title function_">URL</span>(<span class="string">&#x27;/login&#x27;</span>, request.<span class="property">url</span>)</span><br><span class="line">    login.<span class="property">searchParams</span>.<span class="title function_">set</span>(<span class="string">&#x27;from&#x27;</span>, request.<span class="property">nextUrl</span>.<span class="property">pathname</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">NextResponse</span>.<span class="title function_">redirect</span>(login)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="title class_">NextResponse</span>.<span class="title function_">next</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码只做早期跳转，真正的权限校验仍需在服务端数据&#x2F;API 层执行。Cookie 存在不代表有效，更不代表用户有权读取某条数据。Middleware 是用户体验和路由边界，不是唯一安全边界。</p><p>我们一度想在里面查数据库、拼导航和刷新 token，后来放弃。边缘运行时的 API、连接方式和执行成本不同于完整 Node 服务；每个请求都经过的代码应该短、可预测并能失败降级。</p><h2 id="ES-Module-优先暴露了几个老包问题"><a href="#ES-Module-优先暴露了几个老包问题" class="headerlink" title="ES Module 优先暴露了几个老包问题"></a>ES Module 优先暴露了几个老包问题</h2><p>Next.js 12 对 ESM 的支持进一步推进。某个内部包的 <code>module</code> 入口与 <code>main</code> 入口导出不一致，升级后解析到 ESM，页面出现默认导入 undefined。修复不是强制 alias 回 CJS，而是统一包的公开导出并增加两种消费测试。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;exports&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;.&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;import&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/index.mjs&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;require&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/index.cjs&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>框架升级像一盏更亮的灯，常常照出内部包早已存在但没被触发的契约差异。</p><h2 id="输出文件追踪让容器不必带完整仓库"><a href="#输出文件追踪让容器不必带完整仓库" class="headerlink" title="输出文件追踪让容器不必带完整仓库"></a>输出文件追踪让容器不必带完整仓库</h2><p>Next.js 12 会追踪每个页面&#x2F;API 路由运行所需文件，为部署工具生成依赖信息。对于容器部署，这意味着可以基于实际依赖制作更小的运行镜像，而不是把整个 monorepo 和所有开发依赖复制进去。</p><p>我们对生成镜像做了三类冒烟：访问静态页面、请求 SSR 页面、调用需要模板&#x2F;字体等非 JS 文件的 API。文件追踪能处理模块依赖，但代码运行时通过动态字符串读取的资源，仍可能需要显式包含。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> template = <span class="keyword">await</span> fs.<span class="title function_">readFile</span>(</span><br><span class="line">  path.<span class="title function_">join</span>(process.<span class="title function_">cwd</span>(), <span class="string">&#x27;templates&#x27;</span>, <span class="string">&#x27;invoice.html&#x27;</span>),</span><br><span class="line">  <span class="string">&#x27;utf8&#x27;</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>这种资源不一定出现在静态 import 图中，部署后才报 ENOENT 会很难看，所以必须在接近生产的镜像里验证。</p><h2 id="升级清单不是只看页面能否打开"><a href="#升级清单不是只看页面能否打开" class="headerlink" title="升级清单不是只看页面能否打开"></a>升级清单不是只看页面能否打开</h2><p>我们最终的回归范围包括：</p><ol><li>删除自定义 Babel 后，埋点、宏和测试转换是否等价；</li><li>production source map 能否正确还原到源码；</li><li>Middleware 的 matcher 是否误伤静态资源和 API；</li><li>ESM&#x2F;CJS 内部包在 dev、build、test 中是否一致；</li><li>standalone&#x2F;追踪产物是否包含运行时读取的文件；</li><li>冷启动、热更新与 CI 构建的基线有没有真实改善。</li></ol><h2 id="我对-Next-js-12-的印象"><a href="#我对-Next-js-12-的印象" class="headerlink" title="我对 Next.js 12 的印象"></a>我对 Next.js 12 的印象</h2><p>这一版是 Next.js 从“集成 React 的框架”继续走向“深度掌管编译和运行时”的节点。SWC 把性能优化下沉到框架基础设施，Middleware 把框架触角伸到请求入口，输出追踪则连接到部署。</p><p>收益越完整，自定义逃生舱就越需要审视。保留 Babel 当然可以，但要接受性能路径和兼容成本；Middleware 当然能写复杂逻辑，但要承担每次请求的运行风险。框架默认越来越强时，最好的升级方式往往是删掉不再必要的定制，同时把真正的业务规则显式留下。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://nextjs.org/blog/next-12">Next.js 12 发布说明</a></li><li><a href="https://nextjs.org/docs/app/building-your-application/routing/middleware">Next.js：Middleware</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 2021 </tag>
            
            <tag> Next.js </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ChunkLoadError 排查：前端发布后懒加载资源 404</title>
      <link href="/2021/09/17/frontend-debug-chunk-load-error-after-deploy/"/>
      <url>/2021/09/17/frontend-debug-chunk-load-error-after-deploy/</url>
      
        <content type="html"><![CDATA[<p>周五下午发布后，错误平台开始出现一条平时几乎没有的异常：<code>Loading chunk 17 failed</code>。新打开页面的同事无法复现，只有那些早上就开着后台、下午点击“报表”菜单的人会白屏。刷新一下又好了。</p><p>最初大家把它归因于“用户浏览器缓存”，准备在 catch 里直接 <code>location.reload()</code>。但如果不修发布流程，刷新只是把损失转给用户；碰到 Service Worker 或 CDN 版本不一致，自动刷新甚至可能进入循环。</p><h2 id="先还原用户当时拿到的三个版本信息"><a href="#先还原用户当时拿到的三个版本信息" class="headerlink" title="先还原用户当时拿到的三个版本信息"></a>先还原用户当时拿到的三个版本信息</h2><p>排查这类问题，我会先收集：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">页面入口版本：2021.09.17-1</span><br><span class="line">失败资源 URL：https://cdn.example.com/assets/report.a81c2f.js</span><br><span class="line">当前线上版本：2021.09.17-2</span><br><span class="line">HTTP 状态：404</span><br><span class="line">页面打开时间：发布前 4 小时</span><br><span class="line">Service Worker：未注册</span><br></pre></td></tr></table></figure><p>只有一句 ChunkLoadError 不够。动态 import 可能因为 404、CDN 超时、CSP、语法错误或网络离线失败，错误名字也不一定在所有构建器&#x2F;浏览器中相同。失败 URL 与 Network 状态最有价值。</p><p>我们在 HTML 模板中注入构建版本，错误上报附带它：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="property">__APP_VERSION__</span> = <span class="string">&#x27;2021.09.17-2&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(<span class="string">&#x27;error&#x27;</span>, <span class="function"><span class="params">event</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="title function_">reportError</span>(event.<span class="property">error</span> || event.<span class="property">message</span>, &#123;</span><br><span class="line">    <span class="attr">appVersion</span>: <span class="variable language_">window</span>.<span class="property">__APP_VERSION__</span>,</span><br><span class="line">    <span class="attr">pageOpenedAt</span>: <span class="variable language_">window</span>.<span class="property">__PAGE_OPENED_AT__</span>,</span><br><span class="line">    <span class="attr">url</span>: event.<span class="property">filename</span></span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>实际应用还会在动态导入包装层记录请求 URL、online 状态和 SW controller。</p><h2 id="为什么只有没刷新的老页面出错"><a href="#为什么只有没刷新的老页面出错" class="headerlink" title="为什么只有没刷新的老页面出错"></a>为什么只有没刷新的老页面出错</h2><p>早上用户下载的入口 bundle 中记录着：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span>(<span class="string">&#x27;./report.a81c2f.js&#x27;</span>)</span><br></pre></td></tr></table></figure><p>下午发布后，新构建生成了 <code>report.f093ad.js</code>。部署脚本先清空静态目录，再上传新版，所以旧 hash 文件消失。老页面仍在内存中，直到用户第一次打开报表才发起对旧 chunk 的请求，结果 404。</p><p>刷新后浏览器拿到新版 HTML 和入口，自然会请求新 hash，于是显得“刷新就修好了”。真正根因是发布系统只服务当前版本，却存在长时间运行的旧客户端。</p><h2 id="先在源站和-CDN-两边请求失败-URL"><a href="#先在源站和-CDN-两边请求失败-URL" class="headerlink" title="先在源站和 CDN 两边请求失败 URL"></a>先在源站和 CDN 两边请求失败 URL</h2><p>拿到 URL 后不要只在浏览器刷新。我会分别检查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">curl -I https://cdn.example.com/assets/report.a81c2f.js</span><br><span class="line">curl -I https://origin.example.com/assets/report.a81c2f.js</span><br></pre></td></tr></table></figure><p>结果可以快速分流：</p><ul><li>源站 404、CDN 404：文件确实被删除或没上传；</li><li>源站 200、CDN 404：CDN 回源&#x2F;负缓存或刷新策略；</li><li>部分 CDN 节点 404：分发未完成或区域问题；</li><li>返回 200 但 Content-Type 是 text&#x2F;html：SPA fallback 吞掉了资源 404；</li><li>200 且内容是旧&#x2F;错误文件：缓存 key、压缩变体或覆盖发布问题。</li></ul><p>资源请求错误地回退到 <code>index.html</code> 时，浏览器常报告 <code>Unexpected token &#39;&lt;&#39;</code> 或 MIME 错误，而不是清晰的 chunk 404。静态资源路径应直接返回 404，不走页面 fallback。</p><h2 id="发布顺序改成“资源先行，入口后切”"><a href="#发布顺序改成“资源先行，入口后切”" class="headerlink" title="发布顺序改成“资源先行，入口后切”"></a>发布顺序改成“资源先行，入口后切”</h2><p>正确发布模型依赖内容 hash 文件不可变：</p><ol><li>上传本次所有 hash 静态资源，不删除旧文件；</li><li>从多个节点抽样确认关键资源可访问；</li><li>最后切换 HTML&#x2F;入口清单；</li><li>观察错误率和业务指标；</li><li>经过一个回滚窗口后，再清理足够旧的资源。</li></ol><p>静态资源可以长期缓存：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">Cache-Control</span><span class="punctuation">: </span>public, max-age=31536000, immutable</span><br></pre></td></tr></table></figure><p>HTML 则应短缓存或要求重新验证：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">Cache-Control</span><span class="punctuation">: </span>no-cache</span><br></pre></td></tr></table></figure><p><code>no-cache</code> 不是“不存”，而是使用前重新验证。具体策略要结合 CDN，但核心是不让 HTML 长期指向已经清理的资源。</p><h2 id="多节点滚动发布也会拼出混合版本"><a href="#多节点滚动发布也会拼出混合版本" class="headerlink" title="多节点滚动发布也会拼出混合版本"></a>多节点滚动发布也会拼出混合版本</h2><p>即使没有删除文件，HTML 与资源由不同服务器构建&#x2F;提供，也可能拿到不一致版本。例如请求 HTML 命中节点 B 的新版，资源回源到尚未发布的节点 A，短时间 404。</p><p>我们的静态资源改成一次构建产物上传到共享对象存储，不在每个应用节点现场构建；HTML 切换使用同一个 release ID。部署系统保存完整 release 清单，回滚只切入口，不重新拼装文件。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;release&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2021.09.17-2&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;assets&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="string">&quot;app.91de02.js&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;report.f093ad.js&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;report.35c102.css&quot;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>发布前脚本会逐个 HEAD 检查清单中的资源。</p><h2 id="客户端恢复要克制，避免刷新死循环"><a href="#客户端恢复要克制，避免刷新死循环" class="headerlink" title="客户端恢复要克制，避免刷新死循环"></a>客户端恢复要克制，避免刷新死循环</h2><p>即使发布流程修好，用户网络或扩展仍可能导致偶发加载失败。可以提供一次受控恢复，但不能所有 import 错误都无限刷新。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="variable constant_">RELOAD_KEY</span> = <span class="string">&#x27;chunk-reload-version&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">importWithRecovery</span>(<span class="params">loader</span>) &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="variable language_">module</span> = <span class="keyword">await</span> <span class="title function_">loader</span>()</span><br><span class="line">    <span class="variable language_">sessionStorage</span>.<span class="title function_">removeItem</span>(<span class="variable constant_">RELOAD_KEY</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">module</span></span><br><span class="line">  &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">    <span class="keyword">const</span> currentVersion = <span class="variable language_">window</span>.<span class="property">__APP_VERSION__</span></span><br><span class="line">    <span class="keyword">const</span> reloadedFor = <span class="variable language_">sessionStorage</span>.<span class="title function_">getItem</span>(<span class="variable constant_">RELOAD_KEY</span>)</span><br><span class="line"></span><br><span class="line">    <span class="title function_">reportError</span>(error, &#123; <span class="attr">type</span>: <span class="string">&#x27;dynamic-import&#x27;</span>, currentVersion &#125;)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (navigator.<span class="property">onLine</span> &amp;&amp; reloadedFor !== currentVersion) &#123;</span><br><span class="line">      <span class="variable language_">sessionStorage</span>.<span class="title function_">setItem</span>(<span class="variable constant_">RELOAD_KEY</span>, currentVersion)</span><br><span class="line">      location.<span class="title function_">reload</span>()</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="function">() =&gt;</span> &#123;&#125;)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">throw</span> error</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>路由层还要有 Error Boundary&#x2F;错误页，让第二次失败后显示“资源加载失败，请检查网络或手动刷新”，而不是白屏。对用户正在填写的长表单，自动刷新前最好保存草稿或征得确认。</p><h2 id="如果项目有-Service-Worker，再多检查一层"><a href="#如果项目有-Service-Worker，再多检查一层" class="headerlink" title="如果项目有 Service Worker，再多检查一层"></a>如果项目有 Service Worker，再多检查一层</h2><p>Service Worker 可能缓存旧 HTML、拦截 chunk 请求或在激活时清理旧 cache。排查时记录：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(&#123;</span><br><span class="line">  <span class="attr">controller</span>: navigator.<span class="property">serviceWorker</span>?.<span class="property">controller</span>?.<span class="property">scriptURL</span>,</span><br><span class="line">  <span class="attr">appVersion</span>: <span class="variable language_">window</span>.<span class="property">__APP_VERSION__</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>打开 DevTools Application 面板查看 active&#x2F;waiting worker 和 Cache Storage。不要把“Unregister service worker”当生产修复，它只清了测试机器；需要设计 SW 版本升级、旧客户端切换和缓存清理时机。</p><h2 id="监控从异常数量升级为发布指标"><a href="#监控从异常数量升级为发布指标" class="headerlink" title="监控从异常数量升级为发布指标"></a>监控从异常数量升级为发布指标</h2><p>修复后，我们按 release 聚合动态导入失败率，并在发布窗口设置阈值。上报字段至少包含：</p><ul><li>入口应用版本与失败 chunk URL；</li><li>页面打开时间、路由和网络状态；</li><li>HTTP status（能获取时）与 CDN request ID；</li><li>是否有 Service Worker；</li><li>自动恢复是否已执行、执行后是否成功。</li></ul><p>这样能区分某个旧版本资源被删、单一区域 CDN 故障，还是用户整体离线。</p><h2 id="结案"><a href="#结案" class="headerlink" title="结案"></a>结案</h2><p>这次事故最终没有靠 <code>location.reload()</code> 结案。根修复是静态资源不可变、资源先于 HTML 发布、旧文件保留回滚窗口，以及完整 release 可追踪。客户端只提供一次安全恢复和明确错误界面。</p><p>ChunkLoadError 看似前端路由 Bug，根因常在构建 hash、CDN 缓存和发布原子性。下次遇到时，先拿失败 URL、入口版本与 HTTP 状态，还原用户跨过了哪两次发布，比反复清浏览器缓存有效得多。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://webpack.js.org/guides/caching/">webpack：Caching</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Reference/Headers/Cache-Control">MDN：Cache-Control</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端排障 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端排障 </tag>
            
            <tag> 工程化 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Node.js 16：ESM、现代 V8 与前端工具运行时升级</title>
      <link href="/2021/04/20/frontend-chronicle-2021-04-node-16-esm/"/>
      <url>/2021/04/20/frontend-chronicle-2021-04-node-16-esm/</url>
      
        <content type="html"><![CDATA[<p>Node.js 16 发布时，前端团队已经不能再把 Node 当成“装 webpack 的那个东西”。构建、测试、代码生成、SSR 和包发布都运行在 Node 上。开发机是 14、CI 是 12、Docker 又是另一个版本时，同一份配置经常出现三种结果。</p><p>我们升级 Node 16 的同时，把一批工具脚本改成 ESM。真正花时间的不是把 <code>require</code> 换成 <code>import</code>，而是理解 package 边界、文件扩展名、条件 exports 和测试器各自如何解析模块。</p><h2 id="从一个读取配置的脚本开始迁移"><a href="#从一个读取配置的脚本开始迁移" class="headerlink" title="从一个读取配置的脚本开始迁移"></a>从一个读取配置的脚本开始迁移</h2><p>CommonJS 版本：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">&#x27;fs&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">&#x27;path&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> configPath = path.<span class="title function_">join</span>(__dirname, <span class="string">&#x27;site.config.json&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> config = <span class="title class_">JSON</span>.<span class="title function_">parse</span>(fs.<span class="title function_">readFileSync</span>(configPath, <span class="string">&#x27;utf8&#x27;</span>))</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = config</span><br></pre></td></tr></table></figure><p>在 <code>package.json</code> 声明模块类型：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;module&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;engines&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;node&quot;</span><span class="punctuation">:</span> <span class="string">&quot;&gt;=16 &lt;17&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>ESM 中没有 CommonJS 注入的 <code>__dirname</code>，需要基于当前模块 URL：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; readFile &#125; <span class="keyword">from</span> <span class="string">&#x27;node:fs/promises&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> configUrl = <span class="keyword">new</span> <span class="title function_">URL</span>(<span class="string">&#x27;./site.config.json&#x27;</span>, <span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">url</span>)</span><br><span class="line"><span class="keyword">const</span> config = <span class="title class_">JSON</span>.<span class="title function_">parse</span>(<span class="keyword">await</span> <span class="title function_">readFile</span>(configUrl, <span class="string">&#x27;utf8&#x27;</span>))</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> config</span><br></pre></td></tr></table></figure><p>这不是 ESM 少了一个功能，而是模块定位从“当前进程&#x2F;包装函数里的路径”转向标准 URL 语义。工具脚本里随处依赖 <code>__dirname</code> 时，迁移会比业务组件明显得多。</p><h2 id="type-决定-js-的解释方式"><a href="#type-决定-js-的解释方式" class="headerlink" title="type 决定 .js 的解释方式"></a><code>type</code> 决定 <code>.js</code> 的解释方式</h2><p>最近的 <code>package.json</code> 中 <code>&quot;type&quot;: &quot;module&quot;</code> 会让该包范围内的 <code>.js</code> 按 ESM 解释；<code>&quot;type&quot;: &quot;commonjs&quot;</code> 或未声明时按 CommonJS。<code>.mjs</code> 和 <code>.cjs</code> 可以显式覆盖。</p><p>这意味着不能只看源码里有没有 import 来猜。配置文件也在包范围内：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">project/</span><br><span class="line">  package.json       # type: module</span><br><span class="line">  build.config.js    # 会按 ESM 解释</span><br><span class="line">  legacy-tool.cjs    # 明确按 CommonJS</span><br></pre></td></tr></table></figure><p>升级时常见的 <code>require is not defined</code>，就是某个 <code>.js</code> 已经进入 ESM 范围却仍使用 CommonJS 全局。反向的 <code>Cannot use import statement outside a module</code>，通常说明文件仍被当作 CJS。</p><h2 id="ESM-导入对路径更严格"><a href="#ESM-导入对路径更严格" class="headerlink" title="ESM 导入对路径更严格"></a>ESM 导入对路径更严格</h2><p>Node ESM 的相对路径通常要写完整扩展名：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 不再依赖 CommonJS 风格的隐式 .js / index.js 查找</span></span><br><span class="line"><span class="keyword">import</span> &#123; loadConfig &#125; <span class="keyword">from</span> <span class="string">&#x27;./config/load-config.js&#x27;</span></span><br></pre></td></tr></table></figure><p>这种严格一开始显得啰嗦，却让运行时解析更明确。TypeScript 项目要让源码导入、编译输出和 <code>moduleResolution</code> 协调；不能让 IDE 能跳转，实际 Node 却找不到输出文件。</p><p>我们会直接查看 dist，而不是只看 TS 源码：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">node dist/cli.js --<span class="built_in">help</span></span><br></pre></td></tr></table></figure><p>发布包的最终 JavaScript 才是消费者运行的东西。</p><h2 id="CommonJS-与-ESM-互操作不是完全对称"><a href="#CommonJS-与-ESM-互操作不是完全对称" class="headerlink" title="CommonJS 与 ESM 互操作不是完全对称"></a>CommonJS 与 ESM 互操作不是完全对称</h2><p>ESM 可以导入很多 CommonJS 包，但默认&#x2F;命名导出的推断会受包写法影响。CommonJS 则不能同步 <code>require()</code> 一个纯 ESM 模块，通常需要动态 import：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// legacy-loader.cjs</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">loadFormatter</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="variable language_">module</span> = <span class="keyword">await</span> <span class="keyword">import</span>(<span class="string">&#x27;./formatter.js&#x27;</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="variable language_">module</span>.<span class="property">default</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = &#123; loadFormatter &#125;</span><br></pre></td></tr></table></figure><p>动态 import 返回 Promise，这会把异步边界向上传播。一个同步加载配置的 API 可能因此需要重构，而不是简单替换一行语法。</p><h2 id="发布“双格式包”要避免两个实例"><a href="#发布“双格式包”要避免两个实例" class="headerlink" title="发布“双格式包”要避免两个实例"></a>发布“双格式包”要避免两个实例</h2><p>库作者常想同时服务 ESM 和 CommonJS，通过 <code>exports</code> 提供条件入口：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;@acme/format&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;exports&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;.&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;import&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/index.js&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;require&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/index.cjs&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;types&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./dist/index.d.ts&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>但两套入口如果各自创建单例状态，同一进程中分别 import&#x2F;require 可能得到两个实例，形成所谓 dual package hazard。无状态工具问题较小，事件总线、缓存和 class identity 就很危险。</p><p>我们尽量让两个入口共享同一底层状态，或干脆只发布一种模块格式并明确最低 Node 版本。兼容范围越宽，测试矩阵越大。</p><h2 id="前端构建为什么被-Node-版本卡住"><a href="#前端构建为什么被-Node-版本卡住" class="headerlink" title="前端构建为什么被 Node 版本卡住"></a>前端构建为什么被 Node 版本卡住</h2><p>Vite、webpack、ESLint 和测试工具会逐步提高 Node 基线，以使用更新语法、标准 API 和 V8 能力。某次依赖升级在开发机正常，CI 报语法错误，最后只是 CI 镜像仍在 Node 12。</p><p>解决方式不是让每个人记住口头要求，而是把版本写进多个自动化入口：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">.nvmrc / .node-version</span><br><span class="line">package.json engines</span><br><span class="line">CI setup-node version</span><br><span class="line">Dockerfile FROM node:16.x（当时示例）</span><br></pre></td></tr></table></figure><p>同时让安装阶段在不支持版本上尽早失败。开发、CI 与生产尽量使用同一主版本，升级也要作为一次明确变更提交。</p><h2 id="原生依赖和-lockfile-要一起验证"><a href="#原生依赖和-lockfile-要一起验证" class="headerlink" title="原生依赖和 lockfile 要一起验证"></a>原生依赖和 lockfile 要一起验证</h2><p>Node 主版本升级会影响原生 addon ABI、npm 版本与 lockfile 行为。我们检查了图片处理、旧版 Sass 等含原生二进制的依赖，删除 node_modules 后重新安装，而不是把旧目录直接带过去。</p><p>CI 验证包含：全新安装、单元测试、生产构建、SSR 启动和一次容器冒烟。仅仅 <code>node -v</code> 正确，不代表原生包已经为新 ABI 重新构建。</p><h2 id="这次升级留下的模块边界"><a href="#这次升级留下的模块边界" class="headerlink" title="这次升级留下的模块边界"></a>这次升级留下的模块边界</h2><p>Node 16 并不是 ESM 的起点，也不是模块生态问题的终点，但它处在越来越多工具开始把现代 Node 与 ESM 当作正常路径的阶段。对我们来说，最大收获是项目不再混用“能碰巧跑”的模块约定：包级 type 明确、相对导入完整、发布入口受 exports 控制、CI 直接执行最终产物。</p><p>如果现在回头维护那类项目，我仍会先统一 Node 版本，再迁移模块格式；先跑通最终 JavaScript，再修 IDE 配置。模块错误往往不是一行 import 的错，而是源码、编译器、package.json 和运行时对同一个文件做出了不同解释。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://nodejs.org/en/blog/release/v16.0.0">Node.js 16.0.0 发布说明</a></li><li><a href="https://nodejs.org/api/esm.html">Node.js：ECMAScript modules</a></li><li><a href="https://nodejs.org/api/packages.html">Node.js：Packages</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 2021 </tag>
            
            <tag> Node.js </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vite 2：从 Vue 工具成长为框架无关构建平台</title>
      <link href="/2021/02/16/frontend-chronicle-2021-02-vite-2/"/>
      <url>/2021/02/16/frontend-chronicle-2021-02-vite-2/</url>
      
        <content type="html"><![CDATA[<p>Vite 2.0 是我第一次敢把 Vite 用在非 Vue 项目里的版本。它把核心重写成框架无关，Vue、React、Preact 等能力由插件提供；插件接口又尽量复用 Rollup 生态。早期“给 Vue 单文件组件用的快速开发服务器”，到这一版才真正有了通用构建平台的轮廓。</p><p>我当时拿一个独立的 React 数据看板做迁移。项目不大，webpack 配置却有 Babel、CSS Modules、SVG loader、环境变量和代理。这个规模刚好能看到差异，也不至于把主站发布押在一套新工具上。</p><h2 id="迁移第一天：先让入口跑起来"><a href="#迁移第一天：先让入口跑起来" class="headerlink" title="迁移第一天：先让入口跑起来"></a>迁移第一天：先让入口跑起来</h2><p>webpack 入口由配置指定，Vite 默认把 HTML 当作应用入口。旧项目的模板：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;root&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><p>改成显式 ESM：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;root&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">&quot;module&quot;</span> <span class="attr">src</span>=<span class="string">&quot;/src/main.jsx&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure><p>再安装官方 React 插件：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// vite.config.js</span></span><br><span class="line"><span class="keyword">import</span> &#123; defineConfig &#125; <span class="keyword">from</span> <span class="string">&#x27;vite&#x27;</span></span><br><span class="line"><span class="keyword">import</span> react <span class="keyword">from</span> <span class="string">&#x27;@vitejs/plugin-react&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">plugins</span>: [<span class="title function_">react</span>()],</span><br><span class="line">  <span class="attr">server</span>: &#123;</span><br><span class="line">    <span class="attr">proxy</span>: &#123;</span><br><span class="line">      <span class="string">&#x27;/api&#x27;</span>: <span class="string">&#x27;http://localhost:3000&#x27;</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>启动速度明显改善，但“首页能打开”只完成了不到一半。真正迁移成本藏在 webpack 特有行为里。</p><h2 id="把-webpack-特有能力列成清单"><a href="#把-webpack-特有能力列成清单" class="headerlink" title="把 webpack 特有能力列成清单"></a>把 webpack 特有能力列成清单</h2><p>我们没有边报错边搜索，而是先搜索配置和源码：</p><ul><li><code>require.context</code> 是否用于批量加载页面；</li><li>SVG 是 URL、raw 文本还是转换成 React 组件；</li><li>是否依赖 <code>process.env</code> 和 DefinePlugin；</li><li>loader 有没有注入全局变量或修改源码；</li><li><code>publicPath</code> 对应的部署子路径是什么；</li><li>Node 的 <code>Buffer</code>、<code>process</code>、<code>path</code> 是否意外进入浏览器代码；</li><li>动态导入路径能否被静态分析。</li></ul><p>比如 <code>require.context</code> 需要改成 <code>import.meta.glob</code>：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> modules = <span class="keyword">import</span>.<span class="property">meta</span>.<span class="title function_">glob</span>(<span class="string">&#x27;./pages/*.jsx&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">loadPage</span>(<span class="params">name</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> loader = modules[<span class="string">`./pages/<span class="subst">$&#123;name&#125;</span>.jsx`</span>]</span><br><span class="line">  <span class="keyword">if</span> (!loader) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`Unknown page: <span class="subst">$&#123;name&#125;</span>`</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="title function_">loader</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Vite 能看到 glob 模式并生成对应动态导入。把任意用户输入直接拼进 <code>import()</code>，生产构建通常无法知道要包含哪些文件。</p><h2 id="esbuild-快在它负责的那一段"><a href="#esbuild-快在它负责的那一段" class="headerlink" title="esbuild 快在它负责的那一段"></a>esbuild 快在它负责的那一段</h2><p>Vite 2 使用 esbuild 做依赖预构建，把 CommonJS&#x2F;UMD 依赖转换成 ESM，并合并依赖内部的大量模块请求。官方发布说明给出的重依赖示例有显著提升，但项目中的实际数字仍要自己测。</p><p>业务源码的转换、框架 Fast Refresh 和生产打包不是全都由 esbuild 一手包办。React 插件负责 JSX&#x2F;Fast Refresh 语义，生产构建仍基于 Rollup。理解职责以后，遇到问题才能判断是依赖优化、插件 transform 还是生产 bundle。</p><p>有个内部包同时提供 CJS 与 ESM，开发时入口解析正确，构建时却走了另一个条件导出。我们最终修的是包的 <code>exports</code>，不是给 Vite 加更多 alias。工具升级经常把原本模糊的包边界暴露出来。</p><h2 id="环境变量迁移必须逐个确认"><a href="#环境变量迁移必须逐个确认" class="headerlink" title="环境变量迁移必须逐个确认"></a>环境变量迁移必须逐个确认</h2><p>旧项目：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> apiBase = process.<span class="property">env</span>.<span class="property">REACT_APP_API_BASE</span></span><br></pre></td></tr></table></figure><p>Vite 中使用：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> apiBase = <span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">env</span>.<span class="property">VITE_API_BASE</span></span><br></pre></td></tr></table></figure><p>我们把环境变量做成一张表：变量名、哪个环境提供、能否公开、缺失时是否阻止构建。任何进入 <code>import.meta.env</code> 客户端代码的值都可能出现在最终 bundle 中，不能放服务端 secret。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> apiBase = <span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">env</span>.<span class="property">VITE_API_BASE</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!apiBase) &#123;</span><br><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;VITE_API_BASE is required&#x27;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>相比静默使用空字符串，构建或启动时明确失败更容易排查。</p><h2 id="写一个小插件理解两套生命周期"><a href="#写一个小插件理解两套生命周期" class="headerlink" title="写一个小插件理解两套生命周期"></a>写一个小插件理解两套生命周期</h2><p>项目需要在构建时生成版本模块。我们用一个很小的插件完成：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">buildInfoPlugin</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> virtualId = <span class="string">&#x27;virtual:build-info&#x27;</span></span><br><span class="line">  <span class="keyword">const</span> resolvedId = <span class="string">&#x27;\0&#x27;</span> + virtualId</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;build-info&#x27;</span>,</span><br><span class="line">    <span class="title function_">resolveId</span>(<span class="params">id</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (id === virtualId) <span class="keyword">return</span> resolvedId</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="title function_">load</span>(<span class="params">id</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (id === resolvedId) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">`export default <span class="subst">$&#123;<span class="built_in">JSON</span>.stringify(&#123;</span></span></span><br><span class="line"><span class="subst"><span class="string">          version: process.env.APP_VERSION || <span class="string">&#x27;dev&#x27;</span></span></span></span><br><span class="line"><span class="subst"><span class="string">        &#125;)&#125;</span>`</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>resolveId</code>、<code>load</code> 来自 Rollup 风格钩子，开发和构建都能复用。Vite 还提供只在 dev server 或特定阶段执行的扩展。插件作者必须明确钩子运行在哪个环境，不能假设始终存在浏览器或始终存在最终 bundle。</p><h2 id="CSS-和静态资源是最容易漏测的部分"><a href="#CSS-和静态资源是最容易漏测的部分" class="headerlink" title="CSS 和静态资源是最容易漏测的部分"></a>CSS 和静态资源是最容易漏测的部分</h2><p>JS 跑通以后，我们重点回归了：</p><ul><li>CSS Modules 的类名与组合规则；</li><li>Sass 全局变量是否通过额外配置注入；</li><li><code>url()</code> 在嵌套文件中的重写结果；</li><li>小图片内联阈值与 CDN 地址；</li><li>动态路由 chunk 对应 CSS 是否会同步加载；</li><li>部署在 <code>/dashboard/</code> 子路径时 <code>base</code> 是否正确。</li></ul><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">base</span>: <span class="string">&#x27;/dashboard/&#x27;</span>,</span><br><span class="line">  <span class="attr">plugins</span>: [<span class="title function_">react</span>()]</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>本地根路径正常不代表子路径发布正常。我们用和生产一致的前缀启动静态服务器，直接刷新二级路由，检查 HTML、JS、CSS 和图片请求。</p><h2 id="最终为什么留下-Vite-2"><a href="#最终为什么留下-Vite-2" class="headerlink" title="最终为什么留下 Vite 2"></a>最终为什么留下 Vite 2</h2><p>迁移后冷启动与热更新改善是最直观的收益，更长期的价值是配置回到了标准 ESM、Rollup 插件和 Web API 附近。这个 React 项目没有因为 Vite 源于 Vue 而得到“二等支持”，说明框架能力下沉到插件的方向是有效的。</p><p>但我没有因此立刻迁移所有 webpack 项目。依赖 Module Federation、自定义 loader 或复杂多页发布的系统，需要单独计算迁移收益。Vite 2 适合成为新项目和相对标准项目的默认候选，不是删掉既有工具链的行政命令。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://vite.dev/blog/announcing-vite2.html">Vite 2.0 发布说明</a></li><li><a href="https://vite.dev/guide/api-plugin">Vite：Plugin API</a></li><li><a href="https://vite.dev/guide/features.html#glob-import">Vite：Glob Import</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Vite </tag>
            
            <tag> 2021 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>React Server Components：组件边界延伸到服务器</title>
      <link href="/2020/12/21/frontend-chronicle-2020-12-react-server-components-research/"/>
      <url>/2020/12/21/frontend-chronicle-2020-12-react-server-components-research/</url>
      
        <content type="html"><![CDATA[<p>2020 年底第一次看 React Server Components 演示时，我把它误解成“另一种 SSR”。两者都在服务器做事，但解决的问题并不相同：SSR 主要把组件首次渲染成 HTML，Server Components 则让一部分组件长期只在服务器执行，并把结果以 React 能继续组合的形式交给客户端。</p><p>当时它还是研究预览，不适合拿来重写生产项目。它的价值在于提出了一个很有冲击力的问题：既然某些组件只负责读取数据库和拼装内容，为什么必须把它们的代码、依赖和数据获取逻辑全部发到浏览器？</p><h2 id="用一个产品详情页区分三件事"><a href="#用一个产品详情页区分三件事" class="headerlink" title="用一个产品详情页区分三件事"></a>用一个产品详情页区分三件事</h2><p>假设页面包含商品信息、评论和“加入购物车”按钮。</p><p>传统纯客户端渲染大致会发送应用 JavaScript，浏览器执行后再请求数据：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">ProductPage</span>(<span class="params">&#123; id &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [product, setProduct] = <span class="title function_">useState</span>(<span class="literal">null</span>)</span><br><span class="line"></span><br><span class="line">  <span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">fetch</span>(<span class="string">`/api/products/<span class="subst">$&#123;id&#125;</span>`</span>)</span><br><span class="line">      .<span class="title function_">then</span>(<span class="function"><span class="params">response</span> =&gt;</span> response.<span class="title function_">json</span>())</span><br><span class="line">      .<span class="title function_">then</span>(setProduct)</span><br><span class="line">  &#125;, [id])</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!product) <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">Skeleton</span> /&gt;</span></span></span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">ProductView</span> <span class="attr">product</span>=<span class="string">&#123;product&#125;</span> /&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>SSR 可以在服务器首次执行相似组件并返回 HTML，让用户更早看到内容；随后浏览器下载对应 JavaScript 并水合，组件成为可交互应用。</p><p>Server Component 的设想是：读取产品信息的组件只留在服务器，它的实现与数据库依赖不进入客户端 bundle；真正需要状态、事件和浏览器 API 的购物车按钮成为 Client Component。</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 概念示意，具体语法应以所用框架版本为准</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">ProductPage</span>(<span class="params">&#123; id &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> product = <span class="keyword">await</span> db.<span class="property">product</span>.<span class="title function_">findById</span>(id)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">article</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">ProductDescription</span> <span class="attr">product</span>=<span class="string">&#123;product&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">AddToCartButton</span> <span class="attr">productId</span>=<span class="string">&#123;product.id&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">article</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>服务器组件的输出不是简单 HTML 字符串结束，而是能与客户端组件引用组合、逐步传输的描述。这样页面树可以跨越服务器和浏览器边界。</p><h2 id="“零客户端-JS”只属于服务器那一侧"><a href="#“零客户端-JS”只属于服务器那一侧" class="headerlink" title="“零客户端 JS”只属于服务器那一侧"></a>“零客户端 JS”只属于服务器那一侧</h2><p>Server Component 自身代码不发到浏览器，但页面只要有交互，Client Component 及其依赖仍会进入客户端。边界放在哪里直接决定多少 JavaScript 被发送。</p><p>如果把整个页面标成客户端组件，只为了一个按钮使用 <code>useState</code>，它导入的子树可能一起进入客户端边界。反过来，把静态内容与数据读取留在服务器，只把交互叶子下沉为客户端组件，才能保留收益。</p><p>这不是追求“一个 use client 都没有”。编辑器、地图、拖拽等功能本来就需要浏览器。好的边界是让每段代码运行在真正需要它的环境。</p><h2 id="数据访问离组件更近，安全边界也必须更清楚"><a href="#数据访问离组件更近，安全边界也必须更清楚" class="headerlink" title="数据访问离组件更近，安全边界也必须更清楚"></a>数据访问离组件更近，安全边界也必须更清楚</h2><p>服务器组件可以接触数据库、文件和内部服务，这很方便，但不能把“服务器执行”误认为“自动安全”。组件接收的路由参数、Cookie 与客户端输入仍然不可信，查询前要做鉴权和校验。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">AccountOrders</span>(<span class="params">&#123; accountId, viewer &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!viewer) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;Unauthenticated&#x27;</span>)</span><br><span class="line">  <span class="keyword">if</span> (viewer.<span class="property">accountId</span> !== accountId) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;Forbidden&#x27;</span>)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> orders = <span class="keyword">await</span> orderRepository.<span class="title function_">findByAccount</span>(accountId)</span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">OrderList</span> <span class="attr">orders</span>=<span class="string">&#123;orders&#125;</span> /&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>更重要的是只把可安全序列化的数据传给客户端组件。数据库实体可能带内部成本价、权限字段或 token，不能因为 JSX props 写起来方便就整个传下去。</p><h2 id="包体积收益来自依赖留在服务器"><a href="#包体积收益来自依赖留在服务器" class="headerlink" title="包体积收益来自依赖留在服务器"></a>包体积收益来自依赖留在服务器</h2><p>演示中很吸引人的部分，是服务器组件可以使用 Markdown 解析、日期处理等较大依赖，而这些实现不会成为客户端 JavaScript。假设文章页在服务器把 Markdown 转成 React 节点，浏览器只接收结果和需要交互的部分。</p><p>不过“依赖不进客户端”不等于没有成本。它仍占用服务器计算、内存和部署体积，还涉及缓存与流式传输。性能评估要看整体：服务器响应时间、传输内容、客户端 JS 和交互时间，而不是只看 bundle 少了多少。</p><h2 id="与-SSR-组合，而不是互相替代"><a href="#与-SSR-组合，而不是互相替代" class="headerlink" title="与 SSR 组合，而不是互相替代"></a>与 SSR 组合，而不是互相替代</h2><p>一个常见误区是问“用了 RSC 还要不要 SSR”。在框架实现中，服务器组件树仍可以参与服务端生成初始 HTML，客户端组件再水合。RSC 决定组件代码与数据在哪一侧执行&#x2F;传输，SSR 决定首屏 HTML 如何产生，两者可以组合。</p><p>另一个误区是把 RSC 当成 API 路由的简单替代。组件协议需要打包器识别模块边界、框架处理缓存&#x2F;导航&#x2F;流和序列化。官方最初就强调它需要与框架深度集成。业务团队自己拼一套协议，维护成本远高于写一个 fetch。</p><h2 id="2020-年我会怎样对待这项研究"><a href="#2020-年我会怎样对待这项研究" class="headerlink" title="2020 年我会怎样对待这项研究"></a>2020 年我会怎样对待这项研究</h2><p>当时最合理的动作不是生产落地，而是调整组件边界意识：</p><ol><li>不让纯展示组件偷偷依赖 window 或全局可变状态；</li><li>把浏览器交互收敛到较小区域；</li><li>数据访问函数明确权限与序列化边界；</li><li>避免在应用各处重复同一数据请求；</li><li>等成熟框架提供完整实现、升级与部署方案。</li></ol><p>这些准备即使后来不使用 RSC，也能让 SSR、测试和代码分割更清楚。</p><h2 id="回头看，真正改变的是组件的地理边界"><a href="#回头看，真正改变的是组件的地理边界" class="headerlink" title="回头看，真正改变的是组件的地理边界"></a>回头看，真正改变的是组件的地理边界</h2><p>过去“React 组件”几乎默认意味着会在浏览器运行，服务端更多是模板或 API。Server Components 把组件模型延伸到服务器，让数据获取、模块图和渲染边界被同一套框架理解。</p><p>它同时增加了需要学习的新问题：哪些 props 可序列化、缓存何时失效、客户端边界怎样扩散、服务器入口如何鉴权。减少客户端 JavaScript的收益是真的，复杂度也没有消失，只是从手写 API 与 hydration 转移到框架管理的跨环境模型中。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://legacy.reactjs.org/blog/2020/12/21/data-fetching-with-react-server-components.html">React Server Components 研究预览</a></li><li><a href="https://react.dev/reference/rsc/server-components">React：Server Components</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> React </tag>
            
            <tag> 2020 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>webpack 5：长期缓存与 Module Federation</title>
      <link href="/2020/10/10/frontend-chronicle-2020-10-webpack-5/"/>
      <url>/2020/10/10/frontend-chronicle-2020-10-webpack-5/</url>
      
        <content type="html"><![CDATA[<p>webpack 5 升级时，我们同时做了两件性质完全不同的事：一是把日常生产构建从十几分钟降下来，二是验证两个团队能否独立发布同一个后台里的业务模块。前者靠文件系统缓存和更稳定的产物策略，后者用了 Module Federation。</p><p>这两个功能都叫“工程化升级”，风险却不在一个量级。缓存错误最多让构建变慢或产物不对，远程模块则把原本在构建时确定的依赖搬到了用户运行时。后者已经带有分布式系统的味道，不能因为配置只有十几行就低估它。</p><h2 id="先把构建缓存做成可验证的改动"><a href="#先把构建缓存做成可验证的改动" class="headerlink" title="先把构建缓存做成可验证的改动"></a>先把构建缓存做成可验证的改动</h2><p>webpack 5 提供持久化文件系统缓存后，我们没有只加一句 <code>cache: { type: &#39;filesystem&#39; }</code> 就结束：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">&#x27;path&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = &#123;</span><br><span class="line">  <span class="attr">cache</span>: &#123;</span><br><span class="line">    <span class="attr">type</span>: <span class="string">&#x27;filesystem&#x27;</span>,</span><br><span class="line">    <span class="attr">buildDependencies</span>: &#123;</span><br><span class="line">      <span class="attr">config</span>: [__filename]</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">output</span>: &#123;</span><br><span class="line">    <span class="attr">path</span>: path.<span class="title function_">resolve</span>(__dirname, <span class="string">&#x27;dist&#x27;</span>),</span><br><span class="line">    <span class="attr">filename</span>: <span class="string">&#x27;[name].[contenthash:8].js&#x27;</span>,</span><br><span class="line">    <span class="attr">chunkFilename</span>: <span class="string">&#x27;[name].[contenthash:8].js&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">optimization</span>: &#123;</span><br><span class="line">    <span class="attr">moduleIds</span>: <span class="string">&#x27;deterministic&#x27;</span>,</span><br><span class="line">    <span class="attr">chunkIds</span>: <span class="string">&#x27;deterministic&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>验证分三组：清空缓存冷构建、代码不变重复构建、只改一个叶子模块后增量构建。缓存命中带来的时间下降要记录，配置文件、lockfile 或环境变化后也必须正确失效。</p><p>缓存最危险的状态不是“没命中”，而是错误命中并复用过期结果。所以自定义 loader 如果依赖外部配置，需要把依赖告诉 webpack；CI 也不能在不同 Node&#x2F;依赖环境之间盲目复用同一缓存目录。</p><h2 id="contenthash-稳不稳定，用两次构建就能看出来"><a href="#contenthash-稳不稳定，用两次构建就能看出来" class="headerlink" title="contenthash 稳不稳定，用两次构建就能看出来"></a>contenthash 稳不稳定，用两次构建就能看出来</h2><p>长期缓存的目标不是文件名看起来有 hash，而是改一个模块时，无关资源的 hash 不变。我们保存第一次 dist，修改订单页一个文案再构建，然后比较清单：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">find dist -<span class="built_in">type</span> f -maxdepth 2 | <span class="built_in">sort</span></span><br></pre></td></tr></table></figure><p>如果公共运行时代码、所有页面 chunk 都跟着变化，用户仍要重新下载。确定性 module&#x2F;chunk ID 改善了这种稳定性，但 splitChunks、runtime 拆分和插件输出也会影响结果。最后必须看真实产物，而不是只看配置是否用了 <code>contenthash</code>。</p><h2 id="Module-Federation-的-POC-很快就跑通了"><a href="#Module-Federation-的-POC-很快就跑通了" class="headerlink" title="Module Federation 的 POC 很快就跑通了"></a>Module Federation 的 POC 很快就跑通了</h2><p>结算团队暴露购物车模块：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> &#123; <span class="title class_">ModuleFederationPlugin</span> &#125; = <span class="built_in">require</span>(<span class="string">&#x27;webpack&#x27;</span>).<span class="property">container</span></span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = &#123;</span><br><span class="line">  <span class="attr">plugins</span>: [</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">ModuleFederationPlugin</span>(&#123;</span><br><span class="line">      <span class="attr">name</span>: <span class="string">&#x27;checkout&#x27;</span>,</span><br><span class="line">      <span class="attr">filename</span>: <span class="string">&#x27;remoteEntry.js&#x27;</span>,</span><br><span class="line">      <span class="attr">exposes</span>: &#123;</span><br><span class="line">        <span class="string">&#x27;./CartPanel&#x27;</span>: <span class="string">&#x27;./src/CartPanel&#x27;</span></span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="attr">shared</span>: &#123;</span><br><span class="line">        <span class="attr">react</span>: &#123; <span class="attr">singleton</span>: <span class="literal">true</span>, <span class="attr">requiredVersion</span>: <span class="string">&#x27;^17.0.0&#x27;</span> &#125;,</span><br><span class="line">        <span class="string">&#x27;react-dom&#x27;</span>: &#123; <span class="attr">singleton</span>: <span class="literal">true</span>, <span class="attr">requiredVersion</span>: <span class="string">&#x27;^17.0.0&#x27;</span> &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>宿主配置 remote 后，业务里可以动态导入：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title class_">CartPanel</span> = <span class="title class_">React</span>.<span class="title function_">lazy</span>(<span class="function">() =&gt;</span> <span class="keyword">import</span>(<span class="string">&#x27;checkout/CartPanel&#x27;</span>))</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">CheckoutArea</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">ErrorBoundary</span> <span class="attr">fallback</span>=<span class="string">&#123;</span>&lt;<span class="attr">LegacyCartLink</span> /&gt;</span>&#125;&gt;</span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">React.Suspense</span> <span class="attr">fallback</span>=<span class="string">&#123;</span>&lt;<span class="attr">CartSkeleton</span> /&gt;</span>&#125;&gt;</span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">CartPanel</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;/<span class="name">React.Suspense</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">ErrorBoundary</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>本地两台 dev server 上很顺利。真正的问题都在“如果”：如果 remoteEntry 超时，如果远程刚发布宿主还在旧版本，如果共享 React 不兼容，如果 CDN 缓存了入口但 chunk 已更新。</p><h2 id="远程依赖把失败时间推到了用户面前"><a href="#远程依赖把失败时间推到了用户面前" class="headerlink" title="远程依赖把失败时间推到了用户面前"></a>远程依赖把失败时间推到了用户面前</h2><p>普通 npm 依赖版本不兼容，通常在安装、构建或测试阶段暴露。Federation 远程模块在浏览器运行时解析与加载，故障可能只发生在某个地区、某个缓存节点或某组版本组合。</p><p>因此上线前我们补了这些基础设施：</p><ul><li>远程模块加载失败时显示旧入口，不让整个后台白屏；</li><li>remoteEntry 使用短缓存，内容 hash chunk 使用长期不可变缓存；</li><li>每次请求记录宿主版本、远程版本和失败 URL；</li><li>发布后做跨应用契约测试，回滚时保留对应静态资源；</li><li>共享依赖只保留真正需要单例的包，不把所有依赖都 shared。</li></ul><p>React 同时加载两份可能导致 Hooks 与 context 问题，所以常设为 singleton；工具函数库未必需要共享，强行共享反而增加版本协商复杂度。</p><h2 id="独立部署不是由插件赋予的"><a href="#独立部署不是由插件赋予的" class="headerlink" title="独立部署不是由插件赋予的"></a>独立部署不是由插件赋予的</h2><p>Module Federation 经常和微前端一起出现，但配置 remote 不会自动让团队变独立。如果两个模块共用数据库表、同一天必须同步接口、发布还要在一个群里手工排顺序，它们依然强耦合。</p><p>我们用几个问题判断边界是否合理：</p><ol><li>远程模块有独立业务所有者和发布节奏吗？</li><li>输入 props&#x2F;事件是否能形成稳定契约，而不是直接读取宿主内部 store？</li><li>远程不可用时，宿主能否降级？</li><li>设计系统、鉴权和路由由谁提供，版本怎样兼容？</li><li>线上问题能否明确定位到某一版本的某个远程？</li></ol><p>答不清这些问题时，先拆 npm 包或仓库模块通常更便宜。</p><h2 id="Node-polyfill-变化是迁移中的隐藏成本"><a href="#Node-polyfill-变化是迁移中的隐藏成本" class="headerlink" title="Node polyfill 变化是迁移中的隐藏成本"></a>Node polyfill 变化是迁移中的隐藏成本</h2><p>webpack 5 不再像过去那样自动为大量 Node 核心模块提供浏览器 polyfill。某些前端依赖间接使用 <code>crypto</code>、<code>buffer</code> 或 <code>process</code>，升级后会直接报错。</p><p>我们没有第一时间把所有 polyfill 全补回来，而是先追依赖链：浏览器代码为什么需要 Node API？能否换成 Web API 或浏览器版本入口？只有确实合理的依赖才显式配置 fallback。全量补回会把历史包袱和体积一起带入新构建。</p><h2 id="最后的结果"><a href="#最后的结果" class="headerlink" title="最后的结果"></a>最后的结果</h2><p>文件系统缓存给所有开发者带来稳定收益，很快进入主线。Module Federation 只用于两个确实有独立发布需求的区域，没有推广到整站。技术能力可以使用，不意味着适合每一个边界。</p><p>webpack 5 对我最大的提醒，是“构建工具功能”也可能改变系统架构。持久化缓存要验证失效，长期缓存要比较 hash，远程模块要按网络依赖处理。把每项能力放到它真实的失败范围里评估，升级才不会停在一份漂亮配置上。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://webpack.js.org/blog/2020-10-10-webpack-5-release/">webpack 5 发布公告</a></li><li><a href="https://webpack.js.org/concepts/module-federation/">webpack：Module Federation</a></li><li><a href="https://webpack.js.org/guides/build-performance/">webpack：Build Performance</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> webpack </tag>
            
            <tag> 2020 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vue 3：Composition API、Proxy 与 TypeScript 基础重构</title>
      <link href="/2020/09/18/frontend-chronicle-2020-09-vue-3-composition-api/"/>
      <url>/2020/09/18/frontend-chronicle-2020-09-vue-3-composition-api/</url>
      
        <content type="html"><![CDATA[<p>Vue 3 正式发布后，我挑了一张 900 多行的会员列表页做实验。它包含筛选、分页、URL 同步、导出和权限判断。Options API 本身没有坏，真正的问题是同一项功能被拆在 <code>data</code>、<code>computed</code>、<code>watch</code> 和 <code>methods</code> 几个区域里。查“筛选条件为什么重置”时，要在文件里上下翻很久。</p><p>Composition API 让我们可以按业务关注点把代码放回一起；Proxy 则补上 Vue 2 响应式在属性新增、数组索引和集合类型上的一些限制。迁移完以后文件没有神奇地变短，但每一块逻辑的输入、输出和清理路径清楚了很多。</p><h2 id="先不拆-composable，只在-setup-中重新分组"><a href="#先不拆-composable，只在-setup-中重新分组" class="headerlink" title="先不拆 composable，只在 setup 中重新分组"></a>先不拆 composable，只在 setup 中重新分组</h2><p>旧组件的结构类似这样：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="title function_">data</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      <span class="attr">filters</span>: &#123; <span class="attr">keyword</span>: <span class="string">&#x27;&#x27;</span>, <span class="attr">level</span>: <span class="string">&#x27;all&#x27;</span> &#125;,</span><br><span class="line">      <span class="attr">rows</span>: [],</span><br><span class="line">      <span class="attr">loading</span>: <span class="literal">false</span>,</span><br><span class="line">      <span class="attr">page</span>: <span class="number">1</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">computed</span>: &#123;</span><br><span class="line">    <span class="title function_">canExport</span>(<span class="params"></span>) &#123; <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">$store</span>.<span class="property">getters</span>.<span class="title function_">hasPermission</span>(<span class="string">&#x27;member:export&#x27;</span>) &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">watch</span>: &#123;</span><br><span class="line">    <span class="attr">filters</span>: &#123;</span><br><span class="line">      <span class="attr">deep</span>: <span class="literal">true</span>,</span><br><span class="line">      <span class="title function_">handler</span>(<span class="params"></span>) &#123; <span class="variable language_">this</span>.<span class="property">page</span> = <span class="number">1</span>; <span class="variable language_">this</span>.<span class="title function_">loadMembers</span>() &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">methods</span>: &#123;</span><br><span class="line">    <span class="keyword">async</span> <span class="title function_">loadMembers</span>(<span class="params"></span>) &#123; <span class="comment">/* ... */</span> &#125;,</span><br><span class="line">    <span class="keyword">async</span> <span class="title function_">exportMembers</span>(<span class="params"></span>) &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>第一步没有急着抽文件，只按“查询会员”这项能力放在一起：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; computed, reactive, ref, watch &#125; <span class="keyword">from</span> <span class="string">&#x27;vue&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="title function_">setup</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> filters = <span class="title function_">reactive</span>(&#123; <span class="attr">keyword</span>: <span class="string">&#x27;&#x27;</span>, <span class="attr">level</span>: <span class="string">&#x27;all&#x27;</span> &#125;)</span><br><span class="line">    <span class="keyword">const</span> rows = ref&lt;<span class="title class_">Member</span>[]&gt;([])</span><br><span class="line">    <span class="keyword">const</span> loading = <span class="title function_">ref</span>(<span class="literal">false</span>)</span><br><span class="line">    <span class="keyword">const</span> page = <span class="title function_">ref</span>(<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">loadMembers</span>(<span class="params"></span>) &#123;</span><br><span class="line">      loading.<span class="property">value</span> = <span class="literal">true</span></span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">        rows.<span class="property">value</span> = <span class="keyword">await</span> memberApi.<span class="title function_">list</span>(&#123; ...filters, <span class="attr">page</span>: page.<span class="property">value</span> &#125;)</span><br><span class="line">      &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        loading.<span class="property">value</span> = <span class="literal">false</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">watch</span>(</span><br><span class="line">      <span class="function">() =&gt;</span> [filters.<span class="property">keyword</span>, filters.<span class="property">level</span>],</span><br><span class="line">      <span class="function">() =&gt;</span> &#123;</span><br><span class="line">        page.<span class="property">value</span> = <span class="number">1</span></span><br><span class="line">        <span class="title function_">loadMembers</span>()</span><br><span class="line">      &#125;</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &#123; filters, rows, loading, page, loadMembers &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>先保证行为与 Vue 2 版本一致，测试通过后再抽 composable。直接边迁移边重新设计，出问题时很难判断是 API 变化还是业务重构。</p><h2 id="ref-与-reactive-的选择不是宗教问题"><a href="#ref-与-reactive-的选择不是宗教问题" class="headerlink" title="ref 与 reactive 的选择不是宗教问题"></a><code>ref</code> 与 <code>reactive</code> 的选择不是宗教问题</h2><p>我们最后形成的约定很实用：单个值和可能整体替换的对象优先 <code>ref</code>；一组始终作为整体修改的表单字段可以 <code>reactive</code>。重要的是调用方能预测类型和更新方式。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> loading = <span class="title function_">ref</span>(<span class="literal">false</span>)</span><br><span class="line"><span class="keyword">const</span> selectedMember = ref&lt;<span class="title class_">Member</span> | <span class="literal">null</span>&gt;(<span class="literal">null</span>)</span><br><span class="line"><span class="keyword">const</span> filters = <span class="title function_">reactive</span>(&#123; <span class="attr">keyword</span>: <span class="string">&#x27;&#x27;</span>, <span class="attr">level</span>: <span class="string">&#x27;all&#x27;</span> &#125;)</span><br></pre></td></tr></table></figure><p><code>ref</code> 在 JavaScript 中用 <code>.value</code>，模板会自动解包。<code>reactive</code> 返回 Proxy，不能随意解构成普通值：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> state = <span class="title function_">reactive</span>(&#123; <span class="attr">page</span>: <span class="number">1</span>, <span class="attr">pageSize</span>: <span class="number">20</span> &#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// page 只是当前数字，不再与 state.page 建立响应关系</span></span><br><span class="line"><span class="keyword">const</span> &#123; page &#125; = state</span><br><span class="line"></span><br><span class="line"><span class="comment">// 需要解构时保留 ref</span></span><br><span class="line"><span class="keyword">const</span> &#123; <span class="attr">page</span>: pageRef &#125; = <span class="title function_">toRefs</span>(state)</span><br></pre></td></tr></table></figure><p>这种边界如果团队没有约定，composable 之间传来传去，很快会出现“日志已经变了，模板却没更新”的问题。</p><h2 id="Proxy-让新增属性不再需要特殊-API"><a href="#Proxy-让新增属性不再需要特殊-API" class="headerlink" title="Proxy 让新增属性不再需要特殊 API"></a>Proxy 让新增属性不再需要特殊 API</h2><p>Vue 2 基于 <code>Object.defineProperty</code> 初始化已知属性，运行时新增属性需要 <code>Vue.set</code> 才能进入响应系统。Vue 3 的 Proxy 可以拦截新增、删除和集合操作：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> member = <span class="title function_">reactive</span>(&#123; <span class="attr">name</span>: <span class="string">&#x27;Jevon&#x27;</span> &#125;)</span><br><span class="line"></span><br><span class="line">member.<span class="property">level</span> = <span class="string">&#x27;vip&#x27;</span></span><br><span class="line"><span class="keyword">delete</span> member.<span class="property">name</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> selectedIds = <span class="title function_">reactive</span>(<span class="keyword">new</span> <span class="title class_">Set</span>&lt;<span class="built_in">number</span>&gt;())</span><br><span class="line">selectedIds.<span class="title function_">add</span>(<span class="number">42</span>)</span><br></pre></td></tr></table></figure><p>但 Proxy 也带来身份问题。代理对象与原始对象不是严格相等，不能把 raw 和 proxy 混着当 Map key；解构或把属性传给只接收普通值的函数，也可能切断追踪。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> raw = &#123; <span class="attr">id</span>: <span class="number">1</span> &#125;</span><br><span class="line"><span class="keyword">const</span> proxy = <span class="title function_">reactive</span>(raw)</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(raw === proxy) <span class="comment">// false</span></span><br></pre></td></tr></table></figure><p>通常不应该在业务里依赖二者比较。进入响应式层以后尽量使用代理，确实要接第三方实例时再考虑 <code>markRaw</code> 等明确工具。</p><h2 id="composable-要围绕动作，而不是把页面换个文件名"><a href="#composable-要围绕动作，而不是把页面换个文件名" class="headerlink" title="composable 要围绕动作，而不是把页面换个文件名"></a>composable 要围绕动作，而不是把页面换个文件名</h2><p>第一次抽取时，我们做了一个 <code>useMemberPage()</code>，里面有列表、弹窗、权限、导出和路由，足足 500 行。它只是把大组件挪成大函数。</p><p>第二次按能力拆成：</p><ul><li><code>useMemberQuery</code>：筛选、分页、请求与竞态处理；</li><li><code>useMemberSelection</code>：勾选与跨页选择规则；</li><li><code>useMemberExport</code>：导出任务与进度；</li><li>弹窗表单留在自己的子组件。</li></ul><p>一个 composable 需要清楚的输入和返回值，并处理自己创建的资源：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">useMemberQuery</span>(<span class="params"><span class="attr">filters</span>: <span class="title class_">Ref</span>&lt;<span class="title class_">MemberFilters</span>&gt;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> rows = ref&lt;<span class="title class_">Member</span>[]&gt;([])</span><br><span class="line">  <span class="keyword">const</span> pending = <span class="title function_">ref</span>(<span class="literal">false</span>)</span><br><span class="line">  <span class="keyword">let</span> requestId = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">reload</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> id = ++requestId</span><br><span class="line">    pending.<span class="property">value</span> = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="keyword">const</span> result = <span class="keyword">await</span> memberApi.<span class="title function_">list</span>(filters.<span class="property">value</span>)</span><br><span class="line">      <span class="keyword">if</span> (id === requestId) rows.<span class="property">value</span> = result</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (id === requestId) pending.<span class="property">value</span> = <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">watch</span>(filters, reload, &#123; <span class="attr">deep</span>: <span class="literal">true</span>, <span class="attr">immediate</span>: <span class="literal">true</span> &#125;)</span><br><span class="line">  <span class="keyword">return</span> &#123; <span class="attr">rows</span>: <span class="title function_">readonly</span>(rows), <span class="attr">pending</span>: <span class="title function_">readonly</span>(pending), reload &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里用 requestId 忽略过期响应；如果底层支持 AbortSignal，取消旧请求更好。抽成 composable 不会自动解决竞态，资源生命周期仍要自己设计。</p><h2 id="TypeScript-体验改善，但模板边界也要检查"><a href="#TypeScript-体验改善，但模板边界也要检查" class="headerlink" title="TypeScript 体验改善，但模板边界也要检查"></a>TypeScript 体验改善，但模板边界也要检查</h2><p>Composition API 的函数与变量天然适合类型推导，不再大量依赖 <code>this</code> 上的合并类型。props 和 emit 可以明确表达：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> props = defineProps&lt;&#123;</span><br><span class="line">  <span class="attr">memberId</span>: <span class="built_in">number</span></span><br><span class="line">  <span class="attr">readonly</span>?: <span class="built_in">boolean</span></span><br><span class="line">&#125;&gt;()</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> emit = defineEmits&lt;&#123;</span><br><span class="line">  <span class="attr">saved</span>: [<span class="attr">member</span>: <span class="title class_">Member</span>]</span><br><span class="line">  <span class="attr">cancelled</span>: []</span><br><span class="line">&#125;&gt;()</span><br></pre></td></tr></table></figure><p>不过 <code>script setup</code> 是后来成熟的使用方式，迁移时间点要看具体版本与工具支持。Vue 3.0 初期的现实困难更多来自组件库、路由、状态库和编辑器插件尚在迁移。核心 API 可用，不代表企业项目依赖全部就绪。</p><h2 id="我们没有一次性迁完"><a href="#我们没有一次性迁完" class="headerlink" title="我们没有一次性迁完"></a>我们没有一次性迁完</h2><p>最终策略是先升级底层依赖与构建，使用兼容方案跑通，再按路由迁移高变化页面。每个页面都保留功能对照：筛选条件、URL、权限、表单校验和埋点不能因为改写法丢失。</p><p>迁移 Vue 3 最值得避免的两种极端，一种是把 Composition API 当成必须重写所有 Options API，另一种是把它当成纯语法变化。它真正提供的是更灵活的逻辑组合和新的响应式基础；能不能变得更可维护，取决于我们是否按业务边界组织代码、是否理解 ref&#x2F;Proxy，以及是否给副作用留出完整生命周期。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://blog.vuejs.org/posts/vue-3-one-piece">Vue 3.0 发布公告</a></li><li><a href="https://vuejs.org/guide/essentials/reactivity-fundamentals.html">Vue：Reactivity Fundamentals</a></li><li><a href="https://vuejs.org/guide/reusability/composables.html">Vue：Composables</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 2020 </tag>
            
            <tag> Vue </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vue 响应式失效排查：解构、赋值与 watch 时机</title>
      <link href="/2020/08/21/frontend-debug-vue-reactivity-lost/"/>
      <url>/2020/08/21/frontend-debug-vue-reactivity-lost/</url>
      
        <content type="html"><![CDATA[<p>一个 Vue 3 列表页出现过很奇怪的现象：请求返回后控制台能看到 <code>state.rows</code> 已经有数据，模板里的空状态却没有消失。手动切一次筛选条件，列表又突然出现。</p><p>最后问题不是 Vue 没检测到数组变化，而是 composable 返回前把响应式对象普通解构了。模板拿到的是初始化时的值快照，请求修改的是 Proxy 里的另一个属性。两边名字一样，引用却已经断开。</p><p>这篇把几种“数据变了、页面不动”的情况拆开。它们表面相似，修法并不相同。</p><h2 id="第一现场：解构以后只剩普通值"><a href="#第一现场：解构以后只剩普通值" class="headerlink" title="第一现场：解构以后只剩普通值"></a>第一现场：解构以后只剩普通值</h2><p>有问题的 composable：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">useMemberList</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> state = <span class="title function_">reactive</span>(&#123;</span><br><span class="line">    <span class="attr">rows</span>: [] <span class="keyword">as</span> <span class="title class_">Member</span>[],</span><br><span class="line">    <span class="attr">pending</span>: <span class="literal">false</span>,</span><br><span class="line">    <span class="attr">keyword</span>: <span class="string">&#x27;&#x27;</span></span><br><span class="line">  &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">reload</span>(<span class="params"></span>) &#123;</span><br><span class="line">    state.<span class="property">pending</span> = <span class="literal">true</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      state.<span class="property">rows</span> = <span class="keyword">await</span> <span class="title function_">fetchMembers</span>(state.<span class="property">keyword</span>)</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      state.<span class="property">pending</span> = <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> &#123; ...state, reload &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对象展开会读取 Proxy 当时的属性值，返回一个普通对象。<code>rows</code> 初始是空数组引用，后来 <code>state.rows = newRows</code> 替换为新数组，调用方仍持有旧数组。</p><p>修复可以直接返回 <code>state</code>，也可以使用 <code>toRefs</code>：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">useMemberList</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> state = <span class="title function_">reactive</span>(&#123;</span><br><span class="line">    <span class="attr">rows</span>: [] <span class="keyword">as</span> <span class="title class_">Member</span>[],</span><br><span class="line">    <span class="attr">pending</span>: <span class="literal">false</span>,</span><br><span class="line">    <span class="attr">keyword</span>: <span class="string">&#x27;&#x27;</span></span><br><span class="line">  &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="comment">// reload 同上</span></span><br><span class="line">  <span class="keyword">return</span> &#123; ...<span class="title function_">toRefs</span>(state), reload &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>toRefs</code> 为每个属性创建与原对象联动的 ref。模板会自动解包，JavaScript 中访问需要 <code>.value</code>。</p><h2 id="第二种：把-reactive-变量整体换掉"><a href="#第二种：把-reactive-变量整体换掉" class="headerlink" title="第二种：把 reactive 变量整体换掉"></a>第二种：把 reactive 变量整体换掉</h2><p>另一个常见写法：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> form = <span class="title function_">reactive</span>(&#123; <span class="attr">name</span>: <span class="string">&#x27;&#x27;</span>, <span class="attr">phone</span>: <span class="string">&#x27;&#x27;</span> &#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">load</span>(<span class="params"><span class="attr">id</span>: <span class="built_in">number</span></span>) &#123;</span><br><span class="line">  form = <span class="title function_">reactive</span>(<span class="keyword">await</span> <span class="title function_">fetchMember</span>(id))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>模板或其他 composable 可能已经持有旧 Proxy，重新给局部变量赋值不会让这些消费者自动改指向。若对象结构稳定，可以修改原代理：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> form = <span class="title function_">reactive</span>(&#123; <span class="attr">name</span>: <span class="string">&#x27;&#x27;</span>, <span class="attr">phone</span>: <span class="string">&#x27;&#x27;</span> &#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">load</span>(<span class="params"><span class="attr">id</span>: <span class="built_in">number</span></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> data = <span class="keyword">await</span> <span class="title function_">fetchMember</span>(id)</span><br><span class="line">  <span class="title class_">Object</span>.<span class="title function_">assign</span>(form, data)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果业务语义本来就是整体替换，我更倾向使用 <code>ref</code>：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> form = ref&lt;<span class="title class_">MemberForm</span>&gt;(&#123; <span class="attr">name</span>: <span class="string">&#x27;&#x27;</span>, <span class="attr">phone</span>: <span class="string">&#x27;&#x27;</span> &#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">load</span>(<span class="params"><span class="attr">id</span>: <span class="built_in">number</span></span>) &#123;</span><br><span class="line">  form.<span class="property">value</span> = <span class="keyword">await</span> <span class="title function_">fetchMember</span>(id)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>选 <code>reactive</code> 还是 <code>ref</code> 要结合更新方式。经常整体替换的值用 ref，意图更直接。</p><h2 id="第三种：watch-监听的对象不对"><a href="#第三种：watch-监听的对象不对" class="headerlink" title="第三种：watch 监听的对象不对"></a>第三种：watch 监听的对象不对</h2><p><code>watch(state.keyword, callback)</code> 会在调用 watch 时先读取字符串，然后把普通值传进去，并没有可追踪来源。应该传 getter 或 ref：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">watch</span>(</span><br><span class="line">  <span class="function">() =&gt;</span> state.<span class="property">keyword</span>,</span><br><span class="line">  <span class="function">(<span class="params">keyword, previous</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(&#123; previous, keyword &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>多个筛选条件可以返回数组：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">watch</span>(</span><br><span class="line">  <span class="function">() =&gt;</span> [state.<span class="property">keyword</span>, state.<span class="property">level</span>, state.<span class="property">page</span>],</span><br><span class="line">  <span class="function">() =&gt;</span> <span class="title function_">reload</span>()</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>不要为了省事总是 <code>watch(state, ..., { deep: true })</code>。深度监听会遍历嵌套属性，大对象成本高；对象内部修改时，回调中的新旧值还可能指向同一个对象，很难拿来做差异比较。</p><h2 id="派生值不应该用-watch-复制一份"><a href="#派生值不应该用-watch-复制一份" class="headerlink" title="派生值不应该用 watch 复制一份"></a>派生值不应该用 watch 复制一份</h2><p>下面代码维护了两份可以互相推导的状态：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> firstName = <span class="title function_">ref</span>(<span class="string">&#x27;&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> lastName = <span class="title function_">ref</span>(<span class="string">&#x27;&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> fullName = <span class="title function_">ref</span>(<span class="string">&#x27;&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="title function_">watch</span>([firstName, lastName], <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  fullName.<span class="property">value</span> = <span class="string">`<span class="subst">$&#123;firstName.value&#125;</span> <span class="subst">$&#123;lastName.value&#125;</span>`</span>.<span class="title function_">trim</span>()</span><br><span class="line">&#125;, &#123; <span class="attr">immediate</span>: <span class="literal">true</span> &#125;)</span><br></pre></td></tr></table></figure><p>用 computed 不需要同步时序：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> fullName = <span class="title function_">computed</span>(<span class="function">() =&gt;</span></span><br><span class="line">  <span class="string">`<span class="subst">$&#123;firstName.value&#125;</span> <span class="subst">$&#123;lastName.value&#125;</span>`</span>.<span class="title function_">trim</span>()</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>watch 更适合产生外部副作用，例如请求、localStorage 或第三方实例，而不是把 A 状态复制成 B 状态。副本越多，越容易出现“一个变了另一个没变”。</p><h2 id="异步搜索看似响应式失效，其实是竞态"><a href="#异步搜索看似响应式失效，其实是竞态" class="headerlink" title="异步搜索看似响应式失效，其实是竞态"></a>异步搜索看似响应式失效，其实是竞态</h2><p>关键词 A 请求慢，关键词 B 请求快。B 先回来正确显示，A 后回来又覆盖列表，用户会觉得“输入变了页面还是旧内容”。响应式本身完全正常，错的是响应顺序。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="attr">controller</span>: <span class="title class_">AbortController</span> | <span class="literal">null</span> = <span class="literal">null</span></span><br><span class="line"></span><br><span class="line"><span class="title function_">watch</span>(keyword, <span class="keyword">async</span> value =&gt; &#123;</span><br><span class="line">  controller?.<span class="title function_">abort</span>()</span><br><span class="line">  controller = <span class="keyword">new</span> <span class="title class_">AbortController</span>()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    rows.<span class="property">value</span> = <span class="keyword">await</span> <span class="title function_">searchMembers</span>(value, controller.<span class="property">signal</span>)</span><br><span class="line">  &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">    <span class="keyword">if</span> ((error <span class="keyword">as</span> <span class="title class_">Error</span>).<span class="property">name</span> !== <span class="string">&#x27;AbortError&#x27;</span>) <span class="keyword">throw</span> error</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>Vue 的 watch 清理机制也可以用于取消上一次副作用，具体 API 写法应跟随项目版本。关键是新一轮执行之前，让旧请求失效。</p><h2 id="DOM-还没更新，不代表状态没更新"><a href="#DOM-还没更新，不代表状态没更新" class="headerlink" title="DOM 还没更新，不代表状态没更新"></a>DOM 还没更新，不代表状态没更新</h2><p>修改状态后立刻读取元素尺寸，拿到的可能还是上一次 DOM：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">expanded.<span class="property">value</span> = <span class="literal">true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(panel.<span class="property">value</span>?.<span class="title function_">getBoundingClientRect</span>().<span class="property">height</span>)</span><br></pre></td></tr></table></figure><p>Vue 会批量调度 DOM 更新。确实需要等待本轮 DOM 刷新时使用 <code>nextTick</code>：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">expanded.<span class="property">value</span> = <span class="literal">true</span></span><br><span class="line"><span class="keyword">await</span> <span class="title function_">nextTick</span>()</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(panel.<span class="property">value</span>?.<span class="title function_">getBoundingClientRect</span>().<span class="property">height</span>)</span><br></pre></td></tr></table></figure><p>不要把所有状态修改都包进 <code>nextTick</code>。它只解决“等待 DOM 提交”的时机，不会修复丢失代理或错误 watcher。</p><h2 id="用-Devtools-沿数据链找断点"><a href="#用-Devtools-沿数据链找断点" class="headerlink" title="用 Devtools 沿数据链找断点"></a>用 Devtools 沿数据链找断点</h2><p>我现在排查会按以下顺序，而不是先加 <code>forceUpdate</code>：</p><ol><li>在 Vue Devtools 中看组件真正接收到的 prop&#x2F;ref 是否变化；</li><li>用 <code>isRef</code>、<code>isReactive</code>、<code>toRaw</code> 辅助确认值的身份；</li><li>从模板使用点向上追，检查对象展开、普通解构和函数参数；</li><li>暂时去掉复杂 watch，只保留一个 ref + computed 的最小复现；</li><li>若数据正确但 DOM 稍后才变，检查更新批次与 <code>nextTick</code>；</li><li>若最终显示旧请求结果，记录每个请求 ID 和完成顺序。</li></ol><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">table</span>(&#123;</span><br><span class="line">  <span class="attr">rowsIsRef</span>: <span class="title function_">isRef</span>(rows),</span><br><span class="line">  <span class="attr">formIsReactive</span>: <span class="title function_">isReactive</span>(form),</span><br><span class="line">  <span class="attr">sameRaw</span>: <span class="title function_">toRaw</span>(form) === originalForm</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>这些 API 是诊断工具，不建议在业务逻辑中到处依赖 raw&#x2F;proxy 身份。</p><h2 id="不使用“万能修复”"><a href="#不使用“万能修复”" class="headerlink" title="不使用“万能修复”"></a>不使用“万能修复”</h2><p>我见过的临时处理包括给组件加随机 key、调用 forceUpdate、把所有对象 JSON 深拷贝、所有 watch 都开 deep。它们可能让现象暂时消失，却会制造重建组件、丢失状态和性能问题。</p><p>更可靠的判断是：</p><ul><li>解构断开：返回 ref 或保持对象属性访问；</li><li>整体替换：使用 ref，或更新原 reactive 对象；</li><li>派生数据：computed；</li><li>外部副作用：watch，并精确声明来源与清理；</li><li>DOM 提交时机：nextTick；</li><li>请求覆盖：取消或忽略旧请求。</li></ul><p>“响应式失效”不是一个根因，只是一组症状。把值从创建、传递、派生到副作用的链路画出来，找到哪一步从 Proxy&#x2F;ref 变成普通快照，通常就能定位，而不必怀疑整个框架。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://vuejs.org/guide/essentials/reactivity-fundamentals.html">Vue：Reactivity Fundamentals</a></li><li><a href="https://vuejs.org/guide/essentials/watchers.html">Vue：Watchers</a></li><li><a href="https://vuejs.org/api/reactivity-utilities.html">Vue：Reactivity Utilities</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端排障 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Vue </tag>
            
            <tag> 前端排障 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vite 萌芽：原生 ESM 改变开发服务器思路</title>
      <link href="/2020/07/15/frontend-chronicle-2020-07-vite-native-esm-origin/"/>
      <url>/2020/07/15/frontend-chronicle-2020-07-vite-native-esm-origin/</url>
      
        <content type="html"><![CDATA[<p>2020 年那段时间，我维护的一个 Vue 项目冷启动要等四十多秒。改一处底层工具函数，页面上的进度条会先跑一遍模块重建，再刷新。项目越大，等待越长，大家逐渐把“启动后不要关 dev server”当成工作习惯。</p><p>Vite 早期版本让我觉得新鲜的不是配置更少，而是它重新安排了开发阶段的工作：浏览器需要哪个源码模块，服务器再转换并返回哪个；不再在启动前把整个应用先打成 bundle。</p><h2 id="打开-Network，原生-ESM-的链路就在那里"><a href="#打开-Network，原生-ESM-的链路就在那里" class="headerlink" title="打开 Network，原生 ESM 的链路就在那里"></a>打开 Network，原生 ESM 的链路就在那里</h2><p>一个最小入口可能只有：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;app&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">&quot;module&quot;</span> <span class="attr">src</span>=<span class="string">&quot;/src/main.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// /src/main.js</span></span><br><span class="line"><span class="keyword">import</span> &#123; createApp &#125; <span class="keyword">from</span> <span class="string">&#x27;vue&#x27;</span></span><br><span class="line"><span class="keyword">import</span> <span class="title class_">App</span> <span class="keyword">from</span> <span class="string">&#x27;./App.vue&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="title function_">createApp</span>(<span class="title class_">App</span>).<span class="title function_">mount</span>(<span class="string">&#x27;#app&#x27;</span>)</span><br></pre></td></tr></table></figure><p>浏览器请求 <code>/src/main.js</code>，解析到导入后继续请求 <code>App.vue</code> 等模块。开发服务器拦截这些请求，把 <code>.vue</code>、TypeScript 或 JSX 转换成浏览器能执行的 ESM，再返回结果。</p><p>这与传统 bundle-based dev server 的主要差别，是冷启动不必先遍历并打包全部业务模块。首页没访问到的路由，初始阶段可以不处理。模块规模增长时，这种按需模型尤其明显。</p><h2 id="裸导入不是浏览器天然能理解的"><a href="#裸导入不是浏览器天然能理解的" class="headerlink" title="裸导入不是浏览器天然能理解的"></a>裸导入不是浏览器天然能理解的</h2><p>浏览器知道 <code>./App.vue</code> 是相对路径，却不知道下面的 <code>vue</code> 应该去 <code>node_modules</code> 哪个文件：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; createApp &#125; <span class="keyword">from</span> <span class="string">&#x27;vue&#x27;</span></span><br></pre></td></tr></table></figure><p>Vite 会解析并处理这类 bare import。早期实现和后来细节会变化，但核心问题一直存在：npm 包的入口、CommonJS&#x2F;ESM 格式和大量内部模块不适合原样交给浏览器逐个请求。</p><p>依赖预构建承担了两件重要工作：把需要的 CommonJS&#x2F;UMD 依赖转换为 ESM，并把内部模块很多的依赖合并成更少请求。源码按需转换与依赖预构建是不同路径，不能简单总结成“Vite 完全不打包”。</p><p>如果依赖扫描没发现一个只在条件分支里动态导入的包，第一次访问时可能触发重新优化与页面刷新。排查这类问题要看 Vite 的优化日志和依赖入口，而不是只看业务模块。</p><h2 id="一次修改为什么能只更新一个组件"><a href="#一次修改为什么能只更新一个组件" class="headerlink" title="一次修改为什么能只更新一个组件"></a>一次修改为什么能只更新一个组件</h2><p>原生 ESM 让模块边界直接存在于浏览器，但 HMR 仍需要开发服务器维护模块关系。文件变化后，服务器沿模块图找到可接受更新的边界，通过 WebSocket 通知客户端加载带新时间戳的模块。</p><p>框架插件负责保留组件状态等更高层语义。以 Vue 单文件组件为例，修改 <code>&lt;style&gt;</code> 可以只替换样式，修改 template 可以更新 render，尽量不重新创建整个应用。</p><p>自己写模块时也能看到 HMR API 的基本形态：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">formatPrice</span>(<span class="params">value</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`¥<span class="subst">$&#123;value.toFixed(<span class="number">2</span>)&#125;</span>`</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">hot</span>) &#123;</span><br><span class="line">  <span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">hot</span>.<span class="title function_">accept</span>(<span class="function"><span class="params">newModule</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;formatter updated&#x27;</span>, newModule)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>并不是所有变更都能保持状态。模块有顶层副作用、无法找到 accept 边界或插件没有正确实现转换时，仍可能全页刷新。HMR 快不代表可以忽略模块副作用。</p><h2 id="开发快，不等于生产环境直接发源码"><a href="#开发快，不等于生产环境直接发源码" class="headerlink" title="开发快，不等于生产环境直接发源码"></a>开发快，不等于生产环境直接发源码</h2><p>Vite 的开发阶段利用原生 ESM 按需服务模块，生产阶段仍需要打包、Tree Shaking、压缩、代码分割和资源 hash。早期 Vite 选择 Rollup 承担生产构建，这形成了开发与生产两条不同链路。</p><p>这带来一个必须认真对待的问题：dev 正常不代表 build 正常。典型差异包括：</p><ul><li>大小写错误在 macOS 开发机不明显，Linux 构建失败；</li><li>动态导入路径无法被静态分析，生产 chunk 缺失；</li><li>某个包的开发入口是 ESM，生产条件导出走到另一个文件；</li><li>CSS 顺序、base 路径或 public 资源地址只在部署后出错；</li><li>依赖读取 Node 全局变量，开发插件碰巧提供了替代。</li></ul><p>因此 CI 必须真正执行生产构建，并用 <code>vite preview</code> 或部署后的静态服务器做冒烟测试。</p><h2 id="环境变量不是把-process-env-搬过来"><a href="#环境变量不是把-process-env-搬过来" class="headerlink" title="环境变量不是把 process.env 搬过来"></a>环境变量不是把 process.env 搬过来</h2><p>Vite 使用 <code>import.meta.env</code> 暴露构建时环境，并通常只把指定前缀的变量交给客户端：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> apiBase = <span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">env</span>.<span class="property">VITE_API_BASE</span></span><br></pre></td></tr></table></figure><p>带前缀不代表安全。只要进入客户端构建，用户就能在产物中看到它。数据库密码、私钥和服务端 token 绝不能因为变量名改了就注入前端。</p><p>迁移旧项目时，<code>process.env.*</code> 往往散落在业务代码和依赖中。我们先列出每个变量的用途：构建开关、公开 API 地址还是服务端秘密，再决定替换方式，而不是全局 define 一个空的 <code>process.env</code> 把错误压下去。</p><h2 id="Vite-也会慢，通常慢在具体环节"><a href="#Vite-也会慢，通常慢在具体环节" class="headerlink" title="Vite 也会慢，通常慢在具体环节"></a>Vite 也会慢，通常慢在具体环节</h2><p>项目使用 Vite 后，如果启动或热更新重新变慢，我会把问题分开测：</p><ol><li>依赖优化耗时，是否频繁失效缓存；</li><li>某个插件的 transform 是否对每个模块做昂贵工作；</li><li>barrel file 是否让一个小改动牵动巨大模块图；</li><li>浏览器是否同时请求了成千上万个细碎模块；</li><li>类型检查、lint 是否被错误地塞进同步转换链。</li></ol><p>工具的架构能降低基础成本，不能消除不受控依赖和插件。大型图标包、全量 locale 和跨层 barrel export，换构建器后仍然可能拖慢开发。</p><h2 id="当时没有直接迁移全部项目"><a href="#当时没有直接迁移全部项目" class="headerlink" title="当时没有直接迁移全部项目"></a>当时没有直接迁移全部项目</h2><p>新项目试用很顺利，旧项目却有自定义 webpack loader、全局注入和一套特殊的多页构建。我们先迁了内部工具和独立页面，用真实数据记录启动、HMR 与生产构建，再决定主项目是否值得投入。</p><p>这段经历让我对 Vite 的理解不再停在“快”。它改变的是开发服务器的成本模型：业务源码按需转换，依赖单独优化，生产交给构建器。今天 Vite 的内部已经继续演进，但从 Network 面板理解模块请求、从插件和模块图定位性能问题，仍比背一组宣传数字更有用。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://vite.dev/guide/why">Vite：Why Vite</a></li><li><a href="https://vite.dev/guide/dep-pre-bundling">Vite：Dependency Pre-Bundling</a></li><li><a href="https://vite.dev/blog/">Vite 官方博客</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 2020 </tag>
            
            <tag> Vite </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Deno 1.0：JavaScript 运行时重新思考安全与工具集成</title>
      <link href="/2020/05/13/frontend-chronicle-2020-05-deno-1-secure-runtime/"/>
      <url>/2020/05/13/frontend-chronicle-2020-05-deno-1-secure-runtime/</url>
      
        <content type="html"><![CDATA[<p>Deno 1.0 发布时，我拿它写的第一个东西不是 Web 服务，而是一个读取 Markdown frontmatter、检查发布日期的命令行脚本。原因很简单：这类工具足够小，能体验权限、TypeScript 和依赖导入，又不会因为生态不兼容影响生产业务。</p><p>跑通以后最直观的感受不是“比 Node 快多少”，而是脚本需要什么权限被写进了启动命令。它让我重新注意到：一个普通构建脚本默认能读取整个磁盘、访问任意网络和环境变量，其实是非常宽的能力。</p><h2 id="从一个没有-package-json-的脚本开始"><a href="#从一个没有-package-json-的脚本开始" class="headerlink" title="从一个没有 package.json 的脚本开始"></a>从一个没有 package.json 的脚本开始</h2><p>当时可以直接写 TypeScript，并从 URL 导入标准模块。下面是一个简化版检查器：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; walk &#125; <span class="keyword">from</span> <span class="string">&quot;https://deno.land/std/fs/walk.ts&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> <span class="title function_">await</span> (<span class="keyword">const</span> entry <span class="keyword">of</span> <span class="title function_">walk</span>(<span class="string">&quot;./posts&quot;</span>, &#123; <span class="attr">exts</span>: [<span class="string">&quot;.md&quot;</span>] &#125;)) &#123;</span><br><span class="line">  <span class="keyword">const</span> text = <span class="keyword">await</span> <span class="title class_">Deno</span>.<span class="title function_">readTextFile</span>(entry.<span class="property">path</span>)</span><br><span class="line">  <span class="keyword">const</span> matched = text.<span class="title function_">match</span>(<span class="regexp">/^date:\s*(.+)$/m</span>)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!matched) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">warn</span>(<span class="string">`<span class="subst">$&#123;entry.path&#125;</span>: missing date`</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>第一次运行会失败，因为脚本没有读取目录的权限：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">deno run check_posts.ts</span><br></pre></td></tr></table></figure><p>明确允许目标目录后才可执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">deno run --allow-read=./posts check_posts.ts</span><br></pre></td></tr></table></figure><p>这类错误不是“环境没配好”，而是权限模型在工作。相比 <code>--allow-read</code> 放开所有路径，把范围收窄到脚本真正需要的目录更符合最小权限原则。</p><h2 id="网络依赖看起来自由，也需要锁定"><a href="#网络依赖看起来自由，也需要锁定" class="headerlink" title="网络依赖看起来自由，也需要锁定"></a>网络依赖看起来自由，也需要锁定</h2><p>URL import 让依赖来源一目了然，但也产生了新问题：远端内容变化或不可用时，脚本是否还能复现？Deno 会缓存已下载模块，也提供 lock file 来校验依赖完整性。项目一旦进入 CI，就不能只依赖某台开发机的缓存。</p><p>当依赖越来越多，散落在每个源码文件里的长 URL 也不好维护。我们后来集中在一个依赖模块中再导出：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// deps.ts</span></span><br><span class="line"><span class="keyword">export</span> &#123; walk &#125; <span class="keyword">from</span> <span class="string">&quot;https://deno.land/std@0.53.0/fs/walk.ts&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// check_posts.ts</span></span><br><span class="line"><span class="keyword">import</span> &#123; walk &#125; <span class="keyword">from</span> <span class="string">&quot;./deps.ts&quot;</span></span><br></pre></td></tr></table></figure><p>这里的版本号只是当时写法示意，实际项目应使用当前受支持版本。集中入口至少能让升级和审计有明确位置。</p><h2 id="权限不是沙箱的万能证明"><a href="#权限不是沙箱的万能证明" class="headerlink" title="权限不是沙箱的万能证明"></a>权限不是沙箱的万能证明</h2><p>Deno 默认拒绝文件、网络、环境变量和子进程访问，是很好的安全起点。但一旦为了省事使用 <code>-A</code>&#x2F;<code>--allow-all</code>，边界就回到了全开状态。第三方脚本如果同时获得读环境变量和网络权限，理论上就能读取 token 并发送出去。</p><p>我会把任务拆开：格式化脚本不需要网络；抓取数据的脚本只允许目标 API 域名；生成文件只允许输出目录。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">deno run \</span><br><span class="line">  --allow-net=api.example.com \</span><br><span class="line">  --allow-read=./config \</span><br><span class="line">  --allow-write=./generated \</span><br><span class="line">  generate_report.ts</span><br></pre></td></tr></table></figure><p>权限参数仍然需要代码审查，尤其是通配路径、环境变量和运行子进程。默认安全只是让授权动作可见，不会自动判断授权是否合理。</p><h2 id="内置工具减少了“先选工具”的时间"><a href="#内置工具减少了“先选工具”的时间" class="headerlink" title="内置工具减少了“先选工具”的时间"></a>内置工具减少了“先选工具”的时间</h2><p>Deno 1.0 把格式化、lint、测试、文档和打包相关能力放进同一个可执行文件。对于小工具，这种体验很干净：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">deno <span class="built_in">fmt</span></span><br><span class="line">deno <span class="built_in">test</span> --allow-read=./fixtures</span><br><span class="line">deno doc check_posts.ts</span><br></pre></td></tr></table></figure><p>测试同样受权限限制。某条测试如果意外访问网络，会直接失败，而不是悄悄依赖外部服务。这促使我们把纯逻辑和 I&#x2F;O 分开：frontmatter 解析写成纯函数，文件读取留给最外层，单元测试就不需要任何权限。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">readDate</span>(<span class="params"><span class="attr">markdown</span>: <span class="built_in">string</span></span>): <span class="built_in">string</span> | <span class="literal">null</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> markdown.<span class="title function_">match</span>(<span class="regexp">/^date:\s*(.+)$/m</span>)?.[<span class="number">1</span>].<span class="title function_">trim</span>() ?? <span class="literal">null</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title class_">Deno</span>.<span class="title function_">test</span>(<span class="string">&quot;readDate returns null when date is absent&quot;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="title function_">readDate</span>(<span class="string">&quot;# hello&quot;</span>) !== <span class="literal">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;unexpected date&quot;</span>)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><h2 id="没有立刻迁移-Node-服务的原因"><a href="#没有立刻迁移-Node-服务的原因" class="headerlink" title="没有立刻迁移 Node 服务的原因"></a>没有立刻迁移 Node 服务的原因</h2><p>当时生产项目依赖数据库驱动、监控 SDK、公司内部 npm 包和既有容器镜像。Deno 的 API 设计很有吸引力，但“语法能运行”不等于整条运维链路准备好了。</p><p>我们评估的是这些具体问题：</p><ul><li>关键依赖有没有兼容实现，行为边界是否相同；</li><li>日志、指标、trace 和错误上报能否接入现有平台；</li><li>构建镜像、健康检查与优雅退出如何实现；</li><li>团队排障工具是否认识新的运行时；</li><li>锁文件、私有依赖与离线构建怎样管理。</li></ul><p>最后 Deno 留在了内部脚本和实验服务，没有强行替换主业务。这不是试用失败，反而是一个合理的技术结论。</p><h2 id="Deno-1-0-真正推动了什么"><a href="#Deno-1-0-真正推动了什么" class="headerlink" title="Deno 1.0 真正推动了什么"></a>Deno 1.0 真正推动了什么</h2><p>Deno 没有在 1.0 发布后立刻取代 Node.js，但它重新点燃了 JavaScript 运行时竞争：Web 标准 API、默认权限、原生 TypeScript 体验和一体化工具都成为后续讨论的重点。Node 与其他运行时也在不断吸收类似方向。</p><p>回看那次小实验，我最愿意保留的不是 URL import，而是“让能力显式化”的习惯。无论使用什么运行时，构建脚本访问了哪些目录、网络和密钥，都应该能被看见、限制和审计。安全默认值能帮我们迈出第一步，真正的安全仍来自精确授权和完整运维验证。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://deno.com/blog/v1">Deno 1.0 发布说明</a></li><li><a href="https://docs.deno.com/runtime/fundamentals/security/">Deno：Security and permissions</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> Deno </tag>
            
            <tag> 2020 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>TypeScript 3.7：可选链与空值合并进入日常开发</title>
      <link href="/2019/11/05/frontend-chronicle-2019-11-typescript-3-7/"/>
      <url>/2019/11/05/frontend-chronicle-2019-11-typescript-3-7/</url>
      
        <content type="html"><![CDATA[<p>TypeScript 3.7 最先替我修掉的不是类型错误，而是一个被 <code>||</code> 写坏的业务规则。系统允许用户把重试次数设为 0，表示失败后不重试；旧代码却把 0 当成“没有配置”，偷偷改回默认值 3。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> retryCount = config.<span class="property">retryCount</span> || <span class="number">3</span></span><br></pre></td></tr></table></figure><p>可选链和空值合并进入 TypeScript 后，大量层层判断终于可以缩短，但它们不是纯粹的语法糖。<code>?.</code> 与 <code>??</code> 对“缺失”的定义更精确，也迫使我们重新区分 null&#x2F;undefined、空字符串、0 和 false。</p><h2 id="处理的是假值，-处理的是空值"><a href="#处理的是假值，-处理的是空值" class="headerlink" title="|| 处理的是假值，?? 处理的是空值"></a><code>||</code> 处理的是假值，<code>??</code> 处理的是空值</h2><p>JavaScript 中这些值都会被 <code>||</code> 当成 false：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">0</span></span><br><span class="line"><span class="string">&#x27;&#x27;</span></span><br><span class="line"><span class="literal">false</span></span><br><span class="line"><span class="title class_">NaN</span></span><br><span class="line"><span class="literal">null</span></span><br><span class="line"><span class="literal">undefined</span></span><br></pre></td></tr></table></figure><p>配置项如果允许 0、空字符串或 false，它们就不是缺省值。改成 <code>??</code> 后，只有 <code>null</code> 与 <code>undefined</code> 会走右侧默认值：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> retryCount = config.<span class="property">retryCount</span> ?? <span class="number">3</span></span><br><span class="line"><span class="keyword">const</span> nickname = user.<span class="property">nickname</span> ?? <span class="string">&#x27;匿名用户&#x27;</span></span><br><span class="line"><span class="keyword">const</span> showTips = settings.<span class="property">showTips</span> ?? <span class="literal">true</span></span><br></pre></td></tr></table></figure><p>这一处变化让业务语义更准确，但也不能机械替换所有 <code>||</code>。搜索框的展示值如果真的希望空字符串回落到“全部”，<code>keyword || &#39;全部&#39;</code> 仍可能是正确的。选哪个运算符取决于产品如何定义“没有值”。</p><h2 id="可选链消掉了守卫金字塔"><a href="#可选链消掉了守卫金字塔" class="headerlink" title="可选链消掉了守卫金字塔"></a>可选链消掉了守卫金字塔</h2><p>过去读取接口深层字段常写成：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> city = response</span><br><span class="line">  &amp;&amp; response.<span class="property">user</span></span><br><span class="line">  &amp;&amp; response.<span class="property">user</span>.<span class="property">address</span></span><br><span class="line">  &amp;&amp; response.<span class="property">user</span>.<span class="property">address</span>.<span class="property">city</span></span><br></pre></td></tr></table></figure><p>TypeScript 3.7 后可以写：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> city = response?.<span class="property">user</span>?.<span class="property">address</span>?.<span class="property">city</span></span><br></pre></td></tr></table></figure><p>还有方法调用和索引形式：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">logger?.<span class="property">warn</span>?.(<span class="string">&#x27;profile missing&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> firstError = errors?.[<span class="number">0</span>]</span><br></pre></td></tr></table></figure><p>短路只发生在紧挨着 <code>?.</code> 的访问链上。下面的除法仍会执行，<code>foo?.bar</code> 可能得到 undefined，在严格空值检查下也会报出风险：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> ratio = foo?.<span class="property">bar</span> / <span class="number">100</span></span><br></pre></td></tr></table></figure><p>应该先明确缺失时的业务处理：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> ratio = foo?.<span class="property">bar</span> == <span class="literal">null</span> ? <span class="literal">null</span> : foo.<span class="property">bar</span> / <span class="number">100</span></span><br></pre></td></tr></table></figure><h2 id="可选链太多，可能是在掩盖接口问题"><a href="#可选链太多，可能是在掩盖接口问题" class="headerlink" title="可选链太多，可能是在掩盖接口问题"></a>可选链太多，可能是在掩盖接口问题</h2><p>刚支持可选链时，我们一度把接口访问都写成：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> amount = order?.<span class="property">payment</span>?.<span class="property">detail</span>?.<span class="property">amount</span> ?? <span class="number">0</span></span><br></pre></td></tr></table></figure><p>页面不再报错了，但如果“已支付订单必须有 payment.detail”，这里显示 0 元会掩盖脏数据。可选链适合业务上允许缺失的边界，比如访客可能没有头像；对于领域模型中必需的数据，应该在入口校验并形成可靠类型。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> <span class="title class_">PaidOrder</span> = &#123;</span><br><span class="line">  <span class="attr">status</span>: <span class="string">&#x27;paid&#x27;</span></span><br><span class="line">  <span class="attr">payment</span>: &#123;</span><br><span class="line">    <span class="attr">detail</span>: &#123; <span class="attr">amount</span>: <span class="built_in">number</span> &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">parsePaidOrder</span>(<span class="params"><span class="attr">input</span>: <span class="built_in">unknown</span></span>): <span class="title class_">PaidOrder</span> &#123;</span><br><span class="line">  <span class="comment">// 真实项目可使用 schema 库完成运行时校验</span></span><br><span class="line">  <span class="keyword">if</span> (!<span class="title function_">isObject</span>(input) || input.<span class="property">status</span> !== <span class="string">&#x27;paid&#x27;</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;Invalid paid order&#x27;</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 省略其余字段检查</span></span><br><span class="line">  <span class="keyword">return</span> input <span class="keyword">as</span> <span class="title class_">PaidOrder</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>类型只能约束编译时已经信任的数据，接口 JSON、localStorage 和 URL 参数仍要做运行时验证。</p><h2 id="断言函数把运行时检查告诉类型系统"><a href="#断言函数把运行时检查告诉类型系统" class="headerlink" title="断言函数把运行时检查告诉类型系统"></a>断言函数把运行时检查告诉类型系统</h2><p>TypeScript 3.7 还加入了 assertion functions。以前写一个通用断言，执行后类型系统未必知道值已经非空：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> assertDefined&lt;T&gt;(<span class="attr">value</span>: T, <span class="attr">name</span>: <span class="built_in">string</span>): asserts value is <span class="title class_">NonNullable</span>&lt;T&gt; &#123;</span><br><span class="line">  <span class="keyword">if</span> (value == <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`<span class="subst">$&#123;name&#125;</span> is required`</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">renderUser</span>(<span class="params"><span class="attr">user</span>: <span class="title class_">User</span> | <span class="literal">undefined</span></span>) &#123;</span><br><span class="line">  <span class="title function_">assertDefined</span>(user, <span class="string">&#x27;user&#x27;</span>)</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(user.<span class="property">name</span>) <span class="comment">// 此处已经收窄为 User</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这类函数适合程序无法继续的前置条件，例如根 DOM 节点、必要配置和经过鉴权后必须存在的上下文。它不是把值“变安全”，而是在运行时失败路径与编译时收窄之间建立联系。</p><p>如果断言函数实现写错，类型系统会相信错误承诺。因此通用 assertion 应保持短小并有测试，不能只写 <code>return</code> 或滥用 <code>as</code>。</p><h2 id="Optional-call-很适合可选回调"><a href="#Optional-call-很适合可选回调" class="headerlink" title="Optional call 很适合可选回调"></a>Optional call 很适合可选回调</h2><p>组件 API 中的回调经常可选：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> <span class="title class_">SaveOptions</span> = &#123;</span><br><span class="line">  <span class="attr">onSuccess</span>?: <span class="function">(<span class="params"><span class="attr">id</span>: <span class="built_in">string</span></span>) =&gt;</span> <span class="built_in">void</span></span><br><span class="line">  <span class="attr">onError</span>?: <span class="function">(<span class="params"><span class="attr">error</span>: <span class="title class_">Error</span></span>) =&gt;</span> <span class="built_in">void</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">save</span>(<span class="params"><span class="attr">options</span>: <span class="title class_">SaveOptions</span></span>) &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> id = <span class="keyword">await</span> <span class="title function_">requestSave</span>()</span><br><span class="line">    options.<span class="property">onSuccess</span>?.(id)</span><br><span class="line">  &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">    options.<span class="property">onError</span>?.(error <span class="keyword">as</span> <span class="title class_">Error</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它比 <code>if (options.onSuccess)</code> 更紧凑，也准确表达“函数可能不存在”。不过如果属性存在却不是函数，调用仍会抛错；来自不可信 JSON 的值不能只靠可选调用保护。</p><h2 id="升级时不只检查-TypeScript-编译器"><a href="#升级时不只检查-TypeScript-编译器" class="headerlink" title="升级时不只检查 TypeScript 编译器"></a>升级时不只检查 TypeScript 编译器</h2><p>新语法需要整条工具链认识。TypeScript 能编译，不代表旧版 Babel、ESLint、压缩器和测试器都能解析源码。我们升级时检查了：</p><ol><li><code>tsc</code> 输出目标是否符合浏览器范围；</li><li>Babel&#x2F;loader 是否重复转换或破坏 source map；</li><li>ESLint parser 与格式化工具是否支持新语法；</li><li>Jest 等测试环境读取的是源码还是编译产物；</li><li>实际生产包中是否保留目标浏览器不支持的语法。</li></ol><h2 id="写得更短不等于模型更可靠"><a href="#写得更短不等于模型更可靠" class="headerlink" title="写得更短不等于模型更可靠"></a>写得更短不等于模型更可靠</h2><p>TypeScript 3.7 让日常代码舒服了很多。到今天，可选链与空值合并已经像默认语言能力一样自然。但我仍会在评审时问一句：这个值为什么可能为空？</p><p>如果答案是“业务允许缺失”，<code>?.</code>&#x2F;<code>??</code> 很合适；如果答案是“接口偶尔不靠谱”，应该先校验和修数据契约；如果答案是“不知道”，连续十个问号只会把错误推迟到更难发现的位置。简洁语法的最好用法，是更准确地表达边界，而不是让所有异常都沉默。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://www.typescriptlang.org/docs/handbook/release-notes/typescript-3-7.html">TypeScript 3.7 发布说明</a></li><li><a href="https://tc39.es/ecma262/multipage/ecmascript-language-expressions.html#sec-optional-chaining">ECMAScript：Optional Chaining</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 2019 </tag>
            
            <tag> TypeScript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>React Hooks 闭包陷阱：旧状态、无限循环与重复订阅</title>
      <link href="/2019/08/16/frontend-debug-react-hooks-stale-closure/"/>
      <url>/2019/08/16/frontend-debug-react-hooks-stale-closure/</url>
      
        <content type="html"><![CDATA[<p>这是一次定时保存功能引出的 Bug。编辑器右上角显示“已保存”，服务端收到的却偶尔是十几秒前的内容。网络面板没有失败，接口参数也合法，所以问题在测试环境里躲了很久。</p><p>最终原因是：页面首次渲染时创建的 interval 一直持有那次渲染里的 <code>content</code>。用户后续输入会触发新的渲染，但老 interval 不会自动换成新函数。它没有“读错 state”，只是忠实地读了自己闭包中的旧快照。</p><h2 id="事故代码只有十几行"><a href="#事故代码只有十几行" class="headerlink" title="事故代码只有十几行"></a>事故代码只有十几行</h2><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Editor</span>(<span class="params">&#123; documentId &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [content, setContent] = <span class="title function_">useState</span>(<span class="string">&#x27;&#x27;</span>)</span><br><span class="line"></span><br><span class="line">  <span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> timer = <span class="built_in">setInterval</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="title function_">saveDocument</span>(documentId, content)</span><br><span class="line">    &#125;, <span class="number">10_000</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="function">() =&gt;</span> <span class="built_in">clearInterval</span>(timer)</span><br><span class="line">  &#125;, [])</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">textarea</span> <span class="attr">value</span>=<span class="string">&#123;content&#125;</span> <span class="attr">onChange</span>=<span class="string">&#123;e</span> =&gt;</span> setContent(e.target.value)&#125; /&gt;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>空依赖数组让 Effect 只为这次挂载建立 interval。回调捕获的是首次渲染的 <code>documentId</code> 与 <code>content</code>。每次 <code>setContent</code> 都会重新调用组件，产生一个新的 <code>content</code> 常量和新的函数，但已经交给 <code>setInterval</code> 的旧函数仍然存在。</p><p>把函数组件理解成“每次渲染都是一次独立函数调用”，这个问题就不神秘了。</p><h2 id="先做一个能看见快照的最小例子"><a href="#先做一个能看见快照的最小例子" class="headerlink" title="先做一个能看见快照的最小例子"></a>先做一个能看见快照的最小例子</h2><p>为了确认不是保存接口缓存，我把逻辑缩成计数器：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Counter</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [count, setCount] = <span class="title function_">useState</span>(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">  <span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> timer = <span class="built_in">setInterval</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;interval sees:&#x27;</span>, count)</span><br><span class="line">    &#125;, <span class="number">1000</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="function">() =&gt;</span> <span class="built_in">clearInterval</span>(timer)</span><br><span class="line">  &#125;, [])</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;()</span> =&gt;</span> setCount(count + 1)&#125;&gt;&#123;count&#125;<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>按钮数字一直增加，控制台始终打印 0。这个最小例子把请求、编辑器和服务端全部排除，只剩渲染快照与 Effect。</p><h2 id="第一种修法：把真实依赖写完整"><a href="#第一种修法：把真实依赖写完整" class="headerlink" title="第一种修法：把真实依赖写完整"></a>第一种修法：把真实依赖写完整</h2><p>最直接的方式是把依赖加入数组：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> timer = <span class="built_in">setInterval</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">saveDocument</span>(documentId, content)</span><br><span class="line">  &#125;, <span class="number">10_000</span>)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="function">() =&gt;</span> <span class="built_in">clearInterval</span>(timer)</span><br><span class="line">&#125;, [documentId, content])</span><br></pre></td></tr></table></figure><p>它能拿到最新内容，但用户每输入一次都会清理并重建定时器。结果是持续输入时 10 秒倒计时不断重置，可能一直不保存。这不是 React 的问题，而是我们的需求其实不是“内容变化后重建 interval”。</p><p>依赖数组首先服务于正确性，不是性能开关。写全以后发现行为不符合需求，应该重新设计同步模型，而不是删掉依赖让 lint 安静。</p><h2 id="更适合编辑器的是脏标记与稳定调度器"><a href="#更适合编辑器的是脏标记与稳定调度器" class="headerlink" title="更适合编辑器的是脏标记与稳定调度器"></a>更适合编辑器的是脏标记与稳定调度器</h2><p>我们最后把“是否需要保存”与“定时调度”分开。最新草稿放在 ref 中，内容变化时标记 dirty，interval 本身保持稳定：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">useAutoSave</span>(<span class="params">documentId, content</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> latestDraft = <span class="title function_">useRef</span>(&#123; documentId, content &#125;)</span><br><span class="line">  <span class="keyword">const</span> dirty = <span class="title function_">useRef</span>(<span class="literal">false</span>)</span><br><span class="line"></span><br><span class="line">  <span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    latestDraft.<span class="property">current</span> = &#123; documentId, content &#125;</span><br><span class="line">    dirty.<span class="property">current</span> = <span class="literal">true</span></span><br><span class="line">  &#125;, [documentId, content])</span><br><span class="line"></span><br><span class="line">  <span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> timer = <span class="built_in">setInterval</span>(<span class="title function_">async</span> () =&gt; &#123;</span><br><span class="line">      <span class="keyword">if</span> (!dirty.<span class="property">current</span>) <span class="keyword">return</span></span><br><span class="line"></span><br><span class="line">      <span class="keyword">const</span> draft = latestDraft.<span class="property">current</span></span><br><span class="line">      <span class="keyword">await</span> <span class="title function_">saveDocument</span>(draft.<span class="property">documentId</span>, draft.<span class="property">content</span>)</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (latestDraft.<span class="property">current</span> === draft) &#123;</span><br><span class="line">        dirty.<span class="property">current</span> = <span class="literal">false</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;, <span class="number">10_000</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="function">() =&gt;</span> <span class="built_in">clearInterval</span>(timer)</span><br><span class="line">  &#125;, [])</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>真实项目还要处理请求重叠、失败重试、卸载前提示和版本冲突。这里的 ref 不是为了逃避依赖，而是刻意保存一个不参与渲染、需要被异步调度器读取的最新可变快照。</p><p>如果 UI 也要显示 dirty 状态，就应该用 state；ref 变化不会触发渲染。</p><h2 id="计数器则应该使用函数式更新"><a href="#计数器则应该使用函数式更新" class="headerlink" title="计数器则应该使用函数式更新"></a>计数器则应该使用函数式更新</h2><p>另一种常见闭包 Bug 是 interval 内 <code>setCount(count + 1)</code>：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> timer = <span class="built_in">setInterval</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">setCount</span>(count + <span class="number">1</span>)</span><br><span class="line">  &#125;, <span class="number">1000</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="function">() =&gt;</span> <span class="built_in">clearInterval</span>(timer)</span><br><span class="line">&#125;, [])</span><br></pre></td></tr></table></figure><p>回调一直看到 0，所以每次都设置 1。这里不需要读取业务上的“最新 count”，只是要基于上一状态更新。函数式写法最准确：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> timer = <span class="built_in">setInterval</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">setCount</span>(<span class="function"><span class="params">previous</span> =&gt;</span> previous + <span class="number">1</span>)</span><br><span class="line">  &#125;, <span class="number">1000</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="function">() =&gt;</span> <span class="built_in">clearInterval</span>(timer)</span><br><span class="line">&#125;, [])</span><br></pre></td></tr></table></figure><p>React 把最新状态传给 updater，Effect 不再依赖 <code>count</code>。比起用 ref 镜像每一个 state，这种写法更简单。</p><h2 id="加完依赖后无限请求，通常暴露了另一个问题"><a href="#加完依赖后无限请求，通常暴露了另一个问题" class="headerlink" title="加完依赖后无限请求，通常暴露了另一个问题"></a>加完依赖后无限请求，通常暴露了另一个问题</h2><p>有人为了遵守 exhaustive-deps 把函数加入依赖，页面立刻循环：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Search</span>(<span class="params">&#123; keyword &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [result, setResult] = <span class="title function_">useState</span>([])</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> options = &#123; keyword, <span class="attr">pageSize</span>: <span class="number">20</span> &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">search</span>(options).<span class="title function_">then</span>(setResult)</span><br><span class="line">  &#125;, [options])</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>每次渲染都会创建新 <code>options</code>，引用变化让 Effect 再执行，<code>setResult</code> 又触发渲染。这里可以在 Effect 内创建对象，只依赖原始值：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> controller = <span class="keyword">new</span> <span class="title class_">AbortController</span>()</span><br><span class="line">  <span class="keyword">const</span> options = &#123; keyword, <span class="attr">pageSize</span>: <span class="number">20</span> &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">search</span>(options, controller.<span class="property">signal</span>).<span class="title function_">then</span>(setResult)</span><br><span class="line">  <span class="keyword">return</span> <span class="function">() =&gt;</span> controller.<span class="title function_">abort</span>()</span><br><span class="line">&#125;, [keyword])</span><br></pre></td></tr></table></figure><p>不是所有对象都要 <code>useMemo</code>。先缩小 Effect 的输入，通常比到处稳定引用更清楚。</p><h2 id="StrictMode-的“两次执行”帮我们发现了泄漏"><a href="#StrictMode-的“两次执行”帮我们发现了泄漏" class="headerlink" title="StrictMode 的“两次执行”帮我们发现了泄漏"></a>StrictMode 的“两次执行”帮我们发现了泄漏</h2><p>React 18 开发环境的 StrictMode 会额外执行一次 Effect setup&#x2F;cleanup，用来暴露没有正确清理的副作用。我们曾看到 WebSocket 连了两次，第一反应是“React 重复执行”；继续查才发现 Effect 根本没有返回 unsubscribe。</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> connection = <span class="title function_">connect</span>(roomId)</span><br><span class="line">  connection.<span class="title function_">open</span>()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="function">() =&gt;</span> connection.<span class="title function_">close</span>()</span><br><span class="line">&#125;, [roomId])</span><br></pre></td></tr></table></figure><p>在 setup → cleanup → setup 后仍然正确，说明生命周期更稳健。生产环境不一定重复执行，但用户切路由、热更新和错误恢复本来就会发生挂载&#x2F;卸载，清理不是只为 StrictMode。</p><h2 id="我用什么标准选修法"><a href="#我用什么标准选修法" class="headerlink" title="我用什么标准选修法"></a>我用什么标准选修法</h2><p>遇到旧闭包时，不应该统一回答“加 ref”。我会按意图选择：</p><ul><li>只是基于旧 state 更新：使用函数式 updater；</li><li>Effect 确实随某值变化而重新同步：写完整依赖；</li><li>值能在渲染中直接计算：删除 Effect；</li><li>外部长期回调需要读取最新值、但变化不应重建订阅：使用受控的 ref 或框架提供的 Effect Event 能力；</li><li>请求随输入变化：写依赖，同时取消旧请求或忽略过期响应；</li><li>对象&#x2F;函数每次新建：先把创建移动到真正使用的位置，再判断是否需要 memo。</li></ul><h2 id="防止复发比修这一处更重要"><a href="#防止复发比修这一处更重要" class="headerlink" title="防止复发比修这一处更重要"></a>防止复发比修这一处更重要</h2><p>修完自动保存后，我们开启并认真处理 <code>eslint-plugin-react-hooks</code> 的依赖规则，没有再用整页 disable 注释。还给自动保存补了假定时器测试：连续输入、请求失败、切换文档和卸载都要验证。</p><p>闭包不是 Hooks 独有的陷阱，JavaScript 一直如此。Hooks 让每次渲染的值快照更明显，也让错误依赖更容易影响订阅。把每次渲染当成独立快照、把 Effect 当成外部同步过程，很多“React 为什么拿不到最新 state”的问题就能沿着引用关系解释清楚。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://react.dev/learn/state-as-a-snapshot">React：State as a Snapshot</a></li><li><a href="https://react.dev/learn/removing-effect-dependencies">React：Removing Effect Dependencies</a></li><li><a href="https://legacy.reactjs.org/blog/2019/02/06/react-v16.8.0.html">React v16.8：Hooks</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端排障 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> React </tag>
            
            <tag> 前端排障 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Svelte 3：把响应式工作前移到编译阶段</title>
      <link href="/2019/04/22/frontend-chronicle-2019-04-svelte-3-compiler-reactivity/"/>
      <url>/2019/04/22/frontend-chronicle-2019-04-svelte-3-compiler-reactivity/</url>
      
        <content type="html"><![CDATA[<p>第一次看到 Svelte 3 的代码，我以为 <code>$:</code> 只是另一种 computed 语法。真正让我理解它的是打开 REPL 的编译结果：组件并没有把一棵虚拟 DOM 带到浏览器再反复比较，而是编译出创建节点、标记脏值和精确更新文本的命令。</p><p>这篇不做框架跑分，也不试图证明谁“更先进”。我只用一个购物车小组件，看看 Svelte 3 把哪些工作从运行时挪到了编译阶段，以及这种设计给代码带来什么约束。</p><h2 id="先写一个会算总价的组件"><a href="#先写一个会算总价的组件" class="headerlink" title="先写一个会算总价的组件"></a>先写一个会算总价的组件</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line">  export let product</span><br><span class="line"></span><br><span class="line">  let count = 1</span><br><span class="line">  $: subtotal = product.price * count</span><br><span class="line">  $: canCheckout = product.stock &gt;= count &amp;&amp; count &gt; 0</span><br><span class="line"></span><br><span class="line">  function increase() &#123;</span><br><span class="line">    if (count &lt; product.stock) count += 1</span><br><span class="line">  &#125;</span><br><span class="line">&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;article&gt;</span><br><span class="line">  &lt;h2&gt;&#123;product.name&#125;&lt;/h2&gt;</span><br><span class="line">  &lt;button on:click=&#123;increase&#125;&gt;+&lt;/button&gt;</span><br><span class="line">  &lt;span&gt;&#123;count&#125;&lt;/span&gt;</span><br><span class="line">  &lt;strong&gt;¥&#123;subtotal&#125;&lt;/strong&gt;</span><br><span class="line">  &lt;button disabled=&#123;!canCheckout&#125;&gt;结算&lt;/button&gt;</span><br><span class="line">&lt;/article&gt;</span><br></pre></td></tr></table></figure><p><code>subtotal</code> 和 <code>canCheckout</code> 的依赖可以在编译时从变量引用中分析出来。当 <code>count</code> 改变，生成代码知道哪些表达式需要重新计算、哪些 DOM 需要更新，不必在浏览器里重新执行一套通用组件 diff。</p><p>如果只看最终行为，它和其他框架的派生状态没有本质冲突；差别在于很多框架把追踪和比较留给运行时，Svelte 让编译器更深地理解组件语义。</p><h2 id="“赋值触发更新”有一个容易踩的边界"><a href="#“赋值触发更新”有一个容易踩的边界" class="headerlink" title="“赋值触发更新”有一个容易踩的边界"></a>“赋值触发更新”有一个容易踩的边界</h2><p>Svelte 3 的响应式与赋值紧密相关。下面这段数组变更在早期写法中经常让人困惑：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line">  let items = []</span><br><span class="line"></span><br><span class="line">  function addItem(item) &#123;</span><br><span class="line">    items.push(item)</span><br><span class="line">  &#125;</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure><p><code>push</code> 修改了数组内容，但没有对 <code>items</code> 赋值。为了让编译器看到更新，可以创建新数组或补一次赋值：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">function addItem(item) &#123;</span><br><span class="line">  items = [...items, item]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这说明“没有虚拟 DOM”不等于“任意对象变化都能自动知道”。编译器需要可识别的信号。代码写得越隐式，例如把对象传给外部函数深层修改，分析越困难。</p><h2 id="响应式语句既能算值，也能执行副作用"><a href="#响应式语句既能算值，也能执行副作用" class="headerlink" title="响应式语句既能算值，也能执行副作用"></a>响应式语句既能算值，也能执行副作用</h2><p><code>$:</code> 后面不只能赋值，也能放语句：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$: if (count &gt; product.stock) &#123;</span><br><span class="line">  count = product.stock</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这很方便，却可能形成难以看懂的依赖链。多个响应式语句会按依赖排序执行，不按肉眼看到的行号简单决定。若一条语句通过隐藏函数读取变量，编译器未必能从调用点看出完整依赖。</p><p>我会把纯派生值留给 <code>$:</code>，订阅、计时器和网络等外部副作用放进生命周期，并返回清理函数：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line">  import &#123; onMount &#125; from &#x27;svelte&#x27;</span><br><span class="line"></span><br><span class="line">  let online = true</span><br><span class="line"></span><br><span class="line">  onMount(() =&gt; &#123;</span><br><span class="line">    const update = () =&gt; online = navigator.onLine</span><br><span class="line">    window.addEventListener(&#x27;online&#x27;, update)</span><br><span class="line">    window.addEventListener(&#x27;offline&#x27;, update)</span><br><span class="line"></span><br><span class="line">    return () =&gt; &#123;</span><br><span class="line">      window.removeEventListener(&#x27;online&#x27;, update)</span><br><span class="line">      window.removeEventListener(&#x27;offline&#x27;, update)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure><p>语法简洁不应该成为把所有逻辑塞进一个组件的理由，生命周期和资源释放仍然存在。</p><h2 id="编译产物值得看一次，但不必依赖它"><a href="#编译产物值得看一次，但不必依赖它" class="headerlink" title="编译产物值得看一次，但不必依赖它"></a>编译产物值得看一次，但不必依赖它</h2><p>实际生成代码会随版本和编译选项变化，所以不应该在业务里依赖内部结构。不过观察一次大概形态很有帮助。编译器会把模板拆成创建、挂载、更新、销毁等操作，并用脏值标记决定更新哪些位置。</p><p>可以把思路粗略理解为：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">update</span>(<span class="params">changed, state</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (changed.<span class="property">count</span> || changed.<span class="property">product</span>) &#123;</span><br><span class="line">    textNode.<span class="property">data</span> = state.<span class="property">product</span>.<span class="property">price</span> * state.<span class="property">count</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (changed.<span class="property">canCheckout</span>) &#123;</span><br><span class="line">    checkoutButton.<span class="property">disabled</span> = !state.<span class="property">canCheckout</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这不是 Svelte 的真实输出，只是帮助理解：框架的通用工作被编译成组件专用更新路径。小组件通常带来很少的运行时代码，但应用变大后，业务代码、依赖和路由仍然决定总体积，不能拿 Hello World 结果外推所有场景。</p><h2 id="Store-解决跨组件共享，但别把所有东西都塞进去"><a href="#Store-解决跨组件共享，但别把所有东西都塞进去" class="headerlink" title="Store 解决跨组件共享，但别把所有东西都塞进去"></a>Store 解决跨组件共享，但别把所有东西都塞进去</h2><p>Svelte store 使用可订阅契约，组件中用 <code>$store</code> 读取时，编译器会处理订阅和清理：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// cart.js</span></span><br><span class="line"><span class="keyword">import</span> &#123; writable, derived &#125; <span class="keyword">from</span> <span class="string">&#x27;svelte/store&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> cart = <span class="title function_">writable</span>([])</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> total = <span class="title function_">derived</span>(cart, <span class="function"><span class="params">items</span> =&gt;</span></span><br><span class="line">  items.<span class="title function_">reduce</span>(<span class="function">(<span class="params">sum, item</span>) =&gt;</span> sum + item.<span class="property">price</span> * item.<span class="property">count</span>, <span class="number">0</span>)</span><br><span class="line">)</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line">  import &#123; cart, total &#125; from &#x27;./cart.js&#x27;</span><br><span class="line">&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;p&gt;共 &#123;$cart.length&#125; 种商品，合计 ¥&#123;$total&#125;&lt;/p&gt;</span><br></pre></td></tr></table></figure><p>局部输入状态继续放在组件里，跨页面购物车才进入 store。否则 store 很快会退化成全局变量仓库，编译器也无法替我们解决领域边界。</p><h2 id="选型时，我会问这些而不是只问快不快"><a href="#选型时，我会问这些而不是只问快不快" class="headerlink" title="选型时，我会问这些而不是只问快不快"></a>选型时，我会问这些而不是只问快不快</h2><p>Svelte 3 让我确认，前端框架不必都围绕虚拟 DOM 设计。但项目选型还要看：</p><ul><li>团队能否理解编译语义和响应式边界；</li><li>路由、SSR、测试、组件库和调试工具是否满足项目；</li><li>第三方组件和招聘&#x2F;交接成本怎样；</li><li>框架升级时编译输出与生态插件能否稳定迁移；</li><li>性能瓶颈是否真的在框架运行时，而不是网络和业务依赖。</li></ul><p>Svelte 3 的意义不只是一个框架版本。它让“编译器可以理解多少组件语义”成为主流讨论，后来其他框架也越来越重视编译优化、细粒度更新和减少手工 memo。对普通开发者来说，最直接的收获是：写下的一行响应式代码并非魔法，背后总有一套可分析的依赖和更新规则。理解规则，才能知道什么时候它不会按想象工作。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://svelte.dev/blog/svelte-3-rethinking-reactivity">Svelte 3：Rethinking reactivity</a></li><li><a href="https://svelte.dev/playground/reactive-declarations">Svelte Playground：Reactive declarations（历史语法）</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 2019 </tag>
            
            <tag> Svelte </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>React Hooks：状态逻辑从类组件中解放出来</title>
      <link href="/2019/02/06/frontend-chronicle-2019-02-react-hooks/"/>
      <url>/2019/02/06/frontend-chronicle-2019-02-react-hooks/</url>
      
        <content type="html"><![CDATA[<p>React 16.8 发布 Hooks 后，我们没有立刻把类组件全改掉。第一个试点是一张实时物流卡片：它既要订阅订单状态，又要监听页面可见性，还要记录曝光。类组件里，同一条业务逻辑散落在 <code>componentDidMount</code>、<code>componentDidUpdate</code> 和 <code>componentWillUnmount</code>，改一个订阅条件需要来回跳三个位置。</p><p>Hooks 真正吸引我的不是“函数组件也能 setState”，而是可以按业务能力组织状态和副作用。不过在最初几次改造里，我们也把 Effect 当成了新的生命周期容器，写出了依赖缺失、重复订阅和派生状态等问题。Hooks 减少了一类复杂度，同时要求我们更准确地理解渲染与闭包。</p><h2 id="改造前：物流订阅被拆在三个生命周期里"><a href="#改造前：物流订阅被拆在三个生命周期里" class="headerlink" title="改造前：物流订阅被拆在三个生命周期里"></a>改造前：物流订阅被拆在三个生命周期里</h2><p>简化后的类组件是这样：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">ShipmentCard</span> <span class="keyword">extends</span> <span class="title class_ inherited__">React.Component</span> &#123;</span><br><span class="line">  state = &#123; <span class="attr">shipment</span>: <span class="literal">null</span> &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">componentDidMount</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">unsubscribe</span> = shipmentStore.<span class="title function_">subscribe</span>(</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">props</span>.<span class="property">orderId</span>,</span><br><span class="line">      <span class="function"><span class="params">shipment</span> =&gt;</span> <span class="variable language_">this</span>.<span class="title function_">setState</span>(&#123; shipment &#125;)</span><br><span class="line">    )</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">componentDidUpdate</span>(<span class="params">prevProps</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (prevProps.<span class="property">orderId</span> !== <span class="variable language_">this</span>.<span class="property">props</span>.<span class="property">orderId</span>) &#123;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="title function_">unsubscribe</span>()</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">unsubscribe</span> = shipmentStore.<span class="title function_">subscribe</span>(</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">props</span>.<span class="property">orderId</span>,</span><br><span class="line">        <span class="function"><span class="params">shipment</span> =&gt;</span> <span class="variable language_">this</span>.<span class="title function_">setState</span>(&#123; shipment &#125;)</span><br><span class="line">      )</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">componentWillUnmount</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">unsubscribe</span>()</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">ShipmentView</span> <span class="attr">data</span>=<span class="string">&#123;this.state.shipment&#125;</span> /&gt;</span></span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这不是类组件写错了，而是“订阅某个 orderId”这个完整能力被生命周期切开。漏掉更新时重新订阅，切订单后就显示旧数据；漏掉卸载清理，又会在后台继续接收消息。</p><h2 id="第一个自定义-Hook-只做一件完整的事"><a href="#第一个自定义-Hook-只做一件完整的事" class="headerlink" title="第一个自定义 Hook 只做一件完整的事"></a>第一个自定义 Hook 只做一件完整的事</h2><p>改成 Hook 后，订阅与清理靠得更近：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">useShipment</span>(<span class="params">orderId</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [shipment, setShipment] = <span class="title function_">useState</span>(<span class="literal">null</span>)</span><br><span class="line"></span><br><span class="line">  <span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">setShipment</span>(<span class="literal">null</span>)</span><br><span class="line">    <span class="keyword">return</span> shipmentStore.<span class="title function_">subscribe</span>(orderId, setShipment)</span><br><span class="line">  &#125;, [orderId])</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> shipment</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">ShipmentCard</span>(<span class="params">&#123; orderId &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> shipment = <span class="title function_">useShipment</span>(orderId)</span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">ShipmentView</span> <span class="attr">data</span>=<span class="string">&#123;shipment&#125;</span> /&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当 <code>orderId</code> 改变，React 先执行上一个 Effect 的 cleanup，再用新值建立订阅。调用者只看到 <code>useShipment(orderId)</code>，不需要知道内部是 WebSocket、轮询还是 store。</p><p>这才是自定义 Hook 的复用单位：围绕“能力”封装，而不是简单模仿 <code>componentDidMount</code> 写一个 <code>useMount</code>。如果抽象只隐藏了两行代码，却把真实依赖藏起来，反而更难维护。</p><h2 id="Hooks-的规则不是风格偏好"><a href="#Hooks-的规则不是风格偏好" class="headerlink" title="Hooks 的规则不是风格偏好"></a>Hooks 的规则不是风格偏好</h2><p>Hook 必须在组件顶层以稳定顺序调用，不能放在条件和循环中：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 错误：enabled 改变后，Hook 调用顺序可能变化</span></span><br><span class="line"><span class="keyword">if</span> (enabled) &#123;</span><br><span class="line">  <span class="keyword">const</span> [value, setValue] = <span class="title function_">useState</span>(<span class="string">&#x27;&#x27;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以把条件放进 Hook 内部，或拆成子组件：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">ShipmentPanel</span>(<span class="params">&#123; enabled, orderId &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> shipment = <span class="title function_">useShipment</span>(enabled ? orderId : <span class="literal">null</span>)</span><br><span class="line">  <span class="keyword">return</span> enabled ? <span class="language-xml"><span class="tag">&lt;<span class="name">ShipmentView</span> <span class="attr">data</span>=<span class="string">&#123;shipment&#125;</span> /&gt;</span></span> : <span class="literal">null</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对应的 <code>useShipment</code> 也要处理 <code>null</code>，不建立订阅。稳定调用顺序让 React 能把每次渲染中的 Hook 与之前的状态槽对应起来，这不是 lint 工具随意制定的限制。</p><h2 id="我们一度把所有计算都塞进-Effect"><a href="#我们一度把所有计算都塞进-Effect" class="headerlink" title="我们一度把所有计算都塞进 Effect"></a>我们一度把所有计算都塞进 Effect</h2><p>下面是早期很常见的写法：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">ProductList</span>(<span class="params">&#123; products, keyword &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> [visibleProducts, setVisibleProducts] = <span class="title function_">useState</span>([])</span><br><span class="line"></span><br><span class="line">  <span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">setVisibleProducts</span>(</span><br><span class="line">      products.<span class="title function_">filter</span>(<span class="function"><span class="params">product</span> =&gt;</span> product.<span class="property">name</span>.<span class="title function_">includes</span>(keyword))</span><br><span class="line">    )</span><br><span class="line">  &#125;, [products, keyword])</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">List</span> <span class="attr">items</span>=<span class="string">&#123;visibleProducts&#125;</span> /&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>visibleProducts</code> 完全可以由当前 props 算出，却被保存成第二份状态。组件先用旧列表渲染一次，Effect 再 setState 触发第二次渲染，还要维护依赖数组。</p><p>直接计算更简单：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">ProductList</span>(<span class="params">&#123; products, keyword &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> visibleProducts = products.<span class="title function_">filter</span>(<span class="function"><span class="params">product</span> =&gt;</span></span><br><span class="line">    product.<span class="property">name</span>.<span class="title function_">includes</span>(keyword)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">List</span> <span class="attr">items</span>=<span class="string">&#123;visibleProducts&#125;</span> /&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>只有计算确实昂贵、并且能测到收益时才考虑 <code>useMemo</code>。它是性能优化，不是保证语义正确的缓存层。</p><h2 id="依赖数组记录的是闭包用到了什么"><a href="#依赖数组记录的是闭包用到了什么" class="headerlink" title="依赖数组记录的是闭包用到了什么"></a>依赖数组记录的是闭包用到了什么</h2><p>Effect 中读取的 props、state 和组件内函数，通常都应该出现在依赖中。空数组不是“只执行一次”的装饰器，而是声明这段 Effect 不依赖会变化的渲染值。</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">useEffect</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> controller = <span class="keyword">new</span> <span class="title class_">AbortController</span>()</span><br><span class="line"></span><br><span class="line">  <span class="title function_">fetch</span>(<span class="string">`/api/orders/<span class="subst">$&#123;orderId&#125;</span>`</span>, &#123; <span class="attr">signal</span>: controller.<span class="property">signal</span> &#125;)</span><br><span class="line">    .<span class="title function_">then</span>(<span class="function"><span class="params">response</span> =&gt;</span> response.<span class="title function_">json</span>())</span><br><span class="line">    .<span class="title function_">then</span>(setOrder)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="function">() =&gt;</span> controller.<span class="title function_">abort</span>()</span><br><span class="line">&#125;, [orderId])</span><br></pre></td></tr></table></figure><p>如果为了阻止重复请求而删掉 <code>orderId</code>，切换订单后闭包仍保存旧 ID。正确方向是让请求可取消、让数据层去重，而不是对 lint 提示闭眼。</p><p>函数依赖每次变化时，也不要立刻给所有函数套 <code>useCallback</code>。先看能否把函数移进 Effect、把不相关逻辑移出组件，或者使用函数式状态更新减少依赖。过度 memo 会增加比较和理解成本。</p><h2 id="useReducer-适合有事件语义的状态"><a href="#useReducer-适合有事件语义的状态" class="headerlink" title="useReducer 适合有事件语义的状态"></a>useReducer 适合有事件语义的状态</h2><p>多个布尔 state 很容易组成非法状态，例如 <code>loading=true</code> 同时 <code>success=true</code>。物流卡片后来用 reducer 表达状态机式变化：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> initialState = &#123; <span class="attr">status</span>: <span class="string">&#x27;idle&#x27;</span>, <span class="attr">data</span>: <span class="literal">null</span>, <span class="attr">error</span>: <span class="literal">null</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">reducer</span>(<span class="params">state, action</span>) &#123;</span><br><span class="line">  <span class="keyword">switch</span> (action.<span class="property">type</span>) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">&#x27;requested&#x27;</span>:</span><br><span class="line">      <span class="keyword">return</span> &#123; <span class="attr">status</span>: <span class="string">&#x27;loading&#x27;</span>, <span class="attr">data</span>: state.<span class="property">data</span>, <span class="attr">error</span>: <span class="literal">null</span> &#125;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">&#x27;received&#x27;</span>:</span><br><span class="line">      <span class="keyword">return</span> &#123; <span class="attr">status</span>: <span class="string">&#x27;success&#x27;</span>, <span class="attr">data</span>: action.<span class="property">data</span>, <span class="attr">error</span>: <span class="literal">null</span> &#125;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">&#x27;failed&#x27;</span>:</span><br><span class="line">      <span class="keyword">return</span> &#123; <span class="attr">status</span>: <span class="string">&#x27;error&#x27;</span>, <span class="attr">data</span>: <span class="literal">null</span>, <span class="attr">error</span>: action.<span class="property">error</span> &#125;</span><br><span class="line">    <span class="attr">default</span>:</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`Unknown action: <span class="subst">$&#123;action.type&#125;</span>`</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这不是说 state 多了就必须 reducer，而是当更新具有明确事件和约束时，reducer 能把规则集中起来。Hook 只是让它可以在函数组件中自然使用。</p><h2 id="没有进行的“大重写”反而是正确决定"><a href="#没有进行的“大重写”反而是正确决定" class="headerlink" title="没有进行的“大重写”反而是正确决定"></a>没有进行的“大重写”反而是正确决定</h2><p>Hooks 与类组件可以共存。我们优先在新功能和逻辑复用困难的组件中使用，稳定的类组件只在需要修改时再迁移。因为机械转换不会自动改善边界，甚至可能把清晰的生命周期翻译成几个更混乱的 Effect。</p><p>几年以后再看，Hooks 改变了 React 项目的默认组织方式，但它并没有让副作用消失。我的经验可以缩成三句话：渲染阶段只做计算；Effect 用来同步外部系统，并始终考虑清理；自定义 Hook 按业务能力抽象，而不是按代码行数抽象。遵守这三点，比背完全部 Hook API 更有用。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://legacy.reactjs.org/blog/2019/02/06/react-v16.8.0.html">React v16.8：Hooks 发布说明</a></li><li><a href="https://react.dev/reference/rules/rules-of-hooks">React：Rules of Hooks</a></li><li><a href="https://react.dev/learn/you-might-not-need-an-effect">React：You Might Not Need an Effect</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> React </tag>
            
            <tag> 2019 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Babel 7：现代 JavaScript 转译体系重新整理</title>
      <link href="/2018/08/27/frontend-chronicle-2018-08-babel-7-modern-transpilation/"/>
      <url>/2018/08/27/frontend-chronicle-2018-08-babel-7-modern-transpilation/</url>
      
        <content type="html"><![CDATA[<p>有一次测试同事拿着 IE 11 的报错截图来找我：<code>对象不支持“includes”属性或方法</code>。我打开构建产物，箭头函数、模板字符串都已经被 Babel 转成旧语法，于是下意识地认为“Babel 明明生效了”。</p><p>后来才意识到，<code>Array.prototype.includes</code> 是运行时 API，不是可以简单改写的语法。Babel 能把箭头函数变成普通函数，却不能凭空让旧浏览器拥有一个不存在的方法。Babel 7 前后，团队逐渐把“语法转换、目标环境、polyfill”当作三个相连但不同的问题，这比包名升级本身重要得多。</p><h2 id="先把-Babel-的工作分成三层"><a href="#先把-Babel-的工作分成三层" class="headerlink" title="先把 Babel 的工作分成三层"></a>先把 Babel 的工作分成三层</h2><p>以这段代码为例：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">loadUsers</span> = <span class="keyword">async</span> (<span class="params"></span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">&#x27;/api/users&#x27;</span>)</span><br><span class="line">  <span class="keyword">const</span> users = <span class="keyword">await</span> response.<span class="title function_">json</span>()</span><br><span class="line">  <span class="keyword">return</span> users.<span class="title function_">find</span>(<span class="function"><span class="params">user</span> =&gt;</span> user.<span class="property">active</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里混着三类兼容性：</p><ul><li><code>const</code>、箭头函数和 async 函数属于语法；</li><li>async 转换后可能需要 generator&#x2F;runtime 辅助；</li><li><code>fetch</code> 与 <code>Array.prototype.find</code> 是宿主或内建 API。</li></ul><p>Babel 负责解析并转换语法，preset 根据目标环境决定哪些转换需要启用，polyfill 策略则补齐目标运行时缺少的 API。把三者都叫“转译”，排查时就很容易答非所问。</p><h2 id="Babel-7-升级不是只改包名前缀"><a href="#Babel-7-升级不是只改包名前缀" class="headerlink" title="Babel 7 升级不是只改包名前缀"></a>Babel 7 升级不是只改包名前缀</h2><p>官方包进入 <code>@babel</code> 命名空间后，配置也更容易看出职责：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;presets&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;@babel/preset-env&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;targets&quot;</span><span class="punctuation">:</span> <span class="string">&quot;&gt; 0.5%, not dead&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;useBuiltIns&quot;</span><span class="punctuation">:</span> <span class="string">&quot;usage&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;corejs&quot;</span><span class="punctuation">:</span> <span class="string">&quot;3.0&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这只是当时的一种项目配置示意，不能不加判断地复制到今天。关键是 <code>targets</code> 必须对应产品实际支持范围，<code>useBuiltIns</code> 与 core-js 版本必须由项目统一管理。若入口又手工导入整套 polyfill，业务文件里还按需注入，产物可能出现重复代码。</p><p>我们升级时把旧包逐个对应到新名字，而不是同时更换所有 preset：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">babel-core       -&gt; @babel/core</span><br><span class="line">babel-preset-env -&gt; @babel/preset-env</span><br><span class="line">babel-loader     -&gt; 与 Babel 7 兼容的 babel-loader</span><br></pre></td></tr></table></figure><p>先保证转换结果不变，再调整浏览器目标与 polyfill。把“版本迁移”和“兼容策略重构”拆开后，出现问题时才知道是哪一层造成的。</p><h2 id="Browserslist-才是产品支持声明"><a href="#Browserslist-才是产品支持声明" class="headerlink" title="Browserslist 才是产品支持声明"></a>Browserslist 才是产品支持声明</h2><p>以前很多配置直接写 <code>targets: { ie: 11 }</code>，另一个工具又维护自己的浏览器列表。后来我们把目标集中到 <code>package.json</code> 的 Browserslist：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;browserslist&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;production&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;&gt; 0.5%&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;last 2 versions&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;not dead&quot;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;development&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;last 1 chrome version&quot;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>Babel、Autoprefixer 等工具可以共享这份声明。但百分比查询不是永久答案，浏览器使用数据会变化，企业内网、收银设备和 WebView 也未必反映在公共统计里。我们会结合线上 UA&#x2F;能力监控确认，而不是为了少几 KB 擅自放弃仍有用户的环境。</p><h2 id="“编译成功”为什么仍会白屏"><a href="#“编译成功”为什么仍会白屏" class="headerlink" title="“编译成功”为什么仍会白屏"></a>“编译成功”为什么仍会白屏</h2><p>那次 <code>includes</code> 故障最终按下面的顺序定位：</p><ol><li>在目标浏览器打开控制台，记录第一个异常而不是后续连锁报错；</li><li>检查 source map，确认来自业务代码还是第三方依赖；</li><li>在构建产物中搜索对应语法&#x2F;API；</li><li>用兼容性数据确认目标环境是否原生支持；</li><li>检查 polyfill 是否被注入，以及是否早于业务入口执行。</li></ol><p>修复没有选择在全局随手加一段：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="title class_">Array</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">includes</span> = <span class="keyword">function</span> (<span class="params"></span>) &#123; <span class="comment">/* 临时手写实现 */</span> &#125;</span><br></pre></td></tr></table></figure><p>这种补丁很难完整复现规范边界，还可能污染全局。我们改为统一 core-js 策略，并为目标浏览器增加一条真实构建产物的冒烟测试。</p><h2 id="应用与组件库的-polyfill-策略不同"><a href="#应用与组件库的-polyfill-策略不同" class="headerlink" title="应用与组件库的 polyfill 策略不同"></a>应用与组件库的 polyfill 策略不同</h2><p>应用知道自己的运行环境，可以在入口统一补齐能力；发布到 npm 的组件库却不应该随意修改消费者全局对象。库更适合使用 <code>@babel/plugin-transform-runtime</code> 来复用辅助函数，并在文档中声明需要的环境能力。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;plugins&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;@babel/plugin-transform-runtime&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="punctuation">&#123;</span> <span class="attr">&quot;helpers&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span> <span class="attr">&quot;regenerator&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span> <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>否则多个依赖各自内联 <code>_extends</code>、<code>_classCallCheck</code> 等 helper，体积会重复；而库偷偷引入全局 polyfill，又可能与宿主应用版本冲突。</p><h2 id="TypeScript-能解析，不代表做了类型检查"><a href="#TypeScript-能解析，不代表做了类型检查" class="headerlink" title="TypeScript 能解析，不代表做了类型检查"></a>TypeScript 能解析，不代表做了类型检查</h2><p>Babel 7 对 TypeScript 语法的支持让一条快速构建链路成为可能。使用 <code>@babel/preset-typescript</code> 时，Babel 会移除类型语法，但它不负责像 <code>tsc</code> 那样验证类型。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> <span class="title class_">User</span> = &#123; <span class="attr">id</span>: <span class="built_in">number</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="attr">user</span>: <span class="title class_">User</span> = &#123; <span class="attr">id</span>: <span class="string">&#x27;not-a-number&#x27;</span> &#125;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(user.<span class="property">id</span>)</span><br></pre></td></tr></table></figure><p>这段代码可以被 Babel 成功输出 JavaScript，但类型显然错误。因此我们的 CI 仍单独运行 <code>tsc --noEmit</code>。构建速度和类型检查是两个任务，合在同一工具里不是唯一选择，拆开以后也不能漏掉后者。</p><h2 id="不再默认把所有代码压回-ES5"><a href="#不再默认把所有代码压回-ES5" class="headerlink" title="不再默认把所有代码压回 ES5"></a>不再默认把所有代码压回 ES5</h2><p>为了兼容最老环境而把全部代码转换到 ES5，会增加 helper、破坏部分原生优化，也让现代用户下载更多代码。更实际的做法是明确支持矩阵：确实需要旧环境就保留对应构建与测试；只支持现代浏览器时就及时提高目标，不让历史配置永久存在。</p><p>Babel 7 给我留下的经验，是不要把“能构建”当作兼容性证明。每次调整都应该能回答：代码使用了哪些语法和 API，目标环境缺什么，哪一层负责补，最后有没有在真实目标上跑过。只有这条链闭合，转译配置才不是一堆碰巧工作的 JSON。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://babeljs.io/blog/2018/08/27/7.0.0">Babel 7.0 发布说明</a></li><li><a href="https://babeljs.io/docs/babel-preset-env">Babel：@babel&#x2F;preset-env</a></li><li><a href="https://github.com/browserslist/browserslist">Browserslist</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 2018 </tag>
            
            <tag> Babel </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>z-index 失效排查：层叠上下文、定位与遮挡问题</title>
      <link href="/2018/07/20/frontend-debug-css-z-index-stacking-context/"/>
      <url>/2018/07/20/frontend-debug-css-z-index-stacking-context/</url>
      
        <content type="html"><![CDATA[<p>“我已经把 z-index 改成 999999 了，怎么还被导航栏盖住？”</p><p>这个问题我遇到过很多次。数字继续加大通常没有意义，因为参与比较的两个元素根本不在同一个层叠上下文里。它有点像两个小区都各自有一栋 100 号楼，不能因为门牌号一样就判断谁更高。</p><p>下面用一个能复现的商品卡片说明我平时怎么查，不先罗列几十条 CSS 规则。</p><h2 id="复现：卡片里的提示框永远越不过侧栏"><a href="#复现：卡片里的提示框永远越不过侧栏" class="headerlink" title="复现：卡片里的提示框永远越不过侧栏"></a>复现：卡片里的提示框永远越不过侧栏</h2><p>HTML 很普通：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">aside</span> <span class="attr">class</span>=<span class="string">&quot;sidebar&quot;</span>&gt;</span>固定侧栏<span class="tag">&lt;/<span class="name">aside</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">main</span> <span class="attr">class</span>=<span class="string">&quot;page&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">article</span> <span class="attr">class</span>=<span class="string">&quot;card&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">button</span>&gt;</span>显示提示<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;tooltip&quot;</span>&gt;</span>库存只剩 2 件<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">article</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">main</span>&gt;</span></span><br></pre></td></tr></table></figure><p>CSS 中为了“开启 GPU 加速”，有人给 page 加了 <code>transform</code>：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-class">.sidebar</span> &#123;</span><br><span class="line">  <span class="attribute">position</span>: fixed;</span><br><span class="line">  <span class="attribute">z-index</span>: <span class="number">10</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.page</span> &#123;</span><br><span class="line">  <span class="attribute">position</span>: relative;</span><br><span class="line">  <span class="attribute">z-index</span>: <span class="number">1</span>;</span><br><span class="line">  <span class="attribute">transform</span>: <span class="built_in">translateZ</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.tooltip</span> &#123;</span><br><span class="line">  <span class="attribute">position</span>: absolute;</span><br><span class="line">  <span class="attribute">z-index</span>: <span class="number">999999</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>.page</code> 因为定位与 z-index（同时 transform 也会创建）形成自己的层叠上下文。<code>.tooltip</code> 的 999999 只在 <code>.page</code> 内部比较；整个 <code>.page</code> 在根上下文里仍然是 1，而侧栏是 10，所以提示框无法越过去。</p><h2 id="我的排查顺序：从元素向祖先走"><a href="#我的排查顺序：从元素向祖先走" class="headerlink" title="我的排查顺序：从元素向祖先走"></a>我的排查顺序：从元素向祖先走</h2><p>看到遮挡以后，我会在 Elements 面板选中被盖住的元素，然后逐层向上看 computed style。重点不是先盯着它自己的 z-index，而是找第一个创建层叠上下文或发生裁剪的祖先。</p><p>可以临时逐项关闭这些属性来验证：</p><ul><li>带非 <code>auto</code> z-index 的 positioned&#x2F;flex&#x2F;grid 子项；</li><li><code>transform</code>、<code>filter</code>、<code>perspective</code>；</li><li>小于 1 的 <code>opacity</code>；</li><li><code>isolation: isolate</code>；</li><li><code>contain</code>、<code>will-change</code> 等会建立独立绘制边界的属性。</li></ul><p>这个列表会随规范能力扩展，所以我不会只凭记忆。DevTools 中关闭一项、观察结果，比一口气删除整段样式更容易锁定原因。</p><p>如果临时去掉 <code>.page</code> 的 <code>transform</code> 后提示框立刻正常，就已经有很强证据。接下来再判断 transform 是误加的，还是动画确实需要它。</p><h2 id="遮挡和裁剪是两种不同问题"><a href="#遮挡和裁剪是两种不同问题" class="headerlink" title="遮挡和裁剪是两种不同问题"></a>遮挡和裁剪是两种不同问题</h2><p>有时把层级修好，浮层还是只显示一半。这通常不是 z-index，而是祖先的 <code>overflow: hidden</code> 在裁剪。</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-class">.card</span> &#123;</span><br><span class="line">  <span class="attribute">position</span>: relative;</span><br><span class="line">  <span class="attribute">overflow</span>: hidden;</span><br><span class="line">  <span class="attribute">border-radius</span>: <span class="number">12px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>卡片为了圆角裁掉溢出内容，里面的下拉菜单也一起被裁。无论 z-index 多大，子元素都不能绘制到裁剪区域之外。临时关闭 overflow 可以快速区分：</p><ul><li>关闭后完整显示但仍在别的元素下方：还有层叠问题；</li><li>关闭后直接恢复正常：主要是裁剪问题；</li><li>fixed 元素滚动时行为异常：还要检查 transform 是否改变了包含块。</li></ul><p>把所有 overflow 都改成 visible 会破坏圆角和滚动容器。更好的方案通常是让浮层脱离这个 DOM 子树。</p><h2 id="弹窗、Toast-和下拉菜单放到统一出口"><a href="#弹窗、Toast-和下拉菜单放到统一出口" class="headerlink" title="弹窗、Toast 和下拉菜单放到统一出口"></a>弹窗、Toast 和下拉菜单放到统一出口</h2><p>复杂应用里，我会在 body 下放一个 overlay 根节点：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;app&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;overlay-root&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><p>React 可以用 Portal，Vue 可以用 Teleport，把浮层渲染到这里。原组件仍管理打开状态，DOM 不再受卡片的 transform 和 overflow 限制。</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Dialog</span>(<span class="params">&#123; children &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="title function_">createPortal</span>(</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">div</span> <span class="attr">className</span>=<span class="string">&quot;dialog-layer&quot;</span>&gt;</span>&#123;children&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>,</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;overlay-root&#x27;</span>)</span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这并不代表所有 tooltip 都必须传送到 body。紧贴局部按钮、不会越过容器边界的小提示留在原位更简单；跨区域的弹窗、抽屉和全局消息更适合统一 overlay。选型要看它是否需要突破祖先边界。</p><h2 id="我不再使用“最大-z-index”"><a href="#我不再使用“最大-z-index”" class="headerlink" title="我不再使用“最大 z-index”"></a>我不再使用“最大 z-index”</h2><p>项目里一旦开始出现 999、9999、2147483647，后面的人只能继续加。我们后来把层级收敛成几个 token：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-pseudo">:root</span> &#123;</span><br><span class="line">  <span class="attr">--z-base</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attr">--z-sticky</span>: <span class="number">20</span>;</span><br><span class="line">  <span class="attr">--z-dropdown</span>: <span class="number">40</span>;</span><br><span class="line">  <span class="attr">--z-modal</span>: <span class="number">60</span>;</span><br><span class="line">  <span class="attr">--z-toast</span>: <span class="number">80</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>数字之间留间隔只是方便局部插层，重点是每一级有语义。组件不能自己发明全局 z-index；需要跨层时，应该进入对应的 overlay 容器。</p><p>设计系统还要规定同层多个浮层的顺序。比如 modal 中再打开日期选择器，日期浮层应跟随当前 modal 的 stacking scope，而不是因为全局 dropdown&#x3D;40 被 modal&#x3D;60 压住。实际实现可以让每个 modal 内有自己的局部 overlay，或者由浮层管理器按打开顺序分配层级。</p><h2 id="一个容易被误诊的-pointer-events-问题"><a href="#一个容易被误诊的-pointer-events-问题" class="headerlink" title="一个容易被误诊的 pointer-events 问题"></a>一个容易被误诊的 pointer-events 问题</h2><p>视觉上浮层已经在最上面，按钮却点不到，不一定还是 z-index。可能有一层透明遮罩覆盖在上面，或祖先设置了 <code>pointer-events: none</code>。</p><p>在控制台运行 <code>document.elementFromPoint(x, y)</code>，可以看某个坐标真正命中了谁：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> target = <span class="variable language_">document</span>.<span class="title function_">elementFromPoint</span>(<span class="number">420</span>, <span class="number">180</span>)</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(target, <span class="title function_">getComputedStyle</span>(target).<span class="property">pointerEvents</span>)</span><br></pre></td></tr></table></figure><p>这一步经常能找到透明但仍接收点击的元素。层叠顺序解决“画谁在上面”，命中测试决定“点击给谁”，两者不要混为一谈。</p><h2 id="最后如何避免它再次出现"><a href="#最后如何避免它再次出现" class="headerlink" title="最后如何避免它再次出现"></a>最后如何避免它再次出现</h2><p>这类问题修完一页还不够。我会做三件事：</p><ol><li>删除没有证据的 <code>translateZ(0)</code> 和 <code>will-change</code>，不要把它们当通用性能开关；</li><li>在组件文档中增加嵌套在 transform、overflow、滚动容器里的浮层用例；</li><li>让弹窗、抽屉、Toast 通过同一个 overlay 系统，并统一层级 token。</li></ol><p>所以 z-index 失效时，第一反应不应该是继续加数字。先找它所在的层叠上下文，再查祖先是否裁剪，最后确认点击命中。边界找对以后，999999 往往可以改回一个很小、也更可维护的值。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_positioned_layout/Stacking_context">MDN：层叠上下文</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_positioned_layout/Understanding_z-index">MDN：理解 z-index</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Document/elementFromPoint">MDN：elementFromPoint</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端排障 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端排障 </tag>
            
            <tag> CSS </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>webpack 4：从复杂配置走向合理默认值</title>
      <link href="/2018/02/25/frontend-chronicle-2018-02-webpack-4-zero-config/"/>
      <url>/2018/02/25/frontend-chronicle-2018-02-webpack-4-zero-config/</url>
      
        <content type="html"><![CDATA[<p>webpack 4 发布前，我们有一份接近 400 行的配置。里面既有复制粘贴来的“性能优化”，也有早已失效的兼容代码。没有人敢删，因为每一行旁边似乎都能讲出一个理由；也没有人能证明它们还在产生价值。</p><p><code>mode</code> 是 webpack 4 最容易被低估的改变。它没有消灭配置文件，却把开发和生产环境的一组合理默认值收进一个明确的开关。那次升级真正做成的事情，不是再加几个插件，而是逼着我们逐项回答：这段配置到底解决了什么问题？</p><h2 id="先给旧配置做一次体检"><a href="#先给旧配置做一次体检" class="headerlink" title="先给旧配置做一次体检"></a>先给旧配置做一次体检</h2><p>升级之前我保留了三组数据：冷启动时间、生产构建时间和主要 chunk 体积。还在浏览器里跑了一遍登录、首屏、动态路由和错误上报。没有这些基线，所谓“构建变快了”通常只是感觉。</p><p>旧配置里有一段手工设置环境变量、压缩器和 source map 的代码：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> webpack = <span class="built_in">require</span>(<span class="string">&#x27;webpack&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> <span class="title class_">UglifyJsPlugin</span> = <span class="built_in">require</span>(<span class="string">&#x27;uglifyjs-webpack-plugin&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = &#123;</span><br><span class="line">  <span class="attr">devtool</span>: process.<span class="property">env</span>.<span class="property">NODE_ENV</span> === <span class="string">&#x27;production&#x27;</span></span><br><span class="line">    ? <span class="string">&#x27;source-map&#x27;</span></span><br><span class="line">    : <span class="string">&#x27;cheap-module-eval-source-map&#x27;</span>,</span><br><span class="line">  <span class="attr">plugins</span>: [</span><br><span class="line">    <span class="keyword">new</span> webpack.<span class="title class_">DefinePlugin</span>(&#123;</span><br><span class="line">      <span class="string">&#x27;process.env.NODE_ENV&#x27;</span>: <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(process.<span class="property">env</span>.<span class="property">NODE_ENV</span>)</span><br><span class="line">    &#125;),</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">UglifyJsPlugin</span>()</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>到了 webpack 4，最小配置先收缩成：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = <span class="function">(<span class="params">_, argv</span>) =&gt;</span> (&#123;</span><br><span class="line">  <span class="attr">mode</span>: argv.<span class="property">mode</span> || <span class="string">&#x27;development&#x27;</span>,</span><br><span class="line">  <span class="attr">entry</span>: <span class="string">&#x27;./src/index.js&#x27;</span>,</span><br><span class="line">  <span class="attr">output</span>: &#123;</span><br><span class="line">    <span class="attr">filename</span>: <span class="string">&#x27;[name].[contenthash].js&#x27;</span>,</span><br><span class="line">    <span class="attr">clean</span>: <span class="literal">true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p><code>mode: &#39;production&#39;</code> 会启用面向生产的一组优化，development 则保留更适合调试的默认行为。它不是魔法，也不表示所有项目都不用配置；价值在于团队不必各自维护一套“什么才算生产模式”的零散约定。</p><h2 id="删掉配置以后，先看行为有没有改变"><a href="#删掉配置以后，先看行为有没有改变" class="headerlink" title="删掉配置以后，先看行为有没有改变"></a>删掉配置以后，先看行为有没有改变</h2><p>删配置比加配置更需要验证。我们每次只移除一组，连续构建两次并比较产物：</p><ul><li>入口与异步 chunk 是否仍按预期拆分；</li><li>线上 source map 是否只上传到错误平台，而没有公开暴露；</li><li>修改一个业务模块后，无关 vendor 文件的 hash 是否稳定；</li><li>CSS 和图片路径在 CDN 前缀下是否正确；</li><li>老浏览器语法兼容是否由 Babel 继续负责。</li></ul><p>有一项优化删除后，首屏体积反而下降了。原因是旧版 CommonsChunkPlugin 规则把不相关页面的依赖强行收进公共包。配置叫“公共代码提取”，不等于结果一定更好。</p><h2 id="Tree-Shaking-为什么有时毫无效果"><a href="#Tree-Shaking-为什么有时毫无效果" class="headerlink" title="Tree Shaking 为什么有时毫无效果"></a>Tree Shaking 为什么有时毫无效果</h2><p>webpack 4 改进了基于 ES Module 的 Tree Shaking，但它依赖静态模块结构与正确的副作用声明。下面两种入口看起来都能拿到 <code>formatPrice</code>，对构建器的可分析程度却不同：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 更容易静态分析</span></span><br><span class="line"><span class="keyword">import</span> &#123; formatPrice &#125; <span class="keyword">from</span> <span class="string">&#x27;./money.js&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// CommonJS 的动态特性会增加分析难度</span></span><br><span class="line"><span class="keyword">const</span> money = <span class="built_in">require</span>(<span class="string">&#x27;./money.js&#x27;</span>)</span><br><span class="line">money.<span class="title function_">formatPrice</span>(<span class="number">100</span>)</span><br></pre></td></tr></table></figure><p>库的 <code>package.json</code> 如果错误写了 <code>&quot;sideEffects&quot;: false</code>，构建器甚至可能删掉必须执行的 CSS 导入或全局注册代码。反过来，完全不声明副作用，很多可删除模块又会被保留。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;sideEffects&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="string">&quot;*.css&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;./src/polyfills.js&quot;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>所以验证 Tree Shaking 不能只看配置项，应当用 bundle analyzer 或 stats 找出“为什么这个模块还在”，再检查导入方式、包声明和实际副作用。</p><h2 id="开发配置与生产配置不要长成两个项目"><a href="#开发配置与生产配置不要长成两个项目" class="headerlink" title="开发配置与生产配置不要长成两个项目"></a>开发配置与生产配置不要长成两个项目</h2><p>早期我们维护 <code>webpack.dev.js</code> 和 <code>webpack.prod.js</code> 两份完整对象，时间久了 loader 顺序和 alias 都不一样，出现过开发正常、生产找不到模块的情况。后来把公共部分留在基础配置，只在差异点上合并：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> &#123; merge &#125; = <span class="built_in">require</span>(<span class="string">&#x27;webpack-merge&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> common = <span class="built_in">require</span>(<span class="string">&#x27;./webpack.common&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = <span class="title function_">merge</span>(common, &#123;</span><br><span class="line">  <span class="attr">mode</span>: <span class="string">&#x27;production&#x27;</span>,</span><br><span class="line">  <span class="attr">devtool</span>: <span class="string">&#x27;source-map&#x27;</span>,</span><br><span class="line">  <span class="attr">performance</span>: &#123;</span><br><span class="line">    <span class="attr">hints</span>: <span class="string">&#x27;warning&#x27;</span>,</span><br><span class="line">    <span class="attr">maxEntrypointSize</span>: <span class="number">350</span> * <span class="number">1024</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>体积阈值也不是越小越专业。后台系统与内容站的首屏目标不同，阈值应该来自性能预算和真实网络数据。至少它能让一次意外引入整个图表库的提交在 CI 中被看见。</p><h2 id="升级时踩过的几个坑"><a href="#升级时踩过的几个坑" class="headerlink" title="升级时踩过的几个坑"></a>升级时踩过的几个坑</h2><p>webpack 核心升级成功，只代表配置能被解析。真正容易失败的是周边：</p><ol><li>loader&#x2F;plugin 仍调用旧版内部接口；</li><li>Node 版本太旧，构建机与开发机行为不一致；</li><li>Babel 只转译业务源码，某个依赖发布了新语法；</li><li><code>process.env.NODE_ENV</code> 在启动脚本中没有按预期传入；</li><li>动态导入生成的新文件名与 CDN 缓存规则冲突。</li></ol><p>我们没有一次性把所有依赖升级到最新版，而是先升级核心和必要插件，构建通过后再逐项更新。每一步都有产物可对比，也能快速回滚。</p><h2 id="webpack-4-留下的是一套更好的默认意识"><a href="#webpack-4-留下的是一套更好的默认意识" class="headerlink" title="webpack 4 留下的是一套更好的默认意识"></a>webpack 4 留下的是一套更好的默认意识</h2><p>“零配置”这个说法容易让人误会。真实项目始终需要入口、资源处理、缓存和发布策略。webpack 4 的进步，是让默认值覆盖常见场景，让配置重新聚焦项目特有的问题。</p><p>从那次清理以后，我不再把配置长度当成工程能力。每加一段规则，都应该能说清触发条件、验证方法和删除时机；每做一次构建升级，都要同时比较速度、产物和运行行为。能删掉并证明没问题的配置，往往比新加十个插件更有价值。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://webpack.js.org/configuration/mode/">webpack：Mode</a></li><li><a href="https://webpack.js.org/guides/tree-shaking/">webpack：Tree Shaking</a></li><li><a href="https://webpack.js.org/blog/">webpack 官方博客</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> webpack </tag>
            
            <tag> 2018 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>事件循环排障：为什么 Promise、setTimeout 的顺序和预期不同</title>
      <link href="/2017/11/18/frontend-debug-event-loop-async-order/"/>
      <url>/2017/11/18/frontend-debug-event-loop-async-order/</url>
      
        <content type="html"><![CDATA[<p>这个问题来自一次真实的“偶发重复提交”。用户点击保存后，我们先把按钮禁用，再发起请求；自动化测试却偶尔在按钮仍可点击时触发第二次。有人建议在提交函数里加一个 <code>setTimeout(..., 100)</code>，试图让界面“有时间更新”。这当然没有修好，反而让复现更随机。</p><p>最后发现，问题由三个部分叠加：Promise 回调、一个耗时的同步校验，以及对定时器执行时机的错误想象。下面把当时的排查过程完整还原。</p><h2 id="先别背输出顺序，先标出每段代码进入哪个队列"><a href="#先别背输出顺序，先标出每段代码进入哪个队列" class="headerlink" title="先别背输出顺序，先标出每段代码进入哪个队列"></a>先别背输出顺序，先标出每段代码进入哪个队列</h2><p>最小复现是这几行：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;1: script start&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;2: timer&#x27;</span>), <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="title class_">Promise</span>.<span class="title function_">resolve</span>()</span><br><span class="line">  .<span class="title function_">then</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;3: promise&#x27;</span>))</span><br><span class="line"></span><br><span class="line"><span class="title function_">queueMicrotask</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;4: microtask&#x27;</span>))</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;5: script end&#x27;</span>)</span><br></pre></td></tr></table></figure><p>浏览器中的常见输出是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">1: script start</span><br><span class="line">5: script end</span><br><span class="line">3: promise</span><br><span class="line">4: microtask</span><br><span class="line">2: timer</span><br></pre></td></tr></table></figure><p>脚本本身是一项任务。同步代码先跑完；当前任务结束、调用栈清空后，事件循环会清空微任务队列，所以两个微任务先于定时器。<code>Promise.then</code> 与 <code>queueMicrotask</code> 都在微任务队列中，它们按入队先后执行。</p><p>关键不是记住这一个答案，而是每遇到新代码都问：回调何时入队，进入任务队列还是微任务队列，执行它之前有没有一段同步工作没结束。</p><h2 id="setTimeout-fn-0-的-0-不是执行时间"><a href="#setTimeout-fn-0-的-0-不是执行时间" class="headerlink" title="setTimeout(fn, 0) 的 0 不是执行时间"></a><code>setTimeout(fn, 0)</code> 的 0 不是执行时间</h2><p>定时器的延迟表示“至少等待这么久以后，回调可以被调度”，并不保证到点立刻执行。主线程如果正在跑一个 300ms 的循环，定时器只能在它结束后才有机会执行。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> startedAt = performance.<span class="title function_">now</span>()</span><br><span class="line"></span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;timer delay:&#x27;</span>, performance.<span class="title function_">now</span>() - startedAt)</span><br><span class="line">&#125;, <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (performance.<span class="title function_">now</span>() - startedAt &lt; <span class="number">300</span>) &#123;</span><br><span class="line">  <span class="comment">// 模拟阻塞主线程的同步计算</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里日志通常接近 300ms，而不是 0ms。把 UI 修复放进 <code>setTimeout</code>，只是把它排到未来某个不确定时刻；如果前面仍有长任务，它还是不会及时发生。</p><h2 id="回到重复提交现场"><a href="#回到重复提交现场" class="headerlink" title="回到重复提交现场"></a>回到重复提交现场</h2><p>原始代码大致如下：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">handleSubmit</span>(<span class="params">form</span>) &#123;</span><br><span class="line">  <span class="keyword">await</span> <span class="title function_">validateForm</span>(form)</span><br><span class="line">  button.<span class="property">disabled</span> = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> result = <span class="keyword">await</span> <span class="title function_">save</span>(form)</span><br><span class="line">  <span class="title function_">renderSuccess</span>(result)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>validateForm</code> 表面上是异步函数，内部却先执行一大段同步的 schema 遍历，然后才返回已经解决的 Promise。按钮禁用发生在第一次 <code>await</code> 之后，也就是一个后续微任务里。在某些测试驱动和双击场景中，第二次事件已经进入队列。</p><p>第一版修复是把“是否正在提交”作为同步守卫，放在任何 await 之前：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> submitting = <span class="literal">false</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">handleSubmit</span>(<span class="params">form</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (submitting) <span class="keyword">return</span></span><br><span class="line"></span><br><span class="line">  submitting = <span class="literal">true</span></span><br><span class="line">  button.<span class="property">disabled</span> = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> errors = <span class="title function_">validateFormSync</span>(form)</span><br><span class="line">    <span class="keyword">if</span> (errors.<span class="property">length</span>) &#123;</span><br><span class="line">      <span class="title function_">renderErrors</span>(errors)</span><br><span class="line">      <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> result = <span class="keyword">await</span> <span class="title function_">save</span>(form)</span><br><span class="line">    <span class="title function_">renderSuccess</span>(result)</span><br><span class="line">  &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    submitting = <span class="literal">false</span></span><br><span class="line">    button.<span class="property">disabled</span> = <span class="literal">false</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>前端守卫改善了交互，但不能作为重复写入的最终防线。网络重试、多个标签页或代理重放都可能再次提交，所以订单接口后来又增加了幂等键。这也是排障中很重要的一点：事件循环解释了客户端现象，却不代表只在客户端修就足够。</p><h2 id="微任务也能把页面“饿死”"><a href="#微任务也能把页面“饿死”" class="headerlink" title="微任务也能把页面“饿死”"></a>微任务也能把页面“饿死”</h2><p>通常会说“微任务比定时器先执行”，但容易漏掉下一句：处理一个任务以后，浏览器会持续清空微任务队列。如果每个微任务又创建一个微任务，渲染和下一项任务可能长时间没有机会运行。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">loop</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="title function_">queueMicrotask</span>(loop)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title function_">loop</span>()</span><br></pre></td></tr></table></figure><p>这段代码不会像普通递归那样立刻爆栈，却可能让页面无法响应。实际项目中的版本往往没这么明显，例如在 Promise 回调里不断检查某个状态。需要把工作分帧时，可以根据场景使用 <code>requestAnimationFrame</code>、任务调度或 Web Worker，而不是无限续接微任务。</p><h2 id="循环索引问题其实是闭包问题"><a href="#循环索引问题其实是闭包问题" class="headerlink" title="循环索引问题其实是闭包问题"></a>循环索引问题其实是闭包问题</h2><p>另一类常被归到事件循环的现象，是几个回调都输出相同索引：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(i), <span class="number">0</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>定时器执行时循环早已结束，三个闭包读取同一个函数作用域里的 <code>i</code>，所以输出三个 3。改成 <code>let</code> 会为每次迭代创建独立绑定：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(i), <span class="number">0</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里事件循环只决定“稍后执行”，闭包才决定“稍后读取哪个变量”。把两个概念分开，定位会更准确。</p><h2 id="我现在怎样记录异步现场"><a href="#我现在怎样记录异步现场" class="headerlink" title="我现在怎样记录异步现场"></a>我现在怎样记录异步现场</h2><p>只打印一句“开始&#x2F;结束”很难还原竞态。我会给同一次操作生成 ID，并记录创建、开始、完成和取消时间：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">tracedRequest</span>(<span class="params">name, task</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> id = crypto.<span class="title function_">randomUUID</span>()</span><br><span class="line">  <span class="keyword">const</span> start = performance.<span class="title function_">now</span>()</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(&#123; id, name, <span class="attr">phase</span>: <span class="string">&#x27;created&#x27;</span>, start &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> value = <span class="keyword">await</span> <span class="title function_">task</span>()</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(&#123; id, name, <span class="attr">phase</span>: <span class="string">&#x27;fulfilled&#x27;</span>, <span class="attr">cost</span>: performance.<span class="title function_">now</span>() - start &#125;)</span><br><span class="line">    <span class="keyword">return</span> value</span><br><span class="line">  &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">error</span>(&#123; id, name, <span class="attr">phase</span>: <span class="string">&#x27;rejected&#x27;</span>, <span class="attr">cost</span>: performance.<span class="title function_">now</span>() - start, error &#125;)</span><br><span class="line">    <span class="keyword">throw</span> error</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果页面卡顿，再用 Performance 面板查看 Main 线程上的 Long Task。只看日志顺序无法发现中间那段阻塞渲染的同步计算。</p><h2 id="最终检查单"><a href="#最终检查单" class="headerlink" title="最终检查单"></a>最终检查单</h2><p>以后再遇到“顺序不对”，我不会先试着加延迟，而会按这个顺序查：</p><ul><li>同步调用栈什么时候结束，是否存在长任务；</li><li>回调属于任务还是微任务，它在什么时候入队；</li><li>多个请求究竟要求串行、并行还是只接受最后一个结果；</li><li>回调读取的是值快照，还是闭包中的可变引用；</li><li>用户离开或重复操作时，旧任务是否能取消或被忽略；</li><li>写操作在服务端是否具有幂等保护。</li></ul><p>事件循环不是一道只用来猜输出的面试题。它影响按钮反馈、请求竞态、渲染机会和线上日志的解释方式。把队列模型和具体业务状态连起来，才算真正用上它。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Event_loop">MDN：JavaScript 事件循环</a></li><li><a href="https://html.spec.whatwg.org/multipage/webappapis.html#event-loops">HTML Standard：事件循环</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/queueMicrotask">MDN：queueMicrotask</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端排障 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> JavaScript </tag>
            
            <tag> 前端排障 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>React 16 与 Fiber：渲染架构为并发能力打下基础</title>
      <link href="/2017/09/26/frontend-chronicle-2017-09-react-16-fiber/"/>
      <url>/2017/09/26/frontend-chronicle-2017-09-react-16-fiber/</url>
      
        <content type="html"><![CDATA[<p>React 16 发布那天，我所在的项目没有立刻出现“肉眼可见的性能飞跃”。按钮还是那个按钮，列表也没有突然变快。真正让我觉得升级值得的，是一个价格格式化函数抛错以后，购物车区域终于可以单独降级，不再把整棵应用树一起带走。</p><p>Fiber 当然是这个版本最重要的底层重写，但业务开发者不必背内部字段。更值得记录的是：React 从这一版开始，为“渲染工作可以被拆开和调度”换了一副骨架，同时交付了错误边界、Portal、Fragment 等立即能解决工程问题的能力。</p><h2 id="升级前先处理控制台里的旧问题"><a href="#升级前先处理控制台里的旧问题" class="headerlink" title="升级前先处理控制台里的旧问题"></a>升级前先处理控制台里的旧问题</h2><p>我们从 React 15 升级时没有直接改版本号，而是先清理警告。旧生命周期、字符串 ref、依赖 DOM 结构的测试，这些代码在平时看起来不影响运行，却会让真正的升级错误淹没在控制台中。</p><p>当时的顺序大概是：</p><ol><li>在现有版本中先升级兼容的依赖，清空能处理的 warning；</li><li>固定一组下单、登录、搜索和弹窗回归用例；</li><li>升 React 与 ReactDOM，再处理测试工具和第三方组件；</li><li>对错误场景做主动注入，而不只验证正常页面。</li></ol><p>最后一条很关键。错误边界只有子组件真的在渲染阶段抛错时才看得到价值。</p><h2 id="一次商品卡片异常，不该让整个页面白屏"><a href="#一次商品卡片异常，不该让整个页面白屏" class="headerlink" title="一次商品卡片异常，不该让整个页面白屏"></a>一次商品卡片异常，不该让整个页面白屏</h2><p>接口里偶尔会出现缺少货币字段的商品，格式化价格时抛出异常。React 16 之前，组件树中的未捕获渲染错误可能让整棵树失效；加入错误边界后，可以把故障控制在商品推荐区域。</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">ProductAreaBoundary</span> <span class="keyword">extends</span> <span class="title class_ inherited__">React.Component</span> &#123;</span><br><span class="line">  state = &#123; <span class="attr">error</span>: <span class="literal">null</span> &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">static</span> <span class="title function_">getDerivedStateFromError</span>(<span class="params">error</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123; error &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">componentDidCatch</span>(<span class="params">error, info</span>) &#123;</span><br><span class="line">    <span class="title function_">reportError</span>(error, &#123;</span><br><span class="line">      <span class="attr">area</span>: <span class="string">&#x27;product-recommendation&#x27;</span>,</span><br><span class="line">      <span class="attr">componentStack</span>: info.<span class="property">componentStack</span></span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">state</span>.<span class="property">error</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> (</span><br><span class="line">        <span class="language-xml"><span class="tag">&lt;<span class="name">section</span> <span class="attr">role</span>=<span class="string">&quot;status&quot;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">          <span class="tag">&lt;<span class="name">p</span>&gt;</span>推荐商品暂时无法显示。<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">          <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;()</span> =&gt;</span> this.setState(&#123; error: null &#125;)&#125;&gt;重试<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;/<span class="name">section</span>&gt;</span></span></span><br><span class="line">      )</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">props</span>.<span class="property">children</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>边界放在哪里比“有没有边界”更重要。只在应用根部放一个，结果仍然近似整页白屏；每个按钮都包一层又会把结构切得太碎。我们的做法是按可独立降级的业务区域划分：导航、结算、推荐和评论各自有不同兜底，其中结算错误不能静默降级，必须引导用户重试或联系客服。</p><p>错误边界也并非万能。事件处理器、异步回调和边界自身的错误不会自动被它捕获。网络失败更应该进入数据层的失败状态，而不是故意在渲染里抛异常。</p><h2 id="Fragment-解决的是不起眼的-DOM-污染"><a href="#Fragment-解决的是不起眼的-DOM-污染" class="headerlink" title="Fragment 解决的是不起眼的 DOM 污染"></a>Fragment 解决的是不起眼的 DOM 污染</h2><p>表格曾经是升级时最直观的收益。组件需要返回多列，但为了满足单根节点限制而加一个 <code>div</code> 会破坏合法表格结构。Fragment 让组件可以返回一组同级节点：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">OrderCells</span>(<span class="params">&#123; order &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">React.Fragment</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">td</span>&gt;</span>&#123;order.number&#125;<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">td</span>&gt;</span>&#123;order.customer&#125;<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">td</span>&gt;</span>&#123;order.total&#125;<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">React.Fragment</span>&gt;</span></span></span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>今天短语法 <code>&lt;&gt;...&lt;/&gt;</code> 已经随处可见，但在需要 <code>key</code> 的列表中仍要写完整的 <code>React.Fragment</code>。它不是单纯少一个标签，而是让组件边界不必强行对应一个多余的 DOM 节点。</p><h2 id="Portal-让弹窗离开裁剪容器"><a href="#Portal-让弹窗离开裁剪容器" class="headerlink" title="Portal 让弹窗离开裁剪容器"></a>Portal 让弹窗离开裁剪容器</h2><p>另一个老问题是弹窗放在带 <code>overflow: hidden</code> 或 <code>transform</code> 的卡片中，无论怎么调 <code>z-index</code> 都会被裁剪。Portal 让 React 组件在逻辑上仍属于原来的组件树，DOM 却可以渲染到页面顶部的 overlay 节点。</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Modal</span>(<span class="params">&#123; children, onClose &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> root = <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;overlay-root&#x27;</span>)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="title class_">ReactDOM</span>.<span class="title function_">createPortal</span>(</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">div</span> <span class="attr">className</span>=<span class="string">&quot;modal-backdrop&quot;</span> <span class="attr">onClick</span>=<span class="string">&#123;onClose&#125;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;<span class="name">div</span> <span class="attr">className</span>=<span class="string">&quot;modal&quot;</span> <span class="attr">onClick</span>=<span class="string">&#123;event</span> =&gt;</span> event.stopPropagation()&#125;&gt;</span></span><br><span class="line"><span class="language-xml">        &#123;children&#125;</span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>,</span><br><span class="line">    root</span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Portal 改的是 DOM 位置，不会切断 React 树中的 context 和事件传播。也正因为如此，点击 Portal 内按钮时，事件仍可能冒泡到逻辑父组件，不能按普通 DOM 父子关系想当然。</p><h2 id="Fiber-对业务代码提出的隐性要求"><a href="#Fiber-对业务代码提出的隐性要求" class="headerlink" title="Fiber 对业务代码提出的隐性要求"></a>Fiber 对业务代码提出的隐性要求</h2><p>旧的协调过程更像一次同步递归：开始渲染以后会持续工作，直到整棵相关子树处理完。Fiber 把工作表示为更小的单元，为后续按优先级调度、暂停和恢复渲染提供基础。React 16 并没有直接把今天的并发能力全部开放出来，但架构方向已经改变。</p><p>这会反过来约束组件写法：<code>render</code> 必须是纯计算。下面这种代码即使暂时看起来正常，也把副作用藏进了渲染阶段：</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">BadExample</span> <span class="keyword">extends</span> <span class="title class_ inherited__">React.Component</span> &#123;</span><br><span class="line">  <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">    analytics.<span class="title function_">track</span>(<span class="string">&#x27;render-product&#x27;</span>, <span class="variable language_">this</span>.<span class="property">props</span>.<span class="property">product</span>.<span class="property">id</span>)</span><br><span class="line">    cache.<span class="title function_">push</span>(<span class="variable language_">this</span>.<span class="property">props</span>.<span class="property">product</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">Product</span> <span class="attr">product</span>=<span class="string">&#123;this.props.product&#125;</span> /&gt;</span></span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>一旦渲染可能重试或中断，统计会重复、外部数组会被多次修改。正确做法是把订阅、上报和 DOM 操作放到明确的提交后生命周期，并让渲染只由 props&#x2F;state 计算 UI。</p><h2 id="回看这次升级"><a href="#回看这次升级" class="headerlink" title="回看这次升级"></a>回看这次升级</h2><p>React 16 最有意思的地方，是表层 API 和底层架构同时前进。Fragment 与 Portal 修掉日常的 DOM 结构问题，错误边界改善生产可用性，Fiber 则为多年后的并发渲染铺路。</p><p>如果今天还在维护旧 React 项目，我不会让业务代码读取 Fiber 内部结构来“优化性能”。更可靠的准备仍然是纯渲染、稳定 key、可清理副作用、按业务范围设置错误边界，以及真实的用户路径测试。这些原则跨过多个主版本后依旧成立。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://legacy.reactjs.org/blog/2017/09/26/react-v16.0.html">React v16.0 发布说明</a></li><li><a href="https://legacy.reactjs.org/docs/error-boundaries.html">React：错误边界</a></li><li><a href="https://legacy.reactjs.org/docs/portals.html">React：Portal</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 2017 </tag>
            
            <tag> React </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ES2017 与 async/await：异步代码开始像同步代码一样可读</title>
      <link href="/2017/06/30/frontend-chronicle-2017-06-es2017-async-await/"/>
      <url>/2017/06/30/frontend-chronicle-2017-06-es2017-async-await/</url>
      
        <content type="html"><![CDATA[<p>async&#x2F;await 刚进入标准时，我最先做的事是把一段七层回调改成从上往下的代码。改完确实顺眼，测试也全过了，上线后首页接口却慢了接近一倍。</p><p>问题不在语法，而在我无意中把三个并行请求写成了串行。这件小事让我很早就明白：<code>await</code> 解决的是表达方式，不负责替我们设计并发关系。</p><h2 id="一段“更好读”但更慢的代码"><a href="#一段“更好读”但更慢的代码" class="headerlink" title="一段“更好读”但更慢的代码"></a>一段“更好读”但更慢的代码</h2><p>用户进入工作台后，需要同时读取个人资料、权限和未读消息。这三份数据彼此独立，原来的 Promise 版本会一起发出请求：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">loadDashboard</span>(<span class="params">userId</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="title class_">Promise</span>.<span class="title function_">all</span>([</span><br><span class="line">    <span class="title function_">fetchProfile</span>(userId),</span><br><span class="line">    <span class="title function_">fetchPermissions</span>(userId),</span><br><span class="line">    <span class="title function_">fetchUnreadCount</span>(userId)</span><br><span class="line">  ]).<span class="title function_">then</span>(<span class="keyword">function</span> (<span class="params">[profile, permissions, unread]</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123; profile, permissions, unread &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我当时机械地把 <code>.then()</code> 换成 <code>await</code>：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">loadDashboard</span>(<span class="params">userId</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> profile = <span class="keyword">await</span> <span class="title function_">fetchProfile</span>(userId)</span><br><span class="line">  <span class="keyword">const</span> permissions = <span class="keyword">await</span> <span class="title function_">fetchPermissions</span>(userId)</span><br><span class="line">  <span class="keyword">const</span> unread = <span class="keyword">await</span> <span class="title function_">fetchUnreadCount</span>(userId)</span><br><span class="line">  <span class="keyword">return</span> &#123; profile, permissions, unread &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果三个接口分别耗时 300ms、400ms 和 200ms，第二种写法接近 900ms；它的缩进更少，却改变了业务时序。正确改法是先创建任务，再一起等待：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">loadDashboard</span>(<span class="params">userId</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> profileTask = <span class="title function_">fetchProfile</span>(userId)</span><br><span class="line">  <span class="keyword">const</span> permissionTask = <span class="title function_">fetchPermissions</span>(userId)</span><br><span class="line">  <span class="keyword">const</span> unreadTask = <span class="title function_">fetchUnreadCount</span>(userId)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> [profile, permissions, unread] = <span class="keyword">await</span> <span class="title class_">Promise</span>.<span class="title function_">all</span>([</span><br><span class="line">    profileTask,</span><br><span class="line">    permissionTask,</span><br><span class="line">    unreadTask</span><br><span class="line">  ])</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> &#123; profile, permissions, unread &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我现在更习惯先在纸上标出依赖：B 需要 A 的结果，就顺序 <code>await</code>；B 与 A 无关，就同时启动。代码风格不能替代这一步。</p><h2 id="try-catch-终于能包住一段业务流程"><a href="#try-catch-终于能包住一段业务流程" class="headerlink" title="try&#x2F;catch 终于能包住一段业务流程"></a>try&#x2F;catch 终于能包住一段业务流程</h2><p>Promise 链当然可以处理错误，但一旦中间穿插条件分支、循环或回退逻辑，链式写法很容易被拆散。async 函数最大的价值，是可以直接使用 JavaScript 原有的控制结构：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">submitOrder</span>(<span class="params">draft</span>) &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> order = <span class="keyword">await</span> <span class="title function_">createOrder</span>(draft)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (order.<span class="property">needPayment</span>) &#123;</span><br><span class="line">      <span class="keyword">await</span> <span class="title function_">openCashier</span>(order.<span class="property">paymentToken</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">await</span> <span class="title function_">refreshOrder</span>(order.<span class="property">id</span>)</span><br><span class="line">    <span class="keyword">return</span> order</span><br><span class="line">  &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">    <span class="title function_">reportError</span>(error, &#123; <span class="attr">action</span>: <span class="string">&#x27;submit-order&#x27;</span> &#125;)</span><br><span class="line">    <span class="title function_">showMessage</span>(<span class="title function_">getFriendlyMessage</span>(error))</span><br><span class="line">    <span class="keyword">throw</span> error</span><br><span class="line">  &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    <span class="title function_">setSubmitting</span>(<span class="literal">false</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里要小心错误边界的大小。如果 <code>try</code> 把整页初始化都包进去，任何失败最终只剩一句“加载失败”，反而丢掉了哪些请求允许降级的信息。头像失败可以用默认图，权限失败却必须阻止页面继续渲染，它们不应该落进同一个无差别的 <code>catch</code>。</p><h2 id="Promise-all-的失败策略不是总合适"><a href="#Promise-all-的失败策略不是总合适" class="headerlink" title="Promise.all 的失败策略不是总合适"></a><code>Promise.all</code> 的失败策略不是总合适</h2><p><code>Promise.all</code> 中任一任务拒绝，外层就立即拒绝，但其他请求并不会自动取消。对于“要么全部成功、要么页面无法使用”的数据，这很合理；首页的推荐位、公告和统计卡片则可以分别降级。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> results = <span class="keyword">await</span> <span class="title class_">Promise</span>.<span class="title function_">allSettled</span>([</span><br><span class="line">  <span class="title function_">fetchNotice</span>(),</span><br><span class="line">  <span class="title function_">fetchRecommendations</span>(),</span><br><span class="line">  <span class="title function_">fetchStatistics</span>()</span><br><span class="line">])</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> [notice, recommendations, statistics] = results.<span class="title function_">map</span>(<span class="function"><span class="params">result</span> =&gt;</span></span><br><span class="line">  result.<span class="property">status</span> === <span class="string">&#x27;fulfilled&#x27;</span> ? result.<span class="property">value</span> : <span class="literal">null</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>使用 <code>allSettled</code> 也不能把错误吞掉。失败项仍需记录接口名、耗时和原因，否则页面看似正常，监控里却什么也没有。</p><h2 id="超时和取消必须由业务补上"><a href="#超时和取消必须由业务补上" class="headerlink" title="超时和取消必须由业务补上"></a>超时和取消必须由业务补上</h2><p>async&#x2F;await 不会让请求自动超时。用户切换搜索关键词时，旧请求可能比新请求晚回来，然后把界面覆盖成过期结果。我会为每次搜索保留一个 <code>AbortController</code>：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> currentController</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">search</span>(<span class="params">keyword</span>) &#123;</span><br><span class="line">  currentController?.<span class="title function_">abort</span>()</span><br><span class="line">  currentController = <span class="keyword">new</span> <span class="title class_">AbortController</span>()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">`/api/search?q=<span class="subst">$&#123;<span class="built_in">encodeURIComponent</span>(keyword)&#125;</span>`</span>, &#123;</span><br><span class="line">      <span class="attr">signal</span>: currentController.<span class="property">signal</span></span><br><span class="line">    &#125;)</span><br><span class="line">    <span class="keyword">if</span> (!response.<span class="property">ok</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`HTTP <span class="subst">$&#123;response.status&#125;</span>`</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">await</span> response.<span class="title function_">json</span>()</span><br><span class="line">  &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">    <span class="keyword">if</span> (error.<span class="property">name</span> === <span class="string">&#x27;AbortError&#x27;</span>) <span class="keyword">return</span> <span class="literal">null</span></span><br><span class="line">    <span class="keyword">throw</span> error</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>“取消”不一定能让服务器停止工作，但至少能阻止客户端继续消费已过期的响应。提交、付款这类写操作则不能随意重试，还要由服务端用幂等键兜住重复请求。</p><h2 id="循环里的-await-先问清楚意图"><a href="#循环里的-await-先问清楚意图" class="headerlink" title="循环里的 await 先问清楚意图"></a>循环里的 await 先问清楚意图</h2><p>代码评审中经常看到 <code>forEach(async item =&gt; ...)</code>。<code>forEach</code> 不会等待回调，外层函数可能在任务完成前就返回。需要串行时用 <code>for...of</code>：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">const</span> file <span class="keyword">of</span> files) &#123;</span><br><span class="line">  <span class="keyword">await</span> <span class="title function_">uploadFile</span>(file)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>需要有限并发时，也不要一次性把几千个请求塞进 <code>Promise.all</code>。可以分批，或使用一个有并发上限的任务池。异步代码的关键从来不只是“有没有 await”，而是顺序、并发上限、失败、取消与重试是否符合业务。</p><h2 id="ES2017-留下的真正变化"><a href="#ES2017-留下的真正变化" class="headerlink" title="ES2017 留下的真正变化"></a>ES2017 留下的真正变化</h2><p>到今天，async&#x2F;await 已经普通得像 <code>if</code> 和 <code>for</code>。它最重要的贡献不是少写几个 <code>.then()</code>，而是让异步业务重新拥有可读的控制流。与此同时，它也让不合理的串行等待变得更隐蔽。</p><p>我现在写异步流程会先回答四个问题：哪些任务有依赖，失败能否局部降级，用户离开后如何取消，写操作能不能安全重试。答案清楚以后，async&#x2F;await 才会把代码变简单，而不是只把复杂度藏到语法后面。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://262.ecma-international.org/8.0/">ECMAScript 2017 语言规范</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Statements/async_function">MDN：async function</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise#promise_concurrency">MDN：Promise 并发方法</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> 2017 </tag>
            
            <tag> JavaScript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>从 jQuery 出发：DOM 操作时代的成熟与转折</title>
      <link href="/2017/04/15/frontend-chronicle-2017-04-jquery-and-the-dom-era/"/>
      <url>/2017/04/15/frontend-chronicle-2017-04-jquery-and-the-dom-era/</url>
      
        <content type="html"><![CDATA[<p>第一次接手那套运营后台时，页面里没有组件，也没有统一状态。筛选条件藏在表单控件中，当前页码挂在 DOM 的 <code>data-page</code> 上，弹窗是否打开要看某个 class。一个列表页能找到十几处 <code>$(&#39;.result&#39;)</code>，任何一处都可能重画同一块内容。</p><p>这就是我记忆里的 2017 年。Vue 和 React 已经很热门，但大量真正挣钱的项目仍由 jQuery 驱动。回头看，jQuery 并不是“落后的代名词”，它曾经非常准确地解决了选择器、事件、Ajax 和浏览器差异这些现实问题。后来让人吃力的，是页面规模已经变了，代码的组织方式却没跟上。</p><h2 id="那张越改越乱的订单表"><a href="#那张越改越乱的订单表" class="headerlink" title="那张越改越乱的订单表"></a>那张越改越乱的订单表</h2><p>订单表最开始只有查询和翻页，后来陆续加上批量勾选、状态修改、导出和行内编辑。代码仍然沿用“事件发生后直接修改 DOM”的写法：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">$(<span class="string">&#x27;#search&#x27;</span>).<span class="title function_">on</span>(<span class="string">&#x27;click&#x27;</span>, <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  $.<span class="title function_">get</span>(<span class="string">&#x27;/api/orders&#x27;</span>, $(<span class="string">&#x27;#filters&#x27;</span>).<span class="title function_">serialize</span>(), <span class="keyword">function</span> (<span class="params">result</span>) &#123;</span><br><span class="line">    $(<span class="string">&#x27;#result&#x27;</span>).<span class="title function_">html</span>(<span class="title function_">renderRows</span>(result.<span class="property">list</span>))</span><br><span class="line">    $(<span class="string">&#x27;#total&#x27;</span>).<span class="title function_">text</span>(result.<span class="property">total</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">$(<span class="string">&#x27;#result&#x27;</span>).<span class="title function_">on</span>(<span class="string">&#x27;click&#x27;</span>, <span class="string">&#x27;.js-cancel&#x27;</span>, <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> id = $(<span class="variable language_">this</span>).<span class="title function_">closest</span>(<span class="string">&#x27;tr&#x27;</span>).<span class="title function_">data</span>(<span class="string">&#x27;id&#x27;</span>)</span><br><span class="line">  $.<span class="title function_">post</span>(<span class="string">&#x27;/api/orders/&#x27;</span> + id + <span class="string">&#x27;/cancel&#x27;</span>, <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    $(<span class="string">&#x27;#search&#x27;</span>).<span class="title function_">trigger</span>(<span class="string">&#x27;click&#x27;</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>这段代码短，也确实能工作。麻烦在于状态没有明确归属：筛选条件来自 DOM，数据在 Ajax 回调里，分页又可能读全局变量。取消订单后只能模拟点击查询按钮，因为项目里并不存在一个可以复用的“重新加载订单”动作。</p><p>新增需求时，开发者通常再绑一个事件、再找一次元素。时间久了，同一个按钮可能在页面初始化、弹窗打开和局部刷新后被重复绑定。偶发的重复请求不是接口问题，而是初始化函数走了三遍。</p><h2 id="事件委托是优点，也容易藏住边界"><a href="#事件委托是优点，也容易藏住边界" class="headerlink" title="事件委托是优点，也容易藏住边界"></a>事件委托是优点，也容易藏住边界</h2><p>jQuery 的事件委托非常适合动态列表。父元素只绑定一次，后插入的行仍然可以响应点击：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">$(<span class="string">&#x27;#result&#x27;</span>).<span class="title function_">on</span>(<span class="string">&#x27;click.orders&#x27;</span>, <span class="string">&#x27;.js-detail&#x27;</span>, <span class="keyword">function</span> (<span class="params">event</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> orderId = $(event.<span class="property">currentTarget</span>).<span class="title function_">closest</span>(<span class="string">&#x27;tr&#x27;</span>).<span class="title function_">data</span>(<span class="string">&#x27;id&#x27;</span>)</span><br><span class="line">  <span class="title function_">openOrderDetail</span>(orderId)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>这里我特意加了 <code>.orders</code> 命名空间。它允许模块销毁时精确解绑：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">destroyOrderPage</span>(<span class="params"></span>) &#123;</span><br><span class="line">  $(<span class="string">&#x27;#result&#x27;</span>).<span class="title function_">off</span>(<span class="string">&#x27;.orders&#x27;</span>)</span><br><span class="line">  $(<span class="string">&#x27;#filters&#x27;</span>).<span class="title function_">off</span>(<span class="string">&#x27;.orders&#x27;</span>)</span><br><span class="line">  orderTable &amp;&amp; orderTable.<span class="title function_">destroy</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>以前我只关心“点了有没有反应”，后来才意识到初始化和销毁必须成对出现。页面被 PJAX、标签页容器或微前端反复挂载时，没有销毁路径的 jQuery 模块一样会产生监听器和内存泄漏。</p><h2 id="没有先换框架，而是先把状态拿出-DOM"><a href="#没有先换框架，而是先把状态拿出-DOM" class="headerlink" title="没有先换框架，而是先把状态拿出 DOM"></a>没有先换框架，而是先把状态拿出 DOM</h2><p>直接把整张表重写成新框架风险很大：日期插件、打印控件和导出逻辑都缺少测试，业务人员又每天使用。我们先做了一个不起眼的改动，把查询参数和请求动作收进普通 JavaScript 模块。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> orderState = &#123;</span><br><span class="line">  <span class="attr">filters</span>: &#123; <span class="attr">keyword</span>: <span class="string">&#x27;&#x27;</span>, <span class="attr">status</span>: <span class="string">&#x27;all&#x27;</span> &#125;,</span><br><span class="line">  <span class="attr">page</span>: <span class="number">1</span>,</span><br><span class="line">  <span class="attr">pageSize</span>: <span class="number">20</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">loadOrders</span>(<span class="params"></span>) &#123;</span><br><span class="line">  $(<span class="string">&#x27;#result&#x27;</span>).<span class="title function_">addClass</span>(<span class="string">&#x27;is-loading&#x27;</span>)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> $.<span class="title function_">getJSON</span>(<span class="string">&#x27;/api/orders&#x27;</span>, &#123;</span><br><span class="line">    ...orderState.<span class="property">filters</span>,</span><br><span class="line">    <span class="attr">page</span>: orderState.<span class="property">page</span>,</span><br><span class="line">    <span class="attr">pageSize</span>: orderState.<span class="property">pageSize</span></span><br><span class="line">  &#125;).<span class="title function_">then</span>(<span class="keyword">function</span> (<span class="params">result</span>) &#123;</span><br><span class="line">    <span class="title function_">renderOrderTable</span>(result)</span><br><span class="line">    <span class="keyword">return</span> result</span><br><span class="line">  &#125;).<span class="title function_">always</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    $(<span class="string">&#x27;#result&#x27;</span>).<span class="title function_">removeClass</span>(<span class="string">&#x27;is-loading&#x27;</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>搜索按钮只更新 <code>orderState</code>，翻页也只更新 <code>orderState.page</code>，最后都调用 <code>loadOrders()</code>。这一步没有改变视觉效果，却让数据来源第一次变得清楚。等到后来把表格视图换成组件时，请求和业务规则都能继续使用。</p><h2 id="最难搬走的是插件，不是"><a href="#最难搬走的是插件，不是" class="headerlink" title="最难搬走的是插件，不是 $"></a>最难搬走的是插件，不是 <code>$</code></h2><p>把 <code>$(&#39;.title&#39;).text(value)</code> 改成 <code>document.querySelector(&#39;.title&#39;).textContent = value</code> 没有多大价值。真正昂贵的是那些把状态存在元素内部的插件。</p><p>以日期选择器为例，迁移前至少要回答四个问题：</p><ul><li>它在什么时候初始化，重复初始化会不会再插入一份浮层；</li><li>外部值变化时，调用哪个 API 才能同步到控件；</li><li>插件注册了哪些 document&#x2F;window 级监听；</li><li>元素移除之前有没有 <code>destroy</code>，销毁后还能否重新挂载。</li></ul><p>我们给旧插件包了一层适配器，只暴露 <code>mount</code>、<code>setValue</code> 和 <code>destroy</code>。新旧页面都通过这层调用。这样替换插件时改的是适配器，而不是在几十个页面里搜索字符串。</p><h2 id="逐步迁移时留下的几条规矩"><a href="#逐步迁移时留下的几条规矩" class="headerlink" title="逐步迁移时留下的几条规矩"></a>逐步迁移时留下的几条规矩</h2><p>老系统最怕“看起来现代、实际不可回滚”的重写。后来再处理类似项目，我会坚持几条比较朴素的规则：</p><ol><li>先锁住 jQuery 和关键插件版本，避免迁移期间继续漂移。</li><li>用真实业务路径补回归测试，至少覆盖查询、翻页、提交和弹窗关闭。</li><li>新代码不再随处读取 DOM 状态，请求与数据转换先写成普通函数。</li><li>每个旧模块都有明确的初始化和销毁入口。</li><li>以页面区域为单位替换，不让两个视图系统同时修改同一块 DOM。</li></ol><p>今天从零写项目，我不会再用 jQuery 组织复杂应用；但维护老项目时，我也不会把“删掉 <code>$</code>”当成目标。迁移真正要做的是把隐含状态、生命周期和模块边界重新找回来。框架只是接住这些边界的工具。</p><h2 id="资料"><a href="#资料" class="headerlink" title="资料"></a>资料</h2><ul><li><a href="https://blog.jquery.com/2017/03/20/jquery-3-2-1-now-available/">jQuery 3.2.1 发布说明</a></li><li><a href="https://api.jquery.com/on/">jQuery API：<code>.on()</code></a></li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端年鉴 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 前端年鉴 </tag>
            
            <tag> jQuery </tag>
            
            <tag> 2017 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
    
    
    <entry>
      <title>标签</title>
      <link href="/tags/index.html"/>
      <url>/tags/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>分类</title>
      <link href="/categories/index.html"/>
      <url>/categories/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>关于我</title>
      <link href="/about/index.html"/>
      <url>/about/index.html</url>
      
        <content type="html"><![CDATA[<p>你好，我是 <strong>Chili Hong</strong>，一名前端开发者。</p><p>我喜欢把开发中遇到的问题拆开、验证、复盘，再整理成可以重复使用的方法。这个博客既是个人知识库，也是我与更多开发者交流的窗口。</p><h2 id="我关注的方向"><a href="#我关注的方向" class="headerlink" title="我关注的方向"></a>我关注的方向</h2><ul><li>JavaScript、TypeScript 与现代前端框架</li><li>前端工程化、构建工具与质量体系</li><li>Web 性能、可访问性与用户体验</li><li>AI 辅助开发与效率工具</li><li>真实项目中的问题定位和架构取舍</li></ul><h2 id="关于这个博客"><a href="#关于这个博客" class="headerlink" title="关于这个博客"></a>关于这个博客</h2><p>本站使用 <a href="https://hexo.io/">Hexo</a> 构建，采用 <a href="https://github.com/jerryc127/hexo-theme-butterfly">Butterfly</a> 主题，源码保存在 <code>main</code> 分支，静态站点发布到 <code>gh-pages</code> 分支。</p><p>我希望这里的文章尽量做到三点：</p><ol><li>给出问题发生的背景，而不只展示最终代码。</li><li>解释方案背后的取舍，而不把工具当成标准答案。</li><li>提供可以直接实践和验证的步骤。</li></ol><h2 id="找到我"><a href="#找到我" class="headerlink" title="找到我"></a>找到我</h2><p>你可以通过 <a href="https://github.com/95y">GitHub @95y</a> 找到我。</p>]]></content>
      
    </entry>
    
    
  
</search>
